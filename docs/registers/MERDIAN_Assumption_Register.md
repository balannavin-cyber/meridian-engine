# MERDIAN Assumption Register

**Market Structure Intelligence & Options Decision Engine**

---

| Field | Value |
|---|---|
| Document | `MERDIAN_Assumption_Register.md` |
| Location | `docs/registers/` |
| Type | Living register of every unvalidated design decision and its current state |
| Lineage | Promoted from `GammaEngine_Master_V15.1.docx` Appendix D (March 2026) → refreshed post-ADR-007 V18F ICT pivot (May 2026) |
| Established | 2026-05-09 (Session 23 — created as direct follow-up to ADR-007, since ADR-002 cites Appendix D as authoritative and ADR-007's open-follow-up #4 mandates the refresh) |
| Update rule | Inline updates whenever experimental evidence validates / refutes / supersedes an assumption. Superseded rows are annotated, never deleted. Doc Protocol v4 Rule 9.5 + Rule 11.2. |

---

## Purpose

Every threshold, every gate, every regime classifier, every formula in MERDIAN was set by *something* — judgment, NSE convention, theoretical reasoning, prior validation. This register records what that something was, and whether subsequent evidence has confirmed, refuted, or superseded it.

Two uses:

1. **Pre-change discipline.** Before modifying a threshold or gate, look up its row here. If the assumption is still LIVE (unvalidated), the change requires data per Doc Protocol's Measure → Validate → Shadow → Promote framework. If the assumption is VALIDATED, you're proposing to overturn validated work and need an ADR with stronger evidence than the validation cohort. If it's SUPERSEDED or REFUTED, the change may already be moot.
2. **ADR anchor.** ADR-002 cites V15.1 Appendix D as authoritative for the assumption gaps it addresses. Now ADR-002's citation lives here.

---

## Status taxonomy

| Status | Meaning | Required to change |
|---|---|---|
| **LIVE** | Assumption still in effect, unvalidated. May or may not still apply post-pivot. | Per Measure→Validate→Shadow→Promote |
| **VALIDATED** | Empirical evidence confirms the assumption. | ADR with cohort that overturns the validation |
| **REFUTED** | Empirical evidence contradicts the assumption. | Action required: drop the assumption from live code |
| **SUPERSEDED** | Replaced by a different mechanism (typically post-architectural-shift). Annotation links the superseding ADR. | Already handled |
| **RESOLVED** | Was a known bug or gap, now fixed. | Already handled |

---

## D.1 Signal Engine assumptions

V15.1 Appendix D §D.5 originally. Largely SUPERSEDED post-ADR-007 (V18F ICT pivot). The architecture changed from confidence-threshold-gated decision to ICT-pattern-triggered decision; most of the V15.1 signal-engine assumptions belonged to the prior architecture.

| Assumption (V15.1) | How set | Status | Evidence / annotation |
|---|---|---|---|
| `min_confidence_threshold = 60` — signals below 60 are not executed | Seeded in `system_config`; never empirically tested at the time | **SUPERSEDED by ADR-007** | V18F SRB-05: lowered to 40. The V15.1 rule "no threshold change without 30+ sessions of regret-log data" was satisfied by `signal_regret_log` (614 rows, V18A) plus full-year backtest (V18C–F). Lower threshold passes more signals without materially degrading accuracy. |
| `min_dte_threshold = 2` — options with DTE < 2 are not traded | Risk conservatism around expiry-day gamma explosion | **SUPERSEDED by ADR-007** | Replaced by DTE-aware tier rules (V19 §8.3): BULL_OB DTE=0 = TIER1; BEAR_OB DTE=0/1 = SKIP under specific gates; BEAR_OB DTE=4+ MOM_YES = TIER1. Theta-kill rate and tier interact rather than a single global DTE threshold. |
| `reasons` field is dynamically generated from the actual decision path, not templated text | Confirmed in V15 architecture review | **VALIDATED — LIVE** | Carried forward into ICT-era. `signal_snapshots.reasons` reflects the actual conditional branch taken in `build_trade_signal_local.py`. |
| Confidence-score 0–100 is the primary signal trigger | V11→V15.1 architecture | **SUPERSEDED by ADR-007** | Confidence-score is now an adjustment layer beneath ICT tier (V19 §8.4). ICT pattern detection is the trigger. |
| LONG_GAMMA hard block applied regardless of `flip_distance` | Binary regime simplicity; never validated against distance | **VALIDATED post-Exp 17/19** | Counterintuitively, Exp 17 (1m) and Exp 19 (5m) confirmed: BULL_OB 50.5% vs BEAR_OB 49.7% under LONG_GAMMA — pooled 47.7% WR, below random. Binary block is correct. The V15.1-spec'd three-zone refinement is unnecessary. The 11 March 2026 case study insight (binary block was wrong on a 2.2%-from-flip session) is preserved as diagnostic; the pivot to ICT triggers means the block now operates on a different signal-source upstream. |
| Breadth-momentum CONFLICT → unconditional DO_NOTHING | Architectural conservatism; never validated | **SUPERSEDED by ADR-007 SRB-01** | V18F evidence: CONFLICT BUY_CE produces 58.7% WR (NIFTY) / 55.4% (combined). Reversing the lift lowered backtest accuracy. Rule lifted. V15.1 §3.3 "open architectural question" is closed. |

### New post-pivot signal-engine assumptions (LIVE — pending validation as live data accumulates)

| Assumption (post ADR-007) | How set | Status | Evidence / annotation |
|---|---|---|---|
| ICT TIER1/TIER2/TIER3 boundaries (per V19 §10.3) reflect true edge differentials | V18F Exp 15/16 cohort | **LIVE — partial** | Exp 15 backed the gross hierarchy (TIER1 ≈ 90%+ WR, TIER2 ≈ 60–80%, TIER3 ≈ 50%). Live data accumulating since V18G (Phase 4A). Re-validate after 30+ live trades per tier. |
| ENH-37 MTF context boost (HIGH > MEDIUM > LOW) is the right hierarchy | V18F Exp 10c/15 cohort | **LIVE — questioned by TD-059** | Session 16 evidence on N=231 live cohort shows LOW outperforms HIGH on OB patterns. Filed as TD-059 / ENH-89. Current production treatment: annotation-only (do not size up on HIGH). Decision deferred (Option A annotation, B inversion, or C shadow A/B test). |
| Power hour gate: no signals after 15:00 IST | V18F (ENH-35) | **LIVE — convention** | Reasonable practitioner convention; not separately tested for ICT-era. Re-validate when sample size permits. |
| Confidence modifiers (PCR ±5, skew ±4, flow ±3) reflect proportional edge contribution | V18F Exp 21–25 small-but-measurable evidence | **LIVE** | Each ENH-02/04 backed by an experiment showing measurable but small edge. The exact point allocations are heuristic; could be re-fitted with more data. |
| Breadth modifier (BEARISH+BUY_PE = +5, BULLISH+BUY_CE = +5) adds edge | V15.1 carry-over (assumed); V18F Exp 20 contradicts | **REFUTED — ENH-43 candidate** | Exp 20 shows breadth contributes +/- 1.0pp = pure noise. ENH-43 is the candidate to remove. Action required: remove breadth scoring block from `build_trade_signal_local.py`. Estimated 0.5 session of work. |

---

## D.2 Gamma Regime assumptions

V15.1 Appendix D §D.2 originally. Largely INTACT post-ADR-007. The gamma layer was not the locus of the V18F pivot; the pivot was upstream (signal trigger). V15.1's gamma-regime assumptions were either confirmed by Exp 17/19 evidence or remain unvalidated.

| Assumption (V15.1) | How set | Status | Evidence / annotation |
|---|---|---|---|
| Binary LONG/SHORT regime based on `net_gex` sign only. No consideration of distance from flip | Design simplicity; distance was known but not used in regime gate | **VALIDATED — Exp 17/19** | Exp 17 (1m) + Exp 19 (5m) confirmed: pooled 47.7% WR under LONG_GAMMA across both BULL_OB and BEAR_OB. Binary correct. Three-zone model superseded as unnecessary refinement (the `gamma_zone` field still exists in `gamma_metrics` for future research, but its V15.1-spec'd behavioral role is moot). |
| LONG_GAMMA means dealers dampen moves and directional trades have lower expected value | Theoretical — based on dealer hedging mechanics | **VALIDATED indirectly** | Exp 17/19 binary block evidence is the direct empirical confirmation: directional trades under LONG_GAMMA are systematically below random WR. Mechanism (dealer dampening) is consistent. |
| Single expiry dominates GEX calculation. No multi-expiry weighting | Near-term expiry selected | **LIVE** | Not specifically tested in V18F. Multi-expiry GEX would require additional ingestion + computation. Worth experiment on expiry week specifically. Not blocking. |
| `flip_distance_pct` in schema; absolute points in live JSON — unit inconsistency | Documented bug in V15.1 §5.3 | **RESOLVED in V18A** | `flip_distance_pct` is now the canonical field; `flip_distance_points` retained as diagnostic. ADR-002 P1 (zones over points) cites the resolution as the canonical-percent precedent. |
| CE gamma positive, PE gamma negative — sign convention used in net_gex | Standard Black-Scholes gamma sign convention | **VALIDATED — LIVE** | No challenge raised. Industry standard. |
| Near-flip regime (within 0.5%) has different outcome distribution than far-flip (> 1.5%) | V15.1 spec'd as reason for three-zone model | **REFUTED post-Exp 17/19** | Three-zone model would have been the test; ADR-007 establishes that the empirical answer is "the binary regime is correct as a gate but the signal trigger upstream was wrong." `gamma_zone` field still computed, but no behavioral effect in current rules. Could re-emerge as a research artifact; not a live assumption. |
| ADR-002 P5 PINNED gamma regime is a distinct state class beyond LONG/SHORT/NO_FLIP | ADR-002 architectural commitment | **LIVE — pending build** | ENH-82 will deliver. When built, will integrate as additional gate or modifier (structurally similar to LONG_GAMMA / NO_FLIP). |

---

## D.3 Breadth Engine assumptions

V15.1 Appendix D §D.1 originally. Mostly LIVE but the broader role of breadth in MERDIAN is pending deprecation per ENH-43.

| Assumption (V15.1) | How set | Status | Evidence / annotation |
|---|---|---|---|
| Equal-weighting of ~1,385 stocks. Larger index components have no more influence than small caps | Architectural simplicity; never tested | **LIVE — partial mitigation via WCB** | V16 introduced WCB (Weighted Constituent Breadth) which addresses this for the index-driver subset. Equal-weighted breadth survives in `market_breadth_intraday`. Pending ENH-43 decision on whether to deprecate the breadth confidence modifier entirely. |
| BULLISH threshold: `breadth_score >= 62.5` | Judgmental — set in `system_config` | **LIVE** | Not validated for ICT-era. Pending ENH-43 outcome. If ENH-43 removes breadth from confidence scoring, threshold becomes irrelevant for the signal layer (still meaningful for analytical/diagnostic purposes). |
| BEARISH threshold: `breadth_score <= 37.5` | Judgmental — set in `system_config` | **LIVE** | Same. |
| DMAs computed from EOD closes. Intraday DMA values are fixed at yesterday's close during session | Intraday DMA computation not implemented | **LIVE** | Status unchanged. Computation cost is the blocker. Open question: does intraday DMA refresh produce materially different breadth scores? Untested. |
| `breadth_score_change` is a valid short-term momentum proxy | Derived feature; never independently validated | **SUPERSEDED post-ADR-007** | Momentum signals are now ICT-derived (pattern bars, MOM_YES gate, ret_session) plus PO3 session bias. `breadth_score_change` is no longer used as a momentum input in the live signal engine. Field still computed; consumer relationship dropped. |
| Breadth contributes +5 confidence when aligned (BEARISH+BUY_PE, BULLISH+BUY_CE) | V15.1 carry-over | **REFUTED — ENH-43 candidate** | Cross-referenced from D.1. Exp 20 shows breadth +/- 1.0pp = pure noise. ENH-43 to remove from `build_trade_signal_local.py`. |

---

## D.4 Momentum assumptions

V15.1 Appendix D §D.4 originally. Heavily refreshed post-ADR-007. The role of momentum changed from "primary direction signal" to "confirmation modifier" (ret_5m demoted; ret_session built).

| Assumption (V15.1) | How set | Status | Evidence / annotation |
|---|---|---|---|
| `ret_5m` is a valid momentum signal — a single 5-minute return captures direction | Standard short-term momentum measure; not validated in NSE context | **SUPERSEDED by ADR-007** | The 11 March 2026 case study (V15.1 §3.6 / V16 §4) was the founding evidence that single-horizon `ret_5m` produces wrong direction calls on trend days with micro-bounces. Post-ADR-007 `ret_5m` is no longer a primary direction signal. It survives as one of several momentum-confirmation inputs (ENH-01 in V18G adds `ret_session` as the session anchor). |
| VWAP slope is computed over an unspecified window | Implementation detail not captured | **LIVE — TD candidate** | Window still not formally specified in `build_momentum_features_local.py`. Worth a 0.5-session audit to either document the actual window or formally fix it. Not blocking. |
| Breadth-momentum CONFLICT → unconditional DO_NOTHING. No resolution logic | Architectural conservatism; never validated | **SUPERSEDED by ADR-007 SRB-01** | Cross-referenced from D.1. CONFLICT BUY_CE 58.7% WR. Rule lifted. V15.1 §3.3 "open architectural question" is closed. |
| `ret_session` does not exist. Session direction anchor is absent | Measurement gap; never built | **RESOLVED V18G (ENH-01)** | `ret_session` is now live in `momentum_snapshots`. Session-open threshold 03:35 UTC accepts 09:05 IST PreOpen capture. Used as confirmation modifier in confidence scoring, not as a primary trigger. |
| Multi-horizon momentum voting model (ret_5m / ret_15m / ret_60m / ret_session / vwap_slope, ≥2 of 5 agree) | V15.1 §15.2 Item 7 spec | **SUPERSEDED by ADR-007** | Never built. Rendered moot by signal-source change: momentum is now a confirmation modifier, not a vote member with equal weight. Equal weighting would dilute the strongest signals. |

### New post-pivot momentum assumptions (LIVE)

| Assumption (post ADR-007) | How set | Status | Evidence / annotation |
|---|---|---|---|
| MOM_YES gate (momentum aligned with pattern direction) lifts WR materially | V18F Exp 20 evidence | **VALIDATED** | Exp 20: ALIGNED 60.9%, OPPOSED 38.3%, lift +22.6pp. Wired into TIER1/TIER2 criteria (V19 §8.3). |
| PO3 session bias (BSL/SSL liquidity sweep on 1H window) classifies session as PO3_BULLISH / PO3_BEARISH / NEUTRAL | Research Session 11 (ENH-75) | **VALIDATED — LIVE** | `detect_po3_session_bias.py` runs Mon-Fri 10:05 IST. `po3_session_state` table populated. Used in BEAR_OB MIDDAY (ENH-76) and BULL_OB AFTERNOON SENSEX (ENH-77) gates. |

---

## D.5 Volatility assumptions

V15.1 Appendix D §D.3 originally. VIX gate is SUPERSEDED; most other volatility assumptions are intact.

| Assumption (V15.1) | How set | Status | Evidence / annotation |
|---|---|---|---|
| VIX HIGH threshold: `> 20`. Used to gate `trade_allowed = false` at HIGH VIX | NSE convention; not validated for MERDIAN signal quality | **SUPERSEDED by ADR-007 SRB-04** | V18F evidence: HIGH_IV regimes show MORE edge on OBs, not less. Gate counterproductive. Removed. India VIX context retained as informational metadata in `volatility_snapshots`; does not gate any signal. |
| ATM IV = (atm_call_iv + atm_put_iv) / 2. Simple average | Simplicity | **LIVE** | Not specifically challenged. Could investigate whether put_IV or call_IV individually has higher predictive power for the direction being traded. Not blocking. |
| VIX fallback: last known value with `STALE_VIX` caution. Provisional — not confirmed in live code | Provisional spec V15.1 §8.4 | **LIVE — UNRESOLVED** | Status not confirmed against live `fetch_india_vix.py`. Could be a silent regime misclassification risk. Worth a 0.5-session audit + harden as a named feature. File as TD if not already. |
| VIX percentile context model: 5 bands (VERY_LOW / LOW / NORMAL / ELEVATED / EXTREME at 0–20 / 20–40 / 40–60 / 60–80 / 80–100 percentile) | V16 §10.2 | **LIVE — partially used** | `vix_percentile_regime` field populated in `volatility_snapshots` per V18D. Behavioral interpretation of each band is not currently wired into signal logic; it's diagnostic context only. |

### New post-pivot volatility assumptions (LIVE)

| Assumption (post ADR-007) | How set | Status | Evidence / annotation |
|---|---|---|---|
| `basis_pct` > 0.5% adds caution flag (not a block) | V18G ENH-07 | **LIVE** | Wired in `build_trade_signal_local.py`. Does not affect `trade_allowed`. Diagnostic only. |
| HIGH_IV regimes have MORE edge on ICT OB patterns | V18F Exp 5 + ENH-35 sub-analysis | **VALIDATED** | Direct empirical reversal of the V15.1 VIX > 20 gate. Foundation for SRB-04. |

---

## D.6 ICT-era new assumptions (no V15.1 antecedent)

These assumptions did not exist in V15.1 because ICT pattern detection was not yet the trigger. Recorded here so they have a home for future validation / refutation.

| Assumption | How set | Status | Evidence / annotation |
|---|---|---|---|
| ICT pattern detection is meaningful only on 5m bars; 1m bars are too noisy for structural patterns | V18H lesson (sweep detection 0 events on 1m vs 52 on 5m) | **VALIDATED** | Codified as CLAUDE.md Rule 6: "5m bars for ICT pattern detection, never 1m. 1m is for precise entry timing only after HTF confirms." |
| T+30m exit beats both ICT structure-break exit and longer holds | V18F Exp 8/14b/15 multiple confirmations | **VALIDATED** | Exp 15: T+30m 63.8% WR +₹10.43L vs ICT structure break 36.9% WR +₹7.37L. T+30m wins by 41% on total P&L. CLAUDE.md settled-decision. |
| Kelly Half (Strategy C) is the safe live starting point; Kelly Full (Strategy D) is the upgrade after 3–6 months of live data | V18G §10.1 + V18F Exp 16 | **LIVE — proposed** | Current Phase 4A operates with Strategy C. Live data accumulating to validate before upgrading. |
| Capital ceiling: ₹50L hard cap, ₹25L sizing freeze, ₹10K floor | V18F + V18G §10.1 (floor lowered from ₹2L → ₹10K) | **VALIDATED — LIVE** | CLAUDE.md non-negotiable rule 8. Liquidity-driven for NIFTY/SENSEX options. Do not re-litigate. |
| 1H zones (MEDIUM context) add edge over LOW context on ICT patterns | V18F Exp 10c/15 cohort | **CONTESTED — TD-059** | Original Exp 10c/15 evidence supported. Session 16 evidence on N=231 contradicts (LOW > HIGH on OB). Resolution: ENH-89 (annotation only / inversion / shadow A/B test). |
| BEAR_OB AFTERNOON (13:00–14:30 IST) is a hard SKIP | V18F Signal Rule Book v1.1 evidence | **VALIDATED** | 17% WR, -24.7% expectancy. Hard skip. CLAUDE.md settled-decision. |
| `signal_regret_log` should remain populated as ongoing monitoring (not as threshold-change gate, since that role retired with ADR-007) | V18A precedent + ADR-007 evidence | **LIVE — ongoing** | Keeps the audit trail. The V15.1-spec'd role as threshold-change gate is retired (the gate purpose was satisfied at V18F). The diagnostic role continues. |
| Phase 4 promotion gate waiver (10-session shadow → 9-session + 1-year backtest) is acceptable when stronger evidence exists | V18G §10.1 decision | **LIVE — precedent** | One-time waiver. Future shadow-gate waivers require equivalent strength of alternative evidence. Worth ADR if pattern recurs. |

---

## D.7 Validation queue (next experiments to fund)

Open assumptions that would benefit from formal validation, prioritised:

| Priority | Assumption | Suggested experiment |
|---|---|---|
| HIGH | ENH-37 MTF hierarchy correctness (TD-059) | Shadow A/B test with `confidence_score_v1` (current hierarchy) and `_v2` (inverted). Run 4–8 weeks. Compare WR. |
| HIGH | Breadth contribution to confidence (ENH-43 disposition) | Already have Exp 20 evidence (1.0pp = noise). Action: remove breadth scoring block. 0.5 session of work. |
| MEDIUM | VIX fallback STALE_VIX behavior | Audit live `fetch_india_vix.py` against V15.1 §8.4 spec. Harden as named feature. 0.5 session. |
| MEDIUM | VWAP slope window | Read window from `build_momentum_features_local.py`; document; assess fixed vs adaptive. 0.5 session. |
| MEDIUM | Multi-expiry GEX on expiry week | Compare single-expiry vs multi-expiry GEX-driven signals on expiry-week cohort. 1–2 sessions. |
| LOW | Equal-weighted breadth vs cap-weighted | Already addressed by WCB; full revalidation low priority unless ENH-43 keeps breadth in confidence layer. |
| LOW | Intraday DMA refresh impact | Compute intraday DMA for 30 sessions, compare breadth_score deltas. Expected small-impact result. |

---

## D.8 Methodology assumptions (Phase α Q4 — calibration discipline)

Filed S25 2026-05-10 from Phase α Q4 deferred-to-architect resolution. These are not signal-engine assumptions; they are **methodological commitments** that govern how new parameter changes are validated before promotion to production. ADR-009 will codify these; this section is the working draft.

| ID | Assumption | Status | Evidence | Action |
|---|---|---|---|---|
| **D.8.1** | Single-cohort parameter derivation (Exp 15 era — full ~Apr 2025 → Apr 2026 of data used for both calibration and validation, no holdout slice) is not statistically defensible going forward. | **REJECTED S25** (option d in Phase α Q4 framing) | In-sample-only calibration memorizes noise alongside signal; out-of-sample degradation is structural and unmeasurable without a holdout split. | All new parameter changes must follow D.8.2 split discipline. Existing Exp 15-era params get D.8.3 prospective parity check. |
| **D.8.2** | Phase 1 holdout split (now → ~April 2027 / Y2 close): N≥60 → 67/33 split, 10pp tolerance; 30≤N<60 → 75/25 split, 15pp tolerance; N<30 → "low-N calibration-only" tag in `merdian_reference.json`, no split required. | **LIVE S25 — pending ADR-009 codification** | Graduated by N because formal split below N=60 destroys statistical power on holdout slice. | Apply to next parameter-change experiment. Failures (holdout WR outside tolerance) → parameter filed but not shipped. |
| **D.8.3** | Existing Exp 15-era parameters get a **prospective parity check** instead of retroactive split: track live WR for next 60 trading days against calibration WR. Flag drift > 15pp. | **LIVE S25 — implicit; formalized here** | Already happening implicitly via live trading; this just codifies the threshold for "this parameter is degrading." | Operationalize threshold check via dashboard or scheduled audit script. Owner-check-in: ~July 2026 (60 trading days post-S25). |
| **D.8.4** | Phase 2 (Y2 close, ~April 2027): migrate to rolling walk-forward — 12-month calibration / 3-month holdout, slide quarterly. Parameter versioning via git tag + `merdian_reference.json` entry. Old parameters preserved for replay-based comparison. | **DEFERRED to Y2** | Walk-forward operationally too heavy at Y1 data scale; cohort sizes today don't always support it. | ADR-009 codifies trigger date for Phase 1 → Phase 2 cutover. Parameter versioning infrastructure to be designed before Y2. |
| **D.8.5** | Status-quo single-cohort derivation (option d in Phase α Q4 framing) is explicitly **not acceptable** even under the constraint that some experiments have insufficient N for formal split. Discipline at low-N is the "low-N tag" in D.8.2, not silent waiver. | **REJECTED S25** | Single-cohort derivation has produced parameters traded against; degradation discipline matters even at low-N. | No silent waivers. Every parameter change either follows D.8.2 (formal split) or is tagged "low-N calibration-only" under D.8.2. |

**ADR-009 governance language draft (to be ratified when ADR is drafted):**

> *Parameter changes ship to production only after holdout validation. The split is graduated by cohort size: N≥60 takes a formal 67/33 split with 10pp tolerance; 30≤N<60 takes 75/25 with 15pp tolerance; N<30 ships as "low-N calibration-only" with explicit tag and prospective live-tracking commitment. Existing Exp 15-era parameters carry a prospective 60-day parity check against calibration WR. Walk-forward becomes mandatory at Y2 close. Single-cohort silent derivation is not permitted.*

**Open follow-ups:**

1. Operationalize D.8.3 prospective parity check — does it run via dashboard surfacing or a scheduled audit script? File as ENH if not already.
2. Inventory all Exp 15-era parameter values currently in production code paths so the parity check has a concrete target list. Most are in `detect_ict_patterns.py` thresholds and `assign_tier()` routing logic.
3. Define `merdian_reference.json` schema extension for "low-N calibration-only" tag (parameter name, cohort size at calibration, calibration WR, derivation date, N-target for upgrade to formal split).

### D.9 — ENH-55 momentum opposition gate hypothesis (Exp 20) refuted by 24-day production cohort (Session 26)

**First substantive application of D.8.3 prospective parity check.** ENH-55 was promoted on Exp 20 evidence (5m-batch `hist_pattern_signals` cohort, derivation date 2026-04-XX): aligned WR 60.9% vs opposed WR 38.3%, +22.6pp lift, ~+18pp lift over standalone — sufficient to ship as a hard gate (opposition → DO_NOTHING) plus a +10 confidence bonus on alignment. The gate fires when `abs(ret_session) > 0.0005` (0.05%).

A writer bug (TD-101) in `build_momentum_features_local.py::get_session_open_spot()` — unbounded `market_spot_snapshots` query returning the oldest 500 rows since table creation, all of which fail the today-date filter — caused `momentum_snapshots.ret_session` to be persistently NULL. Population started 2026-04-17 and remained NULL through 2026-05-10 (3+ trading weeks; ~5,000 signals). Because ENH-55's inner condition gates on `ret_session is not None and abs(ret_session) > 0.0005`, the gate was a silent no-op for the entire window. The same anti-pattern (unbounded `order_by`+`limit` returning oldest rows) is the OI-18 class fixed at TD-097 site Session 25 — TD-101 is the propagation instance the TD-099 audit missed.

Once TD-101 was fixed (commit `3cb84e2`), retrospective audit on the silent-gate-failure window (2026-04-17 → 2026-05-10) partitioned actionable signals (action ∈ {BUY_CE, BUY_PE} ∧ `trade_allowed=TRUE`) into the buckets ENH-55 *would have* applied:

| Bucket | N | Wins | Losses | T+30m spot-side WR |
|---|---|---|---|---|
| WOULD_HAVE_BLOCKED (opposed) | 44 | 35 | 9 | **79.5%** |
| WOULD_HAVE_ALIGNED_BONUS | 35 | 19 | 16 | 54.3% |
| NEUTRAL_BAND (`abs(ret_session) ≤ 0.0005`) | 1 | 0 | 1 | 0.0% |

Decomposition: All 44 OPPOSED-but-winning trades are BUY_PE in up-sessions (`ret_session > 0` in cohort definition; `action == BUY_PE` selected). 43/44 have `ict_pattern=NONE` — pure momentum-driven signals where recent 15m/30m turn down despite session running up. Signature: intraday-rollover-of-up-session-strength = exhaustion / mean-reversion edge. Zero ENH-76/77/78 PO3-confirmed contributions. The 79.5% WR is on the cohort that would have been blocked; the 54.3% WR is on the cohort that would have received +10 confidence bonus.

Production data on the live cohort over 24 trading days **falsifies the gate hypothesis directionally**: opposed outperforms aligned. The gate, had it fired, would have suppressed a 79.5% WR setup (sample-conservative; even after adjusting for spot-direction-as-proxy approximation, the lift over aligned WR is large). The Exp 20 evidence base was on a different cohort (5m-batch `hist_pattern_signals`, with different filters and outcome metric). Exp 20's lift does not survive translation to the live signal cohort under current selection logic.

| ID | Statement | Status (post-S26) | Confidence | Implication if revised |
|---|---|---|---|---|
| **D.9.1** | ENH-55 momentum opposition rule (ret_session opposed → BUY_CE/BUY_PE blocked) is the right calibration for the live signal cohort. | **REFUTED S26** (production cohort 2026-04-17 → 2026-05-10, N=44 opposed at 79.5% WR). | High — sign of the lift is opposite to Exp 20; magnitude is large enough that even with proxy-WR caveats, gate is hostile to edge. | Disable the gate on the live cohort. Re-validate only with proper option-P&L outcome metric and re-derived alignment thresholds. |
| **D.9.2** | ENH-55 alignment bonus (+10 confidence) is the right calibration for the live signal cohort. | **REFUTED S26 — same evidence base, symmetric claim**. WOULD_HAVE_ALIGNED_BONUS bucket WR 54.3% is approximately neutral; not consistent with the Exp 20 +22.6pp claim. | High — same data; either both directions hold or neither. The Exp 20 hypothesis is symmetric (opposed worse + aligned better); both halves fail in production. | Disable the alignment bonus alongside the opposition block. |
| **D.9.3** | Exp 20 evidence base (5m-batch `hist_pattern_signals`) is representative of the live signal cohort that ENH-55 actually gates. | **REFUTED S26** — cohort-translation hypothesis fails. The 5m-batch cohort has different filter sequence (no LONG_GAMMA gate, no DTE gate, no power-hour gate, no ENH-76/77/78 PO3 gates, different outcome metric), so even if ENH-55's relationship holds in the source cohort, it does not translate to the cohort post-other-gates. | Medium-High — single-instance lesson, but the magnitude of disagreement is too large to attribute to noise alone. | Future calibration parameters require parity check on the cohort they will actually gate, per D.8.3. Exp 20's failure here is the precedent. |
| **D.9.4** | TD-101 ret_session writer fix is unambiguously correct — the writer bug should be fixed regardless of whether the consumed value is gated. | **CONFIRMED S26**. The fix targets a bounded-query semantic that was producing wrong (NULL) data; whether downstream consumers gate on it is orthogonal to whether the writer is correct. | High — the fix is structural. | Independent of any decision about ENH-55. Stays in production. |
| **D.9.5** | The corrective action is to disable ENH-55 by env flag (default OFF) rather than rip from the codebase. Reversibility preserved for re-validation when proper option-P&L outcome metric is available; data continues to flow downstream for any other consumer of `ret_session`. | **DECIDED S26**. `ENH55_ENABLED` env flag added; default `0`; can be re-enabled via `MERDIAN_ENH55_ENABLED=1` in `.env`. ENH-53 breadth modifier remains active (different evidence base). | High — disablement is reversible; re-validation cost is one experiment when warranted. | Future falsifications of similar magnitude follow this pattern: disable by env flag, file as case study, do not strip code. |

**Cross-references:**

- **TD-101** (`build_momentum_features_local.py::get_session_open_spot()` unbounded query) — RESOLVED Session 26, commit `3cb84e2`. Bounded query with `gte("ts", today_start_utc_iso)` filter + limit=20 + 03:35 UTC boundary preserved per ENH-01 / V18G regression history.
- **ENH-55** entry in `MERDIAN_Enhancement_Register.md` — status note added Session 26: COMPLETE (PROMOTED, ENV-DISABLED 2026-05-10) with full 24-day evidence block and re-validation conditions for re-enablement.
- **CLAUDE.md B19** (TD-101 propagation lesson) — when an OI-18-class bug is fixed at one site, audit must runtime-verify (not just grep) all candidate sites, including writers downstream of the symptom site, before declaring the class closed. TD-097 → TD-099 grep-based audit closed only request-side scripts; TD-101 was a writer-side instance the grep couldn't reach because the anti-pattern was inside a helper, not a top-level URL construction.
- **§D.8.3** — Exp 15-era prospective parity check. ENH-55 is the first parameter to fail this check post-codification (S25). The 24-day window (2026-04-17 → 2026-05-10) is shorter than D.8.3's 60-day prospective parity threshold but the magnitude of disagreement (sign-flip on a proxy WR with large effect size) cleared the "flag drift" criterion within the silent-failure window. Discipline applied: gate disabled, finding logged, future re-validation gated on proper option-P&L cohort + re-derivation.
- **§D.8** rejected status-quo single-cohort silent waiver (D.8.5). D.9 demonstrates that the calibration discipline does fire when warranted; status-quo here would have been "ENH-55 disabled silently because TD-101 was the proximate fix" without the audit. Doing the audit before re-enabling production gating is the discipline.

**Open follow-ups (S27+):**

1. **ADR-009 first case study material.** D.9 is the first concrete instance of D.8.2/D.8.3 discipline being applied. ADR-009 draft (P2b S26 carry-forward) should include a §"Worked example — ENH-55 falsification" section pointing to D.9.
2. **Re-validation protocol.** When (or whether) to re-validate ENH-55: requires (a) proper option-P&L outcome metric replacing spot-direction proxy, (b) re-derived alignment threshold, (c) cohort matching production filter sequence (post-DTE, post-power-hour, post-ENH-76/77/78). Until then, gate stays disabled.
3. **ENH-55 retroactive blocks audit (S26 P3 carry-forward).** What fraction of 2026-04-17 → 2026-05-10 losses came from gate-failure (signals that fired with `trade_allowed=true` but lost) vs other? The 79.5% WR figure is on the cohort that would have been blocked; what about the cohort that fired? Cross-check, not just for ENH-55 introspection but as baseline diagnostic of the period.
4. **Cohort-translation discipline (D.9.3 implication).** Inventory all currently-shipped parameters whose evidence base is `hist_pattern_signals` (5m-batch) but which gate the live signal cohort. Flag for parity-check audit. Likely candidates: ENH-53 breadth, ENH-37 MTF context (already SUPERSEDED via TD-059 Session 16), ENH-76/77 BEAR_OB MIDDAY / BULL_OB AFTERNOON gates (Exp 40-derived; cohort match more direct).

---

### D.10 — ADR-002 v2 acceptance: new live assumptions filed (Session 27)

ADR-002 v2 (full rewrite of v1 accepted Session 27, 2026-05-11) introduces two new principles beyond v1's P1-P6 — P7 vol-pricing (RR ratio) and P8 second-order Greeks (vanna/charm) — plus names the buyer/writer inversion as first-class architectural concept. The principles are accepted as architectural commitments; the empirical validation per principle is the Phase 0b overlay calibration study work item. ENH-84 (vol_analytics + RR ratio) and ENH-85 (vanna/charm Phase 3 prep) are filed in scope.

Two related production resolutions in the same session (TD-NEW-2 flip_level regression + TD-NEW-3 net_gex unit standardisation) generated VALIDATED rows from ADR-002 v2's methodology requirements — see D.10.3 and D.10.4.

| ID | Assumption | Status | Evidence | Action |
|---|---|---|---|---|
| **D.10.1** | RR ratio (realized vs implied vol) carries independent edge from direction; RR < 0.85 → premium-buying adverse for buyer; RR > 1.2 → premium-collection adverse for writer. | **LIVE — pending Phase 0b validation** | ADR-002 v2 P7 commitment; thresholds match practitioner-dashboard convention. No MERDIAN empirical evidence yet (Phase 0b dependency on ENH-84 vol_analytics build). | Calibrate against historical signal cohort post-ENH-84. Phase 0b test: does RR < 0.85 conditional WR difference vs RR > 1.2 strata exceed noise on completed BUY signals? |
| **D.10.2** | The same gamma layer serves buyer and writer in mirrored interpretation (buyer/writer inversion). | **LIVE — architectural commitment** | ADR-002 v2 buyer/writer inversion section. v1 risked category error treating writer's intelligence as Phase 3-deferred. | No experiment needed at architectural level; tested via gate-stack strategy-mode parameter design when v2 build sequence reaches buyer-polarity gates. |
| **D.10.3** | Walk-from-ATM is the operationally correct flip-level definition; bottom-up first zero-crossing was fragile to spurious deep-strike contributions. | **VALIDATED Session 27 via TD-NEW-2 production resolution** | TD-NEW-2 patch Part B deployed and verified: HEALTHY 2026-05-07 cycle PATCHED flip 24,773.93 vs LIVE 24,785.97 (delta -12pts/0.05%); BROKEN 2026-05-08 cycle PATCHED flip 25,060 near spot vs stuck 21,250 live. Walk-from-ATM produces operationally meaningful flips on noisy and clean data. Cross-validated Session 28 via P1 backfill: 587 broken-window rows recomputed produced avg_abs_flip_offset 600-2040pts (was -2862 stuck pre-patch) across all 4 day/symbol combinations. | Codified as canonical method; legacy bottom-up walk retained as fallback for callers without spot context. |
| **D.10.4** | `net_gex` is canonically stored in Crore (rupees / 1e7). Raw rupees storage was a unit-scale bug that was silent because all consumers were sign-only. | **VALIDATED Session 27 via TD-NEW-3 production resolution** | TD-NEW-3 patch deployed in three writer files (`compute_gamma_metrics_local.py`, `replay/replay_compute_gamma_metrics.py`, `backfill_gamma_metrics.py`); 2026-05-11 09:30 IST cycle PATCHED 78,544.84 Cr vs LIVE 775,285,881,741 raw rupees; ratio 9.87M ≈ 1e7. Reader codebase audited — zero magnitude thresholds, unit change non-disruptive. Cross-validated Session 28 via P1 backfill: 587 rows recomputed produced backfilled net_gex in Cr range (avg 181K-923K Cr per day/symbol) vs still-broken raw-rupees neighbours (avg 1.6T-10T per day/symbol); per-row ratio ~10⁷ exact. | Canonical Crore unit. Future magnitude-consuming gates must specify Crore explicitly. |
| **D.10.5** | Five named scalars (`gamma_wall_strike`, `short_strike_for_strike`, `gradient_max_strike`, `hedged_long_cr`, `lambda_score_pct`) are the primary consumption surface of the Positioning Landscape, more operationally usable than v1's generic zone bounds. | **LIVE — pending ENH-81 build** | ADR-002 v2 Positioning Landscape section. v1 specified zone bounds only; v2 refines to named scalars (zone bounds retained as auxiliary for edge-case detection). | Build via ENH-81. Each scalar populates `gamma_metrics` as new column. Phase 0 must verify scalar-level computation against source-material dashboard reference cycles before threshold gating. |
| **D.10.6** | Six-scenario dealer-flow simulator output (±0.5%, ±1%, ±2% × {Cr, contracts, trajectory}) carries operationally usable force-magnitude information beyond binary regime classification. | **LIVE — pending ENH-81 build + assumption stack sensitivity test** | ADR-002 v2 P2 force-is-edge commitment. Sensitivity test (Methodology §2 of ADR-002 v2) gates whether scenarios are gate-consumable (<30% spread on assumption grid) or advisory (15-30%) or methodology-blocked (>30%). | ENH-81 builds; assumption stack sensitivity is a Phase 0c work item. |
| **D.10.7** | PINNED regime is empirically observable (i.e., distinct outcome distribution from LONG_GAMMA / SHORT_GAMMA on the live cohort). | **LIVE — pending Experiment 23** | ADR-002 v2 P5 commitment. Threshold for `local_gex_cluster_cr` magnitude that qualifies as PINNED is empirically determined. Phase 0b will identify if ≥5 confirmed mislabeled sessions exist in 12-month lookback (deprioritize if not). | Experiment 23 design + run after ENH-80 per-strike GEX table builds. |
| **D.10.8** | Vanna/charm have material gate value only at Phase 3 (writer's strategy mode). For Phase 1 buyer, vanna is risk-advisory only; charm is not consumed. | **LIVE — pending Phase 3 backtest** | ADR-002 v2 P8 commitment. ENH-85 NEW filed Session 27 verbally, formal Enhancement Register filing Session 28 commit `<S28-doc-close>`. Phase 1/2 consumption deferred until Phase 2 imminent. | Build ENH-85 only when Phase 2 (spreads) deployment plan is committed. |

**Cross-references:**

- **ADR-002 v2** (`docs/decisions/ADR-002-market-structure-philosophy.md`) — supersedes v1; v1 preserved in git history at commit `b46249e` and prior.
- **D.7 Validation queue** — Phase 0b overlay calibration study work item to be appended (post-ENH-84 build).
- **D.8.2 holdout discipline** — applies to RR threshold calibration when historical data accumulates post-ENH-84. Cohort-translation discipline (D.9.3 lesson) applies.
- **TD-NEW-2** — RESOLVED Session 27, commit `241f943`. D.10.3 codifies the walk-from-ATM definition from the resolution. Session 28 P1 backfill provides 587-row cross-validation of the patch.
- **TD-NEW-3** — RESOLVED Session 27, commit `241f943`. D.10.4 codifies the Crore unit convention from the resolution. Session 28 P1 backfill provides 587-row cross-validation of the patch.

**Open follow-ups (S28+):**

1. **ENH-84 vol_analytics build.** Schema spec in ADR-002 v2; build sequence places ENH-84 immediately after ENH-80. Phase 0b RR conditional-WR test requires ENH-84 producing live data. **Session 28 closed P2 (formal Enhancement Register filing) — ENH-84 now in PROPOSED state with full entry in `MERDIAN_Enhancement_Register.md` Part 4. ENH-84 build is the gating prerequisite for Phase 0b — earliest start S29.** ~1-2 sessions.
2. **Phase 0b overlay calibration study.** Retroactive computation of D.10.1, D.10.5, D.10.7 metrics from `option_chain_snapshots` for historical signal cohort. Tag historical signals with would-be overlay; quantify if v2 metrics would have improved or saved signals. **Data prerequisite (P1 broken-window gamma_metrics backfill) CLOSED Session 28 — 587/587 rows clean. Study itself remains blocked on ENH-84 build (live `vol_analytics` rows required for RR ratio dimension).** ~1-2 sessions.
3. **Phase 0c methodology selection.** Run zone-definition Options A/B/C/D side-by-side on backfill data; pick option with strongest WR differential between "spot inside zone" vs "spot outside zone." Dealer-flow assumption stack sensitivity test on representative session. **Data prerequisite CLOSED Session 28. Methodology selection can run on existing data once Phase 0b precedes it for ordering reasons.** ~1 session.
4. **ENH-85 Phase 3 prep.** Build vanna/charm compute when Phase 2 deployment plan is committed. Until then, ADR-002 v2 P8 is documented but inactive. **Session 28 closed formal Enhancement Register filing — ENH-85 in PROPOSED state with full entry. Build deferred pending Phase 2 plan commitment.**

---

### D.11 — Session 28 production architecture findings + Phase 0 P1 closure

Session 28 was scoped as P0 verification of TD-NEW-2 + TD-NEW-3 (Session 27 patches) in the live 09:15 IST cycle. P0 closed in ~10 minutes (verification cycle PASS on both NIFTY + SENSEX). The remaining session executed P1 (broken-window backfill, scheduled work) plus surfaced three previously-latent architecture defects via the operational sequence of investigating TD-080 → discovering TD-NEW-12 → resolving it → triggering TD-NEW-4 surface → resolving it → triggering TD-NEW-13 surface → resolving it → P1 backfill clean 587/587.

The findings here are not new principles per ADR-002 v2 — they are architectural invariants that **should always have been true** but were silently violated. Each VALIDATED row corresponds to a production resolution that ensures the invariant now holds. The Phase 0 P1 closure is the empirical milestone that unblocks downstream Phase 0b/0c work.

| ID | Assumption / Invariant | Status | Evidence | Action |
|---|---|---|---|---|
| **D.11.1** | AWS shadow runner writes to `gamma_metrics_shadow`, not production `gamma_metrics`. The `evaluate_shadow_vs_live.py` evaluation architecture (per Deployment Topology §6.5) requires the shadow table to receive AWS-side compute outputs as the comparison cohort against Local writes. | **VALIDATED Session 28 via TD-NEW-12 production resolution** | Pre-patch (since deployment ~Apr 29): AWS `compute_gamma_metrics_local.py` invocations wrote to `gamma_metrics` (production), double-writing same `(symbol, ts)` rows Local wrote, UPSERT race-condition determining which value persisted. `gamma_metrics_shadow` table existed in Supabase but had 0 rows. Cross-confirmed by 13-day production sample: 5-min cycles produced 2 writes per `(symbol, minute)` bucket on every cycle. Architectural invariant silently violated. Resolution: `--shadow` flag added to `compute_gamma_metrics_local.py` (TARGET_TABLE module-level constant routes both reads via `fetch_prior_gamma_metrics` and writes via `upsert_gamma_metrics` + ExecutionLog telemetry); AWS-side `run_merdian_shadow_runner.py` line 479 appends `"--shadow"` to invocation. Schema drift on shadow table corrected simultaneously: 7 missing columns ADDed via `ALTER TABLE IF NOT EXISTS` (dte, gamma_zone, otm_oi_velocity, raw, run_type, spot_vs_range, straddle_velocity); missing `UNIQUE(symbol, ts)` constraint added. Local smoke test 2026-05-13 confirmed: Path 1 (no flag) writes to `gamma_metrics` actual_writes={"gamma_metrics": 1}; Path 2 (--shadow) writes to `gamma_metrics_shadow` actual_writes={"gamma_metrics_shadow": 1}. AWS smoke test 07:07 IST 2026-05-13 confirmed same behaviour AWS-side. | Canonical TARGET_TABLE pattern adopted. Shadow architecture now fully wired. `evaluate_shadow_vs_live.py` becomes meaningful from S29 09:15 IST cron forward; backfill of `gamma_metrics_shadow` history is optional (forward-only acceptable). |
| **D.11.2** | The `dte` payload field is derived from `result.ts.date()` (the cycle's actual timestamp), not `date.today()` (wall clock at write time). The two diverge whenever compute is re-run on historical data (backfill, replay, repair); only the result-bound date is operationally correct. | **VALIDATED Session 28 via TD-NEW-4 production resolution** | Pre-patch: `upsert_gamma_metrics()` computed `dte` as `(date.fromisoformat(result.expiry_date) - date.today()).days`. Live writes were correct (result.ts ≈ now within seconds). Backfill writes were systematically wrong — running compute on 2026-05-12 NIFTY data on 2026-05-13 produced `dte = -1` instead of `dte = 0` (the run was on its expiry day). Latent bug; surfaced during TD-NEW-12 smoke test which exercised the same pattern. Resolution: `_dte_from_ts(result)` helper added at module level deriving as-of date from `result.ts` in IST; payload line uses helper. Cross-validated on 2026-05-12 NIFTY run_id `e2dd1a09-...`: post-patch dte=0 (correct), pre-patch dte=-1 (wrong). | Codified as canonical pattern: all `dte`-class computations derive as-of date from result's timestamp field, not wall clock. Future temporal-payload fields adopt same pattern. |
| **D.11.3** | Python stdlib `datetime.fromisoformat()` is not portable across runtime versions for variable-microsecond-precision timestamps. Python 3.10 accepts only 3 or 6 microsecond digits; Python 3.12 is permissive. Supabase serializes PostgreSQL timestamps with variable microsecond precision (2-7 digits common). Any timestamp-parse code must normalize before `fromisoformat()` for cross-environment correctness. | **VALIDATED Session 28 via TD-NEW-13 production resolution** | Local Windows runs Python 3.12 — all smoke tests passed. AWS Linux runs Python 3.10 — failed 60/587 backfill cycles with `ValueError: Invalid isoformat string` on timestamps containing 2-, 4-, 5-, or 7-digit microsecond fractions. Resolution: TD-NEW-4 helper `_dte_from_ts` extended with regex normalization that pads short fractions and truncates long ones to exactly 6 digits before `fromisoformat()`. Retry on the 60 failed run_ids: 60/60 success post-patch. | Canonical normalization pattern adopted in helper. CLAUDE.md B22 codifies: any Python module that parses ISO timestamps from Supabase must run on both Python 3.10 (AWS) and Python 3.12 (Local) — verify before relying on `fromisoformat()` permissiveness. Cross-version Python compatibility is a deployment-stack invariant. |
| **D.11.4** | Phase 0 P1 broken-window `gamma_metrics` backfill (data prerequisite for Phase 0b/0c overlay calibration studies) is closed. Both 2026-05-08 + 2026-05-11 (full broken-window of TD-NEW-2 + TD-NEW-3 silent defect period) are now patched data, suitable for retroactive metric computation. | **CLOSED Session 28 — empirical** | 587/587 rows backfilled (149 NIFTY + 150 SENSEX on 2026-05-08, 144 NIFTY + 144 SENSEX on 2026-05-11). All `still_unpatched = 0` per post-backfill diagnostic. `avg_abs_netgex` flipped from trillions (raw rupees) to thousands-of-Cr range. Per-row ratio ~10⁷ exact between pre and post values, confirming TD-NEW-3 unit fix integrity at production scale. `flip_level` dynamic (no longer stuck at deep-ITM), confirming TD-NEW-2 walk-from-ATM works on the cohort. | Phase 0b overlay calibration data prerequisite met. Phase 0c methodology selection data prerequisite met. Both studies remain blocked on **ENH-84 build** (Phase 0b for RR ratio dimension specifically) but no longer blocked on data integrity. The 60-row retry path itself produced TD-NEW-13 surface as a side effect — meta-pattern: each Phase 0 closure exercise surfaces residual defects that would have polluted downstream work. Codified as CLAUDE.md B23 in this session. |

**Cross-references:**

- **TD-NEW-12** — RESOLVED Session 28, commits `72622a9` (compute patch + schema fix) + `de23467` (AWS wrapper). D.11.1 codifies the shadow-vs-live separation invariant from the resolution.
- **TD-NEW-4** — RESOLVED Session 28, commit `72622a9` (bundled with TD-NEW-12). D.11.2 codifies the result-ts-based dte derivation from the resolution.
- **TD-NEW-13** — RESOLVED Session 28, commit `447634c`. D.11.3 codifies the cross-Python-version timestamp normalization pattern from the resolution.
- **P1 broken-window backfill** — closed Session 28 by running patched compute (with all three S28 patches present) over 587 broken-window run_ids; D.11.4 codifies the data-prerequisite-met state.
- **CLAUDE.md B22 + B23** — codified Session 28 in CLAUDE.md v1.18 → v1.19 footer.
- **Deployment Topology §6.5** — original architectural intent of `gamma_metrics_shadow` write boundary; updated Session 28 with TD-NEW-12 resolution.
- **MERDIAN_Enhancement_Register.md** — ENH-84 + ENH-85 PROPOSED entries filed Session 28 (P2 closure); both cross-link back to D.10.1 and D.10.8 respectively.

**Open follow-ups (S29+):**

1. **`gamma_metrics_shadow` history backfill (optional).** Forward-only-from-S29 is acceptable for `evaluate_shadow_vs_live.py`; backfilling history (running patched compute with `--shadow` on the same 587 broken-window cycles + Apr 29 → May 11 production-deployment-window cycles) would give immediate evaluation cohort. Cost: ~30-45 min sequential. Operator decision.
2. **`merdian_order_placer.py` Topology gap.** S28 surfaced that the order placer is a fourth AWS-only service running @reboot since 2026-04-29, but `MERDIAN_Deployment_Topology.md` §3 lists only three AWS-only services. Filed as TD-NEW-11 (S3 — documentation gap, not behavioural). Topology update applied in S28 close docs.
3. **MALPHA-as-third-environment Topology gap.** S28 surfaced that the Topology doc treats MALPHA only inline as "Kite gateway"; the manual Zerodha token propagation step (TD-NEW-7) confirms MALPHA is a distinct environment with operational dependencies. Topology update applied in S28 close docs.

---

---

### D.12 — Session 29 Phase 0b falsification realization + vendor data verification

Session 29 closed the loop on the ADR-002 v2 §P7 4-way RR-regime gate hypothesis filed in §D.10.1 by running the Phase 0b conditional-WR test on a 1,968-signal regime-tagged cohort. The result was a clean falsification: χ²=1.56 p≈0.30 across LOW/COMPRESSED/FAIR/HIGH; salvage test on COMPRESSED-veto binary FAIL on power with bootstrap CI [-3.36, +17.69]pp **INCLUDING ZERO** and median P&L negative both buckets. Per the ADR-002 v2 falsification commitment (and per ADR-009 §Phase 1 graduated-strictness holdout principle), ENH-97 PIVOTED from PROPOSED signal-time gate to **SHIPPED-AS-LOGGING** — `vol_analytics.rr_regime` is written by the forward-cycle writer + backfilled full year, but no signal-time gate consumes it. The dimension is dead as a gate; the data remains research-available.

Concurrent S29 finding: the vendor-purchased historical option data populating `hist_option_bars_1m` was verified to span the full 12-month window Apr 2025 → Mar 2026 with `oi` populated 99.9% (avg ~1M, max 66M across all months). This invalidated the long-standing S22 TD-094 claim that the Kite `historical_data` API doesn't return OI for index option minute bars — that claim was about the original source, but the table has since been replaced by vendor data and the limitation no longer applies. TD-094 was reclassified-stale. The verification unblocks Phase 0b gamma-context dimensions (P1 LONG_GAMMA, P3 flip_distance, P5 PINNED proxy) and ENH-80 per-strike GEX, all of which had been gated on `oi` data quality.

These rows together close the §D.10.1 (RR-ratio independent edge "LIVE pending Phase 0b validation") and update §D.7 (TD-094 OI data limitation, now stale).

| ID | Assumption / Invariant | Status | Evidence | Action |
|---|---|---|---|---|
| **D.12.1** | RR-regime (HIGH/FAIR/LOW/COMPRESSED) has no statistically significant differential effect on signal WR for the ICT cohort, after controlling for zone-class. The 4-way regime gate proposed in §D.10.1 is falsified. | **VALIDATED Session 29 — empirical Phase 0b conditional-WR test** | `phase0b_rr_conditional_wr.py` (462 lines) joined 1,968 regime-tagged signals from `hist_pattern_signals × vol_analytics × hist_atm_option_bars_5m`. Overall WR 46.2%. χ²=1.56 p≈0.30 across regimes. Per-bucket WR deltas all within sampling noise. No regime configuration produces statistically distinguishable WR. Salvage test for COMPRESSED-as-veto-binary via `phase0b_compressed_veto.py` (309 lines): direction right but variance dominates; bootstrap CI [-3.36, +17.69]pp **INCLUDES ZERO**; median P&L negative in both COMPRESSED and non-COMPRESSED buckets. | Per ADR-002 v2 falsification commitment, ENH-97 PIVOTED PROPOSED → SHIPPED-AS-LOGGING. `vol_analytics.rr_regime` column is written by forward-cycle writer + backfilled full year (24,758 rows), but no signal-time gate consumes it. The dimension is preserved as research data, abandoned as a production gate. Cross-link to §D.10.1 (now CLOSED via this falsification). |
| **D.12.2** | The ICT signal cohort is median-negative across all RR regimes when valued as a P&L distribution. Buyer-edge lives in the tails (winning tails for buyer-favourable regime alignments), not at the median. Conditional-WR gates against median-flat cohorts will not survive overhead and slippage even when they are statistically significant at WR. | **VALIDATED Session 29 — empirical P&L distribution analysis** | Phase 0b cohort median P&L negative in both COMPRESSED and non-COMPRESSED buckets (and across all 4 RR regimes individually). The buyer-side edge in MERDIAN's ICT cohort is not concentrated at the median (the typical signal) but in the tails (asymmetric winners on regime-aligned setups). This is consistent with the ADR-002 v2 §P1 buyer-writer inversion model where buyer-edge is sourced from convexity not WR. | Operational discipline: a Phase 0b dimension that improves WR by Δ but leaves the cohort median-negative is not a deployable gate. Cohort median-positivity is a necessary (not sufficient) condition for any conditional gate. Future Phase 0b dimensions must report median P&L alongside WR delta. Codified into ADR-009 §Phase 1 calibration-discipline (S29 case-study addition). |
| **D.12.3** | A Phase 0b verdict of FAIL — i.e. CI for the conditional-WR delta includes zero with adequate N, or median P&L is non-positive — means the dimension is dead as a signal-time gate. The correct response is to abandon the dimension (or ship as logging-only if the data has future-analysis value), not to retry with different parameter calibrations. Variance dominates the signal at MERDIAN's current cohort size; more parameter tweaks ≠ statistical power. | **NEW Session 29 — methodology governance** | ENH-97 RR-regime gate is the first case study. Falsification verdict was clean (χ²=1.56 p≈0.30, CI includes zero, median negative). No parameter sweep on regime-threshold cutoffs would change the verdict because the underlying distribution doesn't separate. Calibration-time parameter sweeps in this state risk re-deriving an in-sample optimum that doesn't generalize (the exact failure mode ADR-009 was created to prevent). | Codified into ADR-009 §Phase 1 graduated-strictness as new sub-rule: "Phase 0b FAIL = abandon dimension as gate; ship as logging if data has future-analysis value; do not retry with parameter tweaks." Cross-link D.8.5 (status-quo silent waiver REJECTED) — same principle applied to a new failure mode (retry-after-fail vs single-cohort-derive). |
| **D.12.4** | `hist_option_bars_1m.oi` is populated 99.9% across the full 12-month window Apr 2025 → Mar 2026 (avg ~1M, max 66M across all months). The S22 TD-094 claim that "Kite historical_data API does not return OI for index option minute bars" no longer applies to current table state — vendor-purchased historical data has replaced the original Kite backfill (replacement date unknown, no commit marker in scope). | **VALIDATED Session 29 — direct per-month rollup query** | `SELECT date_trunc('month', bar_ts) AS m, COUNT(*), AVG(oi)::bigint FROM hist_option_bars_1m GROUP BY m ORDER BY m` returned 12 month buckets Apr 2025 → Mar 2026, each with 3.8M-5.4M rows and avg_oi 875K-1.18M. Plus small May 2026 tail (30,523 rows, avg_oi=0) representing leading-edge slice where vendor updates haven't caught up. Spot side independently verified: `hist_spot_bars_1m` MIN bar_ts 2025-04-01 09:00:59 UTC, MAX 2026-05-15 09:59 UTC — both sides cover full year. | TD-094 RECLASSIFIED-STALE in `tech_debt.md` S29 (retained as documentation history, not active code defect). Unblocks Phase 0b gamma-context dimensions (P1 LONG_GAMMA, P3 flip_distance, P5 PINNED proxy — re-run on full-year cohort is P3b S30 carry-forward). Unblocks ENH-80 per-strike GEX implementation (P5 S30 carry-forward). Operational discipline: when a TD claims a data limitation, verify against current table state before designing around the limitation — vendor data refresh may have closed the gap silently. Codified as CLAUDE.md S29 operational finding. |
| **D.12.5** | `gamma_metrics` table contains ~42,107 pre-existing rows spanning Apr 2025 → May 2026 (~3K rows/month, full 14-month coverage), source and write-date unknown at S29 close. The S29 `backfill_gamma_metrics_to_main.py` run in `--mode gap-only` correctly skipped 29,437 of these and added 7,184 new rows in the gaps. **Cr-vs-raw-rupee unit status of pre-existing rows is UNVERIFIED at S29 close** — pending S30 sample audit. | **PARTIAL Session 29 — empirical row count, unverified unit-correctness** | Per-month rollup `SELECT date_trunc('month', ts), COUNT(*) FROM gamma_metrics GROUP BY m` returned 14 months Apr 2025 → May 2026 with 2.5K-4.2K rows/month. Backfill writer counters: 7,184 written + 29,437 skipped_existing + 2,227 skipped_no_spot + 149 skipped_no_options + 3 skipped_compute_failed = 39,000 vs cycles=36,750 (2,250 multi-bucket overcount). Pre-existing rows imply a prior backfill or live-writer accumulation that S29 was not aware of. | **S30 P0_NEW investigation target** — sample 3-5 rows from Apr/Jul/Oct 2025 in `gamma_metrics`; check `net_gex` magnitude (Cr ±10K-1M range = correct, raw rupees ±trillion range = regressed). If Cr-correct: full-year cohort is research-usable for Phase 0b P5 PINNED re-run + ENH-80; no further backfill needed. If raw: TD-S30-CANDIDATE-1 Cr regression is older than S27 close, widening the git log investigation window. Either way, identify source: query `script_execution_log` for `compute_gamma_metrics_local.py` runs with date_trunc='month' tagging across the pre-existing range. |

**Cross-references:**

- **ENH-97** — SHIPPED-AS-LOGGING Session 29 (Phase 0b FAIL pivot from PROPOSED signal-time gate). D.12.1 + D.12.3 codify the falsification governance.
- **TD-094** — RECLASSIFIED-STALE Session 29 (vendor data populated OI 99.9%). D.12.4 codifies the verification.
- **TD-S30-CANDIDATE-1** — live `compute_gamma_metrics_local.py` Cr unit regression (S30 P0_PRIMARY). D.12.5 codifies the partial verification of pre-existing `gamma_metrics` cohort + the open audit question.
- **§D.10.1** — RR ratio independent edge "LIVE pending Phase 0b validation" → CLOSED via D.12.1 falsification. Cross-link annotated in §D.10 update log row.
- **§D.7** — TD-094 OI data limitation, now stale per D.12.4.
- **ADR-002 v2 §P7** — RR-regime gate falsification commitment realized (the dimension was filed with explicit falsification criterion; D.12.1 + D.12.3 are the realization).
- **ADR-009 §Phase 1** — graduated-strictness holdout principle, with new sub-rule added Session 29: "Phase 0b FAIL = abandon dimension, not retry with parameters." D.12.3 codifies the operational rule.
- **CLAUDE.md S29 operational findings** — B24-B28 + 10 ops findings + 11 settled-decisions; D.12.4 is referenced as "verify TD claims against current table state."
- **`backfill_volatility_snapshots.py` + `backfill_vol_analytics.py` + `phase0b_rr_conditional_wr.py` + `phase0b_compressed_veto.py` + `phase0b_p5_pinned_proxy.py`** — Session 29 scripts. D.12.1-D.12.3 use their outputs as evidence.

**Open follow-ups (S30+):**

1. **S30 P0_NEW gamma_metrics pre-existing cohort audit (D.12.5).** Sample 3-5 rows from Apr/Jul/Oct 2025 in `gamma_metrics`; check `net_gex` magnitude. Identify source via `script_execution_log` query. Determines whether full-year cohort is research-usable as-is.
2. **S30 P3b Phase 0b P5 PINNED proxy re-run on full-year backfilled `gamma_metrics` cohort** (S29 initial run was N=3 due to gamma_metrics sparsity at that point; expected N≈1500-2000 after full-year backfill). Gated on D.12.5 audit verdict.
3. **S30 P4 ADR-005 zone validity codification draft** (S25-S28 carry-forward, unchanged by S29).
4. **S30 ADR-009 status reconciliation** — ADR-009 already ACCEPTED-PRINCIPLE from S25, Phase 0b FAIL case study added S29; CURRENT.md "DRAFT OUTSTANDING" claim is stale and must be corrected in S30 doc-close pass (filed as part of S29 close correction).

---

### D.13 — Session 30 live-cohort cohort-translation save + gate-stack inversion + OB attachment defect

Session 30 opened with the question "what has MERDIAN actually made on its ICT signal cohort?" answered via `s30_path1_live_cohort_pnl_v5.py` on the live `signal_snapshots` ICT-tagged 8-week cohort (2026-03-23 → 2026-05-15). The answer surfaced a cohort-translation save (the principle D.9.3 codified prevented compounding error from S29's Phase 0b investment which had been run on the wrong cohort), a falsification of three production gates (ENH-76/77/88), and an orthogonal signal-builder defect (OB attachment broken at pattern-attachment step, not at detection step).

Five findings codified below. ENH-97 disposition is preserved (S29 SHIPPED-AS-LOGGING on 5m-batch cohort; D.13.1 adds RE-EVALUATE-on-live-cohort queue). Four gates env-flag-disabled (default OFF) pending re-validation on live cohort at N≥30 per pattern. ADR-009 §Phase 1 receives a new sub-rule (cohort-prior gate hazard).

| ID | Assumption / Invariant | Status | Evidence | Action |
|---|---|---|---|---|
| **D.13.1** | The cohort-translation discipline of §D.9.3 is a general principle, not specific to ENH-55. Any signal-time gate whose go/no-go decision was derived from a cohort different from the live runtime cohort requires re-validation on the live cohort before production deployment, regardless of the original derivation's statistical quality. ENH-76/77/88 were all promoted on `hist_pattern_signals` 5m-batch cohort evidence and shipped to production without live-cohort re-validation; S30 measurement showed all three blocked the high-edge live cohort. | **VALIDATED Session 30 — second case study under §D.9.3** | `s30_path1_live_cohort_pnl_v5.py` (live cohort pure-ICT pre-gate-overrides): N=211 setups, WR 58.3%, mean +6.96%, median +3.38%, pooled +1469.13pp across 8 weeks. `s30_gate_audit_and_ob_attachment.py`: of 211 high-edge setups, only 2 (0.9%) passed the existing MERDIAN gate stack with action ∈ {BUY_CE, BUY_PE} and trade_allowed=True. The other 209 (99.1%) were blocked by combinations of LONG_GAMMA (158× rejection in caution-string audit), ENH-77 NIFTY BULL_OB AFTERNOON hard-skip + SENSEX PO3 gate, ENH-88 BULL_FVG standalone block (37×), DTE gate (89×), confidence threshold (172×). | Codified into ADR-009 §Phase 1 as new sub-rule "cohort-prior gate hazard" (cross-link §D.8.2 + §D.9.3). Four env flags added to `build_trade_signal_local.py` commit `2604fc2`: `MERDIAN_ENH76_ENABLED=0`, `MERDIAN_ENH77_ENABLED=0`, `MERDIAN_ENH88_ENABLED=0`, `MERDIAN_TIER_MULT_DISABLE=1`. Defaults disable; re-enable requires N≥30-per-pattern live-cohort validation. Reversible without code change. |
| **D.13.2** | Hold-time persistence on the live cohort is inversely correlated with edge. T+10-20m flip-bucket: WR 64.3% mean +4.00%. T+30m held-full bucket: WR 50% mean -1.80%. T+30m exit timing (Compendium-settled per ENH-37 era) corresponds approximately to the inflection at which the asymmetric winning tail has already capitalized; holding past that point captures more mean-reversion than asymmetric continuation. | **VALIDATED Session 30 — `s30_path1_live_cohort_pnl_v4.py`** | Persistence buckets cut by P&L sign-flip count within the 30m window: 0 flips (immediate winner held) WR 71% mean +12%; 1-2 flips (oscillating) WR 60% mean +4%; held-full-no-flip-no-win WR 50% mean -1.80%. Bucket boundaries match the asymmetric-winners-in-tails finding (D.12.2 within S29 cohort, D.13.3 within S30 cohort): the median signal is unfavorable, but the asymmetric winners are time-localized to the first 10-20 minutes post-entry. | Methodological flag: T+30m exit timing should be re-evaluated against earlier exits on the live cohort. Filed as ENH candidate (S31) for measurement on N≥100 per exit-bucket. Compendium settled is not invalidated (it remains the best available evidence at compendium N); but the live cohort evidence is weakly contradictory at N=211 and warrants formal study. Cross-link TD-S30-NEW-7 (hold-time bucket study scope). |
| **D.13.3** | The pure-ICT pattern stack (BULL_FVG, BEAR_FVG, BULL_OB, BEAR_OB without MERDIAN's downstream gate overrides) shows live-cohort WR meeting or exceeding Compendium settled values for FVG patterns at sub-cohort N≥99. BULL_FVG live WR 58.6% (Compendium settled 50%); BEAR_FVG live WR 58.9% (Compendium settled 46%). SENSEX is overrepresented in the live cohort (123/211 = 58%); SENSEX BULL_FVG sub-cohort WR 64.2% mean +12.49%. | **VALIDATED Session 30 — live cohort empirical** | `s30_path1_live_cohort_pnl_v5.py` per-pattern breakdown after gates-off filter. Two-sided z-test of WR difference vs Compendium settled value (z = (p_live - p_compendium) / sqrt(p_compendium*(1-p_compendium)/n_live)): BULL_FVG z=1.72 p=0.085 (one-sided p=0.043); BEAR_FVG z=2.65 p=0.008. Both clear nominal 5% threshold one-sided; BEAR_FVG clears 1% two-sided. Sub-cohort N: BULL_FVG 99, BEAR_FVG 107. | This contradicts S29 D.12.2 ("ICT cohort median-negative across all regimes") because S29's cohort was 5m-batch `hist_pattern_signals` joined with vol_analytics, not the live `signal_snapshots` ICT-tagged cohort. The two cohorts produce different median P&L distributions. D.13.1 cohort-translation save: S29's median-negative finding stands on its cohort but does NOT generalize. S30 cohort median is +3.38% (positive). Re-evaluation of Phase 0b RR-regime gate on live cohort is queued (S31+) pending live `vol_analytics` accumulation to N≥30 ICT-tagged regime-conditional rows. |
| **D.13.4** | The MERDIAN gate stack as deployed S29-close inverts the edge direction on three context dimensions for the live cohort: (a) gamma regime — LONG_GAMMA WR 55.1% mean +5.37% (gated as BLOCK) vs SHORT_GAMMA WR 73.3% mean +19.67% (gated as PASS) — gating order is correct but the gated-as-PASS bucket is much smaller (N=15 vs N=158) and removing LONG_GAMMA block would re-enable a positive-EV bucket; (b) breadth regime — BEARISH+ALIGNED WR 64.7% (gated as suppressed); (c) vix regime — HIGH WR 61.2% (gated as suppressed on the elevated path). The gate-stack-as-designed favors a small high-WR subset and rejects 99.4% of high-mean signals. | **VALIDATED Session 30 — `s30_gate_audit_and_ob_attachment.py` 8-dimension audit** | Audit script joined `signal_snapshots` × `gamma_metrics` × `volatility_snapshots` × `breadth_features` and binned by each gate's regime field, reporting WR + mean + median + N per bucket. Each of the 8 dimensions (gamma_regime, breadth_regime, vix_regime, ict_mtf_context, dte_bucket, expiry_type, time_bucket, symbol) was independently audited. Findings reported in S30 close notes (see CURRENT.md §S30). | Three gates env-flag disabled at S30 (D.13.1). Two non-gate findings: ict_mtf_context MEDIUM has 6/6 WR=100% but rare (N=6); MEDIUM>LOW>HIGH>VERY_HIGH inverted from Compendium per TD-059 (already known, S30 corroborates). SENSEX dominates volume of edge (58% of cohort). No production change to gamma/breadth/vix gates this session — D.13.1 env-flag pattern requires evidence-first reversal. Investigation queued S31+ (filed as TD-S30-NEW-5). |
| **D.13.5** | OB attachment in the live signal-builder is broken. Of 4,882 spot-price touches inside BULL_OB zone boundaries over the 8-week cohort, only 26 (0.5%) were tagged `ict_pattern = BULL_OB` in `signal_snapshots`. Of 3,139 spot touches inside BEAR_OB zone boundaries, **zero** were tagged `ict_pattern = BEAR_OB`. Detection (in `detect_ict_patterns.py` writing to `ict_zones`) is working — zones exist with correct pattern_type. The defect is at signal-attachment time in `enrich_signal_with_ict()` or upstream — touches inside OB zone boundaries are systematically not being tagged as OB patterns in the signal row. Most touches end up tagged NONE or as FVG when adjacent. | **VALIDATED Session 30 — `s30_gate_audit_and_ob_attachment.py` zone-touch audit** | Audit joined `ict_zones` (where status='ACTIVE' AND pattern_type IN ('BULL_OB','BEAR_OB')) against `signal_snapshots` on (symbol, ts within zone validity window, spot inside [zone_low, zone_high]). 35 OB zones over 8 weeks total. Per-zone counts in audit output. BULL_OB attachment rate: 26/4882 = 0.5%. BEAR_OB attachment rate: 0/3139 = 0.0%. | Filed as **TD-S30-NEW-3 (S1)** in `tech_debt.md`: "OB attachment broken at signal-builder layer." Investigation queued (S31) to localize defect in `enrich_signal_with_ict()` (or wherever pattern-attachment happens between zone-existence check and `signal_snapshots.ict_pattern` write). Once fixed, D.13.3 BULL_OB and BEAR_OB sub-cohort N grows from 5 + 0 to estimated 30-80 each, enabling per-OB-pattern re-validation. Until fixed, all gate-stack analyses on OB patterns are misleading (the OB rows the analysis sees are a 0.5% non-random sample). |

**Cross-references:**

- **ADR-009 §S30 case study** — codifies the cohort-translation save + cohort-prior gate hazard sub-rule. D.13.1 is the Assumption-Register-side codification of the same finding.
- **§D.9.3** — original cohort-translation discipline (S26). D.13.1 promotes from "specific to ENH-55" to "general principle."
- **§D.12.2** — S29 finding "ICT cohort median-negative across all regimes" applies on `hist_pattern_signals` cohort. D.13.3 finds the opposite on `signal_snapshots` cohort. Both stand on their respective cohorts; cohort-translation discipline reconciles.
- **§D.10.1** — RR-regime gate was CLOSED via D.12.1 falsification (5m-batch cohort). D.13.1 adds RE-EVALUATE-on-live-cohort queue (S31+ gated on N≥30 live vol_analytics ICT-tagged regime rows).
- **TD-S30-NEW-3** — OB attachment broken at signal-builder layer (S1). D.13.5 is the empirical finding.
- **TD-S30-NEW-4, TD-S30-NEW-5, TD-S30-NEW-6, TD-S30-NEW-7** — auxiliary findings (DTE=0 cohort, gate inversion, replay ENH-88 parity, hold-time bucket study). See `tech_debt.md` S30 entries.
- **MERDIAN_Enhancement_Register.md ENH-97 RE-EVALUATE** — disposition queue update (S31).
- **Commit `2604fc2`** — `build_trade_signal_local.py` four env-flag patches deployed Local + AWS. `.env` updated both sides with `MERDIAN_TIER_MULT_DISABLE=1` (only flag ON; other three explicit `=0`).
- **CLAUDE.md S30 settled-decisions** — bullet for live cohort empirical edge; bullet for ENH-76/77/88 disabled; bullet for OB attachment broken next-session investigation.

**Open follow-ups (S31+):**

1. **S31 P0_NEW OB attachment investigation (D.13.5 / TD-S30-NEW-3).** Localize defect in `enrich_signal_with_ict()` or upstream. Expected ~1 session. Unblocks per-OB-pattern live-cohort re-validation.
2. **S31 P1 ENH-97 RE-EVALUATE on live cohort (D.13.1 / D.13.3).** Pending live `vol_analytics` accumulation to N≥30 per regime × pattern. Estimate ~3 calendar months. Re-run Phase 0b RR-conditional-WR test on live cohort only.
3. **S31 P2 hold-time bucket study (D.13.2 / TD-S30-NEW-7).** Measure WR + mean P&L per exit-time bucket on N≥100 each. Likely candidate for ADR if T+30m settled timing is challenged.
4. **S31 P3 ENH-76/77/88 individual re-validation (D.13.1).** As each pattern's N≥30 on live cohort, evaluate one at a time. May result in some gates being re-enabled (with parameter changes), some being permanently dropped, or a mix. Sub-rule's "cohort-prior gate hazard" disposition guides.
5. **S31 P4 replay infrastructure parity (ENH-88 / TD-S30-NEW-6).** ADR-008's `replay_build_trade_signal.py` lacks ENH-88 (header line 15 lists ENH-53/55/76/77/78). Replay cannot validate ENH-88 retro-fits on historical data until parity is restored. ~30 min patch work.

---

This register itself follows Doc Protocol v4 Rule 9.5: superseded rows are annotated, never deleted. Significant register events are recorded here.

| Date | Session | Event |
|---|---|---|
| 2026-05-09 | Session 23 | Created. V15.1 Appendix D content promoted. Refreshed for ICT-era post ADR-007. Status assignments grounded in V18F evidence + V19 SRB rules + Session 16/17 findings. |
| 2026-05-10 | Session 25 | **§D.8 added** — Phase α Q4 calibration discipline answer codified into methodology assumptions. 5 rows: D.8.1 (single-cohort REJECTED), D.8.2 (graduated holdout split LIVE pending ADR-009), D.8.3 (Exp 15-era prospective parity check LIVE), D.8.4 (Phase 2 walk-forward DEFERRED to Y2), D.8.5 (status-quo silent waiver REJECTED). ADR-009 governance language draft + 3 open follow-ups recorded. |
| 2026-05-10 | Session 26 | **§D.9 added** — ENH-55 momentum opposition gate hypothesis (Exp 20) REFUTED by 24-day production cohort (2026-04-17 → 2026-05-10, N=44 opposed-but-winning trades at 79.5% WR; aligned bucket 54.3% WR — symmetric claim also REFUTED). 5 rows: D.9.1 (opposition rule REFUTED), D.9.2 (alignment bonus REFUTED), D.9.3 (cohort-translation hypothesis REFUTED), D.9.4 (TD-101 writer fix CONFIRMED orthogonal correct), D.9.5 (env-flag disablement DECIDED). First substantive application of D.8.3 prospective parity check; first parameter to fail post-codification. Cross-refs: TD-101 (writer fix RESOLVED commit `3cb84e2`), ENH-55 (status note ENV-DISABLED), CLAUDE.md B19 (OI-18 propagation lesson). 4 open follow-ups recorded (ADR-009 case study, re-validation protocol, retroactive blocks audit, cohort-translation parameter inventory). |
| 2026-05-11 | Session 27 | **§D.10 added** — ADR-002 v2 acceptance new live assumptions filed. 8 rows: D.10.1 (RR ratio independent edge LIVE pending Phase 0b), D.10.2 (buyer/writer inversion LIVE architectural), D.10.3 (walk-from-ATM flip definition VALIDATED via TD-NEW-2), D.10.4 (Crore unit canonical VALIDATED via TD-NEW-3), D.10.5 (Positioning Landscape five named scalars LIVE pending ENH-81), D.10.6 (six-scenario dealer-flow simulator LIVE pending ENH-81), D.10.7 (PINNED regime empirically observable LIVE pending Exp 23), D.10.8 (vanna/charm Phase 3 only LIVE pending Phase 3 backtest). Cross-refs: ADR-002 v2 (`docs/decisions/ADR-002-market-structure-philosophy.md`), D.7 validation queue, D.8.2 holdout discipline (cohort-translation lesson D.9.3 applies), TD-NEW-2 RESOLVED commit `241f943` (walk-from-ATM codification), TD-NEW-3 RESOLVED commit `241f943` (Crore unit codification). 4 open follow-ups (ENH-84 vol_analytics build, Phase 0b overlay calibration, Phase 0c methodology selection, ENH-85 Phase 3 prep). |
| 2026-05-13 | Session 28 | **§D.11 added** — Session 28 production architecture findings + Phase 0 P1 closure. 4 rows: D.11.1 (shadow-vs-live separation invariant VALIDATED via TD-NEW-12 resolution + cross-validated AWS smoke), D.11.2 (dte from result.ts.date() invariant VALIDATED via TD-NEW-4 resolution), D.11.3 (Python 3.10 microsecond normalization invariant VALIDATED via TD-NEW-13 resolution), D.11.4 (Phase 0 P1 broken-window backfill CLOSED 587/587 rows clean, Phase 0b/0c data prerequisite met). **§D.10 open follow-ups updated** — items 1/4 marked Session 28 P2 closure (ENH-84 + ENH-85 formal Enhancement Register filing complete); item 2 data prerequisite CLOSED (P1 backfill); item 3 data prerequisite CLOSED (P1 backfill). D.10.3 + D.10.4 cross-validated rows extended with Session 28 backfill evidence (587-row corroboration of TD-NEW-2/3 patches). 3 open follow-ups recorded (`gamma_metrics_shadow` history backfill optional, TD-NEW-11 Topology gap, MALPHA-as-third-environment Topology gap — last two applied in S28 close docs). Cross-refs: TD-NEW-12 RESOLVED commits `72622a9` + `de23467`, TD-NEW-4 RESOLVED commit `72622a9`, TD-NEW-13 RESOLVED commit `447634c`, CLAUDE.md B22 (cross-version Python compat) + B23 (Phase 0 closure meta-pattern), MERDIAN_Enhancement_Register.md ENH-84 + ENH-85 PROPOSED filings, MERDIAN_Deployment_Topology.md §6.5 (shadow boundary update). |
| 2026-05-16 | Session 29 | **§D.12 added** — Phase 0b falsification realization + vendor data verification. 5 rows: D.12.1 (RR-regime gate falsified — χ²=1.56 p≈0.30 on N=1,968; COMPRESSED salvage CI [-3.36,+17.69]pp includes zero; ENH-97 PIVOTED PROPOSED → SHIPPED-AS-LOGGING per ADR-002 v2 falsification commitment); D.12.2 (ICT cohort median-negative across all regimes — buyer-edge in tails not median; cohort median-positivity is necessary not sufficient condition for any conditional gate); D.12.3 (NEW methodology governance — Phase 0b FAIL = abandon dimension as gate, not retry with parameter tweaks; codified into ADR-009 §Phase 1 as new sub-rule); D.12.4 (vendor data populated `hist_option_bars_1m.oi` 99.9% across 12 months Apr 2025 → Mar 2026, avg ~1M; TD-094 RECLASSIFIED-STALE; spot side independently verified MIN 2025-04-01 09:00 UTC, MAX 2026-05-15 09:59 UTC; unblocks Phase 0b gamma-context P1/P3/P5 + ENH-80); D.12.5 (PARTIAL — `gamma_metrics` has 42,107 pre-existing rows ~3K/month Apr 2025 → May 2026, source unknown, **Cr-vs-raw-rupee unit status UNVERIFIED**, audit is S30 P0_NEW). **§D.10.1 CLOSED via D.12.1 falsification** (annotation: status updated to CLOSED, evidence cross-link D.12.1). **§D.7 stale** (TD-094 OI limitation no longer applies). 4 open follow-ups recorded (S30 P0_NEW gamma_metrics cohort audit, S30 P3b Phase 0b P5 PINNED proxy re-run gated on D.12.5, S30 P4 ADR-005 zone validity codification, S30 ADR-009 status reconciliation — CURRENT.md DRAFT OUTSTANDING claim is stale). Cross-refs: ENH-97 SHIPPED-AS-LOGGING (Enhancement Register S29 status update), TD-094 RECLASSIFIED-STALE (`tech_debt.md` S29), TD-S30-CANDIDATE-1 (`tech_debt.md` S29), ADR-002 v2 §P7 falsification commitment realized, ADR-009 §Phase 1 new sub-rule added S29 (case-study addition), CLAUDE.md S29 operational findings (verify TD claims against current table state). Scripts producing evidence: `phase0b_rr_conditional_wr.py` (462 lines), `phase0b_compressed_veto.py` (309 lines), `phase0b_p5_pinned_proxy.py` (320 lines), `backfill_volatility_snapshots.py` (Stage 1 19,520 rows), `backfill_vol_analytics.py` (Stage 2 24,758 rows), `backfill_gamma_metrics_to_main.py` (gap-only mode, 7,184 written + 29,437 skipped-existing = D.12.5 discovery). |
| 2026-05-17 | Session 30 | **§D.13 added** — Live-cohort cohort-translation save + gate-stack inversion + OB attachment defect. 5 rows: D.13.1 (cohort-translation discipline general principle; D.9.3 promoted from ENH-55-specific to general; ENH-76/77/88 + tier mult env-flag disabled commit `2604fc2`); D.13.2 (persistence inversely correlated with edge — T+10-20m flip best WR 64.3%, T+30m held-full worst WR 50%); D.13.3 (pure-ICT live cohort WR 58.3% mean +6.96% median +3.38% N=211 8wk; BULL_FVG 58.6% N=99 / BEAR_FVG 58.9% N=107 both beat Compendium settled; two-sided z-test clears 5% threshold); D.13.4 (gate stack inverts on gamma/breadth/vix — LONG_GAMMA WR 55.1% blocked, SHORT_GAMMA WR 73.3% allowed but rare; investigation queued S31+); D.13.5 (OB attachment broken at signal-builder layer — BULL_OB attachment 0.5% on 4,882 zone touches, BEAR_OB attachment 0% on 3,139 — filed TD-S30-NEW-3). ADR-009 §S30 case study added (cohort-translation save + cohort-prior gate hazard sub-rule). ENH-97 disposition preserved (S29 SHIPPED-AS-LOGGING on 5m-batch cohort); RE-EVALUATE-on-live-cohort queue added (S31+ gated on live vol_analytics N≥30 per regime × pattern). 5 open follow-ups recorded (S31 P0 OB attachment fix, S31 P1 ENH-97 RE-EVALUATE, S31 P2 hold-time bucket study, S31 P3 individual gate re-validation, S31 P4 replay ENH-88 parity). Cross-refs: ADR-009 §Phase 1 (cohort-prior gate hazard sub-rule added), §D.9.3 (original cohort-translation discipline), §D.10.1 (RR-regime CLOSED via D.12.1 stays closed; RE-EVALUATE queue added in §D.13.1), §D.12.2 (S29 ICT cohort median-negative stands on its cohort; D.13.3 finds opposite on live cohort — both valid per cohort-translation discipline), TD-S30-NEW-3 (OB attachment broken — filed), TD-S30-NEW-4 (DTE=0 cohort behavior — filed), TD-S30-NEW-5 (gate inversion gamma/breadth/vix — filed), TD-S30-NEW-6 (replay ENH-88 parity — filed), TD-S30-NEW-7 (hold-time bucket study scope — filed), commit `2604fc2` (env-flag deploy), MERDIAN_Enhancement_Register.md ENH-97 RE-EVALUATE queue. Scripts producing evidence: `s30_path1_live_cohort_pnl_v5.py` (live cohort pre-gate-overrides), `s30_gate_audit_and_ob_attachment.py` (gate audit + OB attachment defect), `s30_target_days_audit.py` (per-setup blocked/allowed decomposition May 5/6/13/14), `s30_path1_live_cohort_pnl_v4.py` (persistence × P&L), `apply_s30_patches.py` (env-flag patches). |
| 2026-05-21 | Session 31-B | **§D.14 added** — ADR-004 Wave 1 implementation outcomes + ADR-009 Phase-1 holdout split validation. 5 rows: **D.14.1** (H FVG retest edge VALIDATED on holdout cross-symbol cross-direction — NIFTY H BEAR_FVG retest 75.0→85.2 +10.2pp N=29; NIFTY H BULL_FVG retest 64.3→90.0 +25.7pp N=27; SENSEX H BEAR_FVG retest 76.6→92.0 +15.4pp N=30; SENSEX H BULL_FVG retest 70.2→80.8 +10.6pp N=30; deltas all in favorable direction, holdout exceeds train — opposite of overfit signature; codified into ENH-102 live routing rule with `MERDIAN_ENH102_ENABLED` env flag default OFF pending D.13.1 cohort-translation pre-flight on `signal_snapshots` runtime cohort); **D.14.2** (M5 FVG standalone REFUTED on holdout — all four (symbol × direction) buckets formation + retest collapse to 45-57% at-or-below baseline; coin-flip confirmed; OB-context lift from Exp 15 90-min lookback remains separate test and is NOT refuted by this); **D.14.3** (OB retest paradox — formation > retest by 30-40pp across all OB cells: NIFTY M5 BEAR_OB form 87.0 vs retest 47.4; NIFTY M5 BULL_OB form 77.4 vs retest 58.7; SENSEX M5 BEAR_OB form 82.6 vs retest 47.4 — formation entry beats waiting for retest entry for OB patterns; opposite of FVG behavior where retest > formation; possible explanations: (a) OB retest-entry signal sub-class needs different timing; (b) detector mode='retest' for OBs is mis-classified vs canon; (c) OB retest is correct semantics but the trade architecture should be formation-only for OBs and retest-primary for FVGs; investigation queued Wave 2 or earlier); **D.14.4** (SENSEX H DISPLACEMENT_DOWN reversal anomaly worth investigation — 38.7% train → 28.6% holdout consistent reversal pattern, not edge in assumed direction; NIFTY H DISPLACEMENT_DOWN 68.2% train → 53.3% holdout overfit but at least directionally edge; SENSEX consistently inverse suggests possible inverse signal — flipping the direction of trade for SENSEX H DISPLACEMENT_DOWN may produce 65-71% WR; alternatively detector directionality bug in `_detect_displacement()` for SENSEX path; investigation P3 S32 carry-forward); **D.14.5** (ADR-004 §5.1 body-only OB body bounds + close-through-only breach rule canonical — wave 1 implementation establishes the canonical interpretation; wickExtended toggle deferred to wave 1.5 pending operator decision after H-FVG-retest signal isolation deployment; D-OB definition gap from original Reserved scope of ADR-004 is now subsumed under §5.1 body-only canon). **Cross-refs:** ADR-004 §5.1 OB body-only (D.14.5 source), ADR-004 §5.2 FVG canonical (D.14.1 source primitive type), ADR-009 §Phase 1 holdout split (D.14.1 + D.14.2 validation discipline), ENH-100 (outcomes magnitude extension gates ENH-101 + answers six follow-up questions), ENH-101 (stop-loss optimization blocked by ENH-100), ENH-102 (H-FVG-retest signal isolation — D.14.1 codified as live routing rule), TD-S31B-NEW-1 (Pine v6 visual descope — Pine v6 ergonomic catalog 6 entries), D.13.1 (cohort-translation discipline applies to ENH-102 deployment — live-cohort re-validation required before flag flip at N≥30 with 10pp transfer tolerance per D.8.2). **4 open follow-ups recorded:** (1) S32 P0_PRIMARY ENH-100 outcomes-magnitude extension — gates ENH-101 + answers operator's six S31-B Q2 follow-up questions; (2) S32 P1 ENH-101 stop-loss optimization from MAE distribution (blocked by ENH-100); (3) S32 P1 ENH-102 H-FVG-retest signal isolation — independent for filing, live-deploy blocked by ENH-100 + D.13.1 pre-flight; (4) S32 P3 SENSEX H DISPLACEMENT_DOWN reversal anomaly investigation per D.14.4 (possible inverse signal or detector directionality bug). **Scripts producing evidence:** `ict_primitives.py` (613 lines — 5 detectors per ADR-004 §5.1/§5.2/§6.1/§7.1/§7.2), `build_ict_primitives.py` (1140 lines — writer + outcomes computation; backfill executed 2026-05-21 18:57 IST; NIFTY 8838 + SENSEX 10561 primitives + 1:1 outcomes in 156.3s), ADR-009 Phase-1 holdout split SQL (FULL OUTER JOIN side-by-side per (symbol, TF, primitive_type, mode) with N≥30 per side filter; train 2025-04-01→2026-02-28 vs holdout 2026-03-01→2026-05-19), Q1 counts sanity check SQL (monotonic W<D<H<M5 + PDH=276-277 + PWH/PWL=59 + PMH/PML=13), Q2 WR sanity check SQL (in-sample full window per TF×type×mode). |
| 2026-05-22 | Session 33 | **§D.15 added** — ADR-011 chain-table held-strike ATM PnL rebuild + retest cohort empirical validation of ADR-009 §Phase 1. 4 rows: D.15.1 (chain table canonical / aggregated table for wick-reversal only — ENH-106 v7 IMPLEMENTED), D.15.2 (M5 OB retest paradox confirmed at option-PnL level — REFUTED as edge), D.15.3 (DTE bucketing 3-bucket structure: DTE=0 feast-or-famine, DTE=1-2 M5 sweet spot, DTE≥3 H-TF home — VALIDATED), D.15.4 (D-TF cells need horizons longer than 30m/60m — HYPOTHESIS pending MFE inspection). |
| 2026-05-24 | Session 34 | **§D.16 added** — Universal capture audit + spot-anchored SL doctrine + chain coverage gap. 4 rows: D.16.1 (capture ≥99% across 14-month window — VALIDATED), D.16.2 (selection is the binding constraint — VALIDATED problem articulated), D.16.3 (spot-anchored SL doctrine — VALIDATED codified as ADR-012), D.16.4 (post-Apr-2026 `hist_option_bars_1m` coverage gap — OPEN, TD-S34-NEW-4 filed). |
| 2026-05-25 | Session 35 | **§D.17 added** — Chain-data tier transition resolved as three-surface architectural boundary + HOCS strike-coverage structural limit + Breeze backfill operational viability + ADR-012 v9 writer implementation. 4 rows: D.17.1 (tier transition = three surfaces — VALIDATED via v8/v8.1/v8.2 patches), D.17.2 (HOCS strike-coverage structural limit — VALIDATED & TD-S35-NEW-1 filed), D.17.3 (Breeze API operationally viable — VALIDATED via 107,630-row surgical fill, foundation for ADR-013 PROPOSED), D.17.4 (ADR-012 v9 writer single-cell validation — VALIDATED n=5, cohort expansion deferred S36). **§D.16.4 annotated CLOSED-MECHANICALLY** — root cause confirmed as vendor → MERDIAN-ingest tier transition at 2026-04-01 boundary; ENH-106 v8/v8.1/v8.2 patches deployed dual-source chain reader; 81% recovery (331/409 zone-primitive denom) post-recompute; residual 75 attributed to D.17.2 strike-coverage structural limit. **§D.16.3 annotated IMPLEMENTED via ENH-106-SL v9** — writer-side ADR-012 SL evaluation shipped, single-cell validation cleared on 2026-05-14 NIFTY M5 cohort. |

---

### D.14 — Session 31-B ADR-004 Wave 1 implementation + ADR-009 Phase-1 holdout validation

| Row | Assumption | How set | Status | Evidence / annotation |
|---|---|---|---|---|
| **D.14.1** | H FVG retest is a validated edge cross-symbol cross-direction | S31-B ADR-009 Phase-1 holdout split (train 2025-04-01→2026-02-28 vs holdout 2026-03-01→2026-05-19) executed on full `ict_primitive_outcomes` cohort via FULL OUTER JOIN side-by-side per (symbol, TF, primitive_type, mode); N≥30 per side filter applied | **VALIDATED on holdout (pending D.13.1 cohort-translation pre-flight on signal_snapshots runtime cohort)** | Four buckets simultaneously HOLD AND STRENGTHEN: NIFTY H BEAR_FVG retest 75.0→85.2 (+10.2, N=29 holdout); NIFTY H BULL_FVG retest 64.3→90.0 (+25.7, N=27); SENSEX H BEAR_FVG retest 76.6→92.0 (+15.4, N=30); SENSEX H BULL_FVG retest 70.2→80.8 (+10.6, N=30). Deltas all in favorable direction — opposite of overfit signature. Codified as **ENH-102** with env-flag default OFF pending live-cohort re-validation at N≥30 per (symbol, direction) with 10pp transfer tolerance per D.8.2 graduated-strictness. Live-deploy additionally gated on **ENH-100** for sizing/stop calibration prerequisite. |
| **D.14.2** | M5 FVG standalone is a coin-flip on holdout — NOT a standalone edge | S31-B ADR-009 Phase-1 holdout split surfaced collapse: all four (symbol × direction) M5 FVG buckets formation + retest fall to 45-57% on holdout, at-or-below baseline | **REFUTED as standalone** (LIVE for OB-context-conditional sub-class per Exp 15 90-min lookback — separate test) | M5 FVG standalone collapse: NIFTY M5 BEAR_FVG form 48.7→53.2; NIFTY M5 BULL_FVG form 51.2→47.8; SENSEX M5 BEAR_FVG form 50.9→49.1; SENSEX M5 BULL_FVG form 49.3→45.1; all retest variants similar 51-56% range. **Standalone refuted.** OB-context-conditional sub-class (BULL_FVG with recent BULL_OB at 90-min lookback per Exp 15 Section 18) is a separate hypothesis — not validated AND not refuted by this S31-B test (the sub-class requires joining `ict_primitives` against itself for prior BULL_OB events; that join is not in current Q2 SQL but is a candidate analysis post-ENH-100). ENH-88's BULL_FVG cluster gate (env-disabled per S30 D.13.1) remains separately pending re-validation. |
| **D.14.3** | OB retest paradox: formation > retest by 30-40pp for OB patterns across all TFs (opposite of FVG behavior) | S31-B Q2 WR sanity check in-sample full window: M5 OB 77-87% formation > 47-58% retest by 30-40pp; D/H FVG 56-68% formation < 71-88% retest opposite pattern | **LIVE (paradox documented, investigation queued)** | Possible explanations under investigation: (a) OB retest-entry signal sub-class needs different timing parameters (current detector treats retest as touch-into-zone after breach; OB-specific retest may need depth-into-zone or impulsivity filter); (b) detector mode='retest' for OBs is mis-classified vs ICT canon (a "retest" in ICT for OB may be different semantics than what `_detect_ob_retest()` implements); (c) trade architecture asymmetry — OBs are formation-only signals, FVGs are retest-primary; consistent with ICT instructor interpretations that OBs are "the cause" and FVGs are "the price imbalance that mean-reverts" — they're different signal classes architecturally. Investigation deferred to Wave 2 or earlier session; not blocking for ENH-100/101/102 deployment (which focus on FVG retest where retest > formation holds). |
| **D.14.4** | SENSEX H DISPLACEMENT_DOWN is a candidate inverse signal or detector directionality bug | S31-B ADR-009 Phase-1 holdout: SENSEX H DISPLACEMENT_DOWN 38.7% train → 28.6% holdout consistent reversal pattern; NIFTY H DISPLACEMENT_DOWN 68.2% train → 53.3% holdout (overfit but directionally edge); SENSEX consistently inverse suggests possible inverse signal | **LIVE (investigation queued S32 P3)** | Two hypotheses: (a) **inverse signal** — SENSEX H DISPLACEMENT_DOWN actually predicts price-RISE (regime-specific structural property); flipping direction-of-trade for SENSEX H DISPLACEMENT_DOWN may produce 65-71% WR (1 - observed inverse rate); (b) **detector directionality bug** — `_detect_displacement()` may have a SENSEX-specific path with inverted sign convention; cross-check vs `_detect_displacement()` source code S32 task. Investigation deferred to S32 P3; if (a) confirmed, codify as ENH-103 with direction-flip routing rule; if (b) confirmed, file as TD with detector patch. NIFTY M5 DISPLACEMENT_DOWN holds well (81.3→76.7 holdout) — pattern is not universally inverted. |
| **D.14.5** | ADR-004 §5.1 body-only OB body bounds + close-through-only breach rule is canonical; wickExtended toggle deferred to wave 1.5 | S31-A ADR-004 §5.1 spec authored; S31-B Wave 1 implementation in `ict_primitives.py` per spec; backfill produced 8838 NIFTY + 10561 SENSEX primitives + outcomes (1:1) over 14-month window in 156.3s; ADR-004 status moves Reserved → IMPLEMENTED via Wave 1 | **VALIDATED — IMPLEMENTED** | OB body bounds = candle body only (open/close), wick excluded. Breach rule = close-through-only (price must close beyond zone, intra-bar wick-pierce does not breach). Wave 1 produces healthy OB emission counts (NIFTY W BULL_OB 30 / W BEAR_OB 28 / M5 BULL_OB 730 / M5 BEAR_OB 671; comparable for SENSEX). Implementation has wickExtended toggle scaffolded in detector but defaulted to false; wave 1.5 candidate would enable toggle if operator decision favors broader zone capture. **D-OB definition gap from original Reserved scope of ADR-004 (TD-049) now subsumed under §5.1 body-only canon.** D-zone validity (TD-050) addressed by zone-life logic in `ict_zones` table layer; PDH/PDL ±20pt hardcoded (TD-051) replaced by canonical PDH/PDL detection in `ict_primitives` §6.1. |

---

### D.15 — Session 33 ADR-011 chain-table held-strike ATM PnL rebuild + retest cohort empirical validation of ADR-009 §Phase 1

| Row | Assumption | How set | Status | Evidence / annotation |
|---|---|---|---|---|
| **D.15.1** | Chain table `hist_option_bars_1m` is canonical source for held-strike ATM PnL; aggregated `hist_atm_option_bars_5m` is for wick-reversal analysis only | S33 architectural diagnosis: ENH-100 v3 used `hist_atm_option_bars_5m` with same-strike enforcement → 50-70% attrition (vendor's pre-picked ATM rolls on spot drift ~0.5%); experiments 1-50 used same aggregated table WITHOUT same-strike check → inflated coverage but mathematically wrong (comparing two different vendor-rolled strikes' premiums). ENH-106 v7 reads chain table with manual ATM picking via existing `STRIKE_INTERVAL` constant (NIFTY/50, SENSEX/100) + held strike across horizons. | **VALIDATED — IMPLEMENTED via ADR-011 / ENH-106** | Post-v7 backfill (NIFTY+SENSEX 2025-04-01→2026-05-19, 19,399 outcomes in 1775.9s): coverage 20-44× lift on populated cells (NIFTY M5 BEAR atm_pnl_30m 85→2650, SENSEX M5 BULL 60→2662); median PnL flipped universally negative (v6) → positive on every directional cell with real spot edge (NIFTY M5 BULL_OB +17.2% median 30m / 82.1% opt WR / N=28; NIFTY H DISPLACEMENT_DOWN +34.7% median / 100% / N=17; SENSEX H DISPLACEMENT_UP +22.8% median 30m / 94.4% / N=18). The "negative median everywhere" of ENH-100 v6 was an artifact of source-table mismatch (self-selection bias against winners via same-strike check on rolling-ATM source), not a property of the underlying edge. TD-S32-NEW-4 vendor cross-tier drift on `hist_atm_option_bars_5m` made architecturally moot. Aggregated table remains read by `experiment_26/27/27b` wick experiments — orphan-candidate when those retire (TD-S33-NEW-1). |
| **D.15.2** | M5 OB retest paradox CONFIRMED at option-PnL level — FALSIFIED as edge | S33 v7 retest cohort cross-symbol: NIFTY M5 BEAR_OB retest median 30m -11.1% / opt WR 35.3% / N=17; SENSEX M5 BEAR_OB retest median -5.1% / opt WR 43.5% / N=23; SENSEX M5 BULL_OB retest median -4.3% / opt WR 47.8% / N=23. All M5 retest cells (FVG + OB) negative median across NIFTY+SENSEX. | **REFUTED — M5 OB retest is NOT an edge** | D.14.3's "OB retest paradox investigation queued" promoted to **falsified at option-PnL level**. Trade M5 OBs at formation only (formation cohort: NIFTY M5 BULL_OB +17.2% median 30m / WR 82.1%; BEAR_OB +20.3% median / WR 85.7%). Retest path on M5 OBs is FALSIFIED — no investigation needed; the paradox was real, not a sampling artifact. ICT-instructor architectural framing (OBs are formation-primary signals; FVGs are retest-primary signals) is the empirical reality. OB attachment defect at signal-builder layer (TD-S30-NEW-3 P0_TERTIARY) becomes lower priority since OB retest path is not the tradeable surface. |
| **D.15.3** | DTE bucketing reveals 3-bucket structure on Tier 1 cells — DTE=0 feast-or-famine, DTE=1-2 M5 sweet spot, DTE≥3 H-TF home | S33 v7 DTE bucketing on Tier 1 cells (M5 BULL_OB / M5 BEAR_OB / M5 DISPLACEMENT_UP/DOWN / H DISPLACEMENT_UP/DOWN; per-cell N≥10 filter): DTE=0 NIFTY M5 DISPLACEMENT_UP median 30m +58.7% / median 60m +24.7% (drops); DTE=0 NIFTY M5 DISPLACEMENT_DOWN median 30m +11.0% / median 60m **-4.8%** (turns negative); DTE=1-2 NIFTY M5 BEAR_OB median 30m +24.4% / WR 80%; DTE=1-2 SENSEX M5 DISP_UP median 60m +38.3% / WR 83.3%; DTE≥3 NIFTY H DISP_DOWN median 60m +37.5% / WR 100%; DTE≥3 SENSEX H DISP_UP median 60m +48.2% / WR 92.3%. | **VALIDATED — three operational rules** | (a) **DTE=0:** gamma-driven, exit at 30m mandatory; theta accelerates after favorable move plateaus, especially on DOWN-direction (median 60m turns negative -4.8% to -9.2%). (b) **DTE=1-2:** M5 sweet spot — residual gamma + slower theta; OK to hold to 60m. (c) **DTE≥3:** H-TF home — room for spot move to play out without theta crushing premium; median 60m peaks at +37-48% on H DISPLACEMENT cells. **Direction interacts with DTE on DTE=0:** UP-direction holds positive at 60m, DOWN-direction does not. Not yet investigated whether the asymmetry is a vendor put/call premium structural property or a regime-specific edge. |
| **D.15.4** | D-TF cells need horizons longer than 30m/60m to be tradeable on options — hypothesis | S33 v7 D-TF cohorts: NIFTY D BULL_FVG formation median 30m -1.99% / median 60m -0.92% despite spot WR 67.9% / mean_spot_30m_dir +0.063%; SENSEX D BULL_FVG formation median 30m -4.77% / median 60m -5.71% despite spot WR 66.7% / mean_spot_30m_dir +0.042%. Even retest cohort positive on D-TF (NIFTY D BULL_FVG retest median 30m +11.7%, median 60m +22.3%, EOD +5.7% / WR 88.2% / N=17) but median EOD lower than median 60m. | **HYPOTHESIS — investigation pending** | D-TF spot edge is real (~67% WR, +0.05% mean spot move) but plays out over hours not 30 minutes. Theta drains during 30m/60m hold window before spot move materializes. `mfe_pct` + `time_to_mfe_min` columns (ENH-100) may already answer "what horizon would D-TF cells be tradeable at" — SQL-only analysis, no writer work needed. Carry-forward as TD-S33-NEW-4 S3. If MFE peaks at +90-120m on D-TF cells, consider extending `option_pnl_*` horizons OR using `forward_120m_pct` × derived option-PnL-from-spot model (not held-strike chain read which requires writer work). |

---

### D.16 — Session 34 universal capture audit + spot-anchored SL doctrine empirical foundation + chain-coverage gap

| Row | Assumption | How set | Status | Evidence / annotation |
|---|---|---|---|---|
| **D.16.1** | Universal capture rate of ≥1% intraday index moves by MERDIAN ICT primitive layer is ≥99% over the 14-month vendor window | S34 audit: 119 ≥1% intraday move-days across 2025-04-01 → 2026-05-19 (57 NIFTY + 62 SENSEX, parity threshold both indices) joined to `ict_primitives` with direction-aligned filter (BEAR for DOWN, BULL for UP) + price-zone interaction filter (zone_low ≤ day_high AND zone_high ≥ day_open) + status IN ('ACTIVE','MITIGATED') + source_bar_ts < day_start_ts. Highest-priority primitive per day picked by tf_rank (W=4 > D=3 > H=2 > M5=1) then most recent. | **VALIDATED** | 118 of 119 days captured (99.2%); one miss NIFTY 2025-05-20 DOWN (D DISPLACEMENT_DOWN existed but price didn't reach zone). Origin structure distribution on captured cohort: BEAR_FVG 56 (47%) / BULL_FVG 43 (36%) / BULL_OB 12 (10%) / BEAR_OB 7 (6%) — FVG dominates 84% of origins. Timeframe distribution: D 56 (47%) / W 32 (27%) / H 17 (14%) / M5 13 (11%) — D and W combine 74% of origins. Implication: **MERDIAN's ICT primitive layer sees every ≥1% move structurally; capture is not the binding constraint on trade discoverability.** Standout zone-reuse cases observed: 2026-04-06 BULL_FVG W served as origin for 4 separate ≥1% UP days (Apr 7, Apr 13, May 14, May 18); 2025-04-04 BEAR_FVG D served as origin for the March 2026 crash days (NIFTY 03-23, 03-24; SENSEX 03-23, 03-24, ~354 days old); 2025-04-15 BULL_FVG W triggered 5 reactions across the year. |
| **D.16.2** | Selection is the binding constraint on trade discoverability, not capture; current system has no ranking layer to select among many concurrent active aligned zones | S34 audit corollary: on any given morning 50+ active aligned primitives exist across W/D/H/M5 timeframes; only a subset (typically 1-3) becomes the day's actionable retest. ADR-004 §10 records only `first_retest_ts` — subsequent touches of already-retested zones are invisible to outcomes data; 118/119 captured days are predominantly powered by non-first-touch retests of HTF zones formed days/weeks earlier. | **VALIDATED (problem articulated; solution-research arc framed pending build)** | Selection requires (a) predicting which of 50+ active zones will fire today, (b) per-trigger fire-vs-pass decision, (c) per-fire sizing/exit policy. ENH-107 (D-TF FVG retest signal isolation BEAR=EOD/BULL=60m) addresses one facet. Multi-session research arc proposed: HTF context alignment feature (W direction alignment requirement for D zones); zone freshness (first-touch > N-touch); pre-open structural signature (gap + multi-TF aligned + sweep); same-day momentum confirmation gate; DTE-aware option leg selection. ENH-108 (N-touch retest detection) is the foundational schema unlock for selection research — without per-touch outcomes data, "is this primitive being touched right now for the Nth time and what's the empirical hit rate by N?" cannot be answered. Selection-research arc filed as 3-5 session work item; framework doc deferred to S35+ per operator sequencing (SL fix → file gap → file arc → first feature). |
| **D.16.3** | Stop-loss for ICT retest entries must be anchored to spot zone-invalidation on 5m-close-through, not to option premium decay | S34 SL-variant backtest on 7 same-day retest entries (the available subset of 22 cohort after TD-S34-NEW-4 coverage gap): all 7 directionally correct + held-to-EOD total +₹106,888 on 1 lot. SL variants tested: premium intra-bar 5%/10%/15% (winners 0/0/3 of 7), premium 1m-close-through 5%/10% (0/3), spot-anchored 5m-close-through at 0.3%/0.5%/0.8% adverse (winners 7/7/7, SL triggers 0/0/0 across all three levels). | **VALIDATED — codified as ADR-012** *(S35 update: IMPLEMENTED via ENH-106-SL v9 writer extension; single-cell validation n=5 cleared 2026-05-14 NIFTY M5 cohort — see D.17.4)* | Premium-based stops on ATM held strikes are the wrong instrument-stop pairing — they measure microstructure noise (bid-ask, theta on tight DTE, IV crush on morning gamma compression) which routinely consumes 5-15% in first 30-60 min uncorrelated with structural thesis. Tightest spot SL (0.3% adverse 5m-close-through) never triggered on any of the 7 retest entries — the retest is structurally the moment of maximum local risk; once price has tagged the zone and begun rejecting, it does not return through within the same-day window. Doctrine accepted on n=7 signal strength (every premium-SL variant ≤10% wick-eliminated 100% of structurally-correct winners; every spot-SL variant preserved 100%). Parameter X = 0.5% provisional default until validation cohort reaches n≥50. Validation expansion gated on TD-S34-NEW-4 (15 lost post-Apr-2026 entries unlock) + ENH-108 (N-touch retest detection unlocks the 112 invisible-retest move-days). Per-TF calibration of X (analogous to ADR-004 §11 RETEST_TOLERANCE_PCT) deferred until n≥50. Consistent with ADR-004 §10 primitive breach rule template (close-through, not wick-pierce). ENH-107 (BEAR=EOD / BULL=60m exit) composes orthogonally — time-based exit and SL are independent rules. |
| **D.16.4** | `hist_option_bars_1m` has a coverage gap from 2026-04-01 onwards reflecting a vendor → MERDIAN-ingest tier transition; pre-Apr-2026 data is dense vendor-purchased, post-Apr-2026 data is MERDIAN-ingested with confirmed gaps on multiple probed dates | S34 two-query diagnostic on 22-row retest cohort backtest: zero chain rows for both NIFTY and SENSEX on all nine probed post-2026-04-01 dates (2026-04-01, 04-07, 04-09, 04-13, 04-16, 04-24, 05-12, 05-14, 05-18). Pre-2026-04-01 dates in the same cohort (2025-05-08 through 2026-01-20) returned full chain data. Operator-confirmed framing: pre-Apr-2026 chain is vendor-purchased and full across the 12-month window paid for and verified; post-Apr-2026 chain is MERDIAN-ingested via a different writer path. The "MERDIAN failed to ingest entirely" hypothesis cannot be the full picture — plenty of full-ingestion days have been observed post-Apr-2026 elsewhere in the system (S29-S33 cohort work consumed data from that window). | **OPEN — TD-S34-NEW-4 S2 filed for diagnosis + unification** *(S35 update: CLOSED-MECHANICALLY via D.17.1 dual-source chain reader; 81% recovery achieved; residual structural per D.17.2)* | Most likely explanations: (a) post-Apr-2026 chain data lives in a sibling table or schema not under the `hist_option_bars_1m` name; writer was never extended to mirror to the canonical chain table; (b) partial-ingest failure scoped to a subset of dates within the writer's actual coverage. Diagnosis steps in TD body: locate post-Apr-2026 writer code-paths + scheduler history + DB sibling tables; unify storage (migrate/shadow-write or backfill via `backfill_option_zerodha_OI_FIXED.py` analog); re-run ENH-106 v7 outcomes pass against primitives retested in the affected window. Impact: any backtest cohort or primitive-outcomes consumer that touches post-Apr-2026 dates loses option-PnL columns silently; ADR-009 holdout windows that include post-Apr-2026 lose their option-PnL anchor; S34 spot-anchored SL doctrine validation cannot expand from n=7 to target n≥50 until the gap closes. Workaround: restrict backtests to `trade_date <= '2026-03-30'`. Earlier diagnostic framing "Kite token expiry on MALPHA (TD-080-adjacent)" recorded as superseded; root cause is the vendor → MERDIAN-ingest tier transition, not a token failure. |

---

### D.17 — Session 35 chain-data tier transition resolution + HOCS strike-coverage limit + Breeze backfill viability + ADR-012 v9 writer implementation

| Row | Assumption | How set | Status | Evidence / annotation |
|---|---|---|---|---|
| **D.17.1** | Chain-data tier transition at 2026-04-01 UTC boundary is a three-surface architectural concern (chain prices, expiry calendar, ATM strike picking), not one — the same boundary applies independently to each surface and each must be routed separately by the writer | S35 architectural diagnosis: v8 patch on `build_ict_primitives.py` routed chain prices via `_source_tier()` boundary check (pre→`hist_option_bars_1m`, post→`historical_option_chain_snapshots`), assumed single-surface fix. Single-cell test 2026-05-14 produced `n_tuples=0` because `_enum_anchor` silently early-returned on `_nearest_weekly_expiry` returning None — vendor calendar (`hist_atm_option_bars_5m`) caps at 2026-04-02/04-07 and contains no expiries for retest day. v8.1 patch extended `_load_expiry_calendar` to UNION vendor + HOCS expiries. Second surface surfaced via PostgREST 8s statement_timeout (57014) — DISTINCT on 2.67M-row HOCS table without covering index hit timeout; v8.2 patch added `get_hocs_distinct_expiries(text)` RPC + `idx_hocs_symbol_expiry (symbol, expiry_date)` covering index (Index Only Scan + HashAggregate, 325ms execution). ATM strike picking was already independent in v7 (manual via `STRIKE_INTERVAL` grid), not dependent on either source table — third surface effectively pre-resolved. | **VALIDATED — IMPLEMENTED via v8 + v8.1 + v8.2** | Full recompute 2025-04-01→2026-05-24 W/D/H/M5: NIFTY 8,925 outcomes / 1,028 tuples (833 pre_only + 188 post_only + 7 mixed); SENSEX 10,646 outcomes / 1,745 tuples (1,442 pre_only + 286 post_only + 17 mixed); 19,571 total in 2107s wallclock. option_pnl_source distribution: 13,220 `vendor_hist_1m` (9.1% retest pnl coverage), 1,851 `merdian_hist_5m` (14.9% retest pnl coverage), 4,500 NULL (mostly level primitives PDH/PDL/PWH etc.). Boundary routing verified clean: vendor_hist_1m max retest 2026-03-30, merdian_hist_5m min retest 2026-04-01 — perfect split at boundary. The 24 mixed tuples (7 NIFTY + 17 SENSEX) are tuples where the same (strike, expiry, type) had anchors spanning both tiers; cache merge handled correctly. **General architectural lesson:** when a data-tier transition is identified, audit ALL surfaces that depend on the source table, not just the headline column. Each surface needs its own routing logic at the same boundary. Pattern applies to future migrations (e.g., when MERDIAN-ingest tier is itself replaced by Breeze tier — three surfaces will need re-routing again per ADR-013). |
| **D.17.2** | MERDIAN-ingest tier has a structural strike-coverage ceiling: `ingest_option_chain_local` captures ATM±N strikes per cycle, not full chain. When a primitive forms at spot X and retests at spot X+ΔX, the held-strike walk needs the SAME strike at both anchors; if ΔX exceeds the live-capture strike window, the retest premium lookup misses | S35 diagnostic on the 224 still-NULL post-Apr-2026 retests after v8/v8.1/v8.2 recompute (excluding 132 level-primitive architectural exclusions = 92 zone-primitive retests still NULL out of 409 candidates). Lens decomposition: 14 from 2026-04-16 (genuine ingest outage — recovered via Breeze surgical fill D.17.3); ~75 from HOCS strike-coverage misses (retests at substantially drifted spot vs formation); ~3 edge cases. formation_atm_status lens: 14 `formation_OK_retest_FAIL` (HOCS coverage limit signature — formation strike was captured, retest strike was not), 115 `formation_dte_only_premium_FAIL` (mix of strike coverage + premium=0 days), 136 `formation_TOTAL_FAIL` (broader window coverage gaps). | **VALIDATED — PERMANENT CEILING on MERDIAN-ingest tier (TD-S35-NEW-1 filed)** | This is structural, not a bug. The live `ingest_option_chain_local` writer scope was bounded by API rate limits + storage cost — captures ATM±N around running spot each 5-min cycle. Captures are dense in time at the running ATM but sparse across the strike grid as time progresses. Recovery options: (a) external full-chain backfill on per-date basis (Breeze API per D.17.3 / ADR-013) for any specific date the analysis cares about; (b) extending the live ingestion window (ATM±N where N is wider) — runtime cost grows linearly, storage cost grows linearly; (c) accepting the ceiling and treating retest cohort coverage as "best-effort post-Apr-2026" with the floor at 81% recovery. Operationally: when post-Apr-2026 cohort analyses surface, denominator must exclude both architectural exclusions (level primitives, 132) and strike-coverage misses (~75 currently observed, scales with spot drift) — true tradeable retest denominator is 317 of 409, not 541 of 541. ADR-012 SL cohort threshold (n≥50) achieved on W+D+H zone tier (n=65) — sufficient for validation work to proceed. M5 zone tier coverage further restricted; treat M5 retest cohort with awareness of this ceiling. |
| **D.17.3** | ICICI Breeze API (`breeze-connect` SDK) is operationally viable as one-shot external full-chain backfill source — no ATM±N cap, 3-year lookback, 5000 calls/day, 100/min throttle, static-IP whitelist required | S35 surgical fill demonstration on 2026-04-16: deployed from MERDIAN AWS Elastic IP 13.63.27.85 via `fill_2026_04_16_breeze_v3.py` (v1 had calendar union bug fixed in v2, v2 had run_id NOT NULL bug fixed in v3, v3 SENSEX leg discovered `stock_code='BSESEN'` required not `'SENSEX'`). Final: NIFTY 61,899 rows (3 expiries 04-21/04-28/05-12 × 2 OT, tapering on far-OTM) + SENSEX 45,731 rows (3 expiries 04-16/04-23/05-14, with 2026-04-16 same-day DTE=0 expiry and 2026-05-14 far-monthly leg sparse) = 107,630 rows total. Source-tagged `breeze_backfill_s35`. | **VALIDATED — FOUNDATION FOR ADR-013 PROPOSED** | Operational details captured for institutional knowledge (also filed as TD-S35-NEW-2 partially — re-document the surrounding context for the legacy `hist_option_bars_1m` 54.8M-row vendor table). Breeze auth flow: portal app registration (App Key + Secret Key, one-time) + daily session token via OAuth-style redirect (`https://api.icicidirect.com/apiuser/login?api_key=<key>` → 2FA → `apisession` query param on redirect URL). SDK quirks: imports require working directory containing writable `logs/` dir (will SIGFAULT on read-only mounts); SENSEX symbology not documented (verified empirically `stock_code='BSESEN'` + `exchange_code='BFO'`); rollingoption endpoint has ATM±N cap (use `get_historical_data_v2` for full-chain). Post-fill recompute on 2026-04-15→17 inserted 10 outcomes; verification confirmed all 6 zone-primitive retests on 04-16 populated with plausible PnL (NIFTY H BEAR_FVG pnl_eod=-13.9%, SENSEX M5 BEAR_FVG +134% winner, SENSEX H BEAR_FVG -100% DTE=0 expired worthless, etc.). **Foundation for ADR-013 PROPOSED** (Breeze as canonical historical backfill source replacing third-party vendor dependency). For routine use ADR-013 acceptance is not yet warranted; positioning as long-term fallback per S35 operator framing. Static-IP requirement (per April 2026 SEBI rule) forces deployment from MERDIAN AWS or similar fixed-IP host; home multi-WAN topology is incompatible. |
| **D.17.4** | ADR-012 spot-anchored SL doctrine writer implementation is correct on n=5 single-cell — BEAR SL triggers fire on first 5m close-through, BULL no-trigger correctly degenerates to held-to-EOD | S35 ENH-106-SL v9 writer extension on `build_ict_primitives.py` (4 edits, ast.parse-validated): SL_BUFFER_PCT_DEFAULT=0.005 constant, OutcomeRow 5 SL fields, compute_retest_atm_pnl SL evaluation block walking 5m clock-aligned spot bars from retest_5m+5min through 15:25 IST eod, upsert_outcomes plumbing. DDL added 5 columns to `ict_primitive_outcomes` (sl_level, sl_buffer_pct, sl_triggered_ts, sl_exit_prem, pnl_with_sl_pct). Single-cell test on 2026-05-14 NIFTY M5 cohort (DELETE outcomes + recompute since writer is INSERT-only): 5 fresh rows produced. | **VALIDATED at n=5 — full cohort expansion deferred to S36** | Verification rows: (1) BEAR_FVG zone_low=23526.3 zone_high=23562.8 → sl_level=23680.61 (=zone_high × 1.005 exact), sl_triggered_ts=2026-05-14 06:15:00 UTC, sl_exit_prem=121.10, pnl_with_sl_pct=-35.4% vs option_pnl_eod=-48.4% (saved 13pp); (2) BEAR_FVG zone_low=23495.6 zone_high=23519.4 → sl_level=23637.00 (=23519.4 × 1.005 exact), sl_triggered_ts=2026-05-14 06:10:00 UTC, sl_exit_prem=109.00, pnl_with_sl_pct=-38.8% vs pnl_eod=-53.4% (saved 14.6pp); (3) BULL_FVG zone_low=23577.8 zone_high=23600.9 → sl_level=23459.91 (=23577.8 × 0.995 exact), no trigger, pnl_with_sl_pct=10.0% = pnl_eod (held to EOD); (4) BULL_OB zone_low=23684.95 zone_high=23711.75 → sl_level=23566.53 exact, no trigger, pnl_with_sl_pct=-16.7% = pnl_eod; (5) BULL_FVG zone_low=23697.1 zone_high=23721.55 → sl_level=23578.61 exact, no trigger, pnl_with_sl_pct=-25.8% = pnl_eod. Math validated to 5 sig figs on all 5 rows. Held-to-EOD-fallback correctness confirmed on 3 of 5 (BULL no-trigger paths). **Cohort expansion to n≥50 requires TRUNCATE `ict_primitive_outcomes` + full recompute** (writer is INSERT-only — TD-S35-NEW-4 filed; pre-existing rows from prior recompute era retain NULL SL columns until refreshed). S36 P0 follow-on: full recompute + ADR-012 validation cohort SQL on (timeframe × primitive_type × direction) buckets. |

**Cross-references:**

- **ADR-012 IMPLEMENTED via ENH-106-SL v9** — D.17.4 is the empirical validation; the v9 patch is the implementation; the writer-side ADR-012 deliverable is complete.
- **ADR-013 PROPOSED** — D.17.3 is the empirical foundation; D.16.4 root-cause analysis identified the need; routine-use graduation pending further validation cycles.
- **D.16.4 → CLOSED-MECHANICALLY in S35** — root cause confirmed (vendor → MERDIAN-ingest tier transition at 2026-04-01) + dual-source chain reader shipped + 81% recovery achieved; residual gap is structural (D.17.2).
- **D.16.3 → IMPLEMENTED via D.17.4** — n=7 empirical doctrine acceptance from S34 + n=5 writer validation S35 = ADR-012 mechanically closes. Cohort expansion path defined (S36 full recompute → n≥50 validation).
- **TD-S34-NEW-4 → CLOSED-MECHANICALLY in S35** — see CURRENT.md S35 section for closure details.
- **TD-S35-NEW-1 (HOCS strike-coverage structural limit) — FILED P3** — D.17.2 is the empirical finding.
- **TD-S35-NEW-2 (pre-Apr-2026 vendor not catalogued) — FILED P3** — D.17.3 surrounding context documentation gap.
- **TD-S35-NEW-3 (SENSEX Breeze symbology `BSESEN` not documented) — FILED P4** — D.17.3 implementation detail captured.
- **TD-S35-NEW-4 (writer is INSERT-only on outcomes — schema migration friction) — FILED P3** — D.17.4 cohort-expansion blocker mechanism documented; S36 follow-on triggered.
- **ENH-109 PROPOSED** — D.17.3 graduates from "viable for one-shot" toward "canonical backfill source"; pending broader validation cycles.

**Open follow-ups (S36+):**

1. **S36 P0 full recompute (TRUNCATE + rebuild) to populate v9 SL columns across all 19,571 outcomes** — required because writer is INSERT-only and existing rows from S35 main recompute have NULL SL columns. Expect ~35-45 min wallclock based on S35 precedent.
2. **S36 P1 ADR-012 validation cohort SQL on full recompute** — group by (timeframe × primitive_type × direction); n≥50 threshold target achieved on W+D+H zone cohort (n=65); M5 zone cohort smaller due to D.17.2 strike-coverage ceiling. Empirical test: does spot-anchored SL improve PnL vs held-to-EOD on average across the cohort? If yes, doctrine moves from PROPOSED to OPERATIONAL; if no, parameter X re-tuning required.
3. **S36 P2 ENH-84 vol_analytics build** — original S29 mandate, deferred through S30-S35 (7-session backlog). High operator-priority per memory; unblocked by no upstream dependency now.
4. **S36 P3 ENH-108 N-touch retest detection schema unlock** — invisible-retest move-days (112 captured in S34 audit) unblock selection-research arc (D.16.2). Schema design + writer patch ~1-2 sessions.
5. **S36 P3 MERDIAN AWS instance ID memory update** — operator memory shows `i-0e60e4ed9ce20cefb`, AWS console now shows `i-0878c118835386ec2` for `meridian-server`. Instance was rebuilt at some point post-S28; topology doc needs update.

### D.18 — Session 36 capture-layer resilience codified + writer-state-vs-data-state diagnosis ordering

| Row | Assumption | Evidence (Session 36) | Status | Notes |
|---|---|---|---|---|
| **D.18.1** | When an aggregate-magnitude metric drifts on a derived table, the writer function body must be read before historical-data unit-mix is inspected — aggregate magnitudes are downstream of both, and aggregate evidence alone cannot discriminate between live-writer regression and historical contamination | S36 TD-S30-CANDIDATE-1 diagnosis: 7 sessions (S29 → S35) carried the assumption that live `compute_gamma_metrics_local.py` had regressed to writing raw rupees (`net_gex` magnitudes 10¹⁰-10¹³ instead of 10⁶ Cr) based on aggregate magnitude-by-week SQL. Direct read of the writer function body in S36 first turn (`findstr /N /C:"1e7" backfill_gamma_metrics_to_main.py` + inspection of `signed_gamma_exposure` in `compute_gamma_metrics_local.py`) confirmed the `/1e7` Cr conversion intact at S27 commit `241f943` — the live writer was never regressed. The three-epoch unit-mix (pre-2026-03 Cr-correct backfill / 2026-03-02→2026-05-04 raw rupees from pre-S27 live writer / post-2026-05-11 Cr-correct post-/1e7 patch) was a historical contamination artifact, not a live regression. | **VALIDATED — TD-S30-CANDIDATE-1 CLOSED-MISDIAGNOSIS S36** | Resolution: `python backfill_gamma_metrics_to_main.py --start 2026-03-02 --end 2026-05-04 --mode overwrite --symbol both` recovered the March epoch to Cr (2850 cycles); April-early-May ~4,300 rows DELETEd as confirmed gaps under TD-S35-NEW-1 chain coverage residual; one outlier 2026-05-19 09:55:14 NIFTY -4.7B deleted. Post-cleanup `gamma_metrics` is single-unit Cr globally. **General architectural lesson:** writer-state-vs-data-state diagnosis ordering is a foundational methodology rule for any derived-table integrity investigation. Reverse-order diagnosis (aggregate → live-writer-blame → action) is a known failure mode that cost 7 sessions of carry-forward in this case. Pattern applies to any future cohort where an aggregate metric shows unexpected drift — read the writer first. |
| **D.18.2** | Capture-layer failure modes partition into three independent axes (Mode A quota/429, Mode B token/401, Mode C orphan RUNNING) — each axis requires its own remediation pattern and should be diagnosed against `script_execution_log` before retry-layer extensions are scoped | S36 TD-080 / ENH-99 failure-shape diagnosis on `script_execution_log` queried by (exit_reason, error_message, error_code) revealed three distinct failure modes. Mode A: 2026-05-14 Dhan 429 quota storm — 99/808 cycles, error_code 805 "Too many requests"; existing retry budget 3×5s×1.5× = ~24s vs ~60s quota window → real recurring failure pattern. Mode B: 2026-05-07 token 401 expiry — 151/299 cycles, error_code 808; pre-S29 instrumentation, root cause fixed S29 via `pull_token_from_supabase.py` atomic write + readback + `dhan_token_probe_log`; **zero post-S29 401s observed in 11 instrumented days**; Mode B is solved upstream, not in the retry layer. Mode C: 25 stale `script_execution_log` rows aged 0.7-25 days across 8 weeks across 8 scripts (`compute_gamma_metrics_local`, `capture_spot_1m_v2`, `detect_ict_patterns_runner`, `build_trade_signal_local`, `ingest_option_chain_local`, `backfill_gamma_metrics_to_main`, `build_market_state_snapshot_local`, `compute_options_flow_local`); never reaped, audit-blind. | **VALIDATED — ENH-99 SHIPPED S36 (Components 1+2+3); Mode B DROPPED from scope as upstream-resolved** | Component 1 (Mode A): retry predicate `is_dhan_429` kwarg added to `retry_call` via v3 regex-anchor patch on `gamma_engine_retry_utils.py`; 2 Dhan retry sites in `ingest_option_chain_local.py` bumped to `attempts=6, delay=15.0, backoff_multiplier=1.5, retry_predicate=is_dhan_429` (~96s budget vs ~60s quota window — margin of safety); supabase INSERT retry site at line 417 intentionally not touched (different exception class, no quota semantics). Component 2 (Mode C): `orphan_run_janitor.py` v3 deployed to `C:\GammaEnginePython\` (env-aware raw HTTP per house convention, int4-clamp safe at write time, per-row error tolerant); `MERDIAN_Orphan_Janitor` Task Scheduler task registered weekly Mon-Fri 09:14 IST (Hidden + IgnoreNew + 5min limit; task count 19→20). Smoke-fire test 2026-05-25 17:26:26 closed 22/24 orphans + 2 stragglers via REPL after int4 clamp (D.18.4). Component 3: `[RETRY_BURN_DOWN]` stderr telemetry tag on final retry failure carries script identity + last exception class + attempt count for downstream audit visibility. Component 4 (audit thresholds) DEFERRED — needs `merdian_daily_audit.py` config schema visibility before threshold additions can be scoped. **General architectural lesson:** instrument the failure log before extending the retry layer. The "extend the retry budget" reflex would have burned ~12s per non-429 transient and not addressed Modes B or C. Multi-modal failure-shape diagnosis generalizes — every capture/ingest service has quota / token / orphan axes. |
| **D.18.3** | PostgREST `Prefer: return=representation` interacts with RLS policies on the write path differently than on the read path; the header is safe to include on read-back patterns but unsafe to default-include on PATCH against RLS-protected tables | S36 `orphan_run_janitor.py` iteration: v2 (after v1's supabase-py KeyError on `SUPABASE_KEY` env name was switched to raw HTTP per house convention) included `Prefer: return=representation` on PATCH requests to `script_execution_log`; PostgREST returned 400 on every PATCH despite identical service-role-key headers working on the GET path. v3 dropped the Prefer header (per-row error-tolerant; PATCH writes succeed without return-representation echoing back); janitor immediately closed 22/24 orphans cleanly (2 failures unrelated, see D.18.4). | **VALIDATED — operational pattern codified S36** | The exact RLS interaction shape was not deeply explored (v3 dropped the header and moved on); the operational rule is: drop `Prefer: return=representation` on raw-HTTP PATCH paths when not consuming the response body, regardless of perceived parity with the read path. Pattern applies to future raw-HTTP `requests` against `/rest/v1/*` against RLS-protected tables. House convention from `ingest_option_chain_local.py` reference: PATCH body + `Content-Type: application/json` + `apikey` + `Authorization: Bearer` + (optionally) `Prefer: resolution=merge-duplicates` for upserts; do NOT include `Prefer: return=representation` on PATCH paths. |
| **D.18.4** | `script_execution_log.duration_ms` is int4 in PG schema; orphan-recovery durations for rows exceeding ~24 days age (`2^31 - 1 = 2,147,483,647 ms`) must be clamped at write time before the PATCH is sent, not at compute time after the fact | S36 `orphan_run_janitor.py` smoke-fire test on 24 RUNNING-aged rows: 22 closed cleanly via PATCH, 2 threw PG `22003 numeric_value_out_of_range`. Failing rows had `started_at` 25-26 days prior; `age_ms` values were `2,257,725,199` (~26d) and `2,181,171,397` (~25d) — both above `2^31 - 1`. v3 clamp added: `age_ms = min(int((now - started_at).total_seconds() * 1000), 2_147_483_647)`. Two stragglers re-closed via direct Python REPL PATCH with clamped duration. Final state verified: 0 RUNNING > 5min. | **VALIDATED — operational pattern codified S36 + TD-S36-NEW-4 FILED P3** | Pattern applies to any future writer that computes a duration delta against an old TIMESTAMPTZ and writes back to a `duration_ms`-shaped int4 column. The schema-level fix (migrate to int8) is filed as TD-S36-NEW-4 P3 — the write-time clamp is the operational pattern in the interim. Note: this is not just a janitor concern — any long-running script that started, blocked > 24 days, and tried to write its own duration at end would hit the same limit. Pattern documented in §D.18.4 so future writers in similar shape know to clamp at write time. |

**Cross-references:**

- **TD-S30-CANDIDATE-1 → CLOSED-MISDIAGNOSIS in S36** — see CURRENT.md S36 Last session block + session_log.md S36 entry for closure details. D.18.1 is the foundational methodology lesson; the actual data recovery (2850 cycles March overwrite + April-early-May DELETE) is captured in CURRENT.md.
- **TD-080 → CLOSED via ENH-99 SHIPPED in S36** — see Enhancement Register ENH-99 SHIPPED block for full implementation detail. D.18.2 is the failure-mode taxonomy; the three-component implementation is in the ENH register.
- **TD-S36-NEW-1 (gamma_metrics Apr-early-May row gap) — FILED P3** — architectural residual from D.18.1 cleanup; closeable in same window as Breeze backfill validation cycle.
- **TD-S36-NEW-2 (`MERDIAN_Dhan_Token_Refresh` not instrumented to `script_execution_log`) — FILED P3** — surfaced during D.18.2 failure-shape diagnosis; remediation is straightforward instrumentation pattern alignment.
- **TD-S36-NEW-3 (`dhan_token_probe_log` forward-only from 2026-05-10) — FILED P4** — pre-S29 incidents are invisible to the probe log; documentation-only gap.
- **TD-S36-NEW-4 (`script_execution_log.duration_ms` int4) — FILED P3** — D.18.4 schema constraint; clamp-at-write-time is the operational pattern; schema migration to int8 is the proper fix.
- **ENH-99 SHIPPED (PROPOSED → SHIPPED S36)** — see Enhancement Register. D.18.2 is the empirical evidence; the three-component implementation is the deliverable.
- **D.16.4 chain-coverage gap finding** + **D.17.2 HOCS strike-coverage structural limit** — D.18.1's residual April-early-May row gap (~4,300 deleted rows) is architecturally bounded by the same chain-coverage ceiling; recovery path is ADR-013 Breeze backfill at n≥3 successful Breeze-tier backfills accumulated.

**Open follow-ups (S37+):**

1. **TRUNCATE + full recompute of `ict_primitive_outcomes` to populate v9 SL columns across all 19,571 outcomes** (S35 P0_PRIMARY carry-forward, not actioned S36 due to capture-layer prioritization).
2. **ADR-012 validation cohort SQL on full recompute** (S35 P0_SECONDARY carry-forward) — n≥50 threshold target on W+D+H zone cohort.
3. **MERDIAN AWS instance ID memory update** (S35 P0_TERTIARY carry-forward; `i-0e60e4ed9ce20cefb` → `i-0878c118835386ec2`).
4. **ENH-80 per-strike GEX writer** — substrate for ENH-81 Positioning Landscape + ENH-82 Pin Risk Score continuous; unblocked S29 since TD-094 RECLASSIFIED-STALE; dominant S37+ thrust per operator's sequencing directive (capture/integrity → compute → display → execution).
5. **ENH-99 Component 4 audit thresholds** — needs `merdian_daily_audit.py` config schema visibility before threshold additions for `[RETRY_BURN_DOWN]` tag rate + `notes LIKE 'ORPHAN_RECOVERED%'` rate.
6. **TD-S36-NEW-1 gamma_metrics Apr-early-May row gap** — closeable in Breeze-fallback validation cycle when ADR-013 graduates.
7. **TD-S36-NEW-2 `MERDIAN_Dhan_Token_Refresh` instrumentation** — straightforward `script_execution_log` write at task start/end; quick-win when next operating on the token refresh path.
8. **TD-S36-NEW-4 int4 → int8 schema migration on `script_execution_log.duration_ms`** — DDL change with backfill; not urgent given write-time clamp pattern works.

---

*MERDIAN Assumption Register — established Session 23, 2026-05-09. Last updated Session 36, 2026-05-25. Living document. Update inline as experimental evidence resolves rows; superseded rows are annotated with the superseding ADR/Exp, never deleted.*
