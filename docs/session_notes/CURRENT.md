# CURRENT.md — MERDIAN Live Session State

> **Living file.** Overwritten at the end of every session to reflect what just happened and what the next session is for.
> Claude reads this immediately after `CLAUDE.md` at session start. It replaces the practice of manually pasting a "session resume block."
> **History.** Every session block before S77 lives in [`docs/registers/CURRENT_history.md`](../registers/CURRENT_history.md) — committed to git, **not** uploaded to project knowledge. Split at the S78 doc-close per **TD-S73-NEW-8**. This file carries the current session and one predecessor, and nothing else.

---

## Last session

| Field | Value |
|---|---|
| **Date** | 2026-09-14 (Session 78 — **the canonical data inventory register**. Doc-close. No production code changed; one generator patched, one register written, `CURRENT.md` split.) |
| **Shape** | A question stopped being re-derived, and a vendor question closed from the file side. The register is the session's product; every other document points at it rather than restating it. |
| **(1) HEADLINE — `MERDIAN_Data_Inventory.md`** | Measured against the live database, READ-ONLY, GET-only through a single `_get()` choke point. **74,423 bytes, 476 table rows, 26,172 requests, 0:52:36, `UNRESOLVED = []`** (the script refuses to write while §10 is non-empty). Regenerates via `scripts/build_data_inventory.py`, no arguments. It exists because the extent question had been re-derived from scratch at least a dozen times across 70+ sessions, each time from whichever table that session's brief happened to name, producing a different answer each time. **Use it instead of measuring one table and generalising from it.** |
| **(2) Exact agreement with S75** | Recomputed over S75's own window: trading days **356 / 355**, COMPUTABLE NOW **299 / 299**, COMPUTABLE AFTER DERIVATION **0 / 0**, NOT COMPUTABLE **57 / 56** — **every delta +0**. Two independent measurements six days apart, different code, different access patterns. Full window: **359 / 358** trading days, **302 / 302** computable. Density: **5,182** (symbol, day, relation) cells at `count=exact`, **0 failed, 0 skipped**. |
| **(3) The dual verdict is load-bearing** | `same-relation only` is **113 / 115** against a joined **302 / 302** — so roughly **190 days per symbol are computable only via a measured join**: gamma from `hist_option_greeks_1m`, OI from `hist_option_bars_1m`, neither relation carrying both. The join is licensed by measurement, never by shared column names: **60 sampled tuples, 60 hits, 0 unresolved, 100.0%**, per symbol, on `(instrument_id, bar_ts, expiry_date, strike, option_type)`. A pair is used only at a perfect hit rate, and an unresolved probe counts in the denominator — subtracting it would let 10 hits and 10 timeouts report 100 %, which is method rule 2 running backwards. |
| **(4) Vendor greeks CLOSED from the file side** | The 2025-04 → 2026-03 chain was purchased from **GFDL (Global Financial Datafeeds)**. Delivery schema is **nine columns** — `Ticker, Date, Time, Open, High, Low, Close, Volume, Open Interest` — measured uniform across all **175,304 rows** of `GFDLNFO_BACKADJUSTED_01042025.csv`. **No IV and no greeks were ever delivered; they were not dropped at load.** So S62's `backfill_hist_greeks.py`, which solves iv and gamma itself at a chosen `r_used`, was the correct and only route, not a workaround for a botched ingest. `delta`/`theta`/`vega` for that era **do not exist and cannot be recovered from this vendor**. **Closes TD-S35-NEW-2** (open ~43 sessions); confirms **TD-S58-NEW-1** from the file side. |
| **(5) Method rule 6 — new this session** | *A relation that cannot produce a real empty list cannot satisfy rule 3, and is excluded as **UNMEASURABLE BY THIS METHOD** — by measurement, never by name and never by catalog category.* Each source is probed on a window it certainly holds no rows in; one that must scan its whole extent to prove absence times out instead of returning `[]`, and so fails once per empty day rather than once. One relation excluded: **`v_oi_prev_close_snapshots`**, HTTP 500 at ~8.3 s on every scope tried including an unfiltered day window on 2025-04-01 — cheap when a row matches, timing out when it must prove nothing matches. The exclusion does **not** block the write, but the register then says **nothing** about that relation, neither presence nor absence. |
| **(6) Project knowledge at 92 %** | 5.5 MB across 66 files; six files are 62 % of it. `CURRENT.md` split — **61 of its 62 session blocks were history** (72 of 73 H2 sections moved; partition asserted in both directions) — and two superseded S75 research artefacts dropped from the upload set. **TD-S73-NEW-8 annotated and stays OPEN:** this is partial remediation, and the root cause is unchanged — the eight-fold duplication of each session's findings across `CLAUDE.md`, `CURRENT.md`, `session_log.md`, `tech_debt.md`, System Map, Deployment Topology, Assumption Register and Decision Index. **Governing principle recorded: git is the archive, project knowledge is the retrieval surface.** A file belongs in PK only if it is read at session start or searched during a session. |
| **Artefacts** | `scripts/build_data_inventory.py` and `docs/registers/MERDIAN_Data_Inventory.md` (both NEW), `docs/registers/CURRENT_history.md` (NEW). The §11 vendor closure was spliced into the **generator**, not only the rendered file — the register regenerates and its own header tells the reader to re-run it, so anything spliced only into the output is erased on the next run. Verified by executing the patched §11 emission region and diffing: **byte-identical, 3,257 chars**. |
| **Not verifiable from this box — recorded rather than asserted** | `MERDIAN_Hedgewall_Parity_Spec.md` is **not in the repo**; it was supplied as a chat artefact and has no repo path, so §1.5 of the brief could not be checked here. The **GFDL CSV is not on the AWS box** (`find /home/ssm-user -iname 'GFDL*'` returns nothing) — its schema is established from the operator's local file and cannot be re-verified from AWS. Record where the raw delivery lives before the next session needs it. |
| **Ledger** | **TDs_NEW=7 filed + 1 WITHDRAWN** (TD-S78-NEW-1..8; **-7 withdrawn before filing** — ADR-022 Amendment A1.1 records D1 **CLOSED** and `capture_spot_1m_v2` *deliberately not extended*, so the claimed exposure is a recorded decision, not a gap. Filed: **0×S1, 3×S2, 4×S3**). **TDs_CLOSED=1** (TD-S35-NEW-2). **ADRs_NEW=0. ADRs_AMENDED=0.** Enhancement Register **not triggered** (ninth consecutive) — no architectural thinking occurred; the session measured and recorded. Method rule 6 is a measurement-doctrine amendment recorded in the register that implements it, in the same shape as the S74 Doc Protocol v4 Rule 7 amendment — **a protocol amendment, not an ADR.** |

---

## Previous session S77

| Field | Value |
|---|---|
| **Date** | 2026-09-11 → 2026-09-13 (Session 77 — **F-68 remediation and the ICT zone-layer verdict**. Code changed: `ict_primitives.py`, `build_ict_primitives.py`. One irreversible database operation: DELETE + full recompute of `ict_primitives` and `ict_primitive_outcomes`, snapshots taken first.) |
| **Shape** | A fix, a rebuild, and a null. The fix worked; the rebuild verified; the null is the session's product and it is regime-invariant. |
| **(1) F-68 closed in code and in data** | D2 implemented for the **zone classes**: `ts_close` on `Bar`, set in `_reduce_ohlc` as the bucket's last 1m bar **+ 1 min** (the bare stamp would leave 59 s of lookahead), consumed via a new `_bar_close_ts()` at the three `Primitive.valid_from` sites. Verified by paired natural-key join against the pre-fix snapshot: **`unchanged = 0` on 2,481 OB/FVG rows**, every shift positive, magnitudes M5 1 m–5 m 59 s · H 2 m–1 h 00 m 59 s · D 45 m–6 h 16 m · W 2 d 6 h 16 m–4 d 6 h 16 m — reproducing F-68's independently derived lookahead at all four timeframes. The recurring `06:16:00` is the RTH session length plus the close offset. |
| **(2) Event class deliberately NOT implemented** | Operator option A. `Event.event_ts` stays at bucket-start because `detect_order_blocks` keys `bar_idx` and `fvg_by_ts` on exact equality with it — moving it empties OB detection silently. **15,681 of 20,042 rows (78 %) remain contaminated.** Measured cost: H `DISPLACEMENT_DOWN` reads **41 wins of 41**. Events are *more* contaminated than zones, not less — a displacement is defined by the bar that moved. **TD-S77-NEW-1.** |
| **(3) Recompute** | DELETE + full rebuild, `--start 2025-03-31 --end 2026-06-04`, **20,042 / 20,042**, no gap. Ended before the option-chain gap deliberately — past 2026-06-04 the cohort would inherit F-73's fail-forward expiry, an all-NULL option block, F-72's collateral SL nulls and ADR-022's mid-auction EOD columns. Pre-fix cohort preserved as `ict_primitives_pre_s77` / `ict_primitive_outcomes_pre_s77` (**TD-S77-NEW-6**). Cost: 3,228 s detached after ~40 min lost to a foreground SSM logoff; **8.3 % CPU — the job is ~92 % I/O wait**, SENSEX chain prefetch alone 1,310 s across 1,818 tuples (`pre_only=1454 post_only=347 mixed=17`). Replaces S35's 2,107 s as the planning number. |
| **(4) HEADLINE — the ICT zone layer has no measurable edge** | Clean vs contaminated on identical primitives. H `atm_pnl_30m_pct` **+12.80 % → +0.76 %**; M5 **+5.04 % → −0.42 %**. FVG `respected` **43.7/63.8/77.9/92.3 → 34.9/23.0/43.2/46.2** — the timeframe gradient **inverts**, and the drop per cell tracks the lookahead-to-bar-width ratio. Horizon sweep decays monotonically negative to **−7.19 %** at EOD: theta with no directional edge. Distribution symmetric, **no exploitable tail** (H p10 −20.21 / p90 +21.72). `mean_mfe ≈ \|mean_mae\|` — price moves as far against these zones as for them. |
| **(5) Pre-registered regime test — failed** | Trailing-20-day range, ≥6 % = TREND, success criterion declared **before** the query: ≥1.3 trend and ≤1.1 range. Result **1.10 / 1.10 at H** and **1.01 / 1.03 at M5**. The split discriminated cleanly (4.24 % vs 10.24 % window ranges, balanced n) and found nothing. Volatility scales with regime; edge does not. **The null is regime-invariant.** |
| **(6) And the historical edge is explained** | `experiment_2_options_pnl.py:257-264` fires at bar `i` if `bars[i+5].close` is ≥0.40 % away, then emits and enters at bar `j ≤ i` (`:486`). Every trade begins immediately before a move the selection rule required to have happened. **96 points ÷ the ATM premium at each DTE gives +87/+50/+33/+22 %, blending to ≈+40 % against a published +41.9 %.** The headline was arithmetic; "gamma explosion on expiry day" is a smaller denominator. `BEAR_BREAKER` wins **0 of 46** (P = 1.4 × 10⁻¹⁴) — it enters at the far end of the same displacement. **Win rate tracks lookahead depth, not ICT canon.** See `docs/research/experiment_forensics_2026-09-11.md`. |
| **(7) Live exposure checked** | `MERDIAN_TIER_MULT_DISABLE` is **set** — 3,650 of 3,650 `signal_snapshots` rows over 30 days carry the marker. It covers `ict_size_mult` only: **not** `KELLY_FRACTIONS_C[tier]` (`build_trade_signal_local.py:1035`), **not** `tier == "SKIP"` (`detect_ict_patterns.py:528`). Capital allocation still keys off a mined tier. **TD-S77-NEW-10 / -11.** |
| **Artefacts produced** | `docs/research/experiment_forensics_2026-09-11.md` (CC, read-only forensic audit). `patch_s77_f68_ts_close.py` and `splice_s77_d35.py` committed via `git add -f` (`.gitignore:71` — **TD-S77-NEW-4**). §D.35 extended 17 → 34 rows. |
| **Operator decisions** | **Option A** — event class deferred, scope recorded rather than silently carried. **Window** — `--start 2025-03-31 --end 2026-06-04`, correcting the S77 brief's transplanted `2026-03-30` (that was the audit's *lookback* start for a gap re-run, not a full-recompute start). **Snapshots before DELETE** — made the only irreversible step reversible. |
| **Ledger** | **TDs_NEW=15** (TD-S77-NEW-1..15: **3×S1, 6×S2, 6×S3**). **TDs_CLOSED=0. ADRs_NEW=0. ADRs_AMENDED=1** (ADR-004 Amendment C implemented, zone classes only). Enhancement Register **not triggered** (eighth consecutive). |

---

*Session blocks S76 and earlier: [`docs/registers/CURRENT_history.md`](../registers/CURRENT_history.md).*
