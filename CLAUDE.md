# CLAUDE.md — MERDIAN Engine Orientation

> **Read this first, every session, before doing anything else.**
> This file is the contract between Navin and any Claude session working on MERDIAN.
> If something here conflicts with a `.docx` master, this file wins on operational state.
> The `.docx` masters win on architecture, governance, and historical decisions.

---

## What this project is

MERDIAN — Market Structure Intelligence & Options Decision Engine. A live options decision engine for NIFTY and SENSEX weekly options, with shadow-mode validation, ICT pattern detection, Kelly tiered sizing, and a hist_pattern_signals research store. Two environments: **Local Windows (PRIMARY LIVE)** and **AWS t3.small (SHADOW)**, both pulling from a single Git repo.

---

## Read order at session start

1. **This file** (`CLAUDE.md`) — orientation, rules, pointers
2. **`docs/session_notes/CURRENT.md`** — what the last session did, what this session is for, what NOT to reopen
3. **`docs/registers/tech_debt.md`** — known broken-ish things, workarounds, severity
4. **`docs/registers/MERDIAN_Enhancement_Register_v<latest>.md`** — forward-looking proposals (only if this session touches them)
5. **`docs/registers/merdian_reference.json`** — *targeted lookup only*, not full read. Use for file/table inventory.

That's it. Do **not** auto-load any `.docx` master at session start. They are generated artifacts, not working documents.

---

## Common operations — consult before asking

For any recurring operation (token rotation, runner restart, backfill, credential rotation, broker flow update, etc.), consult `docs/runbooks/README.md` to find the right runbook. Follow the runbook step by step.

| I need to… | Runbook |
|---|---|
| Rotate the Dhan access token | `docs/runbooks/runbook_update_dhan_token.md` |
| Update the Kite broker flow | `docs/runbooks/runbook_update_kite_flow.md` |
| Verify Kite auth before market open | `docs/runbooks/runbook_update_kite_flow.md` Step 3 — runs `/home/ssm-user/meridian-engine/check_kite_auth.py` (persisted Session 10) |
| Restart a stuck runner (Local) | `docs/runbooks/runbook_restart_runner_local.md` |
| Restart a stuck runner (AWS) | `docs/runbooks/runbook_restart_runner_aws.md` |
| Backfill a missing trading day | `docs/runbooks/runbook_backfill_missing_day.md` |
| Resolve Local↔AWS hash mismatch | `docs/runbooks/runbook_resolve_hash_mismatch.md` |
| Recover from DhanError 401 | `docs/runbooks/runbook_recover_dhan_401.md` |
| Add a row to trading_calendar | `docs/runbooks/runbook_add_calendar_row.md` |
| Emergency stop live trading | `docs/runbooks/runbook_emergency_stop.md` |

**Rule:** If the runbook exists but has `⚠ NAVIN: FILL` markers and you need that specific detail now, ask Navin for ONLY that detail, then update the runbook in the same session. If a runbook does not exist yet, ask Navin **once**, then immediately create it from `docs/runbooks/RUNBOOK_TEMPLATE.md` before proceeding with the task.

---

## The single source of truth map

| Question | Where to look |
|---|---|
| "What does this file do? Where is it? What does it write to?" | `merdian_reference.json` → `files.<filename>` |
| "What's the schema / row count / status of this table?" | `merdian_reference.json` → `tables.<tablename>` |
| "How do I do <recurring operation>?" | `docs/runbooks/` — check `README.md` first |
| "Is this issue known? What's the workaround?" | `tech_debt.md` |
| "Is this a critical bug or a forward proposal?" | `merdian_reference.json` → `open_items` (C-N) for critical · Enhancement Register for ENH-N |
| "What did we decide last time about X?" | `docs/decisions/ADR-<N>-<topic>.md` if it exists, else `session_log.md` grep |
| "How do I run preflight / canary / replay?" | `docs/operational/MERDIAN_Testing_Protocol_v1.md` |
| "What's the commit/branch/deploy rule?" | `docs/operational/MERDIAN_Change_Protocol_v1.md` |
| "When do I write an appendix vs a session note?" | `docs/operational/MERDIAN_Documentation_Protocol_v3.md` |
| "How do I keep a session from degrading?" | `docs/operational/MERDIAN_Session_Management_v1.md` |
| "What were the experiment findings?" | `docs/research/MERDIAN_Experiment_Compendium_v<latest>.md` |

---

## Non-negotiable rules

These are hard rules. Do not propose violations. Do not ask "what if we…".

1. **Edit only in Local.** AWS receives code via `git pull` — never direct edits except BREAK_GLASS (see Change Protocol Step 8).
2. **No run without preflight PASS.** Local commit hash must equal AWS commit hash before any live session.
3. **DB is truth.** Logs are supporting evidence. If a query disagrees with a log line, the query wins.
4. **Full-file promotion only.** No hand-patched partials. Every file in Git is a complete, reviewable file.
5. **Patch scripts must end with `ast.parse()` validation** before writing the target. (Lesson from `force_wire_breadth.py` 2026-04-16 — IndentationError discovered at market open.)
6. **5m bars for ICT pattern detection**, never 1m. 1m is for precise entry timing only after HTF confirms.
7. **Options-only.** Futures experiments are permanently closed (Experiment 2b, 2026-04-12).
8. **Capital ceiling is final:** ₹50L hard cap, ₹25L sizing freeze, ₹2L floor. Do not re-litigate.
9. **OpenItems Register is permanently closed (2026-04-15).** Do not create new `OI-*`, `RESEARCH-OI-*`, or `SPO-*` IDs. Persistent items go to Enhancement Register (ENH-N) or tech_debt.md. Critical production bugs use C-N in `merdian_reference.json`.
10. **No new ID prefix without updating the numbering convention** in `MERDIAN_Documentation_Protocol_v3.md` Rule 5.
11. **Do not ask Navin for file paths or routine operational procedures.** File locations live in `merdian_reference.json` → `files` (keyed by filename). Recurring procedures live in `docs/runbooks/`. If the answer isn't in either, say so explicitly and ask ONCE — then capture the answer as a new runbook using `docs/runbooks/RUNBOOK_TEMPLATE.md` before the end of the session. Next session, it will be there.
12. **Project knowledge is not the git working tree.** Local commits to git do NOT auto-sync to Claude.ai project knowledge. Any session that modifies `CURRENT.md`, `session_log.md`, `merdian_reference.json`, `tech_debt.md`, `MERDIAN_Enhancement_Register.md`, this file (`CLAUDE.md`), or any `docs/operational/*` file MUST re-upload those files to project knowledge before the session is considered closed. Failure to do so causes the next session's Claude to read stale state and either invent a different goal or refuse to proceed (failure mode observed Session 6 → Session 7, 2026-04-22). Treat git commit and project knowledge upload as two separate destinations both required for session close.
13. **Data contamination registry.** Before running ANY research query or experiment that reads hist_* tables, check `public.data_contamination_ranges`. See Rule 13 section below.
14. **`ret_30m` in `hist_pattern_signals` is stored as PERCENTAGE POINTS, not decimal fraction.** e.g., 0.1351 means 0.1351% of spot, NOT 13.51%. Divide by 100 before multiplying by spot price. Sign convention: BEAR_OB wins when `ret_30m < 0` (spot fell). BULL_OB wins when `ret_30m > 0`. Confirmed Session 11 via diagnostic query. Any script that uses this field must apply the division. (Lesson from Exp 41 which inflated E4/E5 P&L by 100x — corrected in Exp 41B.)
15. **Supabase hard-caps at 1000 rows per request.** `range(0, 4999)` still returns only 1000. Always set `page_size = 1000` in pagination loops, and terminate when `len(batch) < 1000`. Confirmed Session 11 — Exp 34 initially fetched only 130 of 18,895 bars because page_size was 5000. (Rule added 2026-04-28.)
16. **TD-029 timezone workaround for hist_spot_bars_5m.** `bar_ts` is stored as IST labeled as `+00:00`. Do NOT use `astimezone(IST)` — this adds 5:30 and shifts all bars. Use `dt.replace(tzinfo=None)` to treat the stored value as naive IST directly. Confirmed Session 11 — Exp 34 initial run had 3,450 bars instead of 18,895 due to this bug. (Rule added 2026-04-28.)
17. **`market_spot_session_markers` column names differ from documentation.** `open_0915` does not exist as a column — the live column is `open_0915_ts`. Do NOT query `market_spot_session_markers` for open price or gap data. Derive `open_0915` from the first tick in `market_spot_snapshots` for the OPEN window (09:15–10:00 IST). Derive `gap_open_pct` from `(open_0915 - prev_close) / prev_close * 100` where `prev_close` is the last bar close from `hist_spot_bars_5m`. Confirmed Session 13 — `detect_po3_session_bias.py` bypasses this table entirely. (Rule added 2026-04-29.)
18. **`trading_calendar` is a trust-anchor — validate it against the official NSE source before trusting ANY holiday gate.** Every holiday gate in MERDIAN fail-opens on this table (a wrong/empty calendar silently defeats all of them). The source of truth is `trading_calendar.json` (read by the V18E rule engine `trading_calendar.py`; the table is seeded from it by `seed_trading_calendar.py`). S60 found the JSON held only 2 (one misdated) of the 15 NSE-2026 equity holidays, so the table mismarked every holiday `is_open=true` since ~April and the pipeline ran the full compute chain on Muharram. When adding or trusting a gate, verify the calendar against the official NSE/BSE holiday list first; a gate over a wrong calendar is worse than no gate (it can suppress a real trading day). The canonical gate is `core/trading_calendar_gate.py` (`is_trading_day_today()` / `assert_trading_day_or_exit(log)`) — import it, do not roll a new inline copy. (Rule added 2026-06-26, TD-S60-NEW-2/3.)

---

## Session contract

Every session has exactly ONE concern. If the goal sentence needs a comma, it has two concerns — split it.

| Session type | Goal | Output expected |
|---|---|---|
| Code debug | Fix one specific failing component | Patched file + tech_debt.md update or close + session_log entry |
| Architecture / planning | Design a component or protocol | New ADR markdown OR Enhancement Register entry |
| Documentation | Produce/update a specific document | The document, committed |
| Live canary | Monitor first live cycle | Canary outcome appended to session_log + git tag if PASS |
| Research / experiment | Answer one quantitative question | Result line in Experiment Compendium + commit |

---

## What Claude must do at session end (every session)

Before saying "done":

```
☐ Update CURRENT.md to reflect what THIS session did and what next session should pick up
☐ Update merdian_reference.json if any file/table/item status changed
☐ Update tech_debt.md if any item was added, mitigated, or closed
☐ Update Enhancement Register if any architectural thinking happened
☐ Update or create runbooks for any operational procedure Navin had to explain this session
☐ Append a one-line entry to session_log.md (date · git hash · concern · outcome)
☐ Commit all documentation changes with prefix MERDIAN: [OPS] ...
☐ Re-upload modified docs (CURRENT.md, session_log.md, merdian_reference.json, tech_debt.md, Enhancement Register, this file, operational protocols) to Claude.ai project knowledge — Rule 12 above
```

---

## Anti-patterns Claude should never repeat

These are mistakes that Sessions 1-N have made. Each one cost real time.

- ❌ Treating `signal_snapshots.action='BUY_CE'/'BUY_PE'` as a real trade. Those are decision rows only. Real trades live in `signal_outcomes` once that table exists. (Session 5 / Session 7 / Session 8 / Session 9 all reverted this misunderstanding.)
- ❌ Reading `MERDIAN_Master_V18.docx` etc. at session start. Generated artifacts. Working state lives in this file + CURRENT.md + tech_debt + JSON.
- ❌ Creating a new ID prefix on the fly. Use the existing IDs in `merdian_reference.json` `open_items` or Enhancement Register. New prefix → ADR.
- ❌ Asking "are you sure?" 8 times before doing the obvious thing. Honor preflight + Change Protocol; once those pass, proceed.
- ❌ Asking Navin for the working directory or file path of a known script. Look it up in `merdian_reference.json` `files.<filename>` first. The JSON is authoritative.
- ❌ Setting `page_size > 1000` in Supabase pagination — Supabase hard-caps at 1000 rows per request regardless. Use `page_size = 1000` and loop. (Session 11 bug B2 / Rule 15.)
- ❌ Using `astimezone(IST)` on `hist_spot_bars_5m.bar_ts` — bar_ts is stored as IST labeled +00:00. Converting timezone adds 5:30 and shifts all bars out of market hours. Use `replace(tzinfo=None)` instead. (Session 11 bug B3 / Rule 16.) **NOTE: This is era-conditional. Pre-04-07 use `replace(tzinfo=None)`; post-04-07 use `astimezone(IST)`. See Rule 20 (Session 15) for era-aware helper.**
- ❌ Using `ret_30m` from `hist_pattern_signals` as a decimal fraction — it is stored as percentage points. Divide by 100 first. (Session 11 Rule 14.)
- ❌ Reading a Python source file with `Path.read_text(encoding='utf-8')` then calling `ast.parse()` on it when the file has a UTF-8 BOM — `ast.parse` rejects U+FEFF with `invalid non-printable character`. Always use `read_bytes() + decode('utf-8-sig')` in patch scripts. (Session 11 extension — v1 of F3 patch caught this correctly and aborted.)
- ❌ Writing a patched file back with `Path.write_text(text, encoding=...)` on Windows when the original file has LF line endings — `write_text` translates `\n → \r\n` on output, silently converting the file to CRLF and producing a noisy `git diff` showing every line modified. Always use `write_bytes(text.encode(enc))` for symmetric byte handling. v3 of F3 patch is the canonical pattern. (Session 11 extension.)
- ❌ Trusting the dashboard EXIT AT label for trade exit timing — the label slices the UTC timestamp string directly (`exit_ts[11:16]`), not the IST-converted version. On a 09:31 IST signal this shows 04:31, not 10:01. Compute exit time manually: signal IST + 30 minutes. (TD-038, Session 11 extension.)
- ❌ Trusting `direction_bias` when `wcb_regime=NULL` in `signal_snapshots` — `wcb_regime` has been NULL since 2026-03-19 (regression, only 32/2171 rows ever populated). On BULLISH breadth days this caused `direction_bias=BEARISH` producing BUY_PE on BULL_FVG. Do not trade on `direction_bias` until TD-035 is fixed and `wcb_regime` is populated. (Session 11 extension live session.)
- ❌ Querying `market_spot_session_markers` for `open_0915` or `gap_open_pct` — the live column is `open_0915_ts`, not `open_0915`. Derive open price from `market_spot_snapshots` first tick in OPEN window. Derive gap from `hist_spot_bars_5m` prev_close. (Session 13 Rule 17.)
- ❌ Running `merdian_start.py` on AWS — this script uses `creationflags=CREATE_NO_WINDOW` (Windows-only) and hardcoded Windows paths. It will hang or error on Linux. Session 13: caused a frozen SSM terminal requiring EC2 reboot. AWS uses `python3 gamma_engine_supervisor.py` or individual script launches.
- ❌ Appending to a Windows batch file with `Add-Content` when there is an `exit /b` line — the appended line runs after `exit /b` and never executes. Always use string replacement to insert before the exit line. (Session 13 bat file patch.)

---

## Project file layout

```
C:\GammaEnginePython\                 (Windows local, PRIMARY LIVE)
/home/ssm-user/meridian-engine\        (AWS, SHADOW — git pull only)

  CLAUDE.md                           <- THIS FILE — root entry point
  *.py                                <- engine code
  .env                                <- secrets, never commit

  docs/
    operational/
      MERDIAN_Change_Protocol_v1.md
      MERDIAN_Documentation_Protocol_v3.md   <- supersedes v2
      MERDIAN_Session_Management_v1.md
      MERDIAN_Testing_Protocol_v1.md         <- consolidated preflight/canary/replay

    registers/
      merdian_reference.json          <- machine-queryable inventory (authoritative on op state)
      MERDIAN_Enhancement_Register_v<n>.md
      tech_debt.md                    <- persistent middle-tier issues

    runbooks/                         <- step-by-step procedures for recurring ops
      README.md                       <- index of all runbooks
      RUNBOOK_TEMPLATE.md             <- template for new runbooks
      runbook_update_dhan_token.md
      runbook_update_kite_flow.md
      runbook_*.md                    <- grows as recurring ops surface

    session_notes/
      CURRENT.md                      <- live session resume -- updated EVERY session
      session_log.md                  <- append-only one-line per session
      YYYYMMDD_<topic>.md             <- per-session detail when warranted

    decisions/                        <- optional ADRs (one per major decision)
      ADR-001-options-only.md
      ADR-002-5m-for-ict.md
      ...

    research/
      MERDIAN_Experiment_Compendium_v<n>.md
      merdian_all_experiment_results.md

    masters/                          <- .docx PUBLISHED ARTIFACTS (generated on demand)
      MERDIAN_Master_V<n>.docx

    appendices/                       <- .docx PUBLISHED ARTIFACTS (generated on demand)
      MERDIAN_Appendix_V<n>.docx
```

---

## Quick environment reference

| Field | Local | AWS |
|---|---|---|
| Base path | `C:\GammaEnginePython` | `/home/ssm-user/meridian-engine` |
| Python | `python` (on PATH; use `py` as fallback) | `python3` |
| Scheduler | Windows Task Scheduler | Linux cron |
| Role | PRIMARY LIVE | SHADOW |
| Instance | — | `i-0878c118835386ec2` (eu-north-1) |
| Access | direct | AWS SSM Session Manager |

For env contracts, runner names, and full file paths, use `merdian_reference.json` → `environments`.

---

## Things that are settled — DO NOT REOPEN

These are decisions made and validated. Re-litigating them wastes session time.

- ✅ Options-only framework (Experiment 2b, 2026-04-12)
- ✅ Capital ceiling ₹50L / ₹25L / ₹2L (Appendix V18F v2)
- ✅ T+30m exit timing (Experiment 8/14b/15, multiple confirmations)
- ✅ 1H zones in MEDIUM context (ENH-37, validated)
- ✅ BEAR_OB AFTERNOON → HARD SKIP (Signal Rule Book v1.1, 17% WR)
- ✅ ICT pattern detection on 5m bars (Research Sessions 4-5, 2026-04-17)
- ✅ ENH-42 WebSocket — DEFERRED post-Phase 4, do not build now
- ✅ OpenItems Register closed (2026-04-15)
- ✅ D-06 signal-consumer concerns (resolved earlier; do not rebuild regret log)
- ✅ **Compendium replicates** (Exp 15 re-run 2026-04-27, Session 10) — BEAR_OB ~92% WR, BULL_OB ~84%, MEDIUM context ~77%, combined +193.4% return. The system has real, durable, year-validated edge. Earlier Session 10 wave-1 conclusion that "compendium does not replicate" was **measurement error in Exp 31/32**, explicitly retracted. Do not re-run Exp 31/Exp 32 as evidence of edge absence.
- ✅ **F1 (ICT zone time_zone classification) SHIPPED** (2026-04-27, Session 10) — `fix_ict_time_zone_utc.py` converted UTC→IST before time-bucket assignment. Verified live.
- ✅ **F2 (1H OB threshold tuning) REJECTED** (Exp 29 v2, 2026-04-26, Session 10) — full-year sweep over {0.15, 0.20, 0.25, 0.30, 0.40}% confirmed current 0.40% maximises WR for NIFTY; SENSEX peaks at 0.30%. No threshold cleared the 70%/N≥30 ship bar. Threshold is not the lever for surfacing more MEDIUM-context candidates.
- ✅ **Path A retracted** (Session 10) — the framing "stop pretending ICT is the edge" was wrong. Compendium replicates. Do not re-introduce Path A under different names.
- ✅ **Naked intraday PDH/PDL sweeps have no edge** (Exp 34, Session 11) — WR=11.1% (PDH), 1.8% (PDL) at T+60m. ~0.73 events/session — normal mean reversion, not institutional. Do not retest without structural change.
- ✅ **PDL DTE<3 next-week CE = SKIP** (Exp 35D, Session 11) — T+1D WR=42.9%. EOD bounce is mechanical expiry pinning, not institutional. Fades next day. Confirmed.
- ✅ **BEAR_OB AFTERNOON + PO3_BEARISH = 33.3% WR** (Exp 40, Session 11) — the distribution move is already done by AFTERNOON on bearish-bias sessions. Hard skip. Do not trade.
- ✅ **BULL_OB MIDDAY + PO3_BULLISH = 30.3% WR** (Exp 40, Session 11) — premature. Bullish accumulation doesn't resolve until AFTERNOON London open. Hard skip.
- ✅ **NIFTY BULL_OB AFTERNOON + PO3_BULLISH = 50% WR** (Exp 40, Session 11) — no edge on NIFTY for this signal. SENSEX only (73.7%). Do not route NIFTY here.
- ✅ **Current-week PE beats next-week PE for PDH DTE<3** (Exp 41, Session 11) — NIFTY mean +46% vs +20%, SENSEX mean +125% vs +68%. Current-week captures gamma explosion. Settled.
- ✅ **Entry at T+0 (rejection bar close) always beats waiting** (Exp 41, Session 11) — waiting 1 bar hurts across all edges and both symbols. Never wait.
- ✅ **TD-017 CLOSED** (Session 11 extension, 2026-04-28) — `build_ict_htf_zones.py` now scheduled daily 08:45 IST via `MERDIAN_ICT_HTF_Zones_0845` Task Scheduler. ENH-71 instrumented. Hourly zones (`--timeframe H`) added to bat file Session 13.
- ✅ **TD-030 CLOSED** (Session 11 extension, 2026-04-28) — `recheck_breached_zones()` added; runs AFTER all upserts (ordering bug fixed Session 13). 72 zones now written per run (was 35). Do not reopen.
- ✅ **TD-031 CLOSED** (Session 11 extension, 2026-04-28) — OB/FVG patterns written unconditionally; breach filter retained for PDH/PDL proximity only. D BEAR_OB will appear ACTIVE at 08:45 IST on next down day regardless of overnight recovery. Do not reopen.
- ✅ **TD-032 dashboard opt_type wrong framing SETTLED** — root cause is NOT 'dashboard hardcodes direction off pattern_type'. Root cause IS `build()` read `opt_type` from `ict_zones.opt_type` (ICT zone direction BEFORE ENH-35 gate overrides). Patched Session 11 extension. Pending 10-cycle live verification to formally close. Do not re-introduce the pattern-hardcoding framing.
- ✅ **ENH-75 SHIPPED** (Session 13, 2026-04-29) — PO3 session bias detection live. `detect_po3_session_bias.py` running Mon-Fri 10:05 IST. `po3_session_state` table live. `signal_snapshots.po3_session_bias` column populated. Do not reopen design.
- ✅ **ENH-76 SHIPPED** (Session 13, 2026-04-29) — BEAR_OB MIDDAY 11:30-13:30 IST gated on PO3_BEARISH. 88.2% WR (Exp 40). Wired in `build_trade_signal_local.py`.
- ✅ **ENH-77 SHIPPED** (Session 13, 2026-04-29) — BULL_OB AFTERNOON SENSEX 13:30-15:00 IST gated on PO3_BULLISH. 73.7% WR (Exp 40). NIFTY hard skip (50% WR). Wired.
- ✅ **Exp 42 DONE** (Session 13, 2026-04-29) — BEAR_OB MIDDAY occurs in 72.5% of all sessions. Unfiltered WR=48%, EV negative. PO3_BEARISH is the rare gate (~7% of sessions). Composition rate question answered. Do not re-run.
- ✅ **ENH-85 direction lock REVERTED** (Session 13) — PO3 session lock patch built and reverted. Needs Exp 43 (Signal Direction Stability) before re-implementing. Do NOT re-apply ENH-85 without experiment backing. `build_trade_signal_local.pre_enh85.bak` on disk.
- ✅ **ENH-78 SHIPPED** (Session 14, 2026-04-30) — DTE<3 PDH sweep current-week PE rule live in `build_trade_signal_local.py`. Guarded by `po3_session_bias=PO3_BEARISH AND 1<=dte<=2 AND action=BUY_PE`. Evidence: Exp 35D 90.9% EOD WR (N=11). Stop rule: 40% premium OR PDH reclaim.
- ✅ **ENH-84 SHIPPED** (Session 14, 2026-04-30) — Dashboard 🔄 REFRESH ZONES button + `/refresh_and_download_pine` endpoint. With hotfix for `sys.executable` reference. Live verified.
- ✅ **ENH-86 v1 SHIPPED** (Session 14, 2026-04-30) — WIN RATE legend extended to 7 columns with EV + N. Live rows for E4/E5 added at top. v2 (BLOCKED/ALLOWED visual prominence) deferred — not blocking.
- ✅ **TD-044 CLOSED** (Session 14, 2026-04-30) — ENH-76/77 local var / `out` dict drift fixed. Three-site sync in `build_trade_signal_local.py`. Side effect: file line endings normalised to uniform CRLF. Do NOT reopen — ENH-76/77 gates now correctly persist to DB headline fields.
- ✅ **TD-038 EXIT AT IST PATCH SHIPPED** (Session 14, 2026-04-30) — `merdian_signal_dashboard.py` `card()` now converts UTC→IST for the static EXIT AT label. Mirrors sig_ts conversion. Live verification pending next TRADE_ALLOWED signal.
- ✅ **Breach detection ordering FIXED** (Session 13) — `recheck_breached_zones()` now runs after all `upsert_zones()` calls. Upsert no longer overwrites BREACHED→ACTIVE. Fixed permanently.
- ✅ **ADR-008 Accepted** (Session 24, 2026-05-09) — Replay is a parallel-pipeline sandbox for what-if signal experiments — comparison of two replay runs with one code change, against the same frozen historical data — not a tool to chase bit-identical reproduction of live `signal_snapshots`. Zero touch on live tables; out-of-hours hard guard; boundary-driven orchestrator (scripts in V19 §5.2 order PER BOUNDARY, not script-by-script). Strike-base / spot-granularity / VIX-source divergences from live are documented architectural properties, not bugs. ENH-93 CLOSED via this ADR. The "What 'what-if experiment' means" methodology canonical entry lives in ADR-008.
- ✅ **Phase α Q1 ANSWERED** (Session 25, 2026-05-10) — Zone validity model = (a) pure price-based canonical with timeframe-tiered fallback intraday-only. 1H OB/FVG = price-breach OR 1 week (whichever first); D/W OB/FVG = price-breach only, `valid_to=NULL`; PDH/PDL = date-expire (unchanged). Three implementation actions queued for ADR-005 drafting: `expire_old_zones()` rewrite by `(pattern_type, timeframe)`, `recheck_breached_zones()` as primary D/W transition, backfill pass for D/W zones currently date-EXPIRED unbreached. Independent of ADR-006; can draft anytime. Do not re-litigate; the answer is settled.
- ✅ **Phase α Q2 ANSWERED** (Session 25, 2026-05-10) — AWS migration scope = (a) capture/derived split with four-stage decomposition. Capture stage (`market_spot_snapshots`, `option_chain_snapshots`, `india_vix`, `market_breadth_intraday`, `ict_htf_zones`) → AWS canonical, no Local writers. Derived stage (`gamma_metrics`, `volatility_snapshots`, `momentum_snapshots`, `market_state_snapshots`, `signal_snapshots`) → Local canonical for production; AWS shadow continues writing to `*_shadow` tables. Orchestration stage (runner) → both Local (production) and AWS (shadow) parallel. Operator-facing tooling (dashboard, signal dashboard, exit monitor, trade logger, ICT zone visualizer) → Local only. ADR-006 will codify; do not re-litigate the principle.
- ✅ **Phase α Q3 ANSWERED** (Session 25, 2026-05-10) — Sequencing: token reliability FIRST, then ADR-006 actions. Investigate `refresh_dhan_token.py` failure mode (TD-080) → fix → observe N clean trading days → only then execute ADR-006 disposals. Local writers stay as redundancy until AWS reliability empirically established. **ADR-006 drafting is BLOCKED on TD-080 closure.** Do not re-litigate the order.
- ✅ **Phase α Q4 ANSWERED** (Session 25, 2026-05-10) — Calibration discipline = graduated-strictness holdout (operator deferred to architect recommendation). Phase 1 (now → ~April 2027 / Y2 close): mandatory holdout split scaled by N — N≥60 → 67/33, 10pp tolerance; 30≤N<60 → 75/25, 15pp; N<30 → "low-N calibration-only" tag, no split required. Existing Exp 15-era params get 60-day prospective parity check, flag drift >15pp. Phase 2 (Y2 close): rolling walk-forward — 12-month calibration / 3-month holdout, slide quarterly. Status quo single-cohort silent waiver explicitly REJECTED. ADR-009 will codify; §D.8 of Assumption Register has the working draft.
- ✅ **TD-097 RESOLVED + ENH-96 SHIPPED same-session** (Session 25, 2026-05-10) — Dashboard pre-open status URL-encoding bug producing 0% accuracy widget; root cause: double URL encoding from passing already-encoded query string into `requests.get(..., params={...})`. Fix: collapse into single fully-encoded URL via `urllib.parse.urlencode()`. Patch script `patch_s25_dashboard_preopen_gap.py` (v3 patch canon) deployed 5 substitutions to `merdian_live_dashboard.py`; backups `_PRE_S25.py` + `_PRE_S25b.py` preserved. ENH-96 dashboard "Gap (vs prev close)" card shipped as side-effect — data was already captured in `market_spot_snapshots`, just not surfaced. Do not revert the URL-encoding fix; same bug pattern exists in 5 other scripts (TD-099 sweep pending).
- ✅ **TD-080 REFRAMED** (Session 25, 2026-05-10) — Original framing "Dhan option chain endpoint reliability" narrowed to "AWS Dhan token refresh failure mode" based on cross-script Dhan 401 evidence on 2026-05-07 (PreOpen 03:38 UTC + option chain 09:30-13:30 IST + 14:45-15:25 IST consistent with single token-refresh failure on AWS, not Dhan-side service incident). Investigation surface narrows from "Dhan API" to "`refresh_dhan_token.py` running on AWS at 03:05 UTC". Explicit Blocks: ADR-006 drafting per Phase α Q3. Do not investigate Dhan-vendor-side hypotheses again until AWS-token-refresh hypothesis is fully tested.
- ✅ **TD-078 RESOLVED** (Session 25, 2026-05-10) — TD-070 v2 multi-week BULL_OB lookback verified via SQL. The apparent missing Apr-13 row was a schema-convention misunderstanding (W-timeframe `source_bar_date` is week-start Monday, not arbitrary calendar date). TD-070 v2 fires as designed. **Convention codified:** `ict_htf_zones.source_bar_date` semantics differ by timeframe — W = week-start Monday, D = bar's calendar date, 1H = hour bucket date. Whenever debugging "missing zone row" claims, check the timeframe-aware convention before concluding the row is absent.
- ✅ **`MERDIAN_PreOpen` (Local 09:05 IST) DISABLED** (Session 25, 2026-05-10) — Auction-window writer disposed via PowerShell `Disable-ScheduledTask`, durable across reboots. Operator semantic: "9:05 read meaningless" (call auction prices are not tradeable price discovery). Code dependency check: `ret_session` migrated 09:05 → 09:08 anchor and validated via ADR-008 replay infrastructure (first non-construction use of replay system). AWS sole writer at 09:08. Mon 2026-05-12 verification plan in Topology §9.A. Do not re-enable Local 09:05 task without reverting `ret_session` anchor.
- ✅ **Topology §9 Q1 + Q2 CLOSED** (Session 25, 2026-05-10) — §9 Q1: post-market 16:00 dual-write empirically confirmed across 2026-05-04 → 2026-05-08 (5 trading days); disposition queued for ADR-006 execution gated on TD-080. §9 Q2: original framing inaccurate (no actual dual-write at 09:08 IST; AWS sole writer at that boundary; Local 09:05 was different boundary). §9 Q8 PARTIAL EVIDENCE (Postmarket cron 5-day reliability captured). §9.A NEW documents Local PreOpen disposal + ret_session migration. Do not re-investigate "is there dual-write at 09:08" — answer is no.
- ✅ **ADR-002 v2 ACCEPTED** (Session 27, 2026-05-11) — Market structure philosophy v2 supersedes v1. Six v1 principles preserved (P1 zones, P2 force, P3 trapped sellers, P4 velocity, P5 PINNED, P6 DTE multiplier); two new — P7 vol-pricing (RR ratio thresholds HIGH>1.2 / FAIR / LOW<0.85 / COMPRESSED<0.4) and P8 second-order Greeks (vanna/charm for Phase 3). Buyer/Writer inversion named as first-class architectural concept — same gamma layer, mirrored interpretation by strategy mode. Positioning Landscape refined to five named scalars (gamma_wall_strike, short_strike_for_strike, gradient_max_strike, hedged_long_cr, lambda_score_pct). Six-scenario dealer-flow grid (±0.5%, ±1%, ±2% with contracts + Cr + trajectory). New `vol_analytics` table spec. Methodology specifications mandatory before build. Phase 0 calibration discipline introduced (~3-4 sessions before ENH-80 schema lands). Phase 3 prerequisites named explicitly — four parallel tracks beyond P1-P8 (risk framework, multi-leg execution, SPAN+ELM margin model, tax model). Falsification criteria per principle. ENH-84 NEW (vol_analytics + RR ratio) + ENH-85 NEW (vanna/charm Phase 3 prep) filed. Phase 0a §3 sign-convention audit PASS validates foundation soundness. Build sequence: Phase 0 → ENH-80 → ENH-84 → ENH-81 → Exp 23 → ENH-82 → buyer-polarity gates → ENH-85 → ENH-83. Do not re-litigate; the v2 architecture is settled.
- ✅ **TD-NEW-2 RESOLVED** (Session 27, 2026-05-11) — `flip_level` regression starting 2026-05-08 across both NIFTY and SENSEX (3+ days of stuck values ~21,250 disconnected from operational flip near spot). Root cause: Dhan started returning spurious gamma values ~7e-5 on deep-ITM CE strikes (e.g., strike 21,250 CE with spot 24,200, gamma 0.00007 = 70× ATM gamma; impossible for legitimate deep-ITM option); bottom-up cumulative-walk flip-finder resolved zero-crossing at first spurious contribution, not at operational flip. Latent algorithm fragility surfaced by Dhan response-shape change, NOT a code regression. Fix: two-part patch in `compute_gamma_metrics_local.py` — Part A sanity guard in `signed_gamma_exposure()` (reject `|strike-spot|/spot > 5%` AND `|gamma| > 5e-5`); Part B walk-from-ATM in `compute_flip_level()` (returns zero-crossing nearest spot, legacy fallback preserved). Verified: HEALTHY 2026-05-07 delta -12pts (0.05%, no regression); BROKEN 2026-05-08 patched flip 25,060 near spot vs stuck 21,250 live (+3,810pts correction). Backup `compute_gamma_metrics_local_PRE_TD-NEW-2.py` preserved. Same-session NEW+RESOLVED pattern (precedents: TD-097 S25, TD-101 S26). Codified: deep-ITM input sanity at signed_gamma_exposure layer + operational flip definition (near-spot zero-crossing, not bottom-up first crossing). Commit `241f943`.
- ✅ **TD-NEW-3 RESOLVED** (Session 27, 2026-05-11) — `net_gex` stored ~10³ too large vs operational Crore convention; gamma engine had been writing raw rupees since deployment but field semantics implied Cr; silent because all downstream consumers are sign-only (regime classification, expansion probability, structural manipulation gate). Surfaced via cross-comparison to source-material Cr dashboard values during Phase 0a sign audit. Fix: `/1e7` added to `signed_gamma_exposure()` / `signed_gex()` in three writer files — `compute_gamma_metrics_local.py`, `replay/replay_compute_gamma_metrics.py`, `backfill_gamma_metrics.py`. Reader codebase audited — zero magnitude thresholds, unit change non-disruptive to every gate. Verified: 2026-05-11 09:30 IST cycle LIVE 775,285,881,741 (raw rupees) → PATCHED 78,544.84 (Crore); ratio 9,870,615 (98.7% of expected 1e7, within 5%); operationally sane (100 < |x| < 1M Cr range). Three backups `_PRE_TD-NEW-3.py` preserved. Same-session NEW+RESOLVED pattern (third instance). Codified: when introducing magnitude-consuming gates, audit existing column unit conventions against source-of-truth references FIRST before threshold tuning. Commit `241f943`.
- ✅ **Phase 0a §3 sign-convention audit PASS** (Session 27, 2026-05-11) — MERDIAN `gamma_metrics.net_gex` sign matches external source-material across 25 of 26 sessions in 2026-04-01 to 2026-05-11 lookback (78 SHORT_GAMMA rows on bearish trend day 2026-05-11; 37/76 SHORT on 2026-04-29; 8/149 SHORT on 2026-04-27; sign correctly flips on confirmed bearish sessions). Ref 2 source-vs-MERDIAN disagreement at 2026-04-28 12:23 IST traced to LIVE-vs-PROJECTED reader-side categorisation error (source dashboard panel showed projected dealer-flow scenario for hypothetical -1% spot move, not live net GEX state). Sign convention is sound. Unblocks ADR-002 v2 ENH-80 (per-strike GEX table) + downstream builds. Do not re-audit sign; the answer is verified.
- ✅ **TD-NEW-2 + TD-NEW-3 P0 verification PASS — live cycle** (Session 28, 2026-05-12 09:25 IST) — S28 P0 mandate closed in ~10 minutes after market open. 09:15 IST cycle on Local + AWS confirmed both patches working on live data: `flip_level` dynamic (not stuck), `net_gex` in Crore range (78K–924K Cr per cycle, not trillions of raw rupees). Pre-patch corruption signal absent from `gamma_metrics` rows tagged 2026-05-12 onwards. P0 closure recorded in CURRENT.md. Drift period of ~22h followed P0 closure; surfaced TD-NEW-12 + TD-NEW-4 + TD-NEW-13 (see below). Do not re-verify; the live cycle evidence is the verification.
- ✅ **TD-NEW-12 RESOLVED** (Session 28, 2026-05-13) — Shadow architecture not implemented for `gamma_metrics_shadow`. AWS `compute_gamma_metrics_local.py` had been writing to production `gamma_metrics` table since deployment (~2026-04-29), creating race-condition double-writes with Local — UPSERT semantics determined which value persisted per cycle. `gamma_metrics_shadow` table existed in Supabase but had 0 rows for 13 days. Architectural invariant per Deployment Topology §6.5 silently violated. Fix: two coordinated patches — (1) `compute_gamma_metrics_local.py` (commit `72622a9`) module-level `USE_SHADOW = "--shadow" in sys.argv` sniff + `TARGET_TABLE = "gamma_metrics_shadow" if USE_SHADOW else "gamma_metrics"` constant; SELECT (fetch_prior_gamma_metrics) + UPSERT (upsert_gamma_metrics) + ExecutionLog telemetry all route via TARGET_TABLE. (2) `run_merdian_shadow_runner.py` line 479 (commit `de23467`) appends `"--shadow"` to compute invocation. Schema reconciliation: `gamma_metrics_shadow` had 7 missing columns vs `gamma_metrics` (dte, gamma_zone, otm_oi_velocity, raw, run_type, spot_vs_range, straddle_velocity) — `ALTER TABLE IF NOT EXISTS` to add all; `ADD CONSTRAINT gamma_metrics_shadow_symbol_ts_key UNIQUE (symbol, ts)` for upsert on_conflict; `NOTIFY pgrst, 'reload schema'`. Smoke tests Local + AWS: Path 1 (no flag) writes to `gamma_metrics`; Path 2 (`--shadow`) writes to `gamma_metrics_shadow`; `actual_writes` telemetry honest in both paths. Codified: D.11.1 in Assumption Register; Topology §6.5 update + new §6.8 gotcha. Architecturally enforced going forward; not just narratively. Do not undo the `--shadow` flag plumbing.
- ✅ **TD-NEW-4 RESOLVED** (Session 28, 2026-05-13) — `dte` payload field in `compute_gamma_metrics_local.py::upsert_gamma_metrics()` was computed as `(date.fromisoformat(result.expiry_date) - date.today()).days`. Live writes were correct because result.ts ≈ now (within seconds). Backfill writes were systematically wrong — running compute on 2026-05-12 NIFTY data on 2026-05-13 produced `dte = -1` instead of `dte = 0` (the run was on its expiry day). Latent bug, surfaced during TD-NEW-12 smoke test. Fix: `_dte_from_ts(result)` helper added at module level deriving as-of date from `result.ts` (timestamp field) in IST instead of wall clock; payload line uses helper. Bundled in commit `72622a9` with TD-NEW-12. Cross-validated 2026-05-12 NIFTY run_id `e2dd1a09-...`: post-patch dte=0 (correct), pre-patch dte=-1 (wrong). Codified: D.11.2 in Assumption Register. Canonical pattern: all `dte`-class temporal payloads derive as-of date from result's timestamp field, not wall clock. Do not revert.
- ✅ **TD-NEW-13 RESOLVED** (Session 28, 2026-05-13) — Python stdlib `datetime.fromisoformat()` not portable across runtime versions for variable-microsecond-precision timestamps. Python 3.10 (AWS) accepts only 3 or 6 microsecond digits; Python 3.12 (Local) is permissive. Supabase serializes PostgreSQL timestamps with variable precision (2-7 digits common). TD-NEW-4 `_dte_from_ts` helper passed Local smoke (3.12) but failed 60/587 backfill cycles on AWS (3.10) with `ValueError: Invalid isoformat string`. Fix: regex normalize microseconds to exactly 6 digits via pad/truncate before `fromisoformat()` in `_dte_from_ts` helper. Commit `447634c`. Retry on the 60 failed run_ids: 60/60 success post-patch. Codified: D.11.3 in Assumption Register; Topology §6.9 new gotcha; B22 below. Do not assume Local smoke is sufficient — AWS Python 3.10 has stricter stdlib semantics.
- ✅ **TD-NEW-5 + TD-NEW-6 + TD-NEW-8 RESOLVED** (Session 28, 2026-05-13) — Three S28 config / scheduler fixes: (5) `run_ict_htf_zones_daily.bat` patched to chain `generate_pine_overlay.py` as Call 3 after H-zone build (was previously manual every session). (6) Local Task Scheduler `MERDIAN_WS_Feed_0900` task DISABLED via `Disable-ScheduledTask` (durable) — dead-stub firing daily and polluting logs; actual WS feed is MERDIAN-AWS-only. (7 — numbered TD-NEW-8) MERDIAN AWS crontab `02 10 * * 1-5 pkill -f ws_feed_zerodha.py` upgraded to `pkill -9 -f` (SIGTERM → SIGKILL); WS process ignored SIGTERM, accumulated 9 zombies over Apr 30 → May 11 (1.4GB RAM impact). All three are config-only, no code change. Topology §2 + §7.1 + §7.2 updated. Do not revert.
- ✅ **P1 broken-window `gamma_metrics` backfill CLOSED** (Session 28, 2026-05-13) — 587/587 rows across 2026-05-08 + 2026-05-11 (NIFTY 149+144=293 rows + SENSEX 150+144=294 rows) re-computed via patched `compute_gamma_metrics_local.py` on AWS. Initial 60 failures cleared post-TD-NEW-13 fix. Post-backfill: `still_unpatched=0` on all 4 day/symbol combinations. avg_abs_netgex flipped from trillions to Cr range (181K-924K avg per day/symbol). flip_level dynamic (no longer stuck deep-ITM). Phase 0b overlay calibration data prerequisite met; Phase 0c methodology selection data prerequisite met. Both still blocked on **ENH-84 build** for RR ratio dimension specifically, but data integrity is no longer a blocker. Codified: D.11.4 in Assumption Register. Do not re-backfill; the 587 rows are clean.
- ✅ **P2 Enhancement Register formal filing: ENH-84 + ENH-85 PROPOSED** (Session 28, 2026-05-13) — Deferred from S27 by operator consent. `MERDIAN_Enhancement_Register.md` Part 1 status table + Part 4 full entries added for both. ENH-84 (vol_analytics table + RR ratio writer, ADR-002 v2 P7) Tier 1, gating Phase 0b. ENH-85 (vanna/charm second-order Greeks, ADR-002 v2 P8) Tier 2, deferred until Phase 2 plan committed. Cross-references wired to ADR-002 v2 §Schema + §P7/§P8 and Assumption Register §D.10.1 + §D.10.8. Update log row added. Both entries are scope-formalization (PROPOSED), not shipped-state writeups — the right framing per ADR-002 v2 build sequence. Build is **next session's primary mandate** (S29 = ENH-84 build, ~1-2 sessions). Do not re-debate filing framing.
- ✅ **MALPHA catalogued as third environment in Deployment Topology** (Session 28, 2026-05-13) — Topology §1 expanded to three-environment side-by-side (Local + MERDIAN AWS + MALPHA); new §1.5 explains why MALPHA exists separately (Zerodha browser-redirect TOTP auth; MERDIAN AWS cannot run that flow headless). Current Zerodha token propagation MALPHA → MERDIAN AWS is a manual `sed` step (TD-NEW-7 S1 filed for Supabase-based automation mirroring Dhan flow). Two outages (2026-04-22, 2026-05-12) trace to the manual step. Catalog gap closed; the dependency is unmissable in future Topology reads. Do not re-discover MALPHA — it is now in §1.5.
- ✅ **`merdian_order_placer.py` catalogued as MERDIAN-AWS-only service** (Session 28, 2026-05-13) — Phase 4B Order Placer (HTTP server port 8767, Dhan-IP-whitelisted Elastic IP `13.63.27.85`, `@reboot` cron) has been running deployed since 2026-04-29 but was not in Deployment Topology §3 AWS-only scripts. TD-NEW-10 filed as "process running un-audited" → investigated → CLOSED filed-in-error (intentional Phase 4B service, AWS-only because home network IP unstable for Dhan whitelist). TD-NEW-11 (S3 documentation gap) filed and closed simultaneously in S28 doc-close rewrite. Topology §3, §7.1 @reboot, §8.2 logs all updated. Do not re-flag the process.

- ✅ **ADR-012 ACCEPTED** (Session 34, 2026-05-24) — Spot-anchored stop-loss doctrine for ICT retest entries; supersedes informal Compendium-era 30%+ premium SL default. Governance: *"For ICT retest entries on zone primitives (OB, FVG, Breaker, Mitigation, Premium/Discount), the stop-loss is anchored to spot zone-invalidation on 5m-close-through — NOT to option premium decay. BEAR retest stopped out when 5m spot bar closes ≥ `zone_high * (1 + X)`; BULL retest stopped out when closes ≤ `zone_low * (1 - X)`. Buffer X = 0.5% provisional default. Premium-based stops on ATM held strikes are the wrong instrument-stop pairing — they measure microstructure noise (bid-ask, theta on tight DTE, IV crush) not thesis validity. Time-based exit policy (ENH-107) is orthogonal and composes independently."* Empirical foundation n=7 same-day retest entries S34 backtest: 5% intra-bar premium SL → 0/7 winners; 10% → 0/7; 15% → 3/7; 1m-close-through 5%/10% → 0/3 winners. Spot-anchored 0.3%/0.5%/0.8% adverse 5m-close → **0/0/0 SL triggers, 7/7 winners at every level**. Held-to-EOD total +₹106,888 on 1 lot (NIFTY ₹87,266 + SENSEX ₹19,622). Validation cohort expansion to target n≥50 gated on TD-S34-NEW-4 closure (unlocks 15 lost post-Apr-2026 entries) + ENH-108 N-touch retest detection (unlocks the 112 invisible-retest move-days). Per-TF calibration of X analogous to ADR-004 §11 RETEST_TOLERANCE_PCT deferred until validation cohort reaches n≥50. Doctrine consistent with ADR-004 §10 primitive breach rule template (close-through, not wick-pierce). Implementation pending: signal-emitter `sl_level` computation, order placer 5m bar-close exit logic, Pine overlay SL-line rendering, `ict_primitive_outcomes` SL columns. Do not re-litigate the wick-noise premium-SL approach; premium stops on ATM held strikes are the wrong instrument-stop pair, full stop.
- ✅ **Universal big-move-day capture rate 99.2% (118/119)** (Session 34, 2026-05-24) — MERDIAN's ICT primitive layer sees every ≥1% intraday move structurally over the 14-month vendor window. Audit: 119 ≥1% intraday move-days across 2025-04-01 → 2026-05-19 (57 NIFTY + 62 SENSEX, parity threshold) joined to `ict_primitives` with direction-aligned + zone-interaction filters; 118 captured, 1 miss (NIFTY 2025-05-20 DOWN — DISPLACEMENT_DOWN existed but price didn't reach zone). Origin distribution: FVG 84% (BEAR_FVG 47%, BULL_FVG 36%), OB 16%; timeframe D+W 74% (D 47%, W 27%), H 14%, M5 11%. Standout zone-reuse cases: 2026-04-06 W BULL_FVG fired on 4 ≥1% UP days; 2025-04-04 D BEAR_FVG (~354 days old) anchored the March 2026 crash days; 2025-04-15 W BULL_FVG triggered 5 reactions across the year. **Capture is not the binding constraint on trade discoverability — selection is.** Codified as D.16.1 + D.16.2 in Assumption Register. Do not re-derive capture-rate audit on the same 14-month window; result is established.
- ✅ **Selection problem articulated** (Session 34, 2026-05-24) — Selection (which active zone of 50+ fires today) is the unsolved problem; capture (does MERDIAN have an aligned zone at all) is solved at 99.2%. Selection-research arc framed as 3-5 session work item; framework doc deferred to S35+ per operator sequencing (SL fix → file gap → file arc → first feature). Five candidate selection features identified for future research: pre-open structural signature (gap + multi-TF aligned + sweep); same-day momentum confirmation gate (first H bar close direction); HTF context alignment (require W direction alignment for D zones); zone freshness (first-touch > N-touch retest hit rate); DTE-aware option leg selection (extends S33 DTE 3-bucket finding). ENH-108 N-touch retest detection is the foundational schema unlock for selection research — without per-touch outcomes data, "is this primitive being touched right now for the Nth time and what's the empirical hit rate by N?" cannot be answered. Do not collapse selection into capture; they are distinct problems with distinct solutions.
- ✅ **ENH-108 PROPOSED** (Session 34, 2026-05-24) — Second-touch / N-touch retest detection on ICT primitives. ADR-004 §10 records only `first_retest_ts` and `retest_status` (PENDING/RETESTED/NEVER_RETESTED/BREACHED_BEFORE_RETEST); subsequent touches of the same active zone (mechanically identical retests by ICT canon) are invisible to outcomes data. Of 118 captured ≥1% move-days, only 7 had `first_retest_ts` on the move-day itself — the other 111 are powered by non-first-touch retests of HTF zones formed days/weeks earlier. Recommendation: new `ict_primitive_touches` table (FK to `ict_primitives.id`, one row per discrete touch event with `touch_seq INTEGER` and forward/option PnL columns) over JSONB extension. Foundational for selection-research arc; cost 2-3 sessions; partial backfill on pre-Apr-2026 window feasible immediately, full validation gated on TD-S34-NEW-4 closure. Prior retest-cohort research (D-FVG retest 78-86% WR, H-FVG retest 60-76% WR, S33 v7 cohort findings) holds for the first-touch population it measured but underweights the live universe of actionable structures. Do not interpret ENH-108 as invalidating prior research; it expands the addressable cohort, not refutes the first-touch findings.
- ✅ **TD-S34-NEW-4 FILED — `hist_option_bars_1m` post-2026-04-01 coverage gap (vendor → MERDIAN-ingest tier transition)** (Session 34, 2026-05-24). Two-query diagnostic returned zero chain rows for both NIFTY and SENSEX on all nine probed post-Apr-2026 dates (2026-04-01, 04-07, 04-09, 04-13, 04-16, 04-24, 05-12, 05-14, 05-18). Pre-Apr-2026 dates in the same cohort (2025-05-08 → 2026-01-20) returned full chain data. Operator-confirmed root cause: pre-Apr-2026 chain is vendor-purchased and dense across the 12-month window paid for and verified; post-Apr-2026 chain is MERDIAN-ingested via a different writer path. The "MERDIAN failed to ingest entirely" hypothesis cannot be the full picture — plenty of full-ingestion days post-Apr-2026 have been observed elsewhere in the system (S29-S33 cohort work consumed data from that window). Two diagnostic paths: (a) post-Apr-2026 chain data lives in a sibling table/schema not under the `hist_option_bars_1m` name, writer was never extended to mirror to canonical chain table; (b) partial-ingest failure scoped to a subset of dates. Workaround: restrict backtests to `trade_date <= '2026-03-30'`. Initial diagnostic framing "Kite token expiry on MALPHA (TD-080-adjacent)" superseded per operator. Earlier S29 chain-table operational findings (54.8M-row table, PostgREST 8s timeout, custom index pattern) remain valid for the pre-Apr-2026 window. Codified as D.16.4 in Assumption Register. Do not re-investigate as token failure; the diagnostic path is tier-transition + ingest writer locate.

- ✅ **ADR-015 ACCEPTED** (Session 37, 2026-05-25) — Per-strike GEX schema v2: 12-column minimum-sufficient-statistic on `gex_strike_snapshots` `(run_id, strike, expiry_date, symbol, ts, spot, gamma_call, gamma_put, oi_total_calls, oi_total_puts, gex_cr, source_table)`. Sign convention: positive `gex_cr` = dampening (CE +base, PE -base). UNIQUE (run_id, strike, expiry_date). Supersedes ADR-014 same session — drops single `gamma` column (replaced by `gamma_call`+`gamma_put` to preserve IV skew evidence at NIFTY 24050 ATM gamma_call=0.00164 vs gamma_put=0.00234 +43% PE-richer = put-skew regime) and drops 3 derived booleans (`is_local_max`, `is_flip_zone`, `is_pin_candidate_bool`) that fired on 39% NIFTY / 20% SENSEX rows = near-zero information content. Governance: *"Derivations belong in read layer (SQL views), not write layer. The schema stores what cannot be recomputed; everything derivable from spot + strike + premium + OI is a view, not a column."* Writer shipped via `patch_s37_enh80_writer_v2.py` (marker `# ENH-80 (S37) v2`). Smoke-fire post-migration: NIFTY 144 strikes abs_diff=9.8e-11 Cr against total `net_gex` in `gamma_metrics`; SENSEX 261 strikes abs_diff=1.2e-14 Cr. §F1 dealer-vs-positioning GEX split deferred — `v_oi_prev_close_snapshots` shipped as scaffold for future writer integration via OI-change history. Do not re-add boolean columns to the write layer; views are the answer.
- ✅ **ADR-014 SUPERSEDED same session by ADR-015** (Session 37, 2026-05-25) — Original 16-column `gex_strike_snapshots` schema with `gamma` (single) + `is_local_max` + `is_flip_zone` + `is_pin_candidate_bool` accepted post smoke-fire then immediately superseded upon quant review of marker noise (39%/20% fire rate). Patch trail preserved (`patch_s37_enh80_writer.py` v1, `compute_gamma_metrics_local_PRE_S37.py` backup) for git audit; live schema migrated to v2 via TRUNCATE + ALTER (4 cols dropped, 2 added). Do not restore v1 schema; the marker-noise finding generalizes — derived booleans at write layer almost always misclassify when their threshold is regime-conditional.
- ✅ **ADR-016 PROPOSED, build deferred** (Session 37, 2026-05-25) — Parameter calibration pattern: temporal-immutable `merdian_parameters` table `(id, key, value jsonb, value_type, category, description, min_value, max_value, valid_from, valid_to, changed_by, change_reason)` with mandatory non-empty `change_reason` enforced at DB layer, dot-hierarchical keys (`pin_zone.tau.NIFTY`), TTL-cached read API `core/parameters.py`, CLI `merdian_calibrate.py`. Design surfaced 9 magic-number clusters needing same treatment (pin/accel τ, dealer-flow scenarios ±0.5/1/2%, audit thresholds, retry budgets, NIFTY/SENSEX strike intervals, ENH101 stop_distance_pct, ENH102 H-FVG transfer tolerance, ATM_PNL_WINDOWS_MIN, RR-regime thresholds). ENH-83 build deferred per operator — hardcoded τ=0.3 in ENH-81 views sufficient for single-cohort use; ENH-81 ships with `// TAU_PIN — swap for ENH-83 lookup` markers throughout views for the future plumbing point. Per-parameter audit trail (`changed_by` defaults `'SuperUser'`, `change_reason` non-empty enforced) is the design's anti-drift mechanism. Do not start ENH-83 build until N grows enough for cohort-driven recalibration to become useful.
- ✅ **GEX-as-context-not-gate** (Session 37, 2026-05-25) — Operator framing: *"Meridian doesn't need anymore gates. It's already gating everything."* MERDIAN already operates 5 gating layers (ADR-001 stability+validity two-layer gate; ENH-55 falsified and env-disabled; routing-data-driven SKIP for BEAR_FVG; proximity tiers T1/T2/T3 on Pine overlay; ADR-009 graduated-strictness holdout calibration). ENH-80 + ENH-81 ship as **display layer** (Lovable dashboard for at-a-glance regime read; Pine overlay PIN/ACCEL zones for chart-time corroboration) — not as automated routing inputs to `build_trade_signal_local.py`. PE/CE GEX split preserved in schema (ADR-015 `gamma_call`/`gamma_put`) but not surfaced in dashboard or Pine — research-grade column for future inversion-based dealer-flow research. Operator is the integration layer for GEX-derived decisions. Do not wire GEX as a gate in `build_trade_signal_local.py` without explicit operator request AND N≥30 live-cohort validation per D.13.1 cohort-translation discipline.
- ✅ **ENH-80 per-strike GEX writer SHIPPED** (Session 37, 2026-05-25) — `compute_gamma_metrics_local.py` extended via `patch_s37_enh80_writer_v2.py` (marker `# ENH-80 (S37) v2`, backup `_PRE_S37_v2.py`) to write per-strike GEX rows to new `gex_strike_snapshots` table per ADR-015 v2 schema. NIFTY 144 strikes / 96 call + 73 put gamma coverage per cycle (deep-ITM strikes only have one liquid side, expected). SENSEX 261 strikes / 149 call + 92 put. Falsification gate PASS cross-symbol: net = sum(per-strike `gex_cr`) abs_diff ≤ 1e-10 Cr against total `net_gex` in `gamma_metrics`. Substrate for ENH-81 Positioning Landscape (PIN/ACCEL zones) + ENH-82 Pin Risk Score continuous (depends on live cohort accumulation). Do not re-litigate per-strike vs scalar-only — per-strike is the canonical substrate for any positioning-aware metric.
- ✅ **ENH-81 Positioning Landscape SHIPPED** (Session 37, 2026-05-25) — Three SQL views materializing PIN zone + ACCEL zone + dealer flow sim from `gex_strike_snapshots`: `v_gex_strike_pin_zone` (prominence-around-peak walking by strike-value via recursive CTE with direction tracked via CASE; τ_pin=0.3 hardcoded with `// TAU_PIN — swap for ENH-83 lookup` marker per ADR-016 deferral); `v_gex_strike_accel_zone` (symmetric walk around trough); `v_dealer_flow_sim` (six-scenario grid ±0.5/1/2% per ADR-002 v2 P2 force-not-direction). Falsification gates 4/4 PASS: prominence math correct, zone bounds match peak±τ on test cohort, dealer flow direction-correct on all 12 scenarios (NIFTY+SENSEX × 6 scenarios). Pine overlay v1+v2 SHIPPED via `patch_s37_enh81_pine_overlay.py` + `patch_s37_enh81_pine_overlay_v2.py`: PIN zone box (semi-transparent green) + ACCEL zone box (red) + range labels anchored at zone edges (PIN top via `style_label_lower_left`, ACCEL bottom via `style_label_upper_left`) + `bar_index+90` X-offset to clear ICT labels. Single-strike degeneracy resolved in v2: zones widening ±0.1% of strike (50pt floor) ensures visible band rather than zero-height line. Lovable dashboard live at `lovable.dev/projects/e8fde6f9-58d0-4444-860d-508b3635b014` with NIFTY/SENSEX tabs, REGIME card, total GEX exposure, GEX-by-strike histogram with PIN/ACCEL ReferenceArea bands, summary strip. RLS policies + GRANT SELECT shipped per-table for anon-read (`gex_strike_snapshots`, `gamma_metrics`, `v_gex_strike_pin_zone`, `v_gex_strike_accel_zone`, `v_dealer_flow_sim`). Do not retrofit Pine to render dealer-flow scenarios inline — operator threshold: *"when I find I'm mentally computing."* Per ADR-016, τ_pin/τ_accel will become parameter-table lookups when ENH-83 builds.


- ✅ **ENH-110 Phase 1 SHIPPED** (Session 39, 2026-05-27) — Consolidated Marketview Build Phase 1 live at `http://13.63.27.85/marketview` via Lovable → GitHub → AWS pipeline. Frontend Vite + React + shadcn/ui + Tailwind SPA served from EC2 nginx (i-0878c118835386ec2, EIP 13.63.27.85, port 80, no TLS, no domain — internal operator dashboard). 6 Lovable iteration turns at `lovable.dev/projects/5988f9ff-50e5-483d-8947-c30528ec8960` (operator paid tier upgraded mid-S39): Turn 1 wrong-template `tanstack_start_ts` discarded; Turn 2 SPA conversion `72dc296`; Turn 3 Supabase client `6b95ded` (`.env` committed to PUBLIC repo — anon-key trust model VALIDATED per D.21.2); Turn 4 data wiring with 3 schema adaptations `2998819 Wired Marketview & Settings` (`pin_lower`/`pin_upper` cols + `ict_zones` no `valid_to` use `detected_at_ts` + `signal_snapshots` no `tier` use `entry_quality NOT IN ('SKIP','NO_TRADE')`); Turn 5 polish 6 fixes (label stagger 16px + chart x-clip + stale 60→300s + ICT nearest-10 + entry_quality color chips + SENSEX fallback); Turn 6 final polish (NIFTY 50pt / SENSEX 100pt strike-grid ticks + scroll-zoom/drag-pan/r-reset + ATM straddle VRD-style today vs 5d avg). Live data rendering verified — NIFTY 23,922.6 + ACCEL [23,600-23,900] + PIN [24,200] + ATM straddle ₹308·5d avg ₹299; SENSEX 75,922.22 + dealer flow ±195,839 Cr + ATM straddle ₹43·5d avg ₹204. AWS hosting via `setup_marketview_aws_host.sh` (nginx 1.18.0 + Node 20.20.2 + git clone + vite build + `dist/` → `/var/www/marketview/`). Phase 2 (confluence detection primitive writer + IV skew + journal hooks) and Phase 3 (salience signals + intelligence layers) remain PROPOSED — separate session scope. Do not undo the Lovable→GitHub→AWS pipeline; it is the canonical Marketview ship vector. Hosting via Lovable Cloud + new Supabase project explicitly rejected per operator constraint *"never via Lovable Cloud, only via our existing Supabase project"*.
- ✅ **ENH-83 SHIPPED** (Session 39, 2026-05-27) — Calibration console + temporal-immutable `merdian_parameters` table live. Lovable auto-scaffolded the table mid-session with typed-column schema (4 typed value cols `value_text`/`value_num`/`value_bool`/`value_jsonb` + `value_type` discriminator + 4-way XOR constraint) NOT operator's originally-planned single `value jsonb` design. The typed-column variant was retained — it strictly dominates jsonb at MERDIAN's parameter scale (zero cast overhead in hot-path read RPCs + DB-enforced type safety via `chk_value_present` XOR + self-documenting column names). S39 trimmed ALTER added the constraints + indexes + RPCs not already present: 4 CHECK constraints (`chk_change_reason_nonempty` enforces ADR-016 mandatory non-empty change-reason at DB layer + `chk_value_type_valid` + `chk_value_present` 4-way XOR + `chk_valid_from_to`) + unique partial index `uniq_merdian_parameters_active_key (key) WHERE valid_to IS NULL` (enforces single active row per key) + 3 STABLE SQL read RPCs `get_parameter_num/text/bool` returning typed column directly (INVOKER) + 1 SECURITY DEFINER write RPC `update_parameter(p_key, p_change_reason, p_value_num/text/bool/jsonb default NULL, p_changed_by default 'operator')` enforcing full ADR-016 contract end-to-end (mandatory non-empty change_reason validated before any write + FOR UPDATE row lock + numeric range-check against min_value/max_value + atomic CLOSE-old + INSERT-new in same transaction) + `GRANT EXECUTE TO anon` so Settings tab writes via JWT-scoped function not direct table write + `v_merdian_parameter_audit` reverse-chronological view. 11 active param rows seeded by Lovable (pin/accel τ NIFTY+SENSEX + sl_buffer + retest_tolerance + capital + ict_zone TTL). TD-S37-01 closure path opened — `patch_s39_enh83_view_tau_rewrite.py` staged for operator run against ENH-81 views. Do not re-design `merdian_parameters` schema; typed-column variant is canonical.
- ✅ **Lovable auto-grant safety REFUTED — RLS triplets require pre-deploy anon-grants audit** (Session 39, 2026-05-27) — D.21.1 REFUTED + TD-S39-NEW-1 S2 REMEDIATED. Initial assumption: Lovable auto-scaffold grants anon SELECT-only or no privileges by default; RLS triplet (`ALTER TABLE ENABLE RLS + CREATE POLICY FOR SELECT + GRANT SELECT TO anon`) applied after scaffold should be sufficient. Empirical finding: Lovable granted anon ALL privileges (DELETE/INSERT/UPDATE/TRUNCATE/REFERENCES/TRIGGER/SELECT) on every consumed table+view. Anon-grants audit post-RLS-triplet returned 91 rows = 7 privilege types × 13 surfaces. Threat: anon JWT inlines into Vite bundle at build time via `import.meta.env.VITE_SUPABASE_URL/ANON_KEY` — publicly visible via DevTools on any deployed dashboard; with full grants, anyone holding the site URL could `TRUNCATE signal_snapshots` via PostgREST regardless of RLS (RLS filters rows; GRANT lets role attempt operation). Remediated same-session via PL/pgSQL DO block iterating 13-surface array with `EXECUTE format('REVOKE ALL ON public.%I FROM anon', t) + EXECUTE format('GRANT SELECT ON public.%I TO anon', t)`. Post-remediation audit returned 13 rows all SELECT. Codified: **Any Lovable schema operation requires post-deploy anon-grants audit; the canonical RLS triplet pattern must be extended to `REVOKE ALL FROM anon + GRANT SELECT TO anon` as the security-first sequence, not just policy + grant.** Do not deploy Lovable consumer-facing dashboards without running `audit_anon_grants.sql` (queued for repo, runs as Supabase SQL editor block) pre-deploy on every Lovable iteration that touches the schema.
- ✅ **Anon-key-in-public-repo trust model VALIDATED** (Session 39, 2026-05-27) — D.21.2 VALIDATED. Lovable auto-committed `.env` containing live Supabase anon JWT + project URL to PUBLIC `balannavin-cyber1/meridian-connect` repo without prompting (commit `6b95ded`). Trust model holds: anon JWT inlines into Vite bundle at build time and is publicly visible via DevTools on any deployed dashboard regardless of repo visibility. Adding the key to a public GitHub repo provides no additional exposure beyond what's already accessible to anyone with the site URL. Worst-case exploitation via the public key is reading data already visible via the dashboard URL — provided RLS triplets restrict anon to SELECT-only (D.21.1 + TD-S39-NEW-1 remediation). Trust model is sound; practice is sloppy — TD-S39-NEW-3 S3 filed for operator-side `git rm --cached .env` + push (Lovable cannot push removal commits from its side). Do not treat anon-key-in-public-repo as a security incident absent RLS misconfiguration; the security boundary is the RLS policy + GRANT pair, not key secrecy.
- ✅ **IMDSv2 attached-SG check mandatory before AWS SG edits — REFUTED operator-console-name memory** (Session 39, 2026-05-27) — D.21.3 REFUTED. AWS networking debug arc lost hours because operator was editing orphan `launch-wizard-2` SG (unattached, residue from earlier instance) while attached SG was `launch-wizard-1`. Browser connectivity to `13.63.27.85:80` failed despite VPC route table + NACL + iptables all clean; root cause was SG edits applied to wrong SG. Resolution: IMDSv2 token query — `TOKEN=$(curl -s -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600") && curl -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/security-groups` returned `launch-wizard-1`. Adding TCP 80 from `0.0.0.0/0` to `launch-wizard-1` produced instant connectivity. Codified: **Before editing any AWS SG, run the IMDSv2 attached-SG query from the running instance; do not rely on AWS Console naming or operator memory.** TD-S39-NEW-4 S4 filed to delete orphan `launch-wizard-2` after audit. Do not edit SGs by name without IMDSv2 verification.

- **Orchestrator crontab syntax:** `cd /path && source .env && flock -n /lock timeout 90 python3 script.py` (cd and source BEFORE flock, relative paths work, env available). (S48)
- **Volatility table canonical:** `volatility_snapshots` per ADR-006 Derived layer; write target for both LOCAL (production) and AWS (non-shadow after S48 fix). Not `compute_volatility_metrics`. (S48)
- **Cron graceful degradation:** Script exit_code=0 on known failures OK (404 table missing), but log the condition even on success for operational visibility. (S48)

- **Cron `SHELL=/bin/bash` is mandatory as crontab line 1 on AWS (S53):** cron defaults to `/bin/sh` (dash), which lacks the `source` builtin — every `cd … && source .env && python3 …` chain dies silently with `/bin/sh: source: not found` into discarded cron mail. A dropped SHELL directive (crontab reinstall 00:47 UTC 2026-06-12) caused a ~28h total capture/compute blackout. Probe: `/bin/sh -c 'source .env && echo CHAIN_OK'` plus `pwd; echo HOME=$HOME; SHELL=$SHELL`. Verify the SHELL line is present as line 1 after ANY crontab change.
- **Monitors must not share the failure chain of what they watch (S53):** all four S52 monitors used the same `source .env` prefix as the capture layer and died with it (watchdog + patient together), so the blackout was invisible — the watchdogs were dead too. Monitor env-loading must be independent of the chain it monitors.
- **Live ingest cron form is UNQUOTED `bash run_ingest.sh NIFTY FULL` (S53):** `run_ingest.sh` self-sources `.env`; do NOT reintroduce S49's single-quoted `'NIFTY FULL'` arg. The unquoted form is what is live and working.
- **Upserts can mask per-symbol write loss (S54):** the volatility insert→upsert(on_conflict=symbol,ts) correctly stopped 409 crashes but converted SENSEX's lost/repeated writes from a loud failure into a silent merge — SENSEX compute silently under-writes ~½ of cycles (TD-S54-NEW-1). After any insert→upsert change, audit per-symbol distinct-ts coverage to confirm writes aren't being merged away.

- ✅ **ADR-018 ACCEPTED** (Session 57, 2026-06-19) — Breadth-feed supervision model + signal-subsystem disposition. (D1) `ws_feed_zerodha.py` runs under a `systemd` unit on its canonical host **MALPHA** (Restart=on-failure + single-instance enforcement + journald) — one host owns the Zerodha session end-to-end; unsupervised `screen`/`nohup` deployment of a long-running broker feed is prohibited going forward. WCB cron arg fixed same pass. (The units + wsfeed preflight + alert were already built+committed in S56 — `deploy/systemd/`, afe8112/30cca59/b627914; S57's remaining work is enable/cutover onto MALPHA, not authoring — TD-S57-NEW-1.) (D2) every reader of `market_breadth_intraday` / WCB / `structural_divergence_snapshots` applies a **recency-floor guard** on `fetch_latest_row` so a silent upstream stop self-flags STALE — MANDATORY, hard precondition for ENH-SDM. (D3) **SMDM RETIRED** as built (evidence-based retirement vs ENH-30: Exp 9 NEUTRAL verdict + redundant gamma-squeeze scalar); STOP_HUNT/SQUEEZE flags + gamma-squeeze scalar (Thesis B) DROPPED; manipulation-footprint (Thesis A, redefined) + flow-velocity (Thesis C) carried into the replacement. (D4) **ENH-SDM** structural-divergence monitor (`compute_structural_divergence_local.py` → `structural_divergence_snapshots`; 4 primitives + phase classifier + direction; offensive/defensive modes) lands **AWS orchestrator-integrated per ADR-006** — a kept subsystem, NOT a Local orphan (the deliberate inversion of retired SMDM's Local placement); gated on D1+D2 live + ADR-009 out-of-sample net-of-costs on post-ban data before any capital. Orphaned signal subsystems (options_flow / iv_context / shadow-v3, dropped by the S49 Local-disable) are dispositioned explicitly — retire-with-evidence or port-to-AWS — never left silently dead. Do not re-litigate; settled.

- ✅ **ENH-07 A CLOSED — no-op, flat r** (Session 62, 2026-07-01) — The basis-implied risk-free rate is a no-op on two grounds and stays closed. (1) *Empirical:* a flat r=6.5% Black-Scholes reconstruction validated against live `gamma_metrics` (sign 99% / magnitude 0.96× / sign-regime 95%) — the basis-implied rate is immaterial to the GEX read. (2) *Architectural:* the live gamma engine reads vendor (Dhan) Greeks with no rate injection, so there is no live rate parameter to correct; the rate only ever mattered inside the historical Greeks backfill (TD-S58-NEW-1), which solves at flat r=6.5% (`core/bs_engine.py` substrate). Supersedes the S61 "REFRAMED/open" status. The S57 cost-of-carry / basis-velocity sign-read shipped separately as ENH-07 B (S61). Do not re-open the rate question absent evidence the flat-r reconstruction diverges from live.

- ✅ **Expiry-day 0-DTE flat-vol net_gex is numerically UNRECONSTRUCTIBLE intraday → live-sourced, not reconstructed** (Session 62, 2026-07-01) — As T→0 the ATM gamma blows up and net_gex becomes a near-singular sum of explosive nearly-cancelling terms; a flat-vol reconstruction is stable in the morning (ratio ~0.97) but detonates into sign-flipping chaos in the afternoon (−7×/+181M) — `diag_1125.py` on 2025-11-25. Rule: expiry days are classified **live-sourced** (union live `gamma_metrics` tagged `source='live_gamma_expiry'`; backfill emits an explicit logged `SKIPPED_EXPIRY`), never papered over with a T-floor. This is the same discipline as ADR-001 stable-lies — do not loosen the reconstruction to force a number where the math is singular. (Note: the pre-existing `hist_gamma_metrics` handled expiry days cleanly because it used real vendor Greeks, not flat-vol — it does not have this disease.)

- ✅ **Pre-existing-table discovery discipline; `hist_gamma_metrics` is the canonical historical gamma series** (Session 62, 2026-07-01) — Always check for an existing table before CREATE. A full-window (Apr 2025–Mar 2026) both-symbol 1-minute `hist_gamma_metrics` series already existed (net_gex/flip_level/regime populated) with its ONE gap being an empty `gamma_concentration` column — so the task collapsed from "recompute everything" to "fill one column." Hard-won facts about it: **net_gex is stored UNSCALED** (×1e7 vs live's /1e7 Cr; median ratio ~1.007e7); its `bar_ts` is **IST-clock-as-UTC at `:59`** (live `ts` is real UTC at `:00`; shift = −5h30m + truncate-to-minute); `gamma_concentration = max|gex|/sum|gex|` (Herfindahl dominance ratio, **scale-invariant** — so the 1e7 convention is moot for the fill). This table (post-S62) is the canonical historical gamma series for both symbols, full-window, and the substrate for the ENH-116 expiry-memory seed.

- ✅ **iv is the master key — lean sidecar schema is correct** (Session 62, 2026-07-01) — In the historical per-strike Greeks sidecar (`hist_option_greeks_1m`), storing only `iv`+`gamma` costs nothing: every Greek recomputes from iv+S+K+T+r, and concentration is scale-invariant and needs only gamma. Dropping iv would have been the real mistake. Vendor `hist_option_bars_1m` is never mutated; the sidecar is additive.

- ✅ **"Correct-and-slower beats fast-and-subtly-wrong" — abandon divergent optimizations, never loosen the gate** (Session 62, 2026-07-01) — A `--fast` day-batched concentration path failed `--verify` (max |diff| 0.0754) with a residual divergence that six diagnostic rounds could not isolate (gamma/oi/strikes/expiry all measured bit-identical; the probe's own reconstruction had a bug). It was ABANDONED as dead code behind `--fast`, NOT loosened to pass. Same discipline held on the 2025-11-25 expiry case (skipped, not floored). A `--verify` divergence means the approach is wrong, not the threshold.

- ✅ **Cross-engine (StockMojo) parity isolates bugs** (Session 62, 2026-07-01) — Where two independent GEX engines agree, the structure is real; where they diverge, the divergence pinpoints the bug and its trigger. On the SENSEX flip bug (TD-S62-NEW), StockMojo matched MERDIAN exactly on regime, accel zone, amplify strike, and walls — the flip was the sole outlier, which is what confirmed it as a MERDIAN `compute_flip_level` bug (spurious deep-tail crossing under a NEGATIVE_γ near-spot short-γ pit) rather than a data/chain issue. Use cross-engine parity as a validator; treat the single divergent field as the diagnosis.

- ✅ **ENH-116 Ambient Environment Intelligence BUILT + DEPLOYED end-to-end (backend) — the nightly “what kind of day is it” verdict now writes, accrues, and carries its own historical receipt (Session 64, 2026-07-04).** PROPOSED/specced S62 → built this session as `market_environment_snapshots` (the once-nightly four-lens verdict store) + `expiry_outcomes` (65-row retroactive seed, 29 NIFTY / 36 SENSEX, forward-accruing) + `v_expiry_base_rates` (Phase-B base-rate view). Four writers: `compile_market_environment_local.py` (post-market compiler — L1 gamma-regime persistence, L2 price-vs-breadth divergence, L3 participant tilt from `participant_oi_daily` with an ADR-018 D2 recency guard [STALE board → NULL, never a fabricated tilt], L4 macro NULL until a feed exists; `reconcile()` v2 folds L3 into the verdict — breadth-vs-participant is the divergence pair, gamma the amplification modifier; writes the N-floored Phase-B receipt into `regime_conditional_note`), `relate_ambient_to_open_local.py` (pre-market reconciler, idempotent — strips the prior OPEN relate-segment before appending so a manual run + the cron replace rather than stack), `backfill_expiry_outcomes.py` (the 65-row seed), `accrue_expiry_outcomes.py` (forward-accrual labeler — labels each expiry under the live v2 verdict, refuses to label without an ambient row, `source=expiry_memory_live`). All three loop scripts are `ExecutionLog`-instrumented to `script_execution_log`. Crons on MERDIAN AWS: compile `0 16` (16:00 UTC, after the ENH-115 participant ingest so L3 is fresh), accrue `15 16` (expiry-days only), relate `55 3` (at open). The seed cohort is entirely `ambient_regime=RANGE` — a seed degeneracy that forward accrual fixes as expiries resolve under varied verdicts. Do not re-run the seed; do not schedule the compiler on Local (that double-writes the row, the same class as the S28 `gamma_metrics` race). Remaining: Lens 4 macro (feed-blocked), the Marketview intraday-drift banner (prompt produced, deploy pending), Phase-C analog retrieval (deferred — “maybe-never”).
- ✅ **`ast.parse` alone does not prove a Python edit is safe — a `str_replace` insertion can silently eat a `def` header and leave an orphaned body that parses clean and NameErrors only at runtime (Session 64, `1be5239`→`61df758`).** The Phase-B helper insertion swallowed the `def compile_symbol(...)` header; `ast.parse` passed on the orphaned body, and the break surfaced only when the compiler ran. Rule (already canon, re-instanced here as a live case): every `str_replace` to a Python file is validated with `ast.parse` **and** `py_compile` **and** a named-function-existence check — the bare parse is insufficient. Codified as the S64 instance of the canon-v3 edit gate.
- ✅ **Marketview graduated from an internal `http://13.63.27.85/marketview` IP page to a public, TLS-terminated, Google-auth-gated app at `https://marketview.meridianalpha.in` (Session 64).** GoDaddy A-record `marketview` → `13.63.27.85`; TCP 443 opened on the SG *attached* to `i-0878c118835386ec2` (confirm the attachment via IMDSv2 first — this box has burned hours on orphan `launch-wizard-*` SGs, per S39); certbot Let’s-Encrypt cert + http→https redirect; oauth2-proxy (Google provider) in front of nginx with an `authenticated_emails_file` allow-list (`/etc/oauth2-proxy-emails.txt`). The load-bearing lesson: **`auth_request` must sit at server level on the *serving* 443 block, and the gate is only proven by a cross-browser no-session test — not by `nginx -t` or a reload.** The patcher twice scattered `auth_request` into an orphan certbot `return 404` block, leaving the serving block ungated; the reload succeeded both times and the site let everyone in. Server-level auth covers `/`, `= /index.html` (an exact-match location that would *not* inherit a `location /` gate), and assets together, with `/oauth2/` exempted. Codified in Deployment Topology §S64.
- ✅ **Marketview v5 is a multi-page terminal, not a single scroll — pages split by the question each answers (Session 64).** Six content pages behind a persistent left rail (Home / Positioning / Max Pain / Breadth / Structure / Expiry Memory) + operational sections (Health / Order / Journal / Calibration), a global NIFTY/SENSEX toggle driving every page, a 60s auto-refresh aligned to the 1-minute spot-capture cadence with an “updated Ns ago” motion ticker (ADR-017 motion-over-timestamps). Home surfaces the ENH-116 ambient verdict + four-lens strip + expiry-memory base rates and the open-shift callout; max pain is windowed to ±20 strikes (NIFTY 50-grid / SENSEX 100-grid, sliced before render); a Net Dealer γ intraday card shows Rising/Falling/Flat. The “two clocks” framing is explicit: Home carries OPEN SHIFT (open-vs-settled, once at ~09:25) and — once the drift banner deploys — INTRADAY DRIFT (live-vs-settled, every 60s), so the once-nightly settled verdict is never silently stale.
- ✅ **Dhan EOD historical publish-lag is expected, not a stall — `compute_date_window()` (T−1, 220-day) is correct; don't patch the window to force a missing latest day in (Session 65, 2026-07-07).** 07-03/07-06 absent from `equity_eod` after a sweep was **Dhan-side EOD availability lag**, not an ingest bug: `compute_date_window()` uses a fixed T−1 lookback (`to = today−1`, `from = to−220`) with no lower-bound pin and no cursor-date exclusion, so on 07-07 the window (2025-11-…→2026-07-06) already *includes* the “missing” days — they simply hadn't published yet. Dhan's daily historical endpoint routinely lags 1–3 days on settled EOD (the `EOD_SAFE_LAG_DAYS` machinery exists precisely for this) and the hole self-heals on the next EOD run. Rule: a missing latest-day-or-two is provider publish-lag, not a defect — do not loosen the window or the denominator to paper over it (same discipline family as ADR-001 stable-lies).
- ✅ **The EOD coverage guard's denominator is the active-universe / latest-EOD-date ticker count (~1,159), not the nominal ~1,385 universe; staleness is measured off the last *trading* day with a ~3-day Dhan-lag tolerance (Session 65, TD-S65-NEW-1).** `check_eod_coverage_freshness.py` was mis-tuned against the nominal 1,385 universe over a 5-day window, which produced a `/1` false-OK. `COMPLETE_EOD_THRESHOLD_PCT=95` is correct because it measures against the *active* universe (~1,159, the DMA layer rebuilt clean at 100% of active to 07-02). The guard is display-not-gate — the ingest is correct; only the freshness check needs the grounded 1,159 / last-trading-day / ~3-day-tolerance values.
- ✅ **OAuth credential rotation via a *new* Google client changes BOTH `client_id` and `client_secret` — and the exposure closes only when the OLD client is deleted, not when the new one goes live (Session 65, ENH-117 security fold).** The `client_secret` leaked in the S64 nginx-config dump was rotated by creating a new OAuth client (which changes both fields); update both lines in `/etc/oauth2-proxy.cfg` (leave `provider`/`redirect_url`/`cookie_secret`/`authenticated_emails_file`), set the redirect URI to `https://marketview.meridianalpha.in/oauth2/callback` char-for-char, clear JS origins (server-side flow), restart `oauth2-proxy` (→ `active (running)`), then verify login, delete the old client, and run the non-allow-list denial test. Rotation applied this session; the delete + denial-test closeout is the carry.

If any of these need to change, that is itself an architectural session — write a new ADR.

---

## Rule 13 — Data contamination registry (added Session 7, 2026-04-23)

MERDIAN tracks known data-integrity incidents in the Supabase table `public.data_contamination_ranges`. Before running ANY research query, experiment analysis, or model training that reads fields listed in `field_scope` from tables listed in `affected_tables`, check whether the query time window overlaps with a registered contamination range.

**Standard check — SQL helper:**

```sql
SELECT public.is_breadth_contaminated(ts) FROM your_query;
-- Or filter:
WHERE NOT public.is_breadth_contaminated(ts)
```

**For non-breadth fields:**

```sql
SELECT * FROM public.data_contamination_ranges
WHERE field_scope ILIKE '%your_field%';
```

**When to add a new entry:**

Whenever a new data-integrity incident is diagnosed, INSERT a row into `data_contamination_ranges` with:
- Unique `contamination_id` (pattern: `SCOPE-DESCRIPTION-YYYY-MM-DD`)
- `field_scope` (comma-separated list of affected column/field names)
- `contamination_start` and `contamination_end` (timestamptz, IST)
- `affected_tables` (array of table names, including views' underlying tables)
- `root_cause` (what broke)
- `remediation` (how it was fixed)
- `created_session`

**Current registered contamination ranges (2026-04-23):**
- `BREADTH-STALE-REF-2026-03-27`: 27-day breadth cascade (Session 7). See `merdian_reference.json` TD-NNN for context.

**Anti-pattern:** Running experiments on historical data without first checking `data_contamination_ranges`. Research conclusions drawn on tainted data are worse than no conclusions.

---

## Session 11 engineering discoveries (2026-04-28) — now codified as Rules 14-16

Three bugs were found and fixed across all Session 11 experiment scripts. They are now rules so future sessions don't repeat the debugging cycle:

**Bug B1 → Rule 14:** `hist_pattern_signals.ret_30m` is percentage points, not decimal. Divide by 100.

**Bug B2 → Rule 15:** Supabase pagination max is 1000 rows/request. Use `page_size = 1000`.

**Bug B3 → Rule 16:** `hist_spot_bars_5m.bar_ts` stored as IST labeled `+00:00`. Use `replace(tzinfo=None)`, not `astimezone(IST)`. **(Era-conditional — pre-04-07 only. See Rule 20 for the post-04-07 path: `astimezone(IST_TZ)`.)**

**Bug B4 (non-rule, one-time):** `hist_spot_bars_5m` has no `is_pre_market` column. Filter by `09:15 ≤ bar_ts_IST ≤ 15:30` instead.

---

## Session 11 extension engineering discoveries (2026-04-28) — operational safety

Three operational findings from the Session 11 extension (engineering + live session). These are anti-patterns codified above (Rules not needed — these are one-time discoveries, not recurring schema bugs):

**Patch script encoding hazards (now anti-patterns):**
- BOM: `ast.parse` rejects U+FEFF in string. Use `read_bytes() + decode('utf-8-sig')` in all patch scripts. v1 of F3 patch caught this correctly via abort.
- CRLF: `write_text()` on Windows translates `\n → \r\n`. Use `write_bytes(text.encode(enc))`. v3 of F3 patch is canonical template.

**Live session findings (2026-04-28, 09:15-15:30 IST):**
- **FIRST GATE OPEN**: SENSEX `trade_allowed=true` fired at 09:16 IST (BULL_FVG TIER2 VERY_HIGH MTF). ENH-46-A Telegram delivered simultaneously. First-ever production gate open.
- **wcb_regime regression**: `direction_bias=BEARISH` while `breadth_regime=BULLISH` all session. `wcb_regime=NULL` since 2026-03-19 (32/2171 rows ever populated). Without WCB, direction computation is unreliable on breadth-driven days. **TD-035 elevated to S2. Do not trade direction_bias signals until fixed.**
- **EXIT AT timer shows UTC**: dashboard EXIT AT label slices UTC timestamp directly. Compute exit time manually: signal IST + 30 min. TD-038 filed. **Live trading risk until fixed.**
- **SENSEX DTE=2 on expiry day**: expected DTE=0 on 2026-04-28 monthly expiry. TD-039 filed.

---

## Session 13 engineering discoveries (2026-04-29) — codified as Rule 17

**Bug B5 → Rule 17:** `market_spot_session_markers.open_0915` does not exist in production. Live column is `open_0915_ts`. Never query this table for open price. Use `market_spot_snapshots` first tick instead.

**Operational findings (2026-04-29):**
- **ws_feed_zerodha.py not in any startup sequence** — `market_ticks` table empty all session → breadth ingest returns 0 ticks → breadth = 0 / stale all day. Fixed: `MERDIAN_WS_Feed_0900` task registered.
- **merdian_start.py is LOCAL-ONLY** — contains `creationflags=CREATE_NO_WINDOW` (Windows Win32 API) and hardcoded `C:\GammaEnginePython` base path. Running on AWS causes terminal hang. Requires EC2 reboot to recover. Do not run on AWS.
- **bat file `Add-Content` pitfall** — appending lines after `exit /b` with `Add-Content` means they never execute. Always replace the exit line, don't append after it.
- **Pine `bar_index - look_back` goes negative** — on daily charts, zones >252 trading days old produce negative bar indices, silently killing all drawings. Fix: `math.max(0, bar_index - look_back)`.
- **Pine `draw_zone` forward-reference** — `is_nifty`/`is_sensex` must be declared BEFORE `draw_zone` function, not after. Pine evaluates top-to-bottom; forward references fail at runtime.

---

## Session 14 engineering discoveries (2026-04-30) — codified as Rules 18-19

**Bug B6 → Rule 18:** Patch scripts MUST be line-ending agnostic. After three sessions of patches, files accumulate mixed line endings — Session 14 found `build_trade_signal_local.py` with 1039 CRLF + 87 bare-LF lines. Single-EOL `replace()` fails when the anchor crosses a mixed-EOL boundary. Canonical pattern:

```python
src_raw = TARGET.read_bytes().decode("utf-8-sig")
crlf = src_raw.count("\r\n")
bare_lf = src_raw.count("\n") - crlf
write_eol = "\r\n" if crlf >= bare_lf else "\n"

# Match in LF-space (anchors are LF in patch source)
src_lf = src_raw.replace("\r\n", "\n")
patched_lf = src_lf.replace(OLD, NEW)

# Restore predominant EOL on write
patched_out = patched_lf.replace("\n", write_eol) if write_eol == "\r\n" else patched_lf
TARGET.write_bytes(patched_out.encode("utf-8"))
```

This pattern also normalises mixed line endings as a side effect — file becomes uniformly EOL-consistent post-patch.

**Bug B7 → Rule 19:** Before writing endpoint code that references module-level attributes (`sys.executable`, `os.path`, etc.), grep imports in target file at module level. Imports inside functions don't expose those names to top-level / endpoint scope. Session 14 ENH-84 endpoint used `sys.executable` but `merdian_signal_dashboard.py` only had `import sys as _sys` deep inside `build()` — endpoint scope had no `sys` reference. Hotfix replaced with literal `"python"`.

```bash
# Quick check before referencing module attributes in patches:
grep -n "^import\|^from " target_file.py
```

**Operational findings (2026-04-30 morning):**
- **AWS SSH IP rotation** — operator's home network has multi-WAN with Airtel/BBNL failover. SG inbound rules with `/32` IPs break whenever ISP fails over. Long-term fix: AWS Systems Manager Session Manager (no SSH, no IP, no SG port 22 rule needed). Documented in tech_debt as operational note (not yet a TD).
- **Dashboard zombie listeners** (recurring) — multi-PowerShell-window habit during testing leaves orphan instances bound to port 8766. Standard pattern: `netstat -ano | findstr :8766 | findstr LISTENING` → `taskkill /F /PID <pid>` for each → restart. Long-term fix: move dashboard to Task Scheduler entry instead of foreground PowerShell.
- **`build_ict_htf_zones.py` contract violation noise** — script declares `expected_writes={'ict_htf_zones': 1}` unconditionally; legitimately writes 0 on idempotent reruns and pre-market windows. Fires false-alarm Telegram alerts. Filed TD-046. Operator must correlate alert against `script_execution_log` to identify the canonical 08:45 scheduled run vs noise reruns.
- **`ict_zones` vs `ict_htf_zones` confusion** — TWO TABLES with different schemas. `ict_zones` (BULL_FVG only, 54 rows total, schema: `symbol, trade_date, pattern_type, zone_low, zone_high, detected_at_ts, status`) is largely orphaned for current pipeline. `ict_htf_zones` (OBs+PDH/PDL+W zones, schema: `id, symbol, timeframe, pattern_type, direction, zone_high, zone_low, zone_mid, valid_from, valid_to, source_bar_date, status, broken_at_date, break_price, created_at, updated_at`) is the canonical table — what dashboard, Pine generator, and signal engine consume. Filed TD-047. Always query `ict_htf_zones` for D/W/H zones with `valid_from <= today <= valid_to AND status='ACTIVE'`. Reserve `ict_zones` queries for explicit BULL_FVG intraday questions.
- **Numbering collision in `tech_debt.md`** — TD-038 and TD-039 each appear twice (Active section + Resolved section, different topics). Pre-existing register hygiene issue. Do NOT renumber — the dual entries are now distinguishable by content (TD-038-A=is_pre_market column; TD-038-B=EXIT AT IST). Fixing collisions retroactively risks breaking external references.

---

## Session 15 engineering discoveries (2026-05-02) — codified as Rule 20

**Bug B8 → Rule 20:** Rule 16 (`hist_spot_bars_5m.bar_ts` use `replace(tzinfo=None)` instead of `astimezone(IST)`) is **era-conditional, not universal**. The era boundary is **2026-04-07**.

| Era | Storage convention | Correct handling |
|---|---|---|
| **Pre-04-07** (legacy ingest) | Bars stored as IST clock-time labelled `+00:00` (TD-029 root cause) | `bar_ts.replace(tzinfo=None)` then filter by `09:15 ≤ time ≤ 15:30` (Rule 16 verbatim) |
| **Post-04-07** (current writer) | Bars stored as true UTC | `bar_ts.astimezone(IST_TZ)` then filter by `09:15 ≤ time ≤ 15:30` |

Applying Rule 16 verbatim to post-04-07 data drops most of the day. Concretely: `replace(tzinfo=None)` on a UTC-stored bar produces a UTC clock-time. Filtering UTC clock-time to 09:15-15:30 IST keeps only bars in the UTC 09:15-10:00 window (= IST 14:45-15:30, the last 45 min of session) → ~9 of ~76 in-session bars per day = **27.5% bar coverage** false alarm.

**Canonical era-aware helper** (use this in any script that needs in-session 5m bars across the era boundary):

```python
from datetime import time, timezone, timedelta

IST = timezone(timedelta(hours=5, minutes=30))
ERA_BOUNDARY = "2026-04-07"  # exclusive: dates < this use Rule 16; >= this use astimezone

def in_session_filter(bar_ts, trade_date_str):
    """Returns True if bar is inside 09:15-15:30 IST, era-aware."""
    if trade_date_str < ERA_BOUNDARY:
        clock = bar_ts.replace(tzinfo=None).time()  # Rule 16 (legacy)
    else:
        clock = bar_ts.astimezone(IST).time()       # Post-04-07 (true UTC)
    return time(9, 15) <= clock <= time(15, 30)
```

**Simpler alternative** — use the `trade_date` column for date filters and `bar_ts` only for ordering. Eliminates the era-awareness need entirely. Used in `diagnostic_bar_coverage_audit_v3.py`:

```python
# Filter on trade_date (string) instead of bar_ts time:
rows = sb.table("hist_spot_bars_5m") \
    .eq("symbol", symbol) \
    .gte("trade_date", str(start_date)) \
    .lte("trade_date", str(end_date)) \
    .order("bar_ts") \
    .range(0, 999) \
    .execute().data
```

**Affected scripts identified Session 15** (verbatim Rule 16 → era-aware fix needed):
- `adr003_phase1_zone_respect_rate.py` v1/v2 (verdict INVALID — Phase 1 v3 will use era-aware)
- `experiment_44_inverted_hammer_cascade.py` (verdict survives caveat re-evaluation; v2 cleaner)

**Bug B9 (non-rule, anti-pattern):** `hist_pattern_signals.ret_60m` is uniformly 0 across all rows. Either the writer never populates it, or `hist_market_state.ret_60m` source is empty. Do not use `ret_60m` as outcome metric until TD-054 closed. Use `ret_30m` (TD-039) or compute from `hist_spot_bars_5m` directly.

**Bug B10 (non-rule, anti-pattern):** `hist_pattern_signals.ret_eod` column does not exist. EOD-outcome experiments must JOIN to `hist_spot_bars_5m` and compute the session-end forward return per row. TD-055 filed for schema migration.

**Operational findings (2026-05-02):**

- **Five-step audit pattern** for "type-X missing across detector" defects. When Session 15 found `hist_pattern_signals.BEAR_FVG=0` over 13 months, the discovery sequence was: (S1) distinct pattern_type counts; (S2) schema + direction columns; (S3) sibling table check; (S4) market-structure sanity (bear-day count last 30d); (S5) **manual canonical 3-bar shape scan in `hist_spot_bars_5m`** — the load-bearing step. Result: 1,129 canonical BEAR_FVG shapes existed in raw price data, 0 in signals → bug must be in detector or signal builder. Pattern preserved at `diagnostic_bear_fvg_audit.py`. Reuse the structure for similar defects (e.g. SWEEP_REVERSAL count audit, missing-timeframe-pattern audit).

- **Patched-copy deploy pattern** is the canonical safe-deploy for production scripts. Steps: (1) produce `<script>_PATCHED.py`; (2) dry-run; (3) live run; (4) full verification; (5) ONLY THEN rename `<script>.py` → `<script>_PRE_<session>.py` and `<script>_PATCHED.py` → `<script>.py`. Allows discrete rollback (one rename) without `.bak` file proliferation. Session 15 used this for `build_ict_htf_zones.py` and `build_ict_htf_zones_historical.py` — both renames preserved as `_PRE_S15.py`.

- **Verify experiment results against market reality before believing them.** Operator's chart-based challenge to "0 BEAR_FVG over 13 months" was the only signal that surfaced the 13-month silent bug (TD-048). Multiple sessions had run experiments against the broken signal table without anyone noticing. Discoverable by inspection but not by automated test. Going forward: when an experiment produces a result that contradicts visible market structure, treat the data path as suspect before drawing conclusions about the hypothesis.

- **Code review BEFORE patching when fixing a known-incomplete detector.** Session 15's six-bug review (S1.a/S1.b/S15-1H + S2.a/S2.b/S3.a/S3.b) emerged from one review pass. Spreading discovery across multiple sessions would have been more expensive and missed the explicit decision-rule (fix S1, defer S2/S3 with explicit TD filing).

- **Direction-symmetry verification before patching the wrong layer.** Session 15 checked that `build_hist_pattern_signals_5m.py` was direction-symmetric BEFORE patching the zone builders. Confirmed via 5-step audit S5 (canonical shape scan with no signal-table involvement). Avoided the trap of patching the signal builder symptomatically while leaving the zone-builder root cause intact.

---

## Session 16 engineering discoveries (2026-05-03) — codified as Rule 21

**Rule 21 — Always pipe long-running scripts through Tee-Object.** Any PowerShell invocation of an experiment, simulation, or diagnostic that runs longer than ~5 minutes MUST be invoked with `... 2>&1 | Tee-Object -FilePath "<name>_$(Get-Date -Format yyyyMMdd_HHmm).log"`. The first NIFTY full-year run of `experiment_15_pure_ict_compounding.py` was lost mid-session because it was run without Tee-Object, requiring a re-run that cost ~25 minutes of wall time. PowerShell's terminal scrollback is not a reliable archive. The .log file is.

```powershell
# Canonical invocation pattern:
$env:PYTHONIOENCODING = "utf-8"
python <script>.py 2>&1 | Tee-Object -FilePath "<script>_$(Get-Date -Format yyyyMMdd_HHmm).log"
```

The `$env:PYTHONIOENCODING = "utf-8"` prefix is also required whenever the script outputs box-drawing characters (Section headers in analyzers, table separators) — Windows console default encoding is cp1252 which crashes on `─` and `═`.

**Bug B11 (non-rule, anti-pattern) — Wrong-cohort comparison.** Session 16 mid-session had a near-miss where Items 1-6's "ungated coin-flip on `hist_pattern_signals`" results were almost framed as "Exp 15 framework headlines collapse" — when in fact `hist_pattern_signals` is the 5m-batch detector path and Exp 15's actual edge lives on the 1m live-detector path running through `experiment_15_pure_ict_compounding.py`. Operator pushed back ("are we looking at exp code or just compendium results?"), which triggered the source-archaeology phase that surfaced TD-057 and produced the live-cohort replication. **Going forward: before drawing conclusions about whether an experiment's claims replicate, read the experiment's source code and identify the cohort + outcome metric. Two experiments with similar names but different cohorts/metrics are not comparable.** Captured in CLAUDE.md anti-patterns table.

**Bug B12 (non-rule, anti-pattern) — Silent code drift between publication and audit.** April-13 commit `c78b6ea` modified BOTH `experiment_15_pure_ict_compounding.py` AND `detect_ict_patterns.py` together, including silent MTF tier relabeling: pre-Apr-13 vocabulary (HIGH=W, MEDIUM=D, LOW=none) became post-Apr-13 (VERY_HIGH=W, HIGH=D, MEDIUM=H, LOW=none). The Apr-12 Compendium uses post-Apr-13 vocabulary to describe pre-Apr-13 measurements. **Always run `git log --follow` on the experiment source file before claiming results are inflated/wrong/correct — silent code drift between publication and audit is a real failure mode.** TD-057 captures the broader provenance gap.

**Operational findings (2026-05-03):**

- **Schema-first rule for queries.** Always check `merdian_reference.json` `tables.<tablename>` schema before writing Supabase queries. Session 16 burned 2 iterations on column-name guessing (`hist_ict_htf_zones.tier` does not exist; correct column is `pattern_type`). Cost was minor but compounds across long sessions. Schema lives in JSON for a reason — use it.

- **Confidence intervals before declaring victory.** Session 16's pooled +193% return looked clean until Wilson 95% CIs and concentration check were applied. Result: BULL_FVG CI [42.5, 58.1] spans 50% (statistical coin flip despite N=155); top 7 sessions = 80% of P&L (event-dependent strategy, not steady-yield). The aggregate number alone would have led to over-confident production assumptions. **Always compute CIs on per-pattern WR and a session-concentration check (cumulative P&L share) before drafting Compendium verdicts.** `analyze_exp15_trades.py` Sections 9-18 codify this pattern; reuse for future research.

- **Trade-list CSV dump pattern is the canonical research artifact.** `experiment_15_with_csv_dump.py` is verbatim methodology of `experiment_15_pure_ict_compounding.py` plus a dataclass→CSV tail. The CSV is durable, version-controllable (gitignored under `*.csv` but persistent on disk), and reusable across diagnostic analyzers. Sessions 9-18 of `analyze_exp15_trades.py` ran in <30 seconds against the CSV after a 30-min compute. Future research should follow this pattern: heavy compute → CSV dump → fast diagnostic analyzer. Avoids re-running the simulation each time a new question is asked.

- **Live-detector replay pattern beats batch-table research.** Session 16 demonstrated that running the live `ICTDetector` over `hist_spot_bars_1m` directly (Exp 15 methodology) produces cleaner research data than relying on the precomputed `hist_pattern_signals` 5m-batch table. The live-replay path has correct outcomes (locally-computed forward return), correct signal-detection logic (the same code that fires production signals), and no schema drift between research and production. ENH-87 proposes formalizing this as the canonical research workflow going forward.

- **Read the script before drawing conclusions about what it measures.** Session 16's wrong-cohort overreach (Bug B11) happened because conclusions were drawn from result aggregates without first reading `experiment_15_pure_ict_compounding.py` source to confirm what cohort and outcome metric were being measured. The script reads `hist_spot_bars_1m`, runs live `ICTDetector`, and computes T+30m option-side P&L from `hist_option_bars_1m` — a fundamentally different measurement than `hist_pattern_signals.ret_30m` spot-side. Twenty minutes spent reading the script would have prevented an hour spent drafting wrong conclusions.

---

## Session 17 engineering discoveries (2026-05-03) — codified as Rule 22 + B13/B14

**Rule 22 — Direction-asymmetric defects in detector pairs.** When a direction-asymmetric bug is found in one component (e.g. zone builder missing BEAR_FVG branch), AUDIT the parallel detector component for the same defect — same author, same era, same blind spot likely applies. Session 15 fixed `build_ict_htf_zones.py` BEAR_FVG zone construction. Session 17 found the EXACT mirror defect in `detect_ict_patterns.py` BEAR_FVG signal emission — both untouched since Apr-13 commit `c78b6ea`. The Session 15 fix was correct but incomplete because the parallel live-detector path was never audited at the same time. Going forward, when fixing a direction-asymmetric defect, the deliverables must include (a) the fix itself, (b) explicit search of any parallel detector/builder/consumer, (c) test data demonstrating both paths now produce symmetric output. TD-058 closure confirms this rule with end-to-end validation (BEAR_FVG signal count 0 → 138).

**Bug B13 (non-rule, anti-pattern) — Self-patching loop.** Session 17 entered a state where each Pine readability fix introduced a new compile error (CE10149 → CE10235 → would have continued). After 3 patch iterations on the same feature, recommendation should be: stop and revert to the last-working version, not keep patching. Pine v6's strict typing rules surface multiple errors per attempt because the compiler bails on the first error, hiding subsequent ones. Codified as: **after 2 hotfix rounds on the same feature, default to revert + re-attempt with full-spec design rather than incremental patching**. Hotfix rounds reset on a verified working build; any new feature attempt resets the counter.

**Bug B14 (non-rule, anti-pattern) — Visible-console pollution.** Operator productivity is materially degraded by Task Scheduler tasks spawning visible Python console windows during pre-market and intraday hours. Each window flash interrupts chart prep, leads to mistypes during signal entry. Session Apr-13 fixed this for `run_option_snapshot_intraday_runner.py` subprocess calls but did not propagate the fix to top-level Task Scheduler entry points. Three years of accumulated tasks (13 currently) all spawn visible windows. Codified as: **every new Task Scheduler task MUST use `pythonw.exe` not `python.exe`, and existing tasks need migration**. Filed as TD-061 with proper-fix sketch.

**Operational findings (2026-05-03):**

- **The check_from filter pattern is a structural anti-pattern.** `detect_ict_patterns.py` had a `check_from = max(0, len(bars) - 10)` filter intended for "only check recently-formed patterns in sub-cycle invocations." The intent was correct (avoid re-detecting old patterns) but the implementation was wrong (caller has no control over what "recent" means; runner passes full session every cycle, defeating the optimization and creating a visibility gap instead). The fix removed the filter entirely and pushed the responsibility to the caller via input bar slicing. Codified as: **shared library functions should not encode caller-specific assumptions about input shape; either expose the assumption as a parameter or push the responsibility to the caller via well-formed input**. ENH-90 candidate finding (BEAR_FVG anti-cluster gate, direction-OPPOSITE to BULL_FVG) is a similar warning — symmetric assumptions in detector logic don't always survive measurement.

- **`bars=bars[-30:]` slice at runner entry point is the canonical "recent only" pattern, NOT a check_from filter inside the detector.** Session 17 F4 fix `detect_ict_patterns_runner.py` adds `bars=bars[-30:]` before passing to detector. Caller controls the window; detector remains pure. Same pattern applies to all `detect_*.py` functions that have a "recent only" optimization.

- **Full-day cycle simulator is the only reliable validator for cycle-level bugs.** Session 17 TD-060 diagnosis ran a simulator that replayed 2280 cycles over 14 trading days. The simulator showed `BULL_OB count=0, BEAR_OB count=0` across the entire window. Unit tests against the detector alone showed `BULL_OB count=22 at session-end` — false reassurance. Cycle-level interactions (runner→detector input shape, time-window slicing, cumulative state) are only testable via full-day replay. Codified as: **for any cycle-level bug that involves runner ↔ detector interaction, build a full-day cycle simulator that exercises the actual production invocation pattern, not unit tests against the detector alone.**

- **TD-060 was a 14-day silent production outage.** OB emission was 0 for 14 consecutive trading days (2260+ cycles, 2 symbols) due to F4 + G1 misalignment. No alerts fired because the failure mode was "no signals produced", not "signals produced incorrectly". Codified as: **for any production component whose normal behavior is "produces N signals per day on average", build a watchdog that fires when N=0 for >2 consecutive days. Silent zero is a structural alarm condition.**

---

## Session 20 engineering discoveries (2026-05-05) — codified as Rule 23 + B15/B16

**Rule 23 — Mirror v1 pattern for stage-N patches.** Session 20 introduced Active/Resolved register pattern in `tech_debt.md` for clarity; ENH register adopted mirror pattern same session. Pattern: each TD/ENH lifecycle is recorded as TWO blocks — Active when filed, Resolved when closed. Both blocks stamped with session number. Audit trail visible at a glance.

**Bug B15 + B16:** redacted to keep this entry compact.

**Operational findings (2026-05-05):**

- **Session 20 produced no live trades** (Phase 4A 2026-05-05) — first session with `MERDIAN_PreOpen` still in 09:05 mode + WCB regression still active. No signals cleared TIER1+ filters. Diagnostic only.
- **TD-070 filed** (BULL_OB multi-week lookback) — v1 ship same-session; v2 deploy planned Session 21.

---

## Session 21 engineering discoveries (2026-05-06) — operational findings only

- **Pipeline order matters in idempotent zone management.** The order detect → upsert → recheck → expire is the only correct order: (a) detect produces new candidates; (b) upsert writes ACTIVE for new + leaves existing untouched; (c) recheck flips status based on price action across new + existing; (d) expire flips date-based across all. Reordering any step makes the sequence non-idempotent or produces wrong final state. Session 21 fixed TD-071 by moving expire from BEFORE detect (broken — expires existing zones using last cycle's data) to AFTER recheck. Codified as: **for any pipeline that detects + upserts + recheck-conditions + expires, the only correct order is detect → upsert → recheck → expire. Document this in the script's main() docstring**.

- **Windows Task Scheduler `DisallowStartIfOnBatteries=$true` is silent killer for laptop production systems.** Session 21 traced 22-min cron gap 13:25-13:47 IST to power-source change events. Default Task Scheduler settings disallow battery starts AND stop tasks when going on battery. For laptop-based production system, this drops cycles invisibly. Fix: `DisallowStartIfOnBatteries=$false` AND `StopIfGoingOnBatteries=$false` on every market-hours task. Codified as: **at task creation time, every NEW Task Scheduler task that runs during market hours MUST have battery flags disabled. Audit existing tasks regularly.** This is Rule 23's natural extension (Rule 23 = mirror v1 pattern; this is the explicit battery flag requirement).

- **PowerShell `copy /Y` doesn't work in PS environment.** Session 21 lost ~30 min on TD-070 v2 deploy because `copy /Y file.py file.py` (cmd.exe syntax) returned no error in PowerShell but produced a 0-byte truncated file. The PowerShell-native `Copy-Item -Force` works correctly. Codified as: **NEVER use cmd-style `copy` in PowerShell context. Always `Copy-Item -Force` for file copy in PS.**

- **PowerShell `Add-Content` appends after `exit /b` making lines unreachable.** Session 21 hit this when extending a `.bat` wrapper — `Add-Content` appended new lines AFTER the `exit /b 0` line, making them dead code. Fix: read file content, string-replace to insert before exit line, write back. Codified as: **for `.bat` file edits in PS, use `(Get-Content path) -replace 'exit /b 0', "newcontent`r`nexit /b 0" | Set-Content path`. Never `Add-Content` to a file that has an exit line.**

- **Single-commit-per-session pattern is fragile under cascade.** Session 21 produced 3 patches (TD-070 v1, TD-070 v2, TD-071 + TD-072) but never committed at session end. Session 22 inherited a dirty working tree which compounded with Session 22's own data-recovery dirty state. Codified as: **session_log line and Git commit must happen TOGETHER at session end, no exceptions. If a session ends without commit, the next session's first action is commit-with-S(N-1)-tag-then-start-new-work — never roll forward into new edits with prior session uncommitted.**

---

## Session 22 engineering discoveries (2026-05-07) — codified as B17 + operational findings

**Bug B17 (non-rule, anti-pattern) — Hypothesis exhaustion without controlled test.** Session 22 spent ~6 hours investigating TD-080 Dhan option chain outage by testing and refuting hypotheses (token sync silent failure → battery flag side-effect → AWS competing writer → MALPHA Dhan competition → stale-token daemon → shadow_runner memory cache). Each hypothesis required 20-40 minutes of diagnostic queries. After 6 refutations, root cause still unconfirmed. Codified as: **after 3 refuted hypotheses on the same incident, STOP investigating and design a controlled reproducer test instead.** A reproducer (e.g., "watch tomorrow's 09:15 cron, does it fail or succeed?") can resolve in minutes what 6 hypothesis-refutations failed to resolve. The 7th hypothesis is rarely the right one; the controlled experiment is the only escape.

**Operational findings — Session 22:**

- **Kite returns IST-tagged datetimes for `historical_data` calls — `.replace(tzinfo=ZoneInfo("UTC"))` is the canonical timezone bug pattern, never apply.** TD-084 root cause: `backfill_option_zerodha_OI_FIXED.py` line 184 did `bar["date"].replace(tzinfo=ZoneInfo("UTC")).astimezone(IST)` — Kite already returns IST-tagged, so `.replace(UTC)` shifted timestamps +5h30m, then `is_market_hours()` filter dropped 88% of bars (375 → 46 per strike). Correct pattern: `dt_ist = bar["date"].astimezone(IST) if bar["date"].tzinfo else bar["date"].replace(tzinfo=IST)`. Audit `grep -rn "tzinfo=ZoneInfo.*UTC.*astimezone(IST)"` to find latent instances. Codified as: **NEVER apply `.replace(tzinfo=ZoneInfo("UTC"))` to a Kite `historical_data` row's `date` field. Kite is IST-tagged. The replace is silent data-loss.**

- **Vendor endpoint stability is per-endpoint, not per-vendor.** Session 22 outage: same Dhan token served `/charts/intraday` (capture_spot_1m_v2) at 97% success while `/option-chain` failed 50% of cycles. Endpoints have independent failure modes. Codified as: **when one Dhan/Zerodha/Kite endpoint fails intermittently, do NOT assume the vendor is "down" — test the parallel endpoint. Endpoint-specific failures usually trace to endpoint-specific rate limits, per-endpoint caching, or per-endpoint auth scope.**

- **NIFTY weekly expiry is now Tuesday (NSE 2025+ change), SENSEX stays Thursday (BSE).** Same-day backfill may pull 2 SENSEX expiries (today + next-Thursday) but only 1 NIFTY (next-Tuesday). Session 22 backfill confirmed: NIFTY 22 strikes × 1 expiry (May-12 Tue) = 8,250 bars; SENSEX 44 strikes × 2 expiries (May-7 today + May-14 Thu) = 16,499 bars. Codified as: **NIFTY weekly = Tuesday. SENSEX weekly = Thursday. Don't assume same expiry calendar across symbols.**

- **`find_dotenv()` fails in Python heredoc context — use file approach.** Session 22 hit this twice when running diagnostic Python via `python3 << 'EOF'` heredoc. `find_dotenv()` calls `frame.f_back` which doesn't exist in heredoc execution context, raising AssertionError. Fix: `cat > /tmp/script.py << EOF` then `python3 /tmp/script.py`, with explicit `load_dotenv("/path/.env")` in the script. Codified as: **for any AWS/SSH diagnostic Python that needs env vars, write a file via `cat >` then invoke. Avoid heredoc execution. Always pass explicit path to `load_dotenv()`.**

- **MALPHA AWS Kite gateway is the de-facto recovery path for Dhan outages.** Session 22 validated end-to-end: when Dhan ingest fails, run `backfill_spot_zerodha.py` + `backfill_option_zerodha_OI_FIXED.py` on MALPHA to recover the day's gap. Spot is fully recoverable; option_chain_snapshots full chain is NOT recoverable (Dhan endpoint is real-time only) but per-strike OHLC IS recoverable from Kite. Codified as: **for any Dhan outage of >30 min duration, immediate recovery action = MALPHA Kite backfill of affected day. Document the gap windows; permanent loss is option_chain_snapshots only.**

- **Architecture decisions need ADRs, not session_log notes.** Session 22 architecture conversation Phase α surfaced 4 questions (zone validity model / AWS migration scope / token reliability ordering / today's session shape) that need ADR-grade documentation before implementation. Operator + assistant talked through them but no ADR was drafted. Codified as: **any architectural decision that affects pipeline structure or data model MUST be drafted as `docs/decisions/ADR-NNN-<topic>.md` BEFORE the corresponding code change. ADR is the durable answer to "why did we do it this way?" Tracking it in CURRENT.md or session_log loses the rationale.** Pending ADRs: ADR-005 (zone validity model rewrite per TD-079); ADR-006 (AWS migration scope decision).

---

## Session 25 engineering discoveries (2026-05-10) — codified as B18 + operational findings

**Bug B18 (anti-pattern) — Anti-pattern bugs ship in N silent siblings; fix all when one is found.** Session 25 closed TD-097 (dashboard pre-open URL-encoding bug producing 0% accuracy widget). Root cause was `requests.get(SUPABASE_URL + endpoint, params={...})` double-encoding query strings — Supabase silently returned 200 OK with empty results. The fix took ~30 minutes. The grep for the same anti-pattern took 2 minutes and surfaced **5 more production scripts** with identical defect (filed as TD-099): `build_signal_market_path_audit_v1.py`, `build_signal_outcome_audit_local.py`, `build_signal_regret_log_v1.py`, `build_option_execution_outcomes_v1.py`, `premium_outcome_writer.py`. Each silently under-fetches in production. Codified as: **whenever an anti-pattern bug is fixed in one production code path, immediately `grep -rn` for the same shape across the codebase and treat all matches as the same defect. N silent siblings is the default, not the exception. The grep is the diagnostic; closing only the surfaced site is closure theater.**

**Operational findings — Session 25:**

- **Boundary disposal requires both operator semantic check AND code dependency check.** Session 25 disabled Local `MERDIAN_PreOpen` (09:05 IST) — operator semantic was correct ("9:05 read meaningless" — auction-window prices are not tradeable price discovery), but the task had a real code dependency: `ret_session` computation read from the 09:05 anchor row in `market_spot_snapshots`. Acting on the operator semantic alone would have broken `ret_session`. Investigating the code dependency without the operator semantic would have left orphan auction noise in production. Codified as: **before disabling any Task Scheduler task or AWS cron, run BOTH: (a) operator semantic check — does this writer produce data that's actually used or meaningful? (b) code dependency check — `grep -rn` consumers of the writer's output table for any read paths that target this writer's specific timestamp/anchor. Disposal proceeds only when both checks clear.**

- **Schema-convention semantics deserve formal documentation.** Session 25 closed TD-078 by SQL-verifying TD-070 v2's multi-week BULL_OB lookback. Initial query `WHERE source_bar_date='2026-04-13'` returned empty. Root cause: `ict_htf_zones.source_bar_date` semantics differ by timeframe — for W timeframe it's the **week-start Monday date**, for D it's the bar's calendar date, for 1H it's the hour bucket date. This convention is implicit in `build_ict_htf_zones.py` and not documented elsewhere. Whoever debugs a "missing zone row" claim without this knowledge concludes the row is absent when it's actually present under a different `source_bar_date`. Codified as: **`ict_htf_zones.source_bar_date` is timeframe-aware: W = week-start Monday, D = calendar date, 1H = hour bucket date. When debugging "missing row" claims on this table, check the timeframe-aware convention before concluding the row is absent.** This deserves a System Map §B annotation; filed as follow-up.

- **ADR-008 replay infrastructure is the validation tool for code-dependency migrations; use it before production change, not as a wait-for-Monday substitute.** Session 25 ret_session anchor migration (09:05 → 09:08) was validated via replay over historical days BEFORE the Local 09:05 task was disabled. This is exactly the what-if validation use case ADR-008 was built for. The alternative — disable 09:05 and "see what breaks Monday" — is wait-for-Monday-and-pray, not engineering. Codified as: **for any code change that modifies a data-source anchor, table column, or upstream dependency that production code reads, validate via ADR-008 replay over historical days BEFORE production change. Replay is not just for what-if signal experiments; it is the safety harness for any code-dependency migration whose effects are observable in `*_snapshots` tables.**

- **TD reframing based on cross-script evidence narrows investigation surface materially.** Session 25 reframed TD-080 from "Dhan option chain endpoint reliability" (multi-vendor, multi-endpoint diagnostic) to "AWS Dhan token refresh failure mode" (single-script root-cause: `refresh_dhan_token.py`). The narrowing was earned by cross-script 2026-05-07 evidence — PreOpen 03:38 UTC + option chain 09:30-13:30 IST + 14:45-15:25 IST were all consistent with a single token-refresh failure on AWS, not a Dhan-side service incident. Codified as: **when a TD's investigation surface is broad (multi-component, multi-vendor), re-examine the original incident's cross-script timeline before drafting hypotheses. If the timeline shows correlated failures across components that share a single upstream dependency, narrow the TD to that dependency. The 6-hour hypothesis-refutation marathon Session 22 ran on TD-080 would have completed in 90 minutes had the cross-script narrowing happened first.**

- **Phase α architect-deferral was the right call given low-N — graduated discipline beats uniform discipline at thin data.** Q4 calibration discipline answer: at MERDIAN's Y1 data scale (~250 trading days), strict uniform 67/33 holdout (option a strict) is impractical when half the experiment cohorts have N<60. Status quo single-cohort (option d) leaves overfit risk uncontrolled. Graduated strictness — N≥60 → 67/33 with 10pp tolerance, 30≤N<60 → 75/25 with 15pp, N<30 → "low-N calibration-only" tag — handles data scale honestly without pretending walk-forward is feasible at Y1. Phase 2 cutover at Y2 close commits the discipline forward. Codified as: **methodology decisions at thin data scales require graduated commitment, not uniform commitment. Strict uniform rules either produce unimplementable mandates or get silently waived; both fail. The discipline is to acknowledge the data scale in the rule itself.** ADR-009 will codify; §D.8 of Assumption Register has the working draft.

- **CLAUDE.md update at session close: "settled" bullets are not just for new ADRs; they are also for any answer that future sessions could waste time re-litigating.** Session 25 produced 4 Phase α answers without drafting full ADRs (the ADRs follow in dedicated sessions per Q3 sequencing for ADR-006). The Phase α answers are still durable; if not captured in CLAUDE.md "Things that are settled", a future session could spend hours re-deriving them. Codified as: **at session end, the criterion for adding a "Things that are settled" bullet is not "did we draft an ADR?" — it is "did this session produce an answer that's durable, where re-litigating it would waste future-session time?" If yes, add the bullet, even if the full ADR draft is deferred to a later session. The bullet IS the durable record until the ADR exists.**

## Session 26 engineering discoveries (2026-05-10) — codified as B19 + operational findings

**Bug B19 (anti-pattern) — OI-18 class fix at one site does not close the class; runtime-verify all candidate sites including non-grep-reachable ones.** Session 25 closed TD-097 (dashboard URL-encoding) and immediately filed TD-099 ("same anti-pattern in 5 other production scripts") via `grep -rn "requests.get.*SUPABASE.*params"`. Session 26 proved BOTH legs of TD-099 wrong: (a) the 5 scripts the grep matched were already correct (each emits `requests.get(URL_with_query, params=None)` form, not the broken `requests.get(URL_with_query, params={...})` double-encoding form — TD-099 closed as **filed-in-error**, ~3 hours of unnecessary patching avoided); (b) a real instance of the OI-18 class — unbounded `order_by`+`limit` query that returns oldest rows — existed in `build_momentum_features_local.py::get_session_open_spot()` as a writer-side helper. The grep couldn't find it because the anti-pattern was inside the `supabase_select()` helper call, not a top-level URL construction; the grep rule (`requests.get.*SUPABASE.*params`) was shape-specific to dashboard's request style, not to the bug class. The writer-side instance had been silently NULLing `momentum_snapshots.ret_session` for 3+ trading weeks (2026-04-17 → 2026-05-10, ~5,000 signals), which in turn caused ENH-55 (which gates on `ret_session is not None`) to be a silent no-op for the entire window. Cost of grep-only audit: 24 days of broken gate before the next debugging cycle surfaced it. Codified as: **when an OI-18-class bug ships and is fixed, the closure of the class requires (a) URL-spy or runtime-trace verification of every candidate site, not just grep — the grep is shape-specific and misses helper-buried instances; (b) audit must extend to writer-side helpers downstream of the symptom site, not just request-side construction at the symptom site. Filing rule: a "same anti-pattern in N scripts" claim requires runtime verification before priority assignment. The grep is the trigger to investigate, not the verdict.** TD-097 was fixed; TD-099 was filed-in-error; TD-101 was the real instance the audit missed.

**Operational findings — Session 26:**

- **Production data on a documented period trumps research-cohort hypothesis.** ENH-55 was promoted on Exp 20 evidence (5m-batch `hist_pattern_signals` cohort: opposed 38.3% WR vs aligned 60.9% WR, +22.6pp lift). Once TD-101 fix surfaced 24 days of silent-gate-failure data, retrospective audit on the live signal cohort (action ∈ {BUY_CE, BUY_PE} ∧ `trade_allowed=TRUE`) showed: WOULD_HAVE_BLOCKED bucket 79.5% WR (N=44), WOULD_HAVE_ALIGNED_BONUS bucket 54.3% WR (N=35). Sign of the lift is opposite to Exp 20; magnitude of disagreement clears proxy-WR-noise threshold. ENH-55 was disabled by env flag (default OFF, reversible via `MERDIAN_ENH55_ENABLED=1`). Codified as: **when production data on the cohort that a parameter actually gates contradicts the research-cohort evidence the parameter was promoted on, default to disabling the parameter behind a reversible flag, file the contradiction as an Assumption Register row, and re-validate only with proper outcome metric (option-P&L not spot-direction proxy) on the cohort the parameter actually gates. Calibration discipline first real test (per §D.8.3 prospective parity check) — discipline applied; flag drift criterion fired within 24 days, well inside the 60-day prospective threshold.**

- **Reversible disablement (env flag) > code removal for hypothesis-falsified parameters.** ENH-55's code path stayed in the codebase; only the inner condition was guarded by `if ENH55_ENABLED and ret_session is not None and abs(ret_session) > 0.0005:`. Default `ENH55_ENABLED = os.getenv("MERDIAN_ENH55_ENABLED", "0").strip() == "1"` evaluates to False. Re-enable is a `.env` line addition + restart. The alternative — strip ENH-55 from the codebase — would have made re-validation cost a re-implementation rather than a flag flip. Codified as: **for any production parameter whose hypothesis is falsified by retrospective evidence, the disablement vector is an env flag with default OFF, not code removal. Re-validation cost should be one experiment, not one re-implementation. Code paths that fail empirically are still valuable as documentation of what was tried.**

- **TD-099 lesson — grep-based "same anti-pattern in N other scripts" filings require URL-spy verification before priority assignment.** Session 25 filed TD-099 at S2 HIGH on the strength of a grep match. Session 26 verified each match by intercepting `requests.get` calls and confirmed all 5 scripts emit clean URLs — match was a false positive (the grep regex couldn't distinguish between the broken `URL_with_query + params={...}` double-encoding form and the standard-correct `clean_URL + params={...}` form). 3 hours of patching avoided. Codified as: **before assigning priority to a "same anti-pattern in N scripts" claim, verify with URL-spy or equivalent runtime trace, not grep alone. False-positive grep matches against dashboard-style code patterns are common; the symptom that surfaced the original bug (silent 200-OK with empty results) does not necessarily survive in code-shape grep terms.** Filing pattern going forward: TD-097-style audit-derived TDs require runtime verification of at least one match before filing the rest.

- **TD-101 unbounded query is the OI-18 anti-pattern in writer form.** `supabase_select("market_spot_snapshots", filters={"symbol": symbol}, order_by="ts", desc=False, limit=500)` returns the OLDEST 500 rows in an unbounded table (no date filter), and the inside-loop today-date filter discards all 500. Returns None silently; downstream `compute_return(curr, None)` returns None; stored as NULL. Same shape as TD-097 dashboard query, but inside a helper function rather than at the URL construction site. Fix pattern: bound the query at fetch time (`gte("ts", today_start_utc_iso)` filter, lower limit), keep defense-in-depth date filter inside loop. Threshold preserved at 03:35 UTC per ENH-01 / V18G regression history (catches both 09:05 IST Local PreOpen now-disabled and 09:08 IST AWS PreOpen current anchor). Codified as: **OI-18 (unbounded order_by + limit returning oldest rows) is the canonical Supabase query bug class. Audit shape: any `select()` with `order_by` + `limit` and no time/range filter on the order_by column. Common in helper functions that take a session-scoped time argument but query the full table. Audit broader than just dashboards.**

- **24-day silent-gate-failure window surfaced via writer-bug fix, not via gate-monitoring telemetry.** ENH-55's production telemetry (cautions list, reasons list in `signal_snapshots`) would have shown ENH-55 firing or not firing per signal; since `ret_session` was NULL, the inner condition was False, and the gate produced NO entries — looking telemetrically identical to a gate not firing because ret_session was in the neutral band. There was no signal of the silent failure other than a slow drift in opposed-aligned signal counts that nobody was monitoring. Codified as: **for any gate guarded by a `not None` check on a writer-produced value, ship with a parallel diagnostic that asserts the writer is populating the value at expected cadence (post-cycle: ` SELECT COUNT(*) FILTER (WHERE col IS NULL) / COUNT(*) FROM table_X WHERE date = today` should approach 0). Otherwise a writer regression silently disables the gate. Ship the diagnostic at gate-promotion time, not retrospectively after silent failure surfaces.**

- **Same-session TD close discipline.** TD-101 was filed and closed within Session 26 (NEW S1 → RESOLVED commit `3cb84e2`). Same-session TD lifecycle is acceptable when the diagnostic that surfaces the bug also produces enough evidence for the fix design. Pattern: discovery → SQL diagnostic → bug localization → patch → smoke test → commit, all in one session. The TD entry in `tech_debt.md` records the lifecycle as separate Active and Resolved blocks both stamped Session 26 — the audit trail (NEW + RESOLVED in same session) matters for future sessions reading the register. TD-097 (S25) was the precedent; TD-101 is the second instance.

- **ENH-88 deployment gate satisfied; same-session ship discipline.** ENH-88 (BULL_FVG cluster gate, +12.8pp lift evidence per Session 16 Exp 50b at 90-min lookback N=64) had been BUILT NOT DEPLOYED since Session 17, gated on Mon BULL_OB live data confirmation. Session 26 shipped via `patch_s26_enh88_deploy.py` adding 90-min lookback helper + gate block before `return out, flags` in `build_trade_signal_local.py`. Smoke test PASS (Sunday no data, no errors; ENH88_LOOKBACK_MIN=90 confirmed; both NIFTY+SENSEX False as expected for non-trading day). BEAR-side asymmetry preserved (BEAR_FVG anti-clusters at -16.5pp per ENH-90 — do NOT mirror; BEAR side stays standalone). Codified as: **ENH ship triggers can be data-availability-gated (e.g., "deploy after first cycle confirms data flowing") and that gate can be discharged out-of-band, including on Sunday non-trading days when smoke tests are sufficient. The gate's purpose is to confirm the production data path supports the gate's input, not to wait for a calendar event.**

- **Symmetric ADR-005 zone validity model rewrite — D/W OB/FVG `valid_to=NULL` (price-breach only) + 1H 1-week tactical fallback.** TD-079 fix per Phase α Q1 answer: detection canon-aligned (all four ICT pattern types detected symmetrically), routing data-driven (`assign_tier()` uses empirical edge). 18 SENSEX W BEAR_OB/BEAR_FVG zones above 78k revived from EXPIRED → ACTIVE on backfill. Pine 36 → 62 zones (49 HTF + 13 intraday). `expire_old_zones()` filter widened from `["W","D"]` to `["W","D","H"]`. PDH/PDL date-expiry unchanged (those are intraday liquidity targets, not structural levels). Codified as: **architecture-defect TDs whose Phase α answer is in hand can be implemented BEFORE the formal ADR draft, provided (a) the answer is recorded as a settled decision in CLAUDE.md, (b) the implementation is reversible (snapshot original), (c) the ADR draft follows in a dedicated session to capture rationale + alternatives. ADR-005 codification (P2 S27 carry-forward) follows the implementation, not the other way around. Doc Protocol v4 Rule 10 ADR-mandatory-before-code is satisfied because the architectural decision (Phase α Q1) WAS made in S25 and recorded in Decision Index + Assumption Register §D.7; the ADR draft is the writeup of an already-made decision.**

## Session 28 engineering discoveries (2026-05-13) — codified as B22 + B23 + operational findings

**Bug B22 (anti-pattern) — Cross-Python-version stdlib semantics; Local 3.12 ≠ AWS 3.10 for `fromisoformat()`.** Session 28 TD-NEW-13: a regex-free `datetime.fromisoformat(ts_iso)` call worked on Local Python 3.12 (permissive microsecond precision) but failed on AWS Python 3.10 for 60/587 backfill cycles (`ValueError: Invalid isoformat string`). Supabase serializes PostgreSQL timestamps with variable microsecond precision (2-7 digits common). Python 3.10 accepts only exactly 3 or 6 microsecond digits; Python 3.12 is permissive of arbitrary precision. The TD-NEW-4 helper `_dte_from_ts()` passed Local smoke on 5 sample rows but failed at scale on AWS. Codified as: **any Python module that parses ISO timestamps from Supabase MUST run cross-version-compatible code paths. Normalize the microsecond fraction to exactly 6 digits via regex pad/truncate before `fromisoformat()`. Verify on AWS, not just Local — Local Python 3.12 smoke testing is necessary but not sufficient. Long-term: align Python versions across Local + AWS. Until then, normalize defensively.** Canonical pattern in `_dte_from_ts` helper at `compute_gamma_metrics_local.py` post-S28.

**Bug B23 (anti-pattern) — Phase 0 closure exercises surface residual defects that would have polluted downstream work; treat each closure as a defect-discovery surface.** Session 28 progression: TD-NEW-2/3 P0 verification (P0 closure) → P1 backfill design (Phase 0c data prerequisite) → backfill execution surfaced TD-NEW-12 (shadow architecture not implemented) → TD-NEW-12 patch surfaced TD-NEW-4 (dte from result.ts not date.today()) → TD-NEW-12 patch smoke test surfaced TD-NEW-13 (Python 3.10 microsecond rejection) → 60 retry failures → TD-NEW-13 fix → 587/587 clean → P1 CLOSED. Each closure step exposed a residual defect the prior step had masked. Six fixes in one session as a side effect of closing one P1 task; no defect would have been visible without the closure being attempted. Codified as: **Phase 0 closure exercises (data backfills, schema reconciliations, replay validations) are defect-discovery surfaces. Budget for residual surfaces: scheduling a Phase 0 closure as "30-45 min backfill" routinely produces 3-5 hours of resolution work. Plan accordingly. The discovery rate is high because Phase 0 closure exercises stress-test code paths that production daily cadence doesn't exercise — backfill cohort sizes, off-hours timing, cross-version stdlib, schema drift. None of this is visible during steady-state production. Build the closure exercise schedule with discovery budget, not just execution budget.**

**Operational findings — Session 28:**

- **The AWS shadow architecture being silently violated for 13 days is the consequence of "schema present, behavior absent" deployment.** TD-NEW-12 root cause: `gamma_metrics_shadow` table was created at the time the shadow architecture was envisioned, but the corresponding `--shadow` flag in `compute_gamma_metrics_local.py` was never wired. AWS shadow runner invoked the script without any flag; the script defaulted to writing to production `gamma_metrics`. For 13 days (Apr 29 → May 13), `gamma_metrics_shadow` had 0 rows; production `gamma_metrics` had 2 writes per `(symbol, ts)` row from race-condition double-writes. The architectural intent was documented in Topology §6.5 ("shadow ≠ live") but the code did not enforce it. Codified as: **for any architectural separation that depends on flag/parameter wiring (shadow vs production, dry-run vs live, test vs prod), verify the wiring is exercised end-to-end at deployment time. Schema-only proxies for separation (existence of `<table>_shadow`) are insufficient evidence that the architecture is wired. The deployment smoke test must include `actual_writes` telemetry showing the expected target table.**

- **`PGRST204 schema cache` errors after `ALTER TABLE` require explicit `NOTIFY pgrst, 'reload schema'`.** Session 28 added 7 columns to `gamma_metrics_shadow` via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`. First post-ALTER smoke test returned `PGRST204 column "dte" not found in schema cache for gamma_metrics_shadow`. Resolution: `NOTIFY pgrst, 'reload schema'` cleared the cache; subsequent writes succeeded immediately. Codified as: **after any `ALTER TABLE` on a Supabase-served table, issue `NOTIFY pgrst, 'reload schema'` before smoke testing. PostgREST caches schema for performance and does not auto-reload on DDL. The ALTER lands instantly; the cache lags.**

- **`42P10` ON CONFLICT errors after `CREATE TABLE LIKE` indicate missing UNIQUE constraint, not missing column.** Session 28 attempted UPSERT on `gamma_metrics_shadow` with `on_conflict="symbol,ts"`. Error: `42P10 there is no unique or exclusion constraint matching the ON CONFLICT specification`. Root cause: `CREATE TABLE gamma_metrics_shadow LIKE gamma_metrics` (used at table creation time) copies columns but does NOT copy constraints unless `INCLUDING ALL` is specified. The UNIQUE constraint matching the on_conflict clause must be added separately. Codified as: **when creating a shadow / replica table via `CREATE TABLE LIKE`, always use `INCLUDING ALL` (columns + constraints + indexes + defaults + comments). If discovering the omission post-deployment, `ALTER TABLE <shadow> ADD CONSTRAINT <name>_<cols>_key UNIQUE (<cols>)` is the surgical fix. Audit every `_shadow` paired table for this gap.**

- **Run_id-keyed retry pattern is the canonical backfill recovery for Python-version-failure cohorts.** Session 28 P1 backfill produced 60 failures out of 587 (10.2% failure rate) due to TD-NEW-13. Each failure was logged with its `run_id` (UUID of the originating compute cycle). Post-patch retry: SELECT failed run_ids → re-invoke compute on each → 60/60 success. Total recovery time: ~5 minutes (vs the alternative of re-running the entire 587-row backfill from scratch). Codified as: **for any backfill or batch operation that may have a non-zero per-row failure rate, log each row's identifying key + status code. The log is the retry queue. Filter failed rows + reprocess them after the patch lands; do not re-run the entire batch.**

- **TD-NEW-10 closed as filed-in-error is the canonical "audited live, confirmed intentional" closure pattern.** Session 28 noticed `merdian_order_placer.py` running on MERDIAN AWS with no Topology mention and filed TD-NEW-10 as "process running un-audited". Investigation surfaced: intentional Phase 4B Order Placer (HTTP server port 8767, Dhan-IP-whitelisted Elastic IP, @reboot cron), running deployed since 2026-04-29, just absent from Topology documentation. Closure: TD-NEW-10 was filed-in-error (not a defect; documentation gap). Same session filed TD-NEW-11 (S3 — documentation gap) and closed it by updating Topology §3 + §7.1 + §8.2 in the doc-close rewrite. Codified as: **when an unexpected production process is discovered, file as "unaudited" first (S1-S2), investigate, then close as filed-in-error if intentional. Document the absence in whatever register should have caught it; close the documentation gap in the same session. This separates "real defect" from "documentation drift" cleanly.**

- **Operational firefighting drift after P0 closure is a recurring shape; budget for it explicitly.** Session 28 P0 closed at 09:25 IST (~10 min after market open). ~22 hours of drift work followed: WS feed outage triage (no patch needed; resolved via Zerodha re-accept after ~5 min of 60s reconnect cycles); TD-NEW-5/6/8 config patches; investigation of `merdian_order_placer.py` (TD-NEW-10 → TD-NEW-11); P1 backfill execution → TD-NEW-12 discovery → TD-NEW-12 + TD-NEW-4 + TD-NEW-13 patches → backfill retry → P1 CLOSED → P2 ENH register filing → session close docs. Operator named the drift explicitly mid-session ("drift from primary"). Codified as: **a session-mandate of "verify yesterday's patches in live cycle" routinely produces a P0 closure in <20 minutes of session time; the operator should plan for the remaining session to be drift-shaped (operational firefighting, discovery work, emergent technical-debt resolution). This is not a session-discipline failure — it is the structural shape of post-deployment verification sessions. Plan the next 4-6 hours as drift budget at session-start, not as wasted time.**

---

## Session 29 engineering discoveries (2026-05-14 firefighting + 2026-05-14→2026-05-16 build) — codified as B24 + B25 + B26 + B27 + B28 + operational findings

**Bug B24 (anti-pattern) — `.env` edits do not propagate to running processes.** Token files like `.env` are read at process startup. A long-running consumer (websocket feeder, daemon, cron'd script that doesn't auto-exit) holds whatever value was loaded at startup until killed. After any `.env` mutation, every consumer process that read the affected variable MUST be killed and restarted; verifying the on-disk value matches (e.g., `grep KEY .env`) is necessary but not sufficient. Today's 2026-05-14 incident: operator ran the token-refresh sequence twice with correct end-state in `.env`, but `ws_feed_zerodha.py` had loaded the prior day's token at 09:14 IST cron start and continued failing auth in a silent reconnect loop until manually killed 9 hours later. Symptom is operationally indistinguishable from "Step 2 of token refresh was skipped" — diagnostic discipline requires checking the running consumer's behavior (log tail), not just the on-disk state. Codified as: **`.env` changes require consumer restart, full stop. Filing rule for any new `.env`-mutation runbook: include a "Step Nd — Restart consumers" mandatory clause.**

**Bug B25 (anti-pattern) — TRUNCATE vs DELETE on bloated tables.** For Postgres tables under statement_timeout pressure, `DELETE FROM table WHERE ...` cannot be used to recover bloat — the DELETE itself is what's timing out. `TRUNCATE table` is the only operational primitive that works at scale because it is DDL (drops + recreates the underlying file atomically), runs in O(1) on table size, and resets indexes inline (no `REINDEX`, no `VACUUM FULL`). Constraint: TRUNCATE deletes ALL rows, so only safe for tables whose consumers all read fixed-window-recent data (the ephemeral-rolling-window pattern, e.g. `market_ticks` 10-min window). 2026-05-14: `market_ticks` 62 GB → 856 kB in <1s, with the feeder writing successfully on next batch. Codified as: **for any table whose retention horizon is shorter than its read-window-floor, TRUNCATE is the canonical recovery; DELETE is structurally unsafe.** Apply: design check at every new pg_cron retention job — if the worst-case DELETE workload could ever exceed statement_timeout, the design is wrong; reduce horizon and increase cadence instead.

**Bug B26 (anti-pattern) — `pg_cron` failures are invisible by default.** The `cron.job_run_details` table records every cron run, including failures, but no MERDIAN telemetry polls it. A retention or maintenance job can fail every weekday for weeks before any operator-visible symptom emerges (2026-05-14: `delete-old-market-ticks` failed 14+ consecutive weekdays before producing the 62 GB bloat that triggered Incident 1). Codified as: **any pg_cron job introduced to production must be accompanied by either (a) a polling check that surfaces failures within 24 hours (Telegram alert or dashboard widget), or (b) an explicit entry in the operator session-start checklist.** Filed as TD-NEW-B (S1) for the alerting-layer implementation. Workaround pre-implementation: include `SELECT jobname, status, return_message, start_time FROM cron.job_run_details WHERE start_time > now() - interval '7 days' AND status != 'succeeded'` in session-start gate.

**Bug B27 (anti-pattern) — Generate-series in PostgREST with `timezone()` cast of wall-clock IST strings produces phantom misses.** During 2026-05-14 spot-gap diagnosis: `SELECT generate_series(timezone('Asia/Kolkata','2026-05-14 09:15:00'), timezone('Asia/Kolkata','2026-05-14 15:29:00'), interval '1 minute')` actually generates `14:45 IST → 20:59 IST` (5h30m forward-shifted). The string `'2026-05-14 09:15:00'` is parsed as UTC by default; `timezone('Asia/Kolkata', utc_value)` then converts that UTC value to IST display, but the underlying timestamptz is unchanged. My query produced 245 "phantom missing minutes" in post-market hours, none of which were real. Codified as: **build expected-time series in UTC at actual UTC times** (e.g., `'2026-05-14 03:45:00+00'::timestamptz` for 09:15 IST trading-start) **and let the join handle TZ conversion in output formatting only.** Apply: any audit query that compares IST-window expectations against `bar_ts` storage should use this pattern; never use `timezone()` cast to construct an IST series from a wall-clock string literal.

**Bug B28 (anti-pattern) — Regex capture of arbitrary trailing tokens is unsafe in command-injection contexts.** v1 of `migrate_to_pythonw.ps1` used regex `(?<rest>(\s+\S+)*)\s*$` to capture trailing arguments after a `python script.py` invocation in a `.bat` file. The regex greedily captured shell redirection operators (`>>`, `2>&1`, etc.) which look like normal whitespace-separated tokens. These were then passed to `pythonw.exe` as command-line arguments, which would have caused silent crashes (pythonw treats `>>` as a script-file argument and fails to start, with no console visible). Codified as: **when capturing command-line arguments to pass to a different shell context, explicitly whitelist syntactically-meaningful argument shapes** (e.g., flags starting with `-`/`--`, `.py` files, quoted strings) **and explicitly blacklist shell metacharacters** (`>`, `>>`, `<`, `|`, `&`, `2>`, backticks). Apply: any code that re-emits captured tokens into a different execution context (cross-shell, cross-process) is in scope.

**Operational findings — Session 29 firefighting (2026-05-14):**

- **Compound incidents produce single observable symptoms; don't pattern-match the first hypothesis.** 2026-05-14 had two independent failure modes (token-in-memory consumer + market_ticks bloat) coincidentally surfacing on the same day. The operator pattern-matched to the 2026-04-22 incident (skipped Step 2 of token refresh) and tried that fix twice with no improvement. The pattern-match was a hypothesis, not a conclusion. Codified as: **when a tried fix produces no observable change after one verified attempt, falsify the hypothesis before retrying.** Two attempts with no change = the model is wrong; don't try the same fix a third time. See B17 (3-refutation rule from S22) for the general case.

- **TD body-state must match footer-claim — Doc Protocol v4 candidate Rule N.** Session 18 footer claimed TD-061 RESOLVED; body block remained in Active section; S23 audit confirmed only 4/15 migrated; S29 firefighting found 19 tasks (vs S23's 17) with only 4/19 on pythonw. The signal that "RESOLVED" was wrong was right there in Topology §7.2 audit-insight #1 but never reflected back into `tech_debt.md`. **Filing rule:** a TD is RESOLVED only when (a) its body block is moved to the Resolved section AND (b) the closure block in Resolved contains the patch script / commit / verification evidence. Footer-only resolutions are not allowed and must be re-opened in the next session. S29 doc-close includes an audit pass over `tech_debt.md` to re-classify any other footer-claimed-resolved-but-body-active items.

- **Topology audits go stale silently — Task Scheduler audit cadence required.** S23 PowerShell audit recorded 17 tasks; S29 audit found 19 — 2 new tasks added (`MERDIAN_Dhan_Token_Refresh`, `MERDIAN_Intraday_Session_Start`) without any Topology §7.2 update. Pattern: any session that touches Task Scheduler must include a `Get-ScheduledTask -TaskName 'MERDIAN_*' | Measure-Object` check + reconciliation. Codified as Doc Protocol v4 candidate Rule N (Task Scheduler audit cadence).

- **Wrapper-to-direct migration: comments vs code state alignment.** TD-NEW-J taught the same lesson at a smaller scale: when a string literal in code is renamed (`OUTSIDE_MARKET_HOURS` → `OFF_HOURS`), any prose that references the literal name must be updated in lockstep, OR rewritten to preserve discoverability of the old name. Patch v2 handled this by appending `(was OUTSIDE_MARKET_HOURS pre-TD-NEW-J 2026-05-14)` to the docstring. Same principle applies to wrappers: when a `.bat` is unreferenced, its on-disk presence creates a discoverability trap if anyone greps for `MERDIAN_ICT_HTF_Zones_0845` and finds the `.bat` listed instead of the new orchestrator. Codified as: **when retiring code or renaming a literal, deliberately preserve grep-discoverability of the old name in adjacent documentation** — either by updating prose in lockstep with code, or by appending a "was-X-pre-Y" annotation that survives future searches for the old name.

- **Single-trigger Set-ScheduledTask reliability ≠ multi-trigger.** PowerShell's `Set-ScheduledTask -Settings <obj>` works cleanly on simple-trigger tasks but fails on multi-trigger tasks (like `Intraday_Supervisor_Start` which has both a Weekly+Mon-Fri schedule AND an AtLogon trigger). Workaround: build a full `Register-ScheduledTask` XML and `Force` overwrite, or skip settings-only updates for multi-trigger tasks. Documented in Topology §7.2 Note for `Intraday_Supervisor_Start`. Single Local task remains with loose settings as a known limitation; not blocking.

**Operational findings — Session 29 build work (2026-05-14 → 2026-05-16):**

- **TD-094 was stale documentation, not a code defect.** TD-094 claimed `hist_option_bars_1m.oi = 0` based on S22 Kite-backfill limitation. S29 backfill investigation surfaced: vendor data replaced the broken S22 backfill at some point; OI is populated 99.9% across all 12 months (avg ~1M, max 66M per row). Without verifying current state of the source table, ~1 hour of session time was spent concluding (incorrectly) that gamma_metrics backfill was infeasible. Codified as: **when a TD claims a data limitation, verify against current table state before designing around the limitation.** Source data can be replaced silently; TDs are written at a moment in time and don't auto-update. Add: an audit pass over `tech_debt.md` looking for "data limitation" TDs with no recent verification timestamp.

- **`option_chain_snapshots` vs `hist_option_bars_1m` is the historical-source-of-truth pivot.** `option_chain_snapshots` is a live-only capture table (Feb 2026+); the vendor-backfilled 14-month historical data lives in `hist_option_bars_1m` + `hist_spot_bars_1m`. Confusing the two during gamma backfill design produced a wrong "data is sparse" conclusion. Codified as: **the live writer's source table is not the backfill writer's source table.** Live writers read fresh capture (option_chain_snapshots, market_spot_snapshots); historical backfill reads from the vendor-purchased hist_* tables. Two parallel data lineages; always map both for any new compute task.

- **`hist_option_bars_1m` 5m-boundary access requires a custom index.** The table has 54.8M rows and the unique constraint `(instrument_id, bar_ts, expiry_date, strike, option_type)` does NOT support `bar_ts = exact_value` + `strike BETWEEN lo, hi` lookups efficiently. Default PostgREST 8s statement_timeout drops these queries cleanly. Solution: `CREATE INDEX ix_hist_option_bars_1m_bar_ts_strike ON hist_option_bars_1m (bar_ts, strike, option_type)` — created S29 via direct DB connection with `SET statement_timeout = 0`. Required for gamma_metrics backfill + any future per-strike historical lookups. Codified as: **PostgREST 8s timeout is the binding constraint for ad-hoc queries against multi-million-row tables; if a query times out under default settings, the access pattern needs an index, not retry logic.** Index design lives in the source-of-truth doc (System Map §B) — record new indexes when created.

- **Live writer net_gex magnitudes (trillions/quadrillions) vs backfill (Crores): the live writer regressed on TD-NEW-3.** S29 parity comparison surfaced: backfill `net_gex` is in plausible ±10K-1M Cr range; live `gamma_metrics` rows show ±trillions/quadrillions (~10^7 factor too large). The factor matches exactly the `/1e7` Cr-conversion that TD-NEW-3 was supposed to apply. The live `compute_gamma_metrics_local.py` apparently regressed at some point between S27 commit `241f943` and now. Filed as TD candidate for S30. Phase 0b consumers using `gamma_concentration` + `flip_distance_pct` are unaffected (unit-invariant); consumers using `net_gex` magnitude thresholds would be biased. Codified as: **when comparing live vs backfill data sources, mismatches in magnitude (not direction) indicate unit-convention drift; investigate before threshold-tuning.**

- **Phase 0b verdicts on ADR-002 v2 P7 are FAIL (not inconclusive).** Phase 0b RR-conditional-WR test on 1,968 regime-tagged ICT signals produced χ²=1.56, p≈0.30 — no meaningful separation between regimes. COMPRESSED salvage test failed on power (bootstrap CI [-3.36, +17.69]pp). The ICT cohort itself is median-negative across all regimes (buyer-edge lives in tails, not at median). ENH-97 was scoped to enable the gate; verdict pivots to logging-only — `vol_analytics` regime column written for future analysis, no signal-time gating. Codified as: **a definitive FAIL verdict on a Phase 0b dimension means abandoning that dimension as a gate, not retrying with different parameters.** Variance dominates the signal at this cohort size; throwing more parameter tweaks at the same data ≠ statistical power.

---

## Session 31-B engineering discoveries (2026-05-21) — codified as Pine v6 engineering catalog + ADR-004 Wave 1 IMPLEMENTED + H-FVG-retest holdout-validated edge

**Headline finding — H FVG retest is a validated edge cross-symbol cross-direction on holdout.** S31-B executed ADR-004 Wave 1 (5 primitives × 2 symbols × 4 timeframes × 14-month window) end-to-end: Task 4 backfill 2026-05-21 18:57 IST produced NIFTY 8838 + SENSEX 10561 primitives + 1:1 outcomes (38,798 rows) in 156.3s. ADR-009 Phase-1 holdout split executed same-session as build (train 2025-04-01→2026-02-28 vs holdout 2026-03-01→2026-05-19) returned the headline finding: **4 buckets simultaneously HOLD AND STRENGTHEN on holdout** — NIFTY H BEAR_FVG retest 75.0→85.2 (+10.2pp, N=29); NIFTY H BULL_FVG retest 64.3→90.0 (+25.7pp, N=27); SENSEX H BEAR_FVG retest 76.6→92.0 (+15.4pp, N=30); SENSEX H BULL_FVG retest 70.2→80.8 (+10.6pp, N=30). Deltas all in favorable direction (holdout exceeds train), opposite of overfit signature. This single finding justifies the entire S31-B effort. M5 FVG standalone refuted as coin-flip at-or-below baseline (4 buckets symmetric collapse to 45-57%). OB retest paradox surfaced (formation > retest by 30-40pp across all OB cells — opposite of FVG pattern; investigation queued). SENSEX H DISPLACEMENT_DOWN 28.6% holdout consistent reversal anomaly (possible inverse signal or detector directionality bug; investigation queued S32 P3). Codified as **D.14.1 + D.14.2 + D.14.3 + D.14.4 + D.14.5** in Assumption Register; **ENH-100/101/102** filed for downstream consumption.

**Pine v6 engineering catalog (6 entries) — Pine v6 ergonomic walls are systemic not incidental.** Task 5 Pine v6 visual MTF-breach overlay descoped after 5-version iteration v1 472 lines → v5 537 lines hit six independent ergonomic walls in sequence:

1. **Descending for-loops require explicit `by -1`.** Pine v6 `for i = N-1 to 0` never executes (silent zero iterations, no compile error). Canonical: `for i = N-1 to 0 by -1`. Surface symptom: all zones rendering active regardless of breach. PowerShell one-liner fix shipped during iteration.

2. **if/else branches must return matching types per CE10235 "Return type of if/switch blocks not compatible".** When a `label.new()` call is inside an `if` block without an `else` returning a `label`, Pine v6 type-compat check fails at compile time. Canonical workaround: pull `label.new()` into standalone `if` block (no else — Pine v6 skips type-compat check for if-only blocks). Surface symptom: compile error CE10235 on diagnostic label code.

3. **`var int` global mutation prohibited in functions per CE10088 "Cannot modify global variable in function".** Pine v6 functions cannot mutate `var int` globals — but `var array<int>` is OK because mutation is via `array.set()` not assignment. Canonical workaround: `var array<int> counter = array.from(0)`, mutate via `array.set(counter, 0, new_val)`. Surface symptom: compile error CE10088 on diagnostic counter increment.

4. **`close[1] + lookahead_off` double-shifts.** `request.security(sym, "W", close[1], lookahead=barmerge.lookahead_off)` returns close from 2 periods ago, not 1. The `[1]` index shifts back one bar; `lookahead_off` shifts back one more. Canonical previous-completed-close idiom: `close[1] + lookahead_on` OR `close + lookahead_off` (not both). Surface symptom: per-TF close fetches systematically off-by-one; PDH/PDL diagnostic showing Levels=0 traced to same double-shift bug.

5. **PDH/PDL canonical fetch idiom: `[high[1], low[1]] + lookahead=barmerge.lookahead_on`.** Combining `[1]` index with `lookahead_on` works correctly for both same-TF and cross-TF security calls. This is the canonical pattern; document for future Pine work.

6. **`max_boxes_count=500` hard cap silently GC's overflow boxes.** Pine v6 platform constraint: cannot increase `max_boxes_count` beyond 500. With 1928 zones from Wave 1 backfill, 1428 overflow boxes get GC'd silently. **Mutations on GC'd boxes are silent no-ops, not errors.** This trap produced 4 iterations of S31-B Pine debugging — the diagnostic counter showed Zones=1928 / Active=158 / Broken=1770 / Breach fires=1770 (mutation perfect) but visual rendering only showed 158 active zones because the 1770 broken-state mutations applied to GC'd boxes. Workaround: `showBroken=false` default, broken zones delete entirely freeing slots so 158 active zones render correctly.

**Operator descope decision:** "Its messed up. Lets drop the pine script for now and proceed with the rest." Task 5 deferred to wave 1.5 or later with architectural design prerequisite (TF filtering for <500 zones, GC-aware rendering, alternative display patterns — multi-pane Pine layout, separate Pine script per TF, external rendering tool) not just code-and-paste. Filed as **TD-S31B-NEW-1 S3** with Pine v6 ergonomic catalog (6 entries above) codified inline in the TD body for future Pine work reference.

**Operational findings — Session 31-B:**

- **ADR-004 Wave 1 was the single largest end-to-end build in MERDIAN history.** 5 primitives × 2 symbols × 4 timeframes × 14-month window producing 19,399 primitives + 19,399 outcomes in 156.3s. Build discipline (S31-A spec → Task 1 detector → Task 2 writer → Task 3 DDL → Task 4 backfill → Task 5 visual descoped) held throughout. ADR-009 Phase-1 holdout split as immediate validation produced the headline finding within the same session as the build. Codified as: **for major data-layer builds, design the holdout validation pipeline in the same plan as the build pipeline; execute the holdout split immediately after backfill completes; the validation verdict is the build's primary success criterion not a follow-up task.**

- **Pine v6 visual overlay is no longer a "quick task" — estimate this class of task generously.** Task 5 estimated as 0.5-1 session at S31-A planning grew to 5 iterations and operator descoped. Six independent classes of Pine v6 bugs surfaced across iterations (catalog above). The constraint set was not understood up front; the iteration discovered it. Codified as: **for Pine v6 work involving MTF data + dynamic state mutation + >500-element rendering, design the architectural constraint set BEFORE writing code; explicitly enumerate (a) TF filtering strategy for the max_boxes_count=500 cap, (b) GC-aware rendering pattern, (c) descending-loop ergonomics, (d) variable mutation pattern (`var array` not `var int`); the constraint design is itself a 0.5-1 session task.**

- **Holdout-validation is non-negotiable per ADR-009 Phase 1; S31-B's clean holdout split produced a verdict that survives scrutiny.** Train 10-month / holdout 2.5-month, both with N≥30 per cell on H-FVG-retest buckets, all four buckets independently validate with deltas large and in favorable direction. Compare to S29's Phase 0b verdict that was correct on its cohort but did not transfer to live cohort (S30 D.9.3 cohort-translation save). S31-B's verdict has not yet been transfer-checked against `signal_snapshots` runtime cohort — that pre-flight is gated by D.13.1 before ENH-102 flag flip. Codified as: **holdout-validation on the build cohort is necessary but not sufficient — the cohort-translation pre-flight on the live runtime cohort is the final transferability check. Two layers of validation, not one.**

- **Schema extensions (ENH-100 ADR-010) ship with ADR per Doc Protocol v4 Rule 10.** Additive columns on research-tier table is lightweight ADR but discipline applies; the alternative of "skip ADR because it's just columns" is the failure mode v4 codified out of existence. Codified as: **any change that adds/removes/restructures table columns triggers Rule 10 ADR mandatory — even if "just columns" on a research-tier table. The ADR documents the rationale for the column set (which 8 columns? why MAE not Sharpe? why ATM-only not OTM?); future sessions need this rationale captured before they need the columns.**

- **ENH-99 reservation preserved across three sessions (S29 file → S30 preserve → S31-B explicit skip-and-document).** Doc Protocol v2 Rule 5 monotonic-no-reuse discipline applied correctly. ENH-99 remains reserved for TD-080 Dhan 429 retry layer + circuit breaker ENH spec; ENH-100/101/102 explicitly skipped past it. Codified as: **reserved ENH IDs preserve their reservation across session-close updates; the reservation is communicated by explicit skip-and-document at next-ENH-filing time, not by retroactive consumption of the slot.**

- **OB retest paradox is a structural finding worth investigation, not a noise artifact.** Q2 in-sample WR showed M5 OB form 77-87% vs retest 47-58% across all (symbol × direction) cells — 30-40pp consistent gap. Three hypotheses to test in investigation: (a) OB retest-entry signal sub-class needs different timing parameters; (b) detector mode='retest' for OBs is mis-classified vs ICT canon; (c) trade architecture asymmetry — OBs are formation-only signals, FVGs are retest-primary, consistent with ICT instructor interpretations. **The paradox does NOT block ENH-100/101/102 deployment** (which focus on FVG retest where retest > formation holds). Investigation deferred to Wave 2 or earlier session.

- **SENSEX H DISPLACEMENT_DOWN 28.6% holdout reversal is the cleanest inverse-signal candidate seen to date in MERDIAN.** NIFTY equivalent holds directionally (53.3% holdout — overfit from 68.2% train but still edge in assumed direction). SENSEX consistently inverse across train (38.7%) and holdout (28.6%). Two hypotheses: (a) inverse signal — flipping direction-of-trade for SENSEX H DISPLACEMENT_DOWN may produce 65-71% WR (1 - observed inverse rate); (b) detector directionality bug in `_detect_displacement()` SENSEX-specific path. S32 P3 investigation: cross-check detector source code for SENSEX path before assuming inverse signal; if detector verified correct, codify inverse signal as ENH-103 with direction-flip routing rule. NIFTY M5 DISPLACEMENT_DOWN holds well (81.3→76.7 holdout) — pattern is not universally inverted.

---

**Bug B29 (anti-pattern) — Cohort-prior gate hazard: gates promoted on research cohort and shipped without live-cohort re-validation are silent edge destroyers.** Session 30 ran `s30_path1_live_cohort_pnl_v5.py` against the live `signal_snapshots` ICT-tagged 8-week cohort (gates-off, action derived from direction_bias before downstream overrides). Result: N=211 setups, WR 58.3%, mean +6.96%, median +3.38%, pooled +1469.13pp. BULL_FVG WR 58.6% beats Compendium settled 50% (z=1.72, one-sided p=0.043). BEAR_FVG WR 58.9% beats Compendium settled 46% (z=2.65, two-sided p=0.008). Yet the MERDIAN gate stack (ENH-76 BEAR_OB MIDDAY PO3 gate, ENH-77 BULL_OB AFTERNOON SENSEX PO3 gate + NIFTY hard-skip, ENH-88 BULL_FVG cluster gate, tier multipliers) — all three promoted on `hist_pattern_signals` 5m-batch cohort evidence — rejected 99.4% of these high-edge setups. The 209 of 211 blocked included 99 BULL_FVG (37 explicitly via ENH-88, others via LONG_GAMMA / DTE / confidence cascade) and the entire BEAR_OB cohort (orthogonal defect: OB attachment broken at signal-builder, see operational findings). The cohort-translation lesson §D.9.3 codified for ENH-55 in S26 was always general; S30 promotes it to first-class methodology. Codified as: **any signal-time gate whose go/no-go decision was derived from a cohort other than the live signal-snapshots cohort requires live-cohort re-validation before production deployment. Default ship pattern: env-flag with default OFF until N≥30 per pattern on the live runtime cohort confirms transfer. Reversible without code change (re-enable is .env edit + restart). Falsification mode: live-cohort WR within 10pp of research-cohort WR is the transfer-confirmed threshold (graduated per §D.8.2: 15pp at 30≤N<60).** Cross-link D.8.2 (graduated split low-N tag) + D.9.3 (cohort-translation discipline) + ADR-009 §Phase 1 S30 sub-rule (cohort-prior gate hazard) — three failure modes of the same underlying principle: gate empirical justification ≠ gate empirical performance.

**Operational findings — Session 30:**

- **Cohort-translation discipline (§D.9.3) caught itself.** S29 invested 5 days running Phase 0b RR-regime test on the `hist_pattern_signals` 5m-batch cohort and reached a clean FAIL verdict (χ²=1.56 p≈0.30, median P&L negative both buckets). The verdict was correct on that cohort. S30 measurement on the live `signal_snapshots` cohort showed median P&L +3.38% (positive) and statistically meaningful WR uplift over Compendium on both FVG sub-cohorts. Both cohorts' verdicts are valid; they are about different cohorts. Without the cohort-translation discipline codified S26, the S29 verdict would have transferred unchallenged to a live-cohort production decision. The discipline's existence is what prevented a 5-day investment from compounding into a months-long misallocation. Codified as: **when a major methodology investment (Phase 0b, Phase 0c, similar) reaches a verdict, before propagating that verdict to production action, explicitly ask: "is this verdict on the same cohort that the production gate operates on at runtime?" If not, re-validate on the runtime cohort before acting. The cohort-translation check belongs BEFORE the deployment decision, not after.** Applied retrospectively to ENH-76/77/88 — all three were shipped on research-cohort evidence and could have been caught at gate-promotion time had this discipline existed in its general form.

- **OB attachment broken at signal-builder layer, not at detection layer.** Audit query joined `ict_zones` (where status='ACTIVE' AND pattern_type IN ('BULL_OB','BEAR_OB'), 35 zones over 8 weeks) against `signal_snapshots` on (symbol, ts within zone validity window, spot inside [zone_low, zone_high]). BULL_OB: 4,882 zone-touches, only 26 tagged BULL_OB in signal_snapshots (0.5% attachment rate). BEAR_OB: 3,139 zone-touches, zero tagged BEAR_OB. Most zone-touches end up tagged NONE or as FVG when an adjacent FVG zone exists. Detection works (zones in `ict_zones` are correct); the defect is at attachment-time. Likely in `enrich_signal_with_ict()` or its callers in `build_trade_signal_local.py`. All gate-stack analyses on OB patterns are misleading by construction — the OB-tagged signal rows are a 0.5% non-random sample. Filed as **TD-S30-NEW-3 S1**; S31 P0 investigation queued. Codified as: **when measuring per-pattern edge on `signal_snapshots`, verify that pattern attachment is working at the expected rate against the zone-touch denominator BEFORE drawing conclusions about per-pattern WR. A pattern with low signal-snapshot count could be (a) rare, or (b) under-detected. Distinguish empirically via zone-touch join, not by inference from `signal_snapshots` alone.**

- **The MERDIAN gate stack as deployed at S29 close inverted edge direction on three context dimensions for the live cohort.** Gate audit on 8 dimensions: gamma_regime (LONG_GAMMA WR 55.1% N=158 gated BLOCK vs SHORT_GAMMA WR 73.3% N=15 gated PASS — gating direction correct but rejected bucket is huge), breadth_regime (BEARISH-ALIGNED WR 64.7% — gate suppresses this), vix_regime (HIGH WR 61.2% N=49 — gate suppresses this on the elevated path). Each gate was promoted on research-cohort evidence and on its own evidence path is defensible; the inversion only surfaces when the live cohort is examined directly. Codified as: **a gate's regime-classification edge direction must be verified on the live cohort the gate operates on, not inferred from theoretical mechanism (e.g., "LONG_GAMMA dealers damp moves" is a mechanism hypothesis; the live cohort's WR per regime is the test). Mechanism justifications are heuristic; the live runtime cohort is the verdict.** No production change to these gates this session — D.13.4 codifies the finding, ENH-76/77/88 env-flagged off, gamma/breadth/vix gates left in place pending live-cohort N accumulation. Investigation queued S31+ (filed as TD-S30-NEW-5).

- **Patch script + env-flag pattern is the canonical "ship reversible disablement" workflow.** S30 deployment: `apply_s30_patches.py` (227 lines, idempotent, AST-validated, patched-copy pattern) added four env-flag reads + four use-site gates to `build_trade_signal_local.py`, then `.env` updated with three explicit `=0` lines + one `=1` (`MERDIAN_TIER_MULT_DISABLE=1`). Re-enable any gate is a `.env` edit + restart. No code change required for re-enable. Mirror of S26 ENH-55 env-flag pattern. Codified as: **for any production gate whose empirical justification is challenged by new cohort evidence, the disablement vector is an env flag with default OFF + explicit `=0` documentation in `.env` (not absence-of-line). The explicit `=0` is auditable; absence is ambiguous (default OFF or never wired?). Re-enable is reversible without code change.** Pattern: production gate guarded by `if FLAG and condition:` where `FLAG = os.getenv("MERDIAN_X_ENABLED", "0").strip() == "1"`.

- **AWS sync via `git pull` is the canonical deployment vector; SSH/SCP/heredoc paste is fragile and discouraged.** S30 deployment first attempted SSH/SCP (blocked by IP whitelist), then heredoc paste (multiple syntax-error cascades from clipboard truncation/parsing). Final successful path: Local commit `git push origin main` → AWS `git fetch + git pull origin main`. Both sides now byte-identical at `2604fc2`. Codified as: **for any file deployment to AWS that is not already on disk, the canonical path is git commit Local → push → pull AWS. Heredoc/SCP/clipboard-paste are last-resort fallbacks with known truncation hazards. The 30-second extra round-trip (commit + push) is worth the integrity guarantee — git ensures byte-identical state cross-environment.**

- **`.env` is gitignored — every git pull on AWS requires manual .env reconciliation.** S30 confirmed: after `git pull origin main` brought the patched `build_trade_signal_local.py` to AWS, `.env` still lacked the four new `MERDIAN_*` flag lines. Manual append via heredoc-with-grep-guard pattern: `grep -q "MERDIAN_ENH76_ENABLED" .env || cat >> .env <<'ENV_EOF' ... ENV_EOF`. Codified as: **any commit that adds new env flag dependencies must include `.env` change instructions in the commit message body or in CURRENT.md; AWS-side .env reconciliation is the operator's manual step post-pull. Best practice: include the exact `cat >> .env <<EOF ... EOF` block in the commit message so AWS-side reconciliation is paste-able.**

---

## Session 33 engineering discoveries (2026-05-22) — codified as ADR-011 + chain-table held-strike rebuild + retest cohort empirically validates ADR-009 §Phase 1

**Eight S33 operational findings:**

- **Source-table fitness-for-purpose is a category error class.** `hist_atm_option_bars_5m` was designed for wick-reversal analysis (read by `experiment_26_option_wick_reversal.py` and friends). `hist_option_bars_1m` is the actual vendor chain (54.8M rows, dense, paid-for). Using the aggregated table for held-strike PnL across horizons is a category mismatch because vendor's pre-picked ATM rolls when spot drifts ~0.5% — same-strike enforcement against the aggregated table filters precisely the primitives where the edge plays out (self-selection bias against winners). Codified as: **when a vendor table appears "sparse" for a use case, FIRST verify the table is the canonical source for that use case. Aggregated tables built for analysis X may be wrong source for use case Y even when both involve "options" data.** TD-S32-NEW-4 vendor cross-tier drift made architecturally moot by the rebuild — no longer reads the drifting column.

- **Experiments 1-50 made the inverse error.** `build_hist_pattern_signals_5m.py` joined `hist_atm_option_bars_5m` at signal time AND signal+30m WITHOUT same-strike enforcement — comparing premiums of two different vendor-picked strikes' and calling the % change "PnL". Inflated N counts (Exp 2 BULL_OB N=101 vs ENH-100 N=12-28) but mathematically wrong: a held position holds the original strike, not vendor's rolled strike. The "negative median everywhere" of ENH-100 v6 was the inverse error (mathematically correct held-strike check on the wrong source table). Codified as: **a mathematical correctness check applied to the wrong source table can produce a more wrong answer than no check on the right source. Source choice is upstream of all checks; verify it first.**

- **Operator-prompted source-table challenge surfaced the real bug.** The operator pushed back on the "sparse vendor data" framing with verified knowledge — they had paid for and validated 12-month chain data and known it was dense. The challenge was correct and the architectural error was diagnosed within one push-back cycle. Codified as: **when the operator's domain knowledge contradicts the framing of a diagnostic, the diagnostic is wrong. Re-investigate; do not defend the framing.**

- **Per-tuple range-prefetch is the canonical pattern for chain reads.** Per-primitive query (19,399 × 6 horizons = 116k queries) hits HTTP/2 GOAWAY. Per-tuple range query (~839 NIFTY + ~1417 SENSEX = ~2256 unique (strike, expiry, type) tuples × 1 paginated query each over [min_ts, max_ts+1min)) completes in ~10 min per symbol. Cache keyed by minute-floored real-UTC ISO for O(1) lookup. Codified as: **for chain-table reads at scale, prefetch per (strike, expiry, option_type) tuple over a timestamp range, not per (primitive, horizon) point. The grouping concentrates the I/O and amortizes query overhead.**

- **Empirical expiry calendar beats DOW rules.** NSE/BSE 2025-09-01 weekly-expiry-weekday swap (NIFTY Thu→Tue, SENSEX Tue→Thu) plus ~15% holiday-shifted weeks (Mon/Wed expiries) plus same-week double expiries (monthly + weekly) defeat any pure DOW formula. Sorted list of vendor's actual expiries + bisect_left is uniform across all cases. Codified as: **when vendor data carries the calendar, READ the calendar — do not derive it. DOW + holiday-list reconstruction is a maintenance liability the calendar lookup eliminates.** Calendar loaded via DISTINCT on `hist_atm_option_bars_5m.expiry_date` (small 27k table, fast).

- **Held-strike chain reads flip universally negative median to universally positive on directional cells.** v6 ENH-100/103 reported median 30m option PnL negative on every (symbol, tf, primitive_type, direction) cell. v7 reports +13.6 to +20.3% on Tier 1 cells (M5 OB, M5 DISPLACEMENT cross-symbol) and +22.0 to +34.7% on Tier 2 cells (H DISPLACEMENT cross-symbol). The v6→v7 delta is not noise — it is the architectural fix recovering the edge that v6's same-strike-on-rolling-source filter was suppressing. Codified as: **median PnL universally negative on a cell that has 80%+ spot WR is a source-table smell, not an edge property. Investigate before concluding "no option edge."**

- **M5 OB retest paradox confirmed at option-PnL level.** Earlier S31-B finding (D.14.3) flagged M5 OB retest WR 47-58% vs formation 77-87% — paradox under investigation. v7 held-strike retest cohort confirms it at the option-PnL level: NIFTY M5 BEAR_OB retest median 30m -11.1% / opt WR 35.3% / N=17; SENSEX M5 BEAR_OB retest median -5.1% / N=23; SENSEX M5 BULL_OB retest median -4.3% / N=23. M5 OB retest is FALSIFIED as edge. Codified as: **trade M5 OBs at formation, not at retest. The retest paradox was real, not a sampling artifact.** D.14.3 promoted from "investigation pending" to "falsified at option-PnL level." OB attachment at signal-builder layer (TD-S30-NEW-3 P0_TERTIARY) becomes lower priority since OB retest path is not the tradeable surface.

- **DTE bucketing reveals a 3-bucket structure on Tier 1 cells.** DTE=0 is feast-or-famine on UP displacements (median 30m +58.7% NIFTY / +36.2% SENSEX; median 60m turns NEGATIVE on DOWN-direction at -4.8% to -9.2%; exit at 30m mandatory on DTE=0). DTE=1-2 is the M5 sweet spot (NIFTY M5 BEAR_OB +24.4% / WR 80%, SENSEX M5 DISP_UP median 60m +38.3% / WR 83.3%, both N=15+). DTE≥3 dominates on H-TF (NIFTY H DISP_DOWN +37.5% median 60m / 100% WR, SENSEX H DISP_UP +48.2% median 60m / 92.3% WR). Codified as: **DTE is not a single-direction lever. DTE=0 needs aggressive exit (gamma-driven, theta accelerates after favorable move plateaus). DTE=1-2 sweet-spots M5 (residual gamma + slower theta). DTE≥3 is H-TF home (room for spot move to play out without theta crushing premium).**


- **B31:** The hardest operational questions are architectural framing questions, not data questions. Volatility table name was a data question ("which table do we write to?"); the real question was architectural ("is the table name parameterized or hardcoded?"). When troubleshooting stale data, separate "data not being written" from "architecture misconfigured for write" before investing in data recovery work.

- **B32:** Cron environments are hostile. `.env` is not inherited by cron jobs — explicit `source .env` in the cron entry is mandatory. `flock` scope boundaries are narrow — the lock applies only to the immediately-following command, not to an && chain. Test cron entries manually in bash before deploying them to crontab.

- **B33:** Graceful error handlers mask systemic failures when coupled with silent output. `compute_volatility_metrics_local.py` caught 404 PGRST205 errors and exited `exit_code=0` for 2+ months; orchestrator marked it OK despite total failure; dashboard showed stale data (expected, not alarming). Rule: log the graceful degradation even on `exit_code=0` success for operational visibility. Dashboard "LAST CYCLE FAILED" after the fix showed operators *saw* the failure — the problem was that graceful handlers hide failures in logs too.

- **B34:** Production data can mask upstream failures. NIFTY gamma_metrics kept writing successfully (success visible on dashboard), SENSEX never started (failure invisible because no dashboard alert for missing symbol). Root cause (orchestrator stopped firing) only visible from log. Rule: monitor orchestrator health separately from output metrics.

- **B35:** Hardcoded table names in code are technical debt. `compute_volatility_metrics_local.py` hardcoded `"compute_volatility_metrics"` per stale comment "Once ADR-006 DDL lands". Schema evolved, comment staled, code persisted. Rule: parameterize or version-gate schema-dependent code. Prevent via code review check: "hardcoded strings matching table names must be parameterized or have ADR reference + deadline".

- **B36:** "Silent but working" and "broken but graceful" are opposite conditions masquerading as the same. Four days of "everything looks fine, we're writing to tables, no errors" vs four minutes of "can't find the table it's trying to write to". The difference between silent-okay and silent-failure is one SQL query. Query production data FIRST before reading logs, because logs can be misleading when error handlers are good at their job.


**Settled decisions added:**

- ADR-011 ACCEPTED 2026-05-22 (chain-table held-strike ATM PnL — supersedes ADR-010 v3 §"Source table choice" only; schema unchanged). Governance: *"ATM PnL across horizons is computed from the chain table `hist_option_bars_1m` with manually-picked ATM strike (NIFTY/50, SENSEX/100) held constant — never via vendor's pre-picked atm_strike in `hist_atm_option_bars_5m`. The held position holds the entry strike; the PnL reflects that. Same-strike enforcement on a vendor-rolled-ATM source is a category error — strikes don't roll; vendor's pick does."* Implemented via ENH-106 v7 writer (`build_ict_primitives.py` marker `ENH-106 (S33) v7`); v6 helpers retired as dead code; backup `build_ict_primitives_PRE_S33_v7.py` preserved.

- ENH-100 status v3→v7 IMPLEMENTED (formation-anchored ATM PnL columns rebuilt against chain table with held strike). Coverage jumped 20-44× on populated M5 cells; median PnL flipped positive on directional cells.

- ENH-103 status v6→v7 IMPLEMENTED (retest-anchored option PnL columns rebuilt against chain table with held strike). Coverage at 65% of retested primitives (from 12% under v6).

- ENH-106 IMPLEMENTED (the architectural rebuild itself — chain-table held-strike compute method); single session ship, dry-run + live + TRUNCATE + re-backfill workflow took ~30 min for 19,399 outcomes.

- ADR-009 §Phase 1 H-FVG retest finding empirically confirmed cross-symbol with held-strike option PnL (NIFTY D BULL_FVG retest median 30m +11.7%, median 60m +22.3%, N=17, opt WR 88.2%; NIFTY H BEAR_FVG retest median 30m +4.8% / N=51 / opt WR 72.5%; SENSEX H BEAR_FVG retest median 30m +9.9% / N=45 / opt WR 75.6%). Spot-WR validation from S31-B now strengthened with option-PnL validation — two independent metrics agreeing. D.14.1 updated.

- §D.15 added to Assumption Register (4 rows D.15.1-D.15.4: chain-table canonical for held-strike PnL VALIDATED; M5 OB retest paradox CONFIRMED at option-PnL level — FALSIFIED as edge; DTE 3-bucket structure DISCOVERED on Tier 1 cells; D-TF horizons need extension beyond 30m/60m — investigation HYPOTHESIS).

- TD-S32-NEW-4 vendor cross-tier drift on `hist_atm_option_bars_5m` CLOSED via architectural retirement (writer no longer reads the drifting column).

**New TDs filed S33 (pending S34+):**

- TD-S33-NEW-1 S3 (`hist_atm_option_bars_5m` reason-to-exist re-evaluation post-wick-experiment retirement — currently still read by `experiment_26/27/27b`; revisit when wick experiments are retired; orphan-candidate).
- TD-S33-NEW-2 S4 (v6 dead code cleanup in `compute_atm_pnl_and_dte` — trailing unreachable for-loop after v7 head's early return; `_atm_anchor_at` + `_atm_future_at` helpers now orphan; ~15 min cleanup in next writer refactor; no behavioral impact).
- TD-S33-NEW-3 S2 (post-v7 falsification audit — original ENH-100 5% agreement criterion now tautological since audit and writer read same chain source; replace with strike-rounding + expiry-calendar lookup correctness audit; deferred from S33 close).
- TD-S33-NEW-4 S3 (D-TF horizon extension — D-TF cells show positive spot WR ~67% but negative option PnL median at 30m/60m. `mfe_pct` + `time_to_mfe_min` columns may already answer "what horizon makes D-TF tradeable"; SQL-only analysis; carry-forward).

---

## Session 34 engineering discoveries (2026-05-24) — codified as ADR-012 + spot-anchored SL doctrine + universal big-move-day capture audit + selection-research arc framework

**Six S34 operational findings:**

- **Capture is not the binding constraint; selection is.** Universal audit of ≥1% intraday move-days across 2025-04-01 → 2026-05-19: 119 days qualified (NIFTY 57, SENSEX 62 at parity 1.0% threshold); 118/119 captured (99.2%) by MERDIAN's ICT primitive layer with direction-aligned + zone-interaction filters; one miss (NIFTY 2025-05-20 D DISPLACEMENT_DOWN existed but price didn't reach zone). Origin distribution: FVG 84% (BEAR 47%, BULL 36%), OB 16%; timeframe D+W 74% (D 47%, W 27%), H 14%, M5 11%. The system sees every move structurally. The bottleneck is selection — on any given morning 50+ active aligned zones exist across W/D/H/M5, and the system has no ranking layer to predict which fires today. Codified as: **capture and selection are distinct problems with distinct solutions. Capture-rate ≥99% indicates the structural foundation is sound. Selection-research is the next-layer build, not a capture-layer fix.**

- **The retest is structurally the moment of maximum local risk for an ICT trade.** S34 SL-variant backtest on 7 same-day retest entries: tightest spot-anchored SL (0.3% adverse 5m-close-through from entry) **never triggered on any of the 7**. The retest is the structurally invalidatable moment — price has come back to the zone and is at the decision point; once it has begun rejecting, it does not return through within the same-day window. This is consistent with ICT canon: the retest IS the entry-validation event. Codified as: **for ICT retest entries the SL anchor measures thesis failure (close-through zone invalidation on 5m), not microstructure noise. Once the retest survives the first 5-15 minutes, the trade is what it is — structural pricing.**

- **Premium SL on ATM held strikes is the wrong instrument-stop pairing.** S34 backtest: 5% intra-bar premium SL eliminated 100% of structurally-correct winners (0/7); 10% same (0/7); even 15% intra-bar only saved 3/7. 1m-close-through 5%/10% same pattern. ATM held strikes wick 5-15% in first 30-60 min from bid-ask + theta on tight DTE + IV crush as morning gamma compresses; the wick noise is uncorrelated with whether the structural thesis is intact. Wide premium SLs (Compendium-era 30%+) approximate the right intent indirectly but couple SL hit rate to volatility regime, expiry distance, and bid-ask conditions unrelated to structural premise. Codified as ADR-012: **SL must anchor to spot zone-invalidation on 5m-close-through. Premium-based stops on ATM options measure microstructure, not thesis. Wider premium stops are an indirect approximation; spot stops are the canonical anchor.**

- **Vendor-purchased data and self-ingested data sit in different tiers; transitions between them create silent coverage gaps in downstream consumers.** TD-S34-NEW-4: `hist_option_bars_1m` returned full chain on pre-2026-04-01 dates and zero rows on all nine probed post-Apr-2026 dates. Pre-Apr-2026 was vendor-purchased dense across the 12-month window paid for and verified. Post-Apr-2026 is MERDIAN-ingested via a different writer path. The framing "MERDIAN failed to ingest entirely" cannot be the full picture — plenty of full-ingestion days post-Apr-2026 are observed elsewhere in the system. Diagnostic path is tier-transition + ingest writer locate, not token-failure pattern-match. Codified as: **when a backfill cohort's coverage suddenly stops at a calendar boundary, the first diagnostic is "what tier of data lives on each side of the boundary?" — not "what failed?" The transition may be architectural (storage path mismatch) not operational (ingest failure).** Initial framing of TD-S34-NEW-4 as "Kite token expiry on MALPHA (TD-080-adjacent)" was incorrect and recorded as superseded per operator domain knowledge (S33 push-back pattern applies again).

- **Prior research stands; the ENH-108 gap expands the addressable cohort, not refutes findings.** When S34 cohort SQL surfaced only 7 same-day first-retests landing on ≥1% move-days (vs the implicit expectation of "much more"), there was a momentary alarm about whether prior retest-cohort research (D-FVG retest 78-86% WR, H-FVG retest 60-76% WR) was invalidated. The correct framing: prior research measured the *first-touch retest cohort*, which is thousands of events across the 14-month window; the 7 is a *subset* (first-touch retests that happened on a ≥1% move-day). The 111 other ≥1% move-days are powered by non-first-touch retests, invisible to `ict_primitive_outcomes` per ADR-004 §10's single-`first_retest_ts` schema — a real but bounded model gap (ENH-108 filed). Codified as: **a cohort definition that produces a smaller-than-expected N is not evidence prior research is wrong; it may be evidence the prior research measured a different (correctly-defined) cohort, and the current cohort definition is more restrictive. Distinguish "this metric is unstable" from "this metric measures a different population than I just queried."**

- **Operator domain-knowledge pushback continues to be the highest-bandwidth bug surface.** S33 operator pushed back on "sparse vendor data" framing → diagnosed source-table category error within one cycle. S34 operator pushed back on "sparse data" framing again (mixed-resolution `hist_spot_bars_1m` historical era) → diagnosed switch to canonical `hist_spot_bars_5m` with era-aware time handling. S34 operator pushed back on "Kite token expiry" diagnostic framing of TD-S34-NEW-4 → diagnosed vendor → MERDIAN-ingest tier transition as actual root cause. Codified as: **when operator pushes back on diagnostic framing with verified domain knowledge, the framing is the bug, not the data. Re-investigate the framing before defending it. Pattern observed across S33+S34, consistent with B17 hypothesis-exhaustion rule.**

**Settled decisions added:**

- ADR-012 ACCEPTED 2026-05-24 (Spot-anchored stop-loss doctrine for ICT retest entries; supersedes informal Compendium-era 30%+ premium SL default). Governance: *"For ICT retest entries on zone primitives (OB, FVG, Breaker, Mitigation, Premium/Discount), the stop-loss is anchored to spot zone-invalidation on 5m-close-through — NOT to option premium decay. BEAR retest stopped out when 5m spot bar closes ≥ `zone_high * (1 + X)`; BULL retest stopped out when closes ≤ `zone_low * (1 - X)`. Buffer X = 0.5% provisional default. Premium-based stops on ATM held strikes are the wrong instrument-stop pairing — they measure microstructure noise (bid-ask, theta on tight DTE, IV crush) not thesis validity. Time-based exit policy (ENH-107) is orthogonal and composes independently."* Empirical foundation n=7 same-day retest entries (S34 backtest): premium SL ≤10% → 0/7 winners; spot SL 0.3%-0.8% → 0/0/0 SL triggers, 7/7 winners. Held-to-EOD total +₹106,888 on 1 lot. Validation cohort expansion to n≥50 gated on TD-S34-NEW-4 closure + ENH-108 N-touch retest detection.

- Universal big-move-day capture rate 99.2% (118/119 over 2025-04-01 → 2026-05-19) — MERDIAN's ICT primitive layer sees every ≥1% intraday move structurally. Codified D.16.1 + D.16.2.

- Selection-research arc framed as 3-5 session work item with five candidate features (pre-open structural signature; same-day momentum confirmation gate; HTF context alignment; zone freshness; DTE-aware option leg selection). Framework doc deferred to S35+ per operator sequencing.

- ENH-108 PROPOSED (Second-touch / N-touch retest detection on ICT primitives) — foundational schema unlock for selection research; recommendation is new `ict_primitive_touches` table (FK + touch_seq) over JSONB extension; cost 2-3 sessions; partial backfill on pre-Apr-2026 window feasible immediately, full validation gated on TD-S34-NEW-4.

- §D.16 added to Assumption Register (4 rows D.16.1-D.16.4: universal capture rate 99% VALIDATED; selection problem articulated VALIDATED-problem-statement; spot-anchored SL doctrine VALIDATED → ADR-012; chain coverage gap OPEN → TD-S34-NEW-4).

**New TDs filed S34 (pending S35+):**

- TD-S34-NEW-4 S2 (`hist_option_bars_1m` post-2026-04-01 coverage gap; vendor → MERDIAN-ingest tier transition root cause per operator framing; nine probed dates confirmed empty for both NIFTY+SENSEX; workaround `trade_date <= '2026-03-30'`; diagnosis steps queued — locate post-Apr-2026 writer path / sibling tables / partial-ingest failure mode; 1-2 sessions to fix; not blocked structurally).

---

## Session 35 engineering discoveries (2026-05-25) — codified as ADR-012 IMPLEMENTED + ADR-013 PROPOSED + ENH-106 v8/v8.1/v8.2 dual-source chain reader + ENH-106-SL v9 spot-anchored SL writer extension + TD-S34-NEW-4 mechanical closure

**Nine S35 operational findings:**

- **A data-tier transition is a three-surface architectural concern, not one.** S35 started with v8 patch routing chain prices via 2026-04-01 boundary check (pre→`hist_option_bars_1m`, post→`historical_option_chain_snapshots`). Single-cell test produced `n_tuples=0` because `_enum_anchor` silently early-returned on `_nearest_weekly_expiry` returning None — the vendor expiry calendar `hist_atm_option_bars_5m` caps at 2026-04-02/04-07 and contains no expiries for a post-Apr retest day. v8.1 patch extended `_load_expiry_calendar` to UNION vendor + HOCS expiries. Then PostgREST 8s statement_timeout (57014) surfaced because the new HOCS expiry walk paginated 2.67M rows just to find 10 distinct values. v8.2 patch swapped pagination for `get_hocs_distinct_expiries(text)` RPC + `idx_hocs_symbol_expiry (symbol, expiry_date)` covering index for sub-100ms execution. ATM strike picking was already independent in v7 (manual via `STRIKE_INTERVAL` grid) — third surface pre-resolved. Codified as: **when a data-tier transition is identified, audit ALL surfaces that depend on the source table, not just the headline column. Each surface needs its own routing logic at the same boundary. Single-patch coverage is not closure; iterate through every consumer.**

- **DISTINCT-over-millions-of-rows must live in the DB, not in the client.** Initial v8.1 HOCS calendar walk used `sb.table("historical_option_chain_snapshots").select("expiry_date").eq("symbol", X).range(0, 999)` paginated to find ~10 distinct values across 1.3M-per-symbol rows. PostgREST round-trips alone = 1300 page fetches × ~100ms latency ≈ 2+ minute hang. The right shape is a Postgres function `STABLE LANGUAGE sql` returning `TABLE(expiry_date date)` with `SELECT DISTINCT ... ORDER BY ...` exposed as `sb.rpc("get_hocs_distinct_expiries", {...}).execute()`. With a `(symbol, expiry_date)` covering index, Index Only Scan + HashAggregate clears in 325ms. Codified as: **supabase-py REST has no DISTINCT primitive; any "find unique values for a key prefix" query over >100k rows MUST be a DB function via RPC, not a client-side paginated walk. The cost difference is 4 orders of magnitude.**

- **PostgREST 8s statement_timeout doesn't apply to direct DB connections.** Initial confusion when `EXPLAIN ANALYZE get_hocs_distinct_expiries('NIFTY')` returned 325ms in Supabase SQL editor (direct DB connection via postgres role) but timed out via PostgREST role connection from the script. PostgREST role has a configured 8s `statement_timeout`; direct DB role does not. This is also the same property exploited by `SET statement_timeout=0` for index builds on multi-million-row tables (S29 created `ix_hist_option_bars_1m_bar_ts_strike` this way; S35 created `idx_hocs_symbol_expiry` this way). Codified as: **PostgREST writes/reads share an 8s ceiling that does not bind direct DB sessions; long-running DDL or `EXPLAIN ANALYZE` always goes through Supabase SQL editor or psql, never the JS/Python SDK.**

- **`historical_option_chain_snapshots` is the post-Apr-2026 canonical chain history — and its schema is shaped differently from `hist_option_bars_1m`.** Tier-1 vendor table `hist_option_bars_1m` is keyed on `instrument_id` (uuid), uses `close` as the price column, and is 54.8M rows of vendor-purchased dense data through ~2026-04-01. Tier-2 ingest table `historical_option_chain_snapshots` (HOCS) is keyed on `symbol` (text), uses `ltp` as the price column (5-min point-in-time snapshot not OHLC), and has `run_id` and `source` audit columns absent on the vendor table. The MERDIAN-ingest writer `ingest_option_chain_local.py` (live capture, 5-min cadence) populates HOCS — the file's name reads like a live tool but its persistence layer is canonical historical archive for the post-vendor era. Codified as: **the historical chain source-of-truth pivots from vendor (`hist_option_bars_1m`) to MERDIAN-ingest (`historical_option_chain_snapshots`) at 2026-04-01 UTC. Anything that read the vendor table for post-Apr-2026 dates was silently empty. Always check `option_pnl_source` on outcomes to see which tier fed a given row.**

- **`run_id` NOT NULL constraint on HOCS — backfill scripts that don't set it lie silently when wrapped in try/except.** First Breeze backfill run (v2) appeared to succeed: `wrote 11625 rows to HOCS for NIFTY 2026-04-21 CE` repeated 6 times. Actual HOCS row count: 0. The `chunk = sb.table().insert(rows).execute()` raised `23502 null value in column "run_id"` per chunk, the `except Exception as ex` block caught + logged the error + `break`'d the chunk loop, but the outer logic still ran `total_rows += len(rows_for_block); log("wrote N rows...")` regardless of whether ANY chunk had succeeded. Three concurrent failures: (a) HOCS schema requires `run_id` NOT NULL (matches the writer convention — every cycle gets a UUID), (b) backfill script omitted it, (c) error handling was lossy. v3 fix: `RUN_ID = str(uuid.uuid4())` module-level constant attached to every row dict; success log gated on `inserted_this_block` counter not local-accumulator length. Codified as: **backfill writers must mirror the schema invariants of the live writer they're substituting for; "I'll insert raw rows and let the schema reject what doesn't fit" is a category error when the schema invariant is `NOT NULL` rather than `CHECK`. Audit live-writer row construction before writing a substitute.**

- **`stock_code="BSESEN"` is the canonical SENSEX identifier on Breeze — not "SENSEX", and not documented.** Initial v3 SENSEX leg returned `Status=200 Success=[]` for every (expiry, strike, OT) triple with `stock_code="SENSEX" exchange_code="BFO"`. Six-variant empirical probe (SENSEX/BFO, SENSEX/NFO, SNSXIN/BFO, BSXSEN/BFO, BSESEN/BFO, BSESNS/BFO) — only `BSESEN/BFO` returned non-empty `Success` list. NIFTY uses the obvious `stock_code="NIFTY" exchange_code="NFO"`. SENSEX-on-Breeze identification path is institutional knowledge that has to be empirically discovered each time. Codified as: **for any new vendor integration involving Indian indices, do a multi-variant identifier probe in the first 5 minutes — don't accept "0 rows returned" as definitive without probing canonical alternatives. ICICI Breeze SENSEX = BSESEN/BFO (per S35 empirical).**

- **Writer is INSERT-only on `ict_primitive_outcomes` — new schema columns don't backfill on existing rows.** ENH-106-SL v9 added 5 SL columns. After single-cell test on 2026-05-14 with delete-targeted-subset, the 5 fresh outcomes correctly populated SL columns; one leftover row from prior recompute era (different retest_ts, not covered by the delete window) retained NULL SL columns. The `upsert_outcomes` function is structurally INSERT-only (skips rows whose `primitive_id` already exists in outcomes) for idempotency. Schema column adds therefore require either (a) TRUNCATE + full recompute (canonical for major schema changes), or (b) DELETE-targeted-subset + recompute (canonical for per-date validation). There is no "ALTER COLUMN with backfill in-place" path on the writer side. Codified as: **adding columns to `ict_primitive_outcomes` is an O(N) cohort regeneration not an O(1) ALTER — budget recompute cost (S35 precedent: ~35 min for full 19,571 rebuild) when scoping schema-extension ENHs.**

- **Multi-line nano paste into SSM beats base64 single-line paste for code transfer to MERDIAN AWS.** Single-line clipboard paste through SSM Session Manager truncates at ~4KB (per-line buffer limit observed empirically S35); base64-encoded 19KB Python file paste fragmented. Splitting into 4×5KB chunks still failed PowerShell-side because `C:\Temp\` did not exist; relocating to `C:\GammaEnginePython\` worked but each chunk paste-to-shell was still mangled by here-doc quote handling. Solution: `nano /home/ssm-user/<file>.py` on MERDIAN, Ctrl+A Ctrl+C on the source file open in Notepad locally, paste into nano (line-breaks chunk the input naturally so terminal buffer never hits its single-line limit), Ctrl+O Enter Ctrl+X. md5sum verification cross-environment confirmed byte-identical. Codified as: **for ad-hoc code transfer to a single SSM-only host, multi-line nano paste is the canonical method; here-docs and base64 streaming are anti-patterns. The terminal buffer is per-line, not per-paste, so line-broken content streams cleanly.**

- **Breeze surgical-fill capability is operationally proven for one-shot architectural fallback.** S35 delivered 107,630 HOCS rows in 4-5min wallclock (NIFTY 61,899 + SENSEX 45,731) on a single trading day across 3 expiries × 31 strikes × 2 OT per symbol from MERDIAN AWS via SSM. Full-chain (no ATM±N cap), 3-year lookback, 5000 calls/day, 100/min throttle. SEBI April 2026 rule mandates static IP whitelist — incompatible with home multi-WAN; deployment from MERDIAN AWS Elastic IP `13.63.27.85` succeeded. Daily token refresh via OAuth-style 2FA redirect (`apisession` URL query param) requires manual browser session each morning. Codified as: **Breeze qualifies as architectural fallback for any future single-day chain outage; long-term graduation to canonical historical backfill source proposed as ADR-013 + ENH-109 pending broader validation cycles. For routine use, daily-token-refresh friction is the operational tax.**

**Settled decisions added:**

- ADR-012 ACCEPTED → IMPLEMENTED 2026-05-25. Writer-side ADR-012 deliverable shipped via ENH-106-SL v9 patch on `build_ict_primitives.py` (4 edits: `SL_BUFFER_PCT_DEFAULT=0.005` constant, OutcomeRow 5 SL fields, `compute_retest_atm_pnl` SL evaluation block walking 5m clock-aligned spot bars from `first_retest_ts + 5min` through 15:25 IST EOD bucket close, `upsert_outcomes` plumbing). DDL added 5 columns to `ict_primitive_outcomes` (`sl_level`, `sl_buffer_pct`, `sl_triggered_ts`, `sl_exit_prem`, `pnl_with_sl_pct`) with `COMMENT ON COLUMN` documentation per ADR-012 §3. Trigger rule: BULL primitive → `close < zone_low × (1 − X)`; BEAR primitive → `close > zone_high × (1 + X)`. Level primitives (PDH/PDL etc.) skip SL by design (no zone bounds). `pnl_with_sl_pct` falls back to `option_pnl_eod` value when SL never triggers intra-session. Single-cell n=5 validation on 2026-05-14 NIFTY M5 cohort cleared: 2 BEAR primitives correctly triggered SL exiting 13-14pp better than held-to-EOD at 0.5% buffer; 3 BULL no-trigger correctly degenerated to held-to-EOD. Math validated to 5 sig figs on all 5 rows (e.g., zone_high=23562.8 × 1.005 = 23680.61 exact). Cohort expansion to n≥50 deferred to S36 P0 (full TRUNCATE + recompute required because writer is INSERT-only). Signal-emitter / order-placer / Pine overlay implementation paths remain deferred — backtest infrastructure side is now functional.

- ADR-013 PROPOSED 2026-05-25. Breeze ICICI Direct API as canonical historical options backfill source. Defers third-party vendor renewal cycle dependency for `hist_option_bars_1m`. Full-chain capability (no ATM±N cap), 3-year lookback, 5000 calls/day, 100/min throttle, SEBI static-IP whitelist required post-2026-04-01. SENSEX symbology `BSESEN/BFO` (verified S35). PROPOSED status pending broader validation cycles: (a) operational integration (token-refresh workflow automation), (b) scheduled-run reliability >95% over 30 trading days, (c) cost analysis vs vendor renewal. Architect recommendation pending. Linked to ENH-109.

- ENH-106 v8 + v8.1 + v8.2 IMPLEMENTED 2026-05-25. Dual-source chain reader on `build_ict_primitives.py` — boundary 2026-04-01 UTC, per-tuple split routing pre_only / post_only / mixed. `option_pnl_source TEXT` audit column on `ict_primitive_outcomes` with values `vendor_hist_1m` (pre-boundary anchor) / `merdian_hist_5m` (post-boundary anchor) / NULL (level primitives + non-tradeable rows). v8.1 patch UNIONs vendor + HOCS expiry calendars. v8.2 patch swaps HOCS pagination for RPC `get_hocs_distinct_expiries(text)` STABLE function + `(symbol, expiry_date)` covering index `idx_hocs_symbol_expiry` for sub-100ms DISTINCT under PostgREST 8s window. Full recompute 2025-04-01 → 2026-05-24: NIFTY 8,925 outcomes / 1,028 tuples (833 pre_only + 188 post_only + 7 mixed); SENSEX 10,646 outcomes / 1,745 tuples (1,442 pre_only + 286 post_only + 17 mixed); 19,571 total in 2107s wallclock. Boundary routing verified clean — `vendor_hist_1m` max retest 2026-03-30 / `merdian_hist_5m` min retest 2026-04-01 perfect split.

- ENH-109 PROPOSED 2026-05-25. Graduate Breeze rollingoption + `get_historical_data_v2` as canonical historical options backfill source. Replaces vendor purchase + S22 Kite + Dhan rollingoption ATM±10 cap. Phase 3 prerequisite per ADR-002 v2 GEX time-series requirement. PROPOSED pending broader validation per ADR-013 conditions.

- TD-S34-NEW-4 CLOSED-MECHANICALLY 2026-05-25. 81% zone-primitive recovery on post-Apr-2026 retest cohort via v8 dual-source reader; +14 outcomes via Breeze 2026-04-16 surgical fill; final recovery 331 / 409 zone-primitive denominator = 81% (132 architecturally-exempt level primitives excluded from denominator). Structural residual ~75 carries to TD-S35-NEW-1.

- MERDIAN AWS instance rebuilt to `i-0878c118835386ec2` (per S35 SSM access). Memory shows older `i-0e60e4ed9ce20cefb` from S28 catalog — reconcile in S36 Topology update + memory refresh. Instance public IPv4 = Elastic IP `13.63.27.85` eu-north-1 (confirmed S35 Breeze deployment).

- §D.17 added to Assumption Register (4 rows D.17.1-D.17.4 codifying S35 findings: D.17.1 dual-source chain reader = three-surface architectural concern VALIDATED; D.17.2 HOCS strike-coverage structural limit VALIDATED → TD-S35-NEW-1; D.17.3 Breeze full-chain capability VALIDATED → ADR-013 PROPOSED + ENH-109 PROPOSED; D.17.4 ADR-012 SL writer integration VALIDATED on n=5 single-cell).

**New TDs filed S35 (pending S36+):**

- TD-S35-NEW-1 S2 (HOCS strike-coverage structural limit; `ingest_option_chain_local` captures ATM±N strikes per cycle, not full chain; retests at substantially-drifted spot vs formation miss held-strike at retest moment; ~75 of post-Apr-2026 retests structurally uncoverable from MERDIAN-ingest tier alone; recovery requires either widening live ingestion window OR ADR-013 Breeze canonical adoption per-date backfill).

- TD-S35-NEW-2 S1 (pre-Apr-2026 chain vendor uncatalogued — critical institutional knowledge at risk, bus-factor-of-one; the 54.8M-row `hist_option_bars_1m` vendor identity + contract terms + refresh cadence + stock_code mappings + contact details not documented anywhere accessible to future Claude or successor operator).

- TD-S35-NEW-3 S4 (SENSEX Breeze symbology `BSESEN` not in public docs; document in System Map + Deployment Topology to prevent re-discovery cost on future Breeze integration).

- TD-S35-NEW-4 S3 (writer INSERT-only on `ict_primitive_outcomes` table — schema column adds require manual DELETE before recompute populates new columns; consider UPSERT mode or schema-migration helper script to reduce future column-add friction; particularly relevant for ENH-100/108 future schema extensions).

---

## v1.31 (Session 41, 2026-06-01) — Marketview writer-side gap closure + Health dashboard + Pine GEX overlay polish

### S41 settled-decisions

- **`gamma_metrics` schema extended with 3 numeric columns**: `vix` (India VIX at cycle ts, populated from Dhan security_id=21 IDX_I INDEX), `max_gamma_strike` (FIX-1 argmax positive gex_cr — pin-candidate magnet semantic, NULL if no positive strikes), `pin_risk_score` (0-100 additive-weighted blend of gamma_concentration + spot_proximity + sustained_time + (1-expansion_prob/100); renormalize when sub-factors None). 30-day backfill 3,787 rows shipped (2,909 VIX + 439 max_gamma_strike + 439 pin_risk_score).
- **FIX-1 max_gamma_strike semantic correction is canonical**: argmax of POSITIVE gex_cr only (pin-candidate magnet) rather than argmax(|gex_cr|) which returned amplifying strikes when |negative| > |positive|. Cross-vendor validation against StockMojo confirmed Call Wall 24,000 / Put Wall 23,500 semantic for NIFTY 2026-05-29 EOD; MERDIAN now aligns with that pin-magnet UX semantic.
- **Pine v6 GEX overlay color discipline**: PIN cyan (#00FFFF) + ACCEL magenta (#FF00FF) — both outside ICT family color space (greens/crimsons/salmons/yellows/oranges); prevents visual collision when ICT layers are enabled. Codified D.23.1 (positive-GEX argmax) + D.23.3 (Pine box-count silent-overflow-drop).
- **`max_boxes_count=500` is the Pine v6 default for any MERDIAN overlay** (raised from 250 which silently drops late-emitted boxes when ICT layers consume the budget). Future Pine overlay scripts emit `max_boxes_count=500, max_lines_count=500, max_labels_count=500` at template construction time.
- **RLS rowsecurity=true without policies = silent empty anon datasets**: canonical fix `CREATE POLICY anon_select_<table> ON public.<table> FOR SELECT TO anon USING(true)` on every newly-RLS-enabled ops table; codified D.23.2. Applies to all Lovable-scaffolded surfaces touched by Marketview / Health frontends.
- **ENH-72 ExecutionLog instrumentation pattern is canonical for every writer**: `compute_gamma_metrics_local.py` is the reference implementation. Pattern: parse args BEFORE `ExecutionLog.open()`, open with `expected_writes={"<table>":N}` + `symbol=index_symbol` (Choice A per-symbol cadence), wrap try/except, `record_write()` per target, `complete()` + `exit_with_reason()`, change `main() -> int` and `sys.exit(main())`. S41 applied this to `build_wcb_snapshot_local.py` closing TD-S41-NEW-4 same-session.
- **Cross-host token refresh single-point-of-failure surface elevated to ADR-006 urgency driver**: TD-S41-NEW-2 surfaced that Local Windows is sole refresh path; AWS pull_token_from_supabase.py consumes but doesn't refresh; holiday boundaries (Bakri Id) where Local workstation is off become silent risk windows. Interim mitigation WAKETOWRUN=true on Local task + AWS-side heartbeat staleness check (`pull_token_from_supabase.py` fails loudly if pulled token's `last_refreshed_at` > 18 hours stale). Architectural fix is ADR-006 AWS migration.
- **Observable-surface-first discovery pattern**: Marketview Health dashboard WCB row "never" surfaced TD-S41-NEW-4 in <90 min from dashboard view → diagnostic SQL → writer-source-reading → 2-replacement v3 patch + smoke-fire. Pattern beats log-grep-first when question is "is this writer instrumented?" rather than "is this writer failing?".
- **Cross-vendor sign convention is informational not directional**: StockMojo NIFTY 2026-05-29 EOD showed Net GEX +1.07M Cr; MERDIAN showed -1.07M Cr (inverted sign — speculator-net vs dealer-perspective; magnitudes identical). Operator declined sign flip S41 due to blast radius on downstream consumers. Possible S42+ ENH `local_flip_strike` per-strike GEX sign change for cross-vendor parity (adapter view, not sign flip).
- **Schema-guess three-strike anti-pattern is operational shape of merdian_reference.json drift**: caught twice S41 (script_execution_log columns + dhan_token_probe_log columns). Discipline: before composing SQL against any ops table, run `SELECT column_name FROM information_schema.columns WHERE table_name='<x>'` first. JSON is updated session-close only; intermediate schema changes accumulate. Filed TD-S41-NEW-3 for systemic audit script.

### S41 operational findings

- **Marketview Health dashboard live at `/health` route** — 3 sections SYSTEM STATUS atomic cards (OVERALL/WRITER FLEET/TOKEN STATUS) + WRITER FRESHNESS table of 13 tracked writers + synthesized local_token_refresh row + ERROR RATE LAST 24H stacked hourly bar chart + top-3 errors. 30s polling. Off-hours muting (with known bug: OVERALL CRITICAL doesn't propagate off-hours mute; filed cosmetic).
- **MERDIAN_Marketview_Reference.docx delivered** at `/mnt/user-data/outputs/` — 15,988 bytes / 152 paragraphs / docx skill validates PASS. Layperson-grade exposition for dashboard reading; covers Mental model + Top strip + 7 Key Parameters cards + Positioning Landscape + Max Pain + Pin Risk & ATM + Pin Risk Timeline + Breadth & Volatility + Reading dashboard end-to-end + Quick decision matrix.
- **4 writer-side empty cards from S40 carry-forward CLOSED end-to-end** — India VIX writer + WCB Lovable correction + ExecutionLog instrumentation + Pin Risk Score writer + Pin Risk Timeline auto-populated. Marketview v4 now renders 100% live data.
- **5 production patches deployed** — compute_gamma_metrics_local.py (P0.a writer + FIX-1), build_wcb_snapshot_local.py (ExecutionLog), generate_pine_overlay.py (color + boxcount). 1 DDL (gamma_metrics 3-col add). 2 anon RLS policies (script_execution_log + dhan_token_probe_log).
- **1 same-session NEW+RESOLVED**: TD-S41-NEW-4 (13th same-session NEW+RESOLVED pattern across MERDIAN history; after S25 TD-097, S26 TD-101, S27 TD-NEW-2/3, S28 6x, S29 5x, S40 2x).

*CLAUDE.md v1.43 — 2026-07-09 (Session 66 close). Added: 5 S66 settled-decision bullets (above) — (1) the equity daily-breadth chain is DECOUPLED from `equity_eod` (candle fetch != DMA rebuild; the DMA builder is a separate, currently-frozen writer — TD-S66-NEW-4); (2) a Dhan DH-901 from one writer while other endpoints 200 is token-TIMING of the L18 load-once `DHAN_API_TOKEN` global, not expiry — read-token-at-use (TD-S66-NEW-1); (3) freshness guards measure per-ticker coverage% not table-max (the shipped guard + `build_daily_map` both went blind to a 91%-stale table — TD-S66-NEW-2/3); (4) `v_expiry_base_rates` is live-cohort-only, the seed is degenerate by construction ([M4] hardcoded div=NEUTRAL, all-RANGE, re-label dead end — `4d25733`); (5) diagnose by running the failing call + verify schema before SQL (six wrong theories fell; session halted on a hallucinated `equity_eod.trade_date` column). No new ADR — S66 was closing S65 carries + a guard tune (`5de5c85`, TD-S65-NEW-1 CLOSED) + a base-rate view segregation (`4d25733`) + a pine regen (`4699e41`) + an unresolved breadth-freeze diagnosis; none meets Doc Protocol v4 Rule 10's mandatory-ADR bar; CLAUDE.md updated per Rule 0 for the rebuild-grade lessons. Recorded alongside the S66 eight-file doc-close per Doc Protocol v4 Rule 3 full-file no-crunch: session_log.md (S66 prepended), this CLAUDE.md v1.42→v1.43, tech_debt.md (5 TD-S66-NEW prepended Active + TD-S65-NEW-1 CLOSED), MERDIAN_Enhancement_Register.md (ENH-116 base-rate cohort-segregation Part-4 note + Change Log row), MERDIAN_System_Map.md (guard + view + breadth-chain decoupling note + footer S66), MERDIAN_Deployment_Topology.md (Dhan token-flow reality: pull_token vs refresh_dhan_token dual-writer + load-once-global hazard + footer S66), merdian_reference.json v44→v45 (S66 change_log), CURRENT.md (S66 Last session + S65 demoted to Previous S65 + This session rolled to S67). MERDIAN_Decision_Index.md footer-only (no new ADR S66). Assumption Register NOT triggered (S66 lessons are durable settled bullets per the v1.16/S25 precedent, not new assumption rows). Commits: `5de5c85` (guard tune), `4d25733` (base-rate view SQL), `4699e41` (pine regen); HEAD both sides = `4d257336d91eb044563c4bd6e609719c56e1de86` (off `e2d58a1`, AWS ff-synced). Supabase live: `v_expiry_base_rates` amended to live-cohort-only. OPEN into S67: the breadth/DMA freeze — ~91% of the equity daily universe stranded at 06-04 across two decoupled frozen layers (per-ticker DMA builder unidentified + `equity_eod` 07-06 frontier); the S65 “DMA clean to 07-02 at 100% active” claim is REFUTED and needs re-verification. Full v1.42 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.42 — 2026-07-07 (Session 65 close). Added: 3 S65 settled-decision bullets (above) — (1) Dhan EOD historical publish-lag is expected, not a stall — `compute_date_window()` (T−1, 220-day) is correct, don't patch the window to force a missing latest day (07-03/07-06 absence = provider publish-lag, self-heals on the next EOD run, `EOD_SAFE_LAG_DAYS` exists for this; same discipline as ADR-001 stable-lies); (2) the EOD coverage guard's denominator is the active-universe/latest-EOD-date ticker count (~1,159), not the nominal ~1,385, and staleness is off the last trading day with a ~3-day Dhan-lag tolerance — the 1,385/5-day guesses produced a `/1` false-OK (TD-S65-NEW-1; guard is display-not-gate, ingest is correct); (3) OAuth rotation via a *new* Google client changes BOTH `client_id` and `client_secret`, and the exposure closes only on OLD-client delete, not on adding the new one (ENH-117 security fold). No new ADR — S65 was an OAuth credential rotation + an intraday-drift banner prompt + a breadth-thread diagnosis + a guard-tune spec; none meets Doc Protocol v4 Rule 10's mandatory-ADR bar; CLAUDE.md updated per Rule 0 for the rebuild-grade lessons. Recorded alongside the S65 eight-file doc-close per Doc Protocol v4 Rule 3 full-file no-crunch: session_log.md (S65 prepended), this CLAUDE.md v1.41→v1.42, tech_debt.md (TD-S65-NEW-1 prepended Active + footer), MERDIAN_Enhancement_Register.md (ENH-117 rotation-APPLIED + ENH-116 intraday-drift-banner-prompt-ready Part-1 status + Change Log row + Last-updated header), MERDIAN_System_Map.md (Update-log row + footer S65), MERDIAN_Deployment_Topology.md (Update-log row + footer S65 — OAuth client rotated in `/etc/oauth2-proxy.cfg`, old-client delete pending), merdian_reference.json v43→v44 (S65 change_log), CURRENT.md (S65 Last session + S64 demoted to Previous + S63 tagged Previous S63 + This session reset for S66). MERDIAN_Decision_Index.md footer-only (no new ADR S65). Assumption Register NOT triggered (the S65 lessons are durable settled bullets per the v1.16/S25 precedent, not new assumption rows). New files (staged, commit unconfirmed): 5 `docs/lovable_prompts/*.md` Lovable prompt docs (the S64 “3” count corrected to 5) + `sql/2026-07-03_enh116_console_anon_grants.sql` (was untracked). `check_eod_coverage_freshness.py` guard-tune teed up (TD-S65-NEW-1), not finalized. 0 confirmed git commits (item-2 commit sequence provided, land unconfirmed). Google OAuth client rotated on `i-0878c118835386ec2`: new client_id+client_secret in `/etc/oauth2-proxy.cfg`, `oauth2-proxy` `active (running)` PID 710516 @ 2026-07-04 08:05:42 UTC; login-verify + old-client delete + non-allow-list denial test PENDING. Breadth/`equity_eod` thread resolved (07-03/07-06 = Dhan publish-lag, not a bug; `compute_date_window` T−1/220 proven correct; universe ceiling grounded ~1,159 active). Carry to S66: confirm/land the item-2 commit + AWS ff-sync; deploy the intraday-drift banner; finish the OAuth closeout; finalize `check_eod_coverage_freshness.py` (TD-S65-NEW-1); Lens 4 macro feed; ENH-117 branded sign-in. Full v1.41 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.41 — 2026-07-04 (Session 64 close). Added: 5 S64 settled-decision bullets (above) — (1) ENH-116 Ambient Environment Intelligence BUILT + DEPLOYED end-to-end backend (schemas `market_environment_snapshots` + `expiry_outcomes` + view `v_expiry_base_rates`; four writers compile/relate/backfill/accrue; reconcile v2 folds Lens 3; Phase-B receipt into `regime_conditional_note`; three AWS crons `0 16`/`15 16`/`55 3`; seed all-RANGE, forward accrual varies it); (2) `ast.parse` alone is insufficient — a `str_replace` can eat a `def` header and NameError at runtime; `py_compile` + defs-check are mandatory (S64 live case `1be5239`→`61df758`); (3) Marketview is now public + TLS + Google-auth-gated at `https://marketview.meridianalpha.in` — `auth_request` at server level on the serving 443 block, proven only by a cross-browser no-session test; (4) Marketview v5 six-page terminal restructure (left rail, global symbol toggle, 60s ticker, max-pain ±20, Net Dealer γ intraday, two-clocks framing). No new ADR — S64 was an ENH build/deploy + a frontend restructure + a domain/TLS/auth infra stand-up + one carry-in bug-fix + the ENH-07A rate-floor commit; none meets Doc Protocol v4 Rule 10’s mandatory-ADR bar; CLAUDE.md updated per Rule 0 for the rebuild-grade lessons. Recorded alongside the S64 eight-file doc-close per Doc Protocol v4 Rule 3 full-file no-crunch: session_log.md (S64 prepended), this CLAUDE.md v1.40→v1.41, tech_debt.md (TD-S62-NEW-2 RESOLVED in place + moved to Resolved with the 375-bar/0-null evidence), MERDIAN_Enhancement_Register.md (ENH-116 PROPOSED→BUILT/DEPLOYED Part-1 row + Part-4 status block + ENH-117 NEW Part-1 row + Part-4 block + Scope ENH-01..ENH-117 + Change Log row + Last-updated header; ENH-115 Part-1 row P2 subsumed into Marketview v5), MERDIAN_System_Map.md (§S64), MERDIAN_Deployment_Topology.md (§S64 — three ambient crons + domain/TLS/oauth2-proxy stand-up + new tables/view), merdian_reference.json v42→v43 (S64 change_log), CURRENT.md (S64 Last session + S63 demoted to Previous + S62 tagged Previous S62 + This session reset for S65). MERDIAN_Decision_Index.md footer-only (no new ADR S64). Assumption Register NOT triggered (the S64 lessons are durable settled bullets per the v1.16/S25 precedent, not new assumption rows). New engine files: `compile_market_environment_local.py`, `relate_ambient_to_open_local.py`, `backfill_expiry_outcomes.py`, `accrue_expiry_outcomes.py`, `sql/` ENH-116 Phase-1 DDL + `v_expiry_base_rates` DDL (console anon-grants SQL + 3 Lovable prompt `.md` docs remain uncommitted — carry to S65). New tables: `market_environment_snapshots`, `expiry_outcomes`; view `v_expiry_base_rates`. 12 engine commits (`43176b8` Phase-1 → `21b08d9` ENH-07A rate-floor → `cbbe81e` gitignore → `62cd2de` ICT-zone regen → `0ff8c04` Lens 3 → `7250b11` reconcile v2 → `185c48e` idempotent → `dae1799` labeler → `1da0af6` Phase-B view → `1be5239` receipt → `61df758` header fix → `61ab702` observability); HEAD `61ab702`, AWS ff-synced. Marketview v5 shipped via meridian-connect (Lovable→GitHub→AWS). Three ambient crons + domain/TLS/oauth2-proxy on MERDIAN AWS. ENH-117 filed (Marketview-branded sign-in, PROPOSED P3). TD-S62-NEW-2 RESOLVED. Carry to S65: commit the console anon-grants SQL + 3 Lovable prompt docs; deploy the intraday-drift banner; Lens 4 macro feed; ENH-117; rotate the exposed Google `client_secret` + run the non-allow-list denial test. Full v1.40 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.40 — 2026-07-02 (Session 63 close). Added: 3 S63 settled-decision bullets (above) — (1) ENH-115 P1 FII/DII participant/cash EOD positioning *source* LIVE (participant OI NSE-only, cash consolidated NSE+BSE+MSEI, BSE participant stub dropped; display-not-gate, feeds ENH-116 Lens 3; source only — magnitude stays ADR-002 v2 `v_dealer_flow_sim`); (2) historical backfills must not trust the DB `trading_calendar` gate — local weekday filter + API 404 as holiday ground truth (Rule 18 corollary), list-of-tuples PostgREST params; (3) SENSEX `compute_flip_level` regime-conditional fix (near-spot sign-change walk + short-γ display guard, `dc63bb3`, TD-S62-NEW). No new ADR — S63 was a bug-fix deploy + an ENH-115 P1 build + documentation of three read-only ENH-07 A Phase-2 commits; none meets Doc Protocol v4 Rule 10's mandatory-ADR bar; CLAUDE.md updated per Rule 0 for the rebuild-grade lessons. Recorded alongside the S63 eight-file doc-close per Doc Protocol v4 Rule 3 full-file no-crunch: session_log.md (S63 prepended), this CLAUDE.md v1.39→v1.40, tech_debt.md (TD-S62-NEW RESOLVED in place + Resolution row; TD-S62-NEW-2 carried), MERDIAN_Enhancement_Register.md (ENH-115 P1 BUILT Part-1 row + Part-4 block + Change Log row + Last-updated header; ENH-07 A/B Part-1 reconcile preserved), MERDIAN_System_Map.md (§S63), MERDIAN_Deployment_Topology.md (§S63 ENH-115 cron + tables), merdian_reference.json v41→v42 (S63 change_log + `measure_rate_sensitivity.py` artifact), CURRENT.md (S63 Last session + S62 demoted to Previous + This session reset for S64). MERDIAN_Decision_Index.md footer-only (no new ADR S63). Assumption Register NOT triggered (the S63 lessons are durable settled bullets per the v1.16/S25 precedent + a Rule 18 corollary, not new assumption rows). New files: `parse_participant_oi.py`, `ingest_participant_positioning.py`, `backfill_participant_oi.py`, `sql/2026-07-02_enh115_participant_positioning.sql`. New tables: `participant_oi_daily`, `fii_dii_cash_daily`, `v_participant_oi_latest` (view). 5 git commits (`dc63bb3` flip fix; `48e25c4`→`afa273a`→`31713c7`→`f00eca9` ENH-115 P1). 2 cron lines on MERDIAN AWS. Carry to S64: SENSEX 2026-01-19 concentration resume (TD-S62-NEW-2); ENH-116 build + Lens 3 wiring + ENH-115 P2 Marketview card; commit the uncommitted `measure_rate_sensitivity.py` Local-drift edit. Full v1.39 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.39 — 2026-07-01 (Session 62 close). Added: 6 S62 settled-decision bullets (above) — (1) ENH-07 A CLOSED no-op flat r (empirical + architectural; supersedes the S61 REFRAMED status); (2) expiry-day 0-DTE flat-vol net_gex is numerically unreconstructible intraday → live-sourced not reconstructed (`diag_1125.py`; explicit `SKIPPED_EXPIRY`, do not floor); (3) pre-existing-table discovery discipline + `hist_gamma_metrics` as the canonical historical gamma series (net_gex UNSCALED ×1e7, `bar_ts` IST-as-UTC `:59` shift −5h30m+truncate, concentration = max|gex|/sum|gex| scale-invariant); (4) iv is the master key — lean iv+gamma sidecar schema correct, vendor bars unmutated; (5) correct-and-slower beats fast-and-subtly-wrong — divergent `--fast` path abandoned not loosened (a `--verify` divergence means the approach is wrong, not the threshold); (6) cross-engine StockMojo parity isolates bugs (the single divergent field is the diagnosis). No new ADR — S62 was a historical/research backfill run + an ENH spec (ENH-116 PROPOSED, not accepted) + a no-op ENH ruling + a parity diagnosis; none meets Doc Protocol v4 Rule 10's mandatory-ADR bar; CLAUDE.md updated per Rule 0 for the rebuild-grade lessons. Recorded alongside the S62 eight-file doc-close per Doc Protocol v4 Rule 3 full-file no-crunch: session_log.md (S62 prepended), this CLAUDE.md v1.38→v1.39, tech_debt.md (TD-S62-NEW flip bug + TD-S62-NEW-2 the 2026-01-19 SENSEX gap prepended Active; TD-S58-NEW-1 CLOSED + moved to Resolved with evidence), MERDIAN_Enhancement_Register.md (ENH-07 A → CLOSED no-op + ENH-116 PROPOSED P2 Part-1 row + Part-4 block + scope bump ENH-01..ENH-116 + Change Log row), MERDIAN_System_Map.md (§S62), MERDIAN_Deployment_Topology.md (§S62), merdian_reference.json v40→v41, CURRENT.md (S62 Last session + S61 demoted to Previous + This session reset for S63). MERDIAN_Decision_Index.md footer-only (no new ADR S62). Assumption Register NOT triggered (the S62 findings are durable settled bullets per the v1.16/S25 precedent, not new assumption rows). New files: `backfill_hist_greeks.py`, `fill_gamma_concentration.py`, `run_fullwindow.py`, `docs/decisions/ENH-116-ambient-environment-intelligence.md` + SQL DDLs (`hist_option_greeks_1m`, `chk_backfill_status_valid` widen). New/filled tables: `hist_option_greeks_1m` (NEW sidecar), `hist_gamma_metrics` (pre-existing; `gamma_concentration` filled full-window both symbols bar 2026-01-19 SENSEX). 0 git commits (backfill scripts + ENH-116 spec staged — carry to S63, along with the three undocumented ENH-07 A Phase-2 commits on `main` from S61). Full v1.38 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.38 — 2026-06-27 (Session 61 close). Added: 3 S61 settled-decision bullets (below) — ENH-07 A no-live-solver reframe (the live gamma engine reads vendor Dhan Greeks; the risk-free rate is a parameter inside the TD-S58-NEW-1 historical Greeks backfill, not a live-writer change; `core/bs_engine.py` built+validated as that substrate); `hist_future_bars_1m` + `hist_spot_bars_1m` both store IST-clock-as-UTC → ZERO pairing shift (TD-S61-NEW-2; the assumed −5h30m shift dropped 86% of pairs, 14% yield → ~99%; refines the TD-087 note); ADR-018 D2 recency floor re-audited across signal readers (two more floorless readers fixed — `_fetch_options_flow` whose ENH-02/04 ±3/4/5 modifiers had run off a ~24-day-stale row since S49, and the new `_fetch_basis_context`; both 15-min floors; basis is display-only context-not-gate). No new ADR — S61 was a carry-forward sweep (bug-fix + ENH-02 wiring + a Measure-stage context writer + a research backfill); none meets Doc Protocol v4 Rule 10's mandatory-ADR bar; CLAUDE.md updated per Rule 0 for the rebuild-grade lessons. Recorded alongside the S61 eight-file doc-close per Doc Protocol v4 Rule 3 full-file no-crunch: session_log.md (S61 prepended), this CLAUDE.md v1.37→v1.38, tech_debt.md (TD-S61-NEW-1/2/3 + TD-S59-NEW-2 CLOSED), MERDIAN_Enhancement_Register.md (ENH-02 WIRED + ENH-07 B SHIPPED-Measure + ENH-07 A reframed), MERDIAN_System_Map.md (§S61), MERDIAN_Deployment_Topology.md (§S61), merdian_reference.json v39→v40 (+ vestigial _meta resync, TD-S61-NEW-3), CURRENT.md (S61 Last session + S60 demoted). MERDIAN_Decision_Index.md + Assumption Register NOT triggered (no ADR, no new assumption row). New files: `compute_basis_context_local.py`, `backfill_basis_context.py`, `core/bs_engine.py`, + 2 SQL migrations. New tables: `basis_context_snapshots` (LIVE), `hist_basis_context`. 4 code commits (`3533d22`, `8ddbc78`, `d16986c`, `141386d`) + backfill commit; all production patches canon-v3 with `_PRE_S61` backups. Full v1.37 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.37 — 2026-06-26 (Session 60 close). Added: Non-negotiable **Rule 18** (`trading_calendar` is a trust-anchor — validate against the official NSE source before trusting any holiday gate; canonical gate is `core/trading_calendar_gate.py`, import not inline) + 2 S60 settled-decision bullets (trading_calendar was wrong ~3 months and silently defeated every gate, fixed at source; the canonical self-sufficient fail-open gate helper). No new ADR — S60 was bug-fixes (marker-header phantom +4.34%; trading_calendar 2-of-15 holidays misdated) + one compute writer (ENH-SDM P2) + a holiday-noise data repair + a gate-helper consolidation; none meets Doc Protocol v4 Rule 10's mandatory-ADR bar. CLAUDE.md still updated per Rule 0 because the calendar-trust-anchor lesson is non-negotiable-grade independent of the ADR path. Recorded alongside the S60 eight-file doc-close per Doc Protocol v4 Rule 3 full-file no-crunch: session_log.md (S60 prepended), this CLAUDE.md v1.36→v1.37, tech_debt.md (TD-S60-NEW-1..5 + TD-S59-NEW-2 carry), MERDIAN_Enhancement_Register.md (ENH-SDM P2 BUILT), MERDIAN_Deployment_Topology.md (marker cron + orchestrator holiday gate + core gate helper), MERDIAN_System_Map.md (§S60), merdian_reference.json v38→v39, CURRENT.md (S60 Last session + S59 demoted). MERDIAN_Decision_Index.md + Governance Framework + Assumption Register NOT triggered (no ADR, no governance change, no new assumption row — the lesson is a re-run of the existing C-09/ADR-001 family). New file: `docs/decisions/ENH-SDM_structural_divergence_monitor_spec.md` (as-built spec, ratifies classifier enums, closes the S57 spec-debt). 8 commits S60 (4f676e1 marker postmarket-ts guard; 5066d81 marker freshness guard; c9c2ab3 marker open+prevclose v2; 4bd3bf5 SDM writer; 8cec587 SDM orchestrator wiring; bafddc2 trading_calendar 15-holiday fix; af74d0c orchestrator holiday gate; 2d1375c→3b3b8ee gate helper v1→v2; 38a82ff orchestrator gate cutover). All production patches canon-v3 with `_PRE_S60` backups. Full v1.36 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.36 — 2026-06-24 (Session 59 close). Added: Three S59 operational findings — (1) freshness must be measured on the upsert-touched column, never on row-birth `created_at`: `equity_intraday_last` carries both `ts` (upsert-updated by `refresh_equity_intraday_last.py`, the real freshness column) and `created_at` (`DEFAULT now()`, static after insert); a freshness check anchored on `created_at` false-alarms forever (proven S59 — `ts` newest = today vs `created_at` newest = 2026-05-20); (2) a stale/wrong reference price silently invalidates correct data and presents as plausible output not an error — re-run of C-09/ADR-001: breadth read BULLISH 882/428 on a 0.37-A/D down day because the prev-close baseline was frozen ~5 weeks behind a missing AWS cron; suspect the reference baselines freshness before the compute logic when a derived read contradicts an independent source; (3) the decisive tell that a present-vs-absent diff is a write-time filter (not bad data) is when `--dry-run` writes a row the DB lacks — confirm the read path against the live row before declaring upstream data corrupt. No new ADR (two bug fixes — Doc Protocol v4 Rule 10 excludes bug fixes from ADR triggers; operational findings codified as durable settled bullets per the v1.16/S25 precedent + §D.25 of the Assumption Register). Settled decisions added (S59 bullets below): equity_intraday_last freshness-column rule + breadth prev-close cron on MERDIAN AWS + daily-vs-weekly ICT PDH/PDL write rule. Recorded alongside the S59 eight-file doc-close per Doc Protocol v4 Rule 3 + Rule 9: tech_debt.md (TD-S59-NEW-1/2/3 prepended Active), session_log.md (S59 prepended), CURRENT.md (S59 Last session + S58 demoted), merdian_reference.json v37→v38 (S59 change_log), MERDIAN_System_Map.md (§S59), MERDIAN_Deployment_Topology.md (§S59 AWS cron), MERDIAN_Assumption_Register.md (§D.25 + footer S48→S59), this CLAUDE.md v1.35→v1.36. MERDIAN_Decision_Index.md footer-only (no new ADR S59). Enhancement Register NOT triggered (the top-5 read changed no ENH status). 2 commits (`6b58587` eod_health_check reference-freshness guard, `2b40a4b` daily PDH/PDL unconditional); 2 production patches (both canon-v3, `_PRE_S59` backups). Full v1.35 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.35 — 2026-06-19 (Session 57 close). Added: ADR-018 ACCEPTED settled-decision bullet (breadth-feed supervision model + signal-subsystem disposition — D1 systemd-on-MALPHA + single-instance + WCB cron fix; D2 mandatory recency-floor guard on all breadth/divergence readers; D3 SMDM evidence-based retirement vs ENH-30 Exp 9 NEUTRAL; D4 ENH-SDM structural-divergence monitor AWS orchestrator-integrated per ADR-006). No CLAUDE.md operational-canon rule changes beyond the ADR-018 bullet (S57 was data-audit + diagnosis + remediation + architecture-decision + spec; the deployment-topology change is captured in ADR-018 + Deployment Topology, the durable settled answer in the bullet above). Recorded alongside the S57 eight-file doc-close (session_log [S57 + reconstructed S56] + CURRENT + tech_debt + Decision Index [ADR-018 row] + merdian_reference v35→v36 + System Map + Deployment Topology) + 2 new doc files (ADR-018 + ENH-SDM spec). Note: CLAUDE.md was untouched S54→S56 (bug-fix / infra / ops sessions with no rule changes), so this follows v1.33/S53-54 directly. Full v1.33 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.33 — 2026-06-16 (Session 53–54 close). Added: Four operational-canon bullets from the S53 blackout recovery + S54 audit — (1) cron `SHELL=/bin/bash` mandatory as crontab line 1 on AWS (dash lacks `source`; a dropped directive caused a ~28h blackout); (2) monitors must not share the `source .env` failure chain of what they watch (all four S52 monitors died with the capture layer); (3) the live ingest cron form is unquoted `bash run_ingest.sh NIFTY FULL` (run_ingest self-sources .env; do not re-quote per S49); (4) upserts can mask per-symbol write loss (the S54 volatility insert→upsert silenced SENSEX's lost writes into merges — TD-S54-NEW-1). No new ADR (bug-fix + audit session per Doc Protocol v4 Rule 10; operational findings codified as durable settled bullets per the v1.16/S25 precedent that CLAUDE.md bullets are for any durable answer, not only ADRs). Recorded alongside the S53/S54 six-file doc-close (session_log + CURRENT + tech_debt + Deployment Topology + System Map + merdian_reference v34). Note: CLAUDE.md was untouched S42→S52 (no ADR / non-negotiable-rule changes in those bug-fix/infra sessions), so this follows v1.32/S41 directly. Full v1.32 → v1.1 history preserved verbatim below.*

*CLAUDE.md v1.32 — Session 41, 2026-06-01. Supersedes v1.30. Full v1.30 → v1.1 history preserved verbatim below.*

---

*CLAUDE.md v1.30 — 2026-05-29 (Session 40 close). Added: Seven S40 operational findings (Lovable auto-scaffold's column DEFAULT clauses for temporal-immutable tables must be audited at table-create time — `merdian_parameters.valid_to` shipped with `DEFAULT now()` from Lovable's scaffold which silently breaks `valid_to > valid_from` CHECK under PostgreSQL transaction-time semantics (all `now()` calls within one transaction return identical timestamps), `update_parameter()` RPC failed `chk_valid_from_to` violation on every invocation until `ALTER TABLE ... ALTER COLUMN valid_to DROP DEFAULT` was applied — canonical post-Lovable-scaffold audit pattern is `SELECT column_name, column_default FROM information_schema.columns WHERE table_name='X' AND column_default LIKE '%now()%'` for any temporal-immutable column; atomic single-metric cards beat collapsed Hero rollups for operator-scan decision surfaces — each card answers exactly ONE question, paragraph-Hero cards violate ADR-017 P1 three-filter rule because the paragraph form prevents per-metric isolation during the 3-second scan that ADR-017 was designed to enable, when in doubt split; stacked-by-strike axis alignment is mandatory for cross-reading two charts over the same domain — Positioning + Max Pain stacked full-width with shared x-axis made the 100pt convergent pin pressure signal (max-γ 24,000 vs max-pain 23,900) obvious as vertical alignment; side-by-side would have broken cross-reference eye-tracking; schema verification via live curl with service-role key before Lovable column-name assumption is canonical workflow — Lovable's column-name guesses were directionally reasonable but didn't match actual `gamma_metrics` schema (net_dealer_gamma_cr vs net_gex, atm_straddle_premium vs straddle_atm, pin_probability vs 100-expansion_probability complement, regime LONG_GAMMA vs display POSITIVE_γ); any Lovable wiring prompt for a Supabase-backed dashboard must include the live column inventory as Appendix to prevent guess-and-iterate cycles; AWS clone + on-AWS build pipeline beats scp-of-built-html for ergonomic deploys — pre-S40 deploy was operator scp'ing `dist/` from Lovable preview to AWS `/var/www/marketview/`; S40 codified the 3-line `git pull && npm run build && sudo rsync && sudo systemctl reload nginx` pipeline at `/home/ssm-user/meridian-connect/` (use `npm install` not `npm ci` because Lovable doesn't commit `package-lock.json`); source-of-truth lives in GitHub and AWS builds from source rather than receiving pre-built artifacts; long-format → wide-format pivot via `MAX(CASE WHEN option_type='CE' THEN oi END)` aggregation pattern is canonical for max-pain valley computation against `option_chain_snapshots` (one row per strike+option_type with single `oi` column) — first attempt assumed wide-format with separate `ce_oi` + `pe_oi` columns and failed; verification NIFTY max_pain=23,900 / SENSEX max_pain=76,000 both aligning with spot near-expiry pin pattern; end-to-end round-trip smoke-fires surface schema-defect bugs that table-creation tests miss — TD-S40-NEW-2 (`valid_to DEFAULT now()` CHECK violation) only surfaced when the calibration round-trip attempted an actual `update_parameter` call inside a transaction; any schema-add-constraints + RPC pair must include a same-session round-trip smoke-fire to surface CHECK-violation interactions; writer-side empty-card detection on Marketview is a feature not a bug — 4 cards correctly degraded to empty states (VIX, WCB, Pin Risk Score, Pin Risk Timeline) when their backing columns/tables were absent or NULL; dashboard does not silently fabricate values; operator immediately distinguishes frontend wiring issues from writer-side gaps by which cards are empty). Settled decisions added: TD-S37-01 CLOSED 2026-05-29 via `patch_s40_enh83_view_tau_rewrite.py` v3-pattern (BOM-safe `read_bytes() + decode('utf-8-sig')` + predominant-EOL preservation + `ast.parse` validation before file write + `_PRE_S40.sql` backups + idempotency guards + cp1252 ASCII-only fix) — two surgical replacements of hardcoded `0.3::numeric AS tau_pin` + `0.3::numeric AS tau_accel` with `get_parameter_num('pin.tau.'||p.symbol)::numeric AS tau_used` + `get_parameter_num('accel.tau.'||p.symbol)::numeric AS tau_used` respectively in `sql/2026-05-25_enh81_v_gex_strike_pin_zone.sql` + `sql/2026-05-25_enh81_v_gex_strike_accel_zone.sql`; both view DDLs re-applied via Supabase SQL editor; smoke-fire `SELECT DISTINCT tau_used FROM v_gex_strike_pin_zone WHERE symbol IN ('NIFTY','SENSEX')` returned 0.30 cross-symbol; calibration round-trip 0.30→0.25→0.30 verified via now-functional `update_parameter` RPC after TD-S40-NEW-2 same-session fix; TD-S40-NEW-2 DISCOVERED-CLOSED-SAME-SESSION 2026-05-29 — `update_parameter()` SECURITY DEFINER RPC violated `chk_valid_from_to` CHECK constraint because Lovable auto-scaffold gave `merdian_parameters.valid_to` `DEFAULT now()`; under PostgreSQL transaction-time semantics `now()` returns identical timestamps across all calls within one transaction; fix `ALTER TABLE public.merdian_parameters ALTER COLUMN valid_to DROP DEFAULT;` zero-downtime; verified 0.30→0.25→0.30 round-trip clean temporal chain; codified §D.22.1 Lovable temporal-immutable column DEFAULT audit pattern REFUTED; Marketview v4 atomic-card layout SHIPPED 2026-05-29 — 4-iteration mockup arc (v1→v4) replacing single Hero rollup with atomic single-metric cards + Hero content moved to Narrative modal triggered by header button; 7-tile compact KEY PARAMETERS strip + Positioning Landscape full-width + Max Pain stacked full-width (NOT side-by-side per D.22.3) + Pin Risk Score/Probability/ATM Straddle row + Pin Risk Timeline + WCB/Market Breadth/India VIX/IV Skew 4-col + ICT Zones + Signals stream; Lovable build prompt 515 lines + correction prompt 6 column-name fixes applied; codified D.22.2 atomic-card-vs-Hero-rollup + D.22.3 stacked-by-strike axis alignment VALIDATED; AWS deploy pipeline established 2026-05-29 — fresh clone at `/home/ssm-user/meridian-connect` (since prior workflow only scp'd `dist/` with no source on AWS); canonical 3-line deploy `cd ~/meridian-connect && git pull && npm run build && sudo rsync -av --delete dist/ /var/www/marketview/` + `sudo systemctl reload nginx`; use `npm install` not `npm ci` (Lovable doesn't commit `package-lock.json`); build vite 7.3.3 + bundle `index-vDqPX1iO.js` 537KB; HTTP 200 confirmed at `http://13.63.27.85/marketview`; `v_max_pain_by_strike` view created 2026-05-29 — long-format-aware DDL with `MAX(CASE WHEN option_type='CE' THEN oi END)` pivot to wide before max-pain valley `MIN(call_pain_above + put_pain_below)` over candidate strikes; granted SELECT to anon (consumed by Marketview Max Pain card); verification NIFTY max_pain=23,900 / SENSEX max_pain=76,000; §D.22 added to Assumption Register with 3 rows (D.22.1 Lovable temporal-immutable column DEFAULT audit REFUTED — canonical post-scaffold audit pattern `SELECT column_name, column_default FROM information_schema.columns WHERE column_default LIKE '%now()%'` for any temporal-immutable column + DROP DEFAULT if present; D.22.2 atomic-card layout VALIDATED — single-metric cards each answering ONE question strictly dominate Hero rollup for fast operator scan + Narrative modal repository for interpretive synthesis; D.22.3 stacked-by-strike charts VALIDATED — vertical strike-axis alignment is mandatory when two charts measure different metrics over the same strike domain). New TDs filed S40 (pending S41+): TD-S40-NEW-1 S4 RESOLVED-SAME-SESSION (`patch_s40_enh83_view_tau_rewrite.py` v1 contained cp1252-incompatible Unicode minus-sign in comment which failed on Windows cp1252 console; v2 ASCII-clean shipped same-session; codifies Windows patch-script ASCII-only authoring discipline); TD-S40-NEW-2 S3 DISCOVERED-CLOSED-SAME-SESSION (`update_parameter` RPC `chk_valid_from_to` violation from Lovable auto-scaffold `valid_to DEFAULT now()`; `ALTER COLUMN valid_to DROP DEFAULT` zero-downtime fix); TD-S40-NEW-3 S2 DEFERRED (TradingView Pine overlay extension for PIN + ACCEL zones — `generate_pine_overlay.py` 505 lines currently ICT-only; ~30-40min extension to read `v_gex_strike_pin_zone` + `v_gex_strike_accel_zone` and render as Pine box overlays; operator viewed first 80 lines but didn't proceed pending full file review; carry-forward S41+). 1 TD CLOSED S40 (TD-S37-01 closure). 0 new ADRs S40 (execution against existing ADR-016 calibration pattern via Settings end-to-end + ADR-017 console design principles via atomic-card redesign). Zero production Python code patches S40 (SQL DDL + frontend Lovable iterations + AWS deploy infra + 9-canonical-file doc-close pack; no Python production code edits on `build_trade_signal_local.py` / `compute_gamma_metrics_local.py` / detector files). Zero git commits, zero tags (`session-40-close` pending operator). 5 new artifacts staged in `/mnt/user-data/outputs/` for operator reference: `patch_s40_enh83_view_tau_rewrite.py` (ASCII-clean post cp1252 fix, APPLIED), `marketview_v4_mockup.html` + `marketview_v4_render.png` (final UI mockup, APPLIED via Lovable), `lovable_build_prompt_marketview_v4.md` (515 lines, APPLIED), `lovable_correction_prompt.md` (APPLIED — dashboard now populated post-correction). 4 writer-side gaps confirmed empty by design on Marketview (not frontend bugs, carry-forward P1 writer-side TDs to S41+): India VIX (`gamma_metrics.vix` NULL, needs writer-side India VIX scrape ~30-60min); WCB (`market_breadth_intraday.wcb` column doesn't exist, needs writer + schema ADD COLUMN ~60-90min if formula settled); Pin Risk Score (`gamma_metrics.pin_risk_score` doesn't exist, needs writer-side formula combining max-γ concentration × spot-proximity × sustained-time × inverse-expansion-prob ~60-120min); Pin Risk Timeline (auto-populates once pin_risk_score is shipped). Nine canonical files modified at session close per Doc Protocol v4 Rule 3 + Rule 9 + Rule 11 — full-file no-crunch discipline maintained throughout: this `CLAUDE.md` v1.29 → v1.30 update preserving full v1.29 → v1.1 history verbatim + 7 S40 settled-decisions bullets added; `CURRENT.md` S40 Last session block prepended + S39 demoted to Previous via header rename only content verbatim per Rule 7 + This session block reset for S41; `session_log.md` S40 paragraph prepended at line 1 per newest-first convention; `tech_debt.md` 3 new TD-S40-NEW-* prepended above latest TDs in Active section + TD-S37-01 closure block prepended in Resolved section + footer S39 → S40; `MERDIAN_Decision_Index.md` footer-only update — Session 39 → Session 40 maintenance note (no new ADRs S40); `MERDIAN_Assumption_Register.md` §D.22 added between D.21 and footer with 3 rows D.22.1-D.22.3 + footer S39 → S40; `MERDIAN_System_Map.md` Update log row added for S40 (v_max_pain_by_strike view + merdian_parameters valid_to DROP DEFAULT + AWS clone path + Marketview v4 deploy + TD-S37-01 closure + TD-S40-NEW-1/2 RESOLVED + TD-S40-NEW-3 deferred Pine extension) + footer S36 → S40; `MERDIAN_Deployment_Topology.md` §8.2 expanded with Marketview frontend sub-block cataloging `/home/ssm-user/meridian-connect/` + `/home/ssm-user/meridian-connect/dist/` + `/var/www/marketview/` + canonical 3-line deploy command codified + Update log row added + footer S36 → S40; `merdian_reference.json` v30 → v31 with S40 change_log + session_log entries + new file inventory `patch_s40_enh83_view_tau_rewrite.py` + `_PRE_S40.sql` backups for both view DDLs + `sql/v_max_pain_by_strike.sql` + `marketview_v4_mockup.html` + `marketview_v4_render.png` + `lovable_build_prompt_marketview_v4.md` + `lovable_correction_prompt.md` + AWS infrastructure entry updated with `/home/ssm-user/meridian-connect/` clone path. Zero new doc files this session. Project knowledge upload pending S41 = 12-session backlog of all modified files.* *CLAUDE.md v1.29 — 2026-05-27 (Session 39 close). Added: Five S39 operational findings (Lovable auto-grant default is NOT SELECT-only — auto-scaffold grants anon FULL privileges DELETE/INSERT/UPDATE/TRUNCATE/REFERENCES/TRIGGER/SELECT on every consumed table+view, anon-grants audit post-RLS-triplet revealed 91 rows = 7 privilege types × 13 surfaces, remediated same-session via PL/pgSQL DO block `REVOKE ALL + GRANT SELECT` over 13 surfaces, codified as security-first RLS quadruplet `REVOKE ALL + ENABLE RLS + CREATE POLICY FOR SELECT + GRANT SELECT` not just triplet; anon-JWT-in-public-repo trust model is sound when RLS restricts anon to SELECT-only since the JWT ships in browser bundle anyway via `import.meta.env.VITE_SUPABASE_URL/ANON_KEY` and is publicly visible via DevTools on any deployed dashboard regardless of repo visibility — adding key to public GitHub provides no additional exposure beyond dashboard URL access, but practice remains sloppy hygiene-wise and `.env` should be `.gitignore`d locally even when anon-only; IMDSv2 attached-SG check is mandatory before AWS SG edits — operator wasted hours editing orphan `launch-wizard-2` while attached SG was `launch-wizard-1`, IMDSv2 token query `curl -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/security-groups` returns ground truth, AWS Console naming and operator memory are unreliable; Lovable framework default may NOT match nginx static-host target — Turn 1 generated `tanstack_start_ts_2026-05-12` SSR + Cloudflare Workers requiring full SPA re-conversion (commit `72dc296` vite_react_shadcn_ts), explicit framework specification in initial prompt prevents the wrong-framework discard cycle; typed-column variant strictly dominates single-jsonb at MERDIAN parameter scale — Lovable auto-scaffolded `merdian_parameters` with 4 typed cols + `value_type` discriminator + 4-way XOR constraint instead of operator's planned single `value jsonb`, retained because zero cast overhead in hot-path read RPCs + DB-enforced type safety + self-documenting column names + identical ADR-016 contract enforcement). Settled decisions added: ENH-110 Phase 1 SHIPPED end-to-end via Lovable → GitHub → AWS pipeline at `http://13.63.27.85/marketview` (6 iteration turns + backend `merdian_parameters` constraints/RPCs/view live + RLS triplets on 8 tables + 5 views + AWS nginx hosting via setup_marketview_aws_host.sh on EC2 i-0878c118835386ec2 EIP 13.63.27.85 port 80 internal operator dashboard not public production; ENH-110 Phase 2 + Phase 3 remain PROPOSED separate session scope); ENH-83 SHIPPED calibration console + temporal-immutable `merdian_parameters` table (Lovable auto-scaffolded the table mid-session with typed-column schema before operator's planned single-jsonb DDL ran; S39 trimmed ALTER added 4 CHECK constraints chk_change_reason_nonempty + chk_value_type_valid + chk_value_present XOR + chk_valid_from_to + unique partial index uniq_merdian_parameters_active_key + 3 STABLE SQL read RPCs get_parameter_num/text/bool returning typed column directly INVOKER + 1 SECURITY DEFINER write RPC update_parameter enforcing full ADR-016 contract end-to-end with mandatory non-empty change_reason validated + FOR UPDATE row lock + numeric range-check + atomic CLOSE-old + INSERT-new in same transaction + GRANT EXECUTE TO anon so Settings tab writes via JWT-scoped function + v_merdian_parameter_audit view; 11 active param rows seeded by Lovable pin/accel τ NIFTY+SENSEX + sl_buffer + retest_tolerance + capital + ict_zone TTL); Lovable auto-grant safety REFUTED — RLS triplet pattern extended to security-first quadruplet `REVOKE ALL FROM anon + ENABLE RLS + CREATE POLICY FOR SELECT + GRANT SELECT TO anon` is the canonical sequence; anon-key-in-public-repo trust model VALIDATED — anon JWT inlines into Vite bundle at build time via `import.meta.env`, publicly visible via DevTools on any deployed dashboard regardless of repo visibility, so public GitHub adds no additional exposure beyond dashboard URL access provided RLS restricts anon to SELECT-only; IMDSv2 attached-SG check mandatory REFUTED operator-console-name memory — TOKEN=$(curl -s -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600") && curl -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/security-groups returns ground truth, AWS Console naming and operator memory unreliable. §D.21 added to Assumption Register with 4 rows (D.21.1 Lovable auto-grant safety REFUTED + D.21.2 anon-key-in-public-repo trust model VALIDATED + D.21.3 IMDSv2 attached-SG check mandatory REFUTED + D.21.4 Lovable framework default SPA-conversion REFUTED). New TDs filed S39 (pending S40+): TD-S39-NEW-1 S2 REMEDIATED (Lovable auto-grant exposure — proper-fix patch script `audit_anon_grants.sql` queued for repo, runs as Supabase SQL editor block pre-deploy on any future Lovable iteration); TD-S39-NEW-2 S3 (Marketview ICT zone label collision cosmetic — TIER2 BULL_FVG / TIER2 BEAR_FVG labels stack vertically in dense band near spot, label stagger 16px Turn 5 partially resolved at moderate cluster density); TD-S39-NEW-3 S3 (`.env` in public GitHub repo hygiene — pending operator local `git rm --cached .env` + push; Lovable cannot push removal commits); TD-S39-NEW-4 S4 (launch-wizard-2 orphan SG cleanup — verify zero attached resources via IMDSv2 audit then delete); TD-S39-NEW-5 S3 (Marketview `stale 612s` badge live-day verification — current 300s threshold needs live-day confirmation that 600s+ values reflect genuine staleness rather than display bug; post-15:30 IST close on S39 demo data shows 612s = 10-min stamp from last cycle which is legitimate post-market staleness). 0 TDs CLOSED S39 (TD-S37-01 closure via `patch_s39_enh83_view_tau_rewrite.py` PENDING operator run against `sql/2026-05-25_enh81_v_gex_strike_*.sql` swapping hardcoded τ=0.3 for `get_parameter_num('pin.tau.{symbol}')` lookup — patch script staged at `/home/claude/patch_s39_enh83_view_tau_rewrite.py`). 0 new ADRs S39 (execution against ADR-016 parameter calibration pattern via `merdian_parameters` ALTER + ADR-017 Operator Console Design Principles via Marketview ship — Decision Index footer maintenance note only). 0 production code patches S39 (all changes were backend DDL via Supabase SQL editor + frontend Lovable iterations + AWS hosting infra; no local Python writer patches). MERDIAN_Decision_Index.md UNCHANGED in structure (footer maintenance note Session 38 → Session 39 only). Eight canonical files modified at session close per Doc Protocol v4 Rule 3 + Rule 9 + Rule 10 + Rule 11 — full-file no-crunch discipline maintained throughout: this `CLAUDE.md` v1.28 → v1.29 update preserving full v1.28 → v1.1 history verbatim + 5 S39 settled-decisions bullets added before "If any of these need to change" line; `CURRENT.md` S39 Last session block prepended + S38 demoted to Previous via header rename only content verbatim; `session_log.md` S39 paragraph prepended at line 1 per newest-first convention; `tech_debt.md` 5 new TD-S39-NEW-* prepended above TD-S38-NEW-1 in Active section + footer S38 → S39; `MERDIAN_Assumption_Register.md` §D.21 added between D.20 and footer with 4 rows + footer S38 → S39; `merdian_reference.json` v29 → v30 with S39 change_log + session_log entries + ENH-110 Phase 1 SHIPPED + ENH-83 SHIPPED inventory + AWS Marketview hosting infrastructure entry; `MERDIAN_Decision_Index.md` footer-only update — Session 38 → Session 39 maintenance note (no new ADRs S39); `MERDIAN_Enhancement_Register.md` ENH-110 Phase 1 + ENH-83 SHIPPED status updates via Part 4 detail blocks added + Scope row bumped to ENH-01..ENH-112 + Part 5 Change Log row + Last updated S36 → S39 prepended. Zero new doc files. Zero AWS deployments via SSH (all AWS ops via Systems Manager Session Manager + IMDSv2 metadata API). Lovable iteration count S39 = 6 turns (paid tier upgraded mid-session). GitHub repo `balannavin-cyber1/meridian-connect` public with latest commit `2998819 Wired Marketview & Settings`. Project knowledge upload pending S40 = 11-session backlog of all modified files.* *CLAUDE.md v1.28 — 2026-05-26 (Session 38 close). Added: Seven S38 operational findings (live integration check validates PIN-edge framing across full trading day not just 60-min window — NIFTY morning prediction held + extended afternoon, max γ inside W/D BEAR_FVG cluster gated entire day's distribution; SENSEX confirmed PIN-inactive + ICT-solo carries day; consolidation debt surfaces from operator switching between 3 dashboards + external tooling — codified as ADR-017 three-filter rule + role contract / Marketview / Health / Settings; confluence-as-edge is bidirectional context not unidirectional amplification — Exp 53 holdout finding was avoidance signal not confirmation signal, M5 BEAR_FVG p1 within 0.25% of flip is destroyer-class PnL sink not amplifier; WR-only reporting masks PnL signal recurrently — S30 first instance + S38 second instance of same framing failure, re-instances ADR-009 §S30 rule that median P&L alongside WR delta required for cohort verdict; LATERAL join correctness between ict_primitives and gamma_metrics has recurrent edge cases — formation-anchor NULL on zone primitives + classifier bin collision with regime-conditional NULL flip_level surfaced twice across Exp 53 arc, canonical helper view at next research-tier sprint; TDs declared RESOLVED with documented residuals should be re-audited at sprint cadence — S29 TD-061 declared RESOLVED with 5 residuals, 3 of 5 were actually trivially-actionable when S38 walked them <30 min; H FVG retest signal has decisive initial 30-min directional move with sharp EOD decay — Exp 53c holdout p0_no_data cohort showed 96.7%→50.0% BEAR + 100.0%→20.8% BULL divergence, exit-window optimization may be more actionable than proximity gating). Settled decisions added: ADR-017 ACCEPTED 2026-05-26 (Operator Console Design Principles — 6 principles + keyboard corollary: three-filter rule cut implicit/redundant/systemic, role contract Marketview/Health/Settings, asymmetric surfacing silence=healthy, confluence-as-bidirectional-context can confirm OR invalidate, motion replaces timestamps, distance×time salience for PIN/ACCEL, keyboard-first ergonomics); ENH-110 PROPOSED Consolidated Marketview Build (Phase 1 Marketview + Settings consolidation + Phase 2 confluence detection primitive + IV skew + journal + Phase 3 salience signals + intelligence; Lovable prompts embedded as Appendix A+B verbatim for paste-and-build); ENH-83 graduated from PROPOSED build-deferred → required by ENH-110 Phase 1 Settings tab Calibration; ENH-111 PROPOSED M5 BEAR_FVG flip_proximity_pct<0.25% anti-edge gate (holdout-validated; train NIFTY+SENSEX combined p1 WR ~35%, holdout p1 mean PnL -38 NIFTY / -37 SENSEX; deployable as MERDIAN_ENH111_ENABLED env flag default OFF pending live N≥30 per D.13.1 cohort-translation discipline); ENH-112 PROPOSED H FVG retest 30m-vs-EOD exit-window decay study (surprise finding from Exp 53c p0_no_data cohort showing 96.7%→50% BEAR + 100%→20.8% BULL WR divergence; ~30 min SQL extension on existing outcomes data); P5 1H zone regression CLOSED-NO-BUG (S28 carry-forward observation reframed as pre-S26-patch leftover artifacts; live rule per ADR-005 functioning correctly; 11 leftover lifetime_zero OB/FVG zones operationally inert); TD-061 FINAL LONG-TAIL CLOSED 2026-05-26 (S29 declared RESOLVED with 5 residuals; S38 actioned 3 of 5 via 3 PowerShell commands eliminating 0 live window-flash sources; 16/20 pythonw + 19/20 hardened final state; documented residuals: Watchdog PowerShell-by-design + Intraday_Supervisor_Start multi-trigger XML quirk filed as TD-S38-NEW-1 + Market_Tape_1M DISABLED + WS_Feed_0900 DISABLED S28); §D.20 added to Assumption Register with 4 rows D.20.1-D.20.4 (D.20.1 confluence-as-bidirectional-context VALIDATED via Exp 53 PnL framing; D.20.2 LATERAL join canonical helper view needed; D.20.3 H FVG retest exit-window decay hypothesis OPEN pending ENH-112; D.20.4 PnL-framing rule re-instanced per ADR-009 §S30). New TDs filed S38 (pending S39+): TD-S38-NEW-1 S4 (MERDIAN_Intraday_Supervisor_Start multi-trigger XML settings quirk — workaround via full Register-ScheduledTask XML + Force overwrite instead of Set-ScheduledTask; cosmetic — last 5% of TD-061 hardening, no operational impact); TD-S38-NEW-2 S4 (dead TIMEOUT_LIVE_DEFAULT + TIMEOUT_SHADOW_DEFAULT constants in run_option_snapshot_intraday_runner.py declared but unused; remove or wire to per-step timeouts in next writer refactor ~5 min); TD-S38-NEW-3 S3 (ict_primitives × gamma_metrics LATERAL joins need canonical helper view — recurrent correctness bugs across Exp 53/53b/53c arc; ~1-2 hour design + ship at next research-tier sprint). Zero production code patches deployed: 3 PowerShell Task Scheduler edits + 8 doc updates + 2 new doc files only. Zero AWS deployments (all Local). Confluence experiment arc verdict: 1 holdout-validated shippable gate (ENH-111) + 1 inconclusive H-FVG proximity resonance (defer Wave 2) + 1 surprise H FVG retest exit-window decay (ENH-112 verification queued). Eight canonical files modified at session close per Doc Protocol v4 Rule 3 + Rule 9 + Rule 10 + Rule 11 — full-file no-crunch discipline maintained throughout: MERDIAN_Decision_Index.md ADR-017 row prepended above ADR-015 (Rule 11.1 newest-first); CLAUDE.md v1.27 → v1.28 this footer prepended preserving v1.27 → v1.1 history verbatim + 7 S38 settled-decisions bullets added before "If any of these need to change" line; CURRENT.md S38 Last session block prepended + S37 demoted to Previous via header rename only content verbatim; session_log.md S38 paragraph prepended at line 1 per newest-first convention; tech_debt.md 3 new TD-S38-NEW-* prepended in Active section + TD-061 final closure addendum block in Resolved section + footer S37→S38; MERDIAN_Assumption_Register.md §D.20 added between D.19 and footer with 4 rows + footer S37→S38; merdian_reference.json v28→v29 with S38 change_log + session_log entries + ADR-017 + ENH-110/111/112 inventory; MERDIAN_Enhancement_Register.md ENH-110/111/112 entries appended + ENH-83 status row update + footer S37→S38. Two new doc files: docs/decisions/ADR-017-operator-console-design-principles.md + docs/enhancements/ENH-110-consolidated-marketview-build.md (with Lovable prompts as Appendix A+B). Project knowledge upload pending S39 = 10-session backlog.* *CLAUDE.md v1.27 — 2026-05-25 (Session 37 close). Added: Six S37 operational findings (minimum-sufficient-statistic at write layer is the canonical schema design pattern when derivations are regime-conditional — ADR-014→ADR-015 same-session supersession surfaced that derived booleans `is_local_max` + `is_flip_zone` + `is_pin_candidate_bool` fire on 39%/20% of rows producing marker noise indiscriminable from signal; derivations belong in SQL views like `v_gex_strike_pin_zone`, write layer stores only what cannot be recomputed from raw inputs; generalizes beyond GEX schema to any case where a column's value depends on a relative-to-cohort threshold; IV skew evidence is material and must be preserved in schema — at NIFTY 24050 ATM `gamma_call=0.00164` vs `gamma_put=0.00234` = +43% PE-richer = put-skew regime; collapsing to single `gamma` column destroys this signal; `gamma_call`/`gamma_put` split costs one column but preserves regime classification capability; same-session ADR supersession is a valid pattern when quant review surfaces architectural defect post smoke-fire — ADR-014 was technically correct (fidelity OK, sign convention OK) but functionally noisy; v1 patch trail + backup preserved for git audit, live schema migrated to v2 via TRUNCATE + ALTER before any consumer code locked against v1; supersession in same session is preferable to shipping v1 + planning v2 because there are no consumers yet to migrate; GEX-as-context-not-gate framing — operator's explicit *"Meridian doesn't need anymore gates. It's already gating everything"* rejection of gate-mode integration is settled architectural decision per ADR-002 v2 P5/P7 + ADR-009 graduated-strictness holdout; ENH-80/81 ship as display layer only (Lovable dashboard + Pine overlay) not as routing inputs to `build_trade_signal_local.py`; operator is the integration layer for GEX-derived decisions; this framing inverts the assumption that every new signal becomes a gate — research-grade columns and views earn their place by informing operator judgment, not by silently routing trades; calibration-deferred-by-design pattern — ADR-016 parameter calibration pattern PROPOSED but build deferred per operator until N grows enough for cohort-driven recalibration to be useful; ENH-81 views ship with hardcoded τ_pin=τ_accel=0.3 and inline `// TAU_PIN — swap for ENH-83 lookup` markers at every site for the future plumbing point; the marker pattern is the discipline that prevents τ from drifting into magic-number territory while keeping the calibration build queued; codified as: when a calibration mechanism's value depends on cohort size still accumulating, defer the mechanism but mark every value-site in code so the eventual plumb is mechanical not archaeological; Pine v6 visual disambiguation patterns — label anchor points at zone edges (`style_label_lower_left` for top-anchor, `style_label_upper_left` for bottom-anchor) + `bar_index+90` X-offset for X-position separation prevent collision with ICT-layer labels at midpoint; single-strike PIN zone degeneracy resolved via ±0.1% of strike widening with 50pt floor — generalizable to any Pine box rendering of empirically-degenerate ranges; SENSEX 77000 single-strike PIN renders as 154pt band labeled *"PIN 77000 (single strike, +56K Cr)"* with honest framing; codified as: when an empirical range can degenerate to zero-height, supply both a relative widening factor and an absolute floor; Lovable per-table RLS pattern — anon-read access via `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + `CREATE POLICY ... FOR SELECT TO anon USING (true)` + `GRANT SELECT ... TO anon` is the canonical three-line pattern per table/view; brittleness surfaced in TD-S37-03 — Lovable client uses anon-key directly so any RLS misconfiguration silently produces empty datasets instead of explicit auth failure; mitigation is explicit policy + grant pair per object, no implicit defaults; codified as: when shipping read-only consumer-facing dashboards on Supabase, document the exact RLS triplet inline in commit message). Settled decisions added: ADR-015 ACCEPTED 2026-05-25 (per-strike GEX schema v2 — 12-col minimum-sufficient-statistic with `gamma_call`/`gamma_put` split preserving IV skew; supersedes ADR-014 same session; writer shipped via `patch_s37_enh80_writer_v2.py`; smoke-fire post-migration NIFTY abs_diff=9.8e-11 Cr / SENSEX abs_diff=1.2e-14 Cr; §F1 dealer-vs-positioning split deferred — `v_oi_prev_close_snapshots` scaffolded); ADR-014 ACCEPTED then SUPERSEDED same session by ADR-015 (16-col v1 schema with derived booleans; v1 patch trail + backup preserved for git audit; schema migrated via TRUNCATE + ALTER 4 cols dropped + 2 added before any consumer locked); ADR-016 PROPOSED build deferred (parameter calibration pattern — temporal-immutable `merdian_parameters` table with mandatory `change_reason`; design surfaced 9 magic-number clusters; ENH-83 build deferred until N grows for cohort-driven recalibration; ENH-81 ships with `// TAU_PIN — swap for ENH-83 lookup` markers throughout for the future plumb); ENH-80 SHIPPED per-strike GEX writer (`compute_gamma_metrics_local.py` extended; NIFTY 144 strikes / SENSEX 261 strikes per cycle; falsification gate PASS abs_diff ≤ 1e-10 Cr); ENH-81 SHIPPED Positioning Landscape (3 SQL views `v_gex_strike_pin_zone` + `v_gex_strike_accel_zone` + `v_dealer_flow_sim` with τ=0.3 hardcoded; 4/4 falsification gates PASS including direction-correctness on 12 dealer-flow scenarios; Pine overlay v1+v2 SHIPPED with PIN/ACCEL boxes + range labels at zone edges + single-strike widening); GEX-as-context-not-gate decision codified (5 existing gating layers sufficient per operator; ENH-80/81 as display layer not routing input; operator is integration layer); Lovable dashboard live at `lovable.dev/projects/e8fde6f9-58d0-4444-860d-508b3635b014` with NIFTY/SENSEX tabs + REGIME card + GEX-by-strike histogram with PIN/ACCEL ReferenceArea bands + summary strip + RLS+GRANT per-table; §D.19 added to Assumption Register (3 rows D.19.1 minimum-sufficient-statistic at write layer / D.19.2 IV skew preservation via gamma_call/put split / D.19.3 GEX-as-context-not-gate framing). New TDs filed S37 (pending S38+): TD-S37-01 S3 (hardcoded τ_pin=τ_accel=0.3 in ENH-81 views — formalize as `merdian_parameters` lookup when ENH-83 builds; `// TAU_PIN — swap for ENH-83 lookup` markers in place at every site); TD-S37-02 S3 (§F1 dealer-vs-positioning GEX split scaffolded via `v_oi_prev_close_snapshots` view but writer integration deferred — needs OI-change history wiring into `gex_strike_snapshots` writer); TD-S37-03 S3 (Lovable anon-key brittleness — RLS misconfiguration produces silent empty datasets not auth errors; per-table RLS+GRANT triplet must be documented inline in commit message + smoke-tested via direct anon-key probe before deploying any new view). Four production patches deployed: `patch_s37_enh80_writer.py` v1 → ADR-014 schema (briefly accepted, schema migrated to v2 mid-session); `patch_s37_enh80_writer_v2.py` → ADR-015 schema (canonical); `patch_s37_enh81_pine_overlay.py` v1 (PIN/ACCEL boxes + labels at midpoint); `patch_s37_enh81_pine_overlay_v2.py` (labels at zone edges + single-strike widening + X-offset). All AST-validated pre+post. Four backups preserved Local: `compute_gamma_metrics_local_PRE_S37.py` + `compute_gamma_metrics_local_PRE_S37_v2.py` + `generate_pine_overlay_PRE_S37_ENH81.py` + `generate_pine_overlay_PRE_S37_ENH81_v2.py`. New schema: `gex_strike_snapshots` table created via `sql/2026-05-25_enh80_gex_strike_snapshots.sql`; migrated v1→v2 via `sql/2026-05-25_enh80_schema_v2_migration.sql`. New views: `v_gex_strike_pin_zone` + `v_gex_strike_accel_zone` + `v_dealer_flow_sim` + `v_oi_prev_close_snapshots` (dealer-flow-proxy scaffold deferred). Zero AWS deployments this session (all Local). MERDIAN AWS instance ID memory update: `i-0878c118835386ec2` (S35 carry-forward P0_TERTIARY now actioned). Seven canonical files modified at session close per Doc Protocol v4 Rule 3 + Rule 9 + Rule 10 + Rule 11 — full-file no-crunch discipline maintained throughout: `MERDIAN_Decision_Index.md` 106→112 lines ADR-015 + ADR-014 rows prepended above ADR-012 + Pending ADRs table ADR-013 row added + ADR-014/015/016 rows added + ADR-017+ next-free; `CLAUDE.md` v1.26→v1.27 this footer prepended preserving v1.26 → v1.1 history verbatim + 6 S37 settled-decisions bullets added before "If any of these need to change" line; `CURRENT.md` S37 Last session block prepended + S36 demoted to Previous via header rename only content verbatim; `session_log.md` S37 one-liner paragraph prepended at line 1 per newest-first convention; `tech_debt.md` 3 new TD-S37-* prepended above latest TDs in Active section + footer S36→S37; `MERDIAN_Assumption_Register.md` §D.19 added between D.18 and footer + footer S36→S37; `merdian_reference.json` v27→v28 with S37 change_log + session_log + new table/views/files inventory entries. Three new ADR files created: ADR-014-per-strike-gex-schema.md + ADR-015-per-strike-gex-schema-v2.md + ADR-016-parameter-calibration-pattern.md. Project knowledge upload pending S38.* *CLAUDE.md v1.26 — 2026-05-25 (Session 36 close). Added: Six S36 operational findings (writer-state-vs-data-state diagnosis ordering — when an aggregate-magnitude metric drifts on a derived table, read the writer function body BEFORE inferring from aggregate magnitudes; aggregate magnitudes are downstream of both live-writer state and historical-data unit-mix, and aggregate evidence alone cannot discriminate between live regression and historical contamination; the reverse-order diagnosis cost 7 sessions of carry-forward on TD-S30-CANDIDATE-1 S29 → S35; capture-layer failure modes partition into three independent axes Mode A 429 quota / Mode B 401 token / Mode C orphan RUNNING — instrument the failure log against `script_execution_log` BEFORE extending the retry layer, since the "extend the retry budget" reflex burns budget on non-target exceptions and does not address Modes B or C; Mode B 401 token expiry was SOLVED upstream at S29 via `pull_token_from_supabase.py` atomic write + readback verify + `dhan_token_probe_log` audit — zero post-S29 401s in 11 instrumented days; a retry-layer 401 handler at this point would mask token re-regression and is intentionally NOT included in the predicate; retry predicate kwarg `retry_predicate: Callable[[Exception], bool] | None = None` is the canonical surgical pattern for adding exception-class-specific retry behavior to a shared `retry_call` utility — non-predicate exceptions raise immediately without consuming the retry budget, backwards-compatible with all existing call sites that don't pass the kwarg; PostgREST `Prefer: return=representation` interacts with RLS on PATCH path differently than read path — drop the header on raw-HTTP PATCH paths when not consuming the response body, regardless of perceived parity with the read path; `script_execution_log.duration_ms` is int4 — orphan-recovery durations exceeding `2^31 - 1 = 2,147,483,647 ms` (~24.85 days) must be clamped at write time via `min(int((now - started_at).total_seconds() * 1000), 2_147_483_647)` not at compute time after the fact; house DB convention for Local Supabase access is raw HTTP via `requests` against `{SUPABASE_URL}/rest/v1/*` with `apikey` + `Authorization: Bearer` headers (env var `SUPABASE_SERVICE_ROLE_KEY` not `SUPABASE_KEY`), not supabase-py — reference implementation is `ingest_option_chain_local.py`; multi-modal failure-shape diagnosis pattern is generalizable: for any capture/ingest service exhibiting recurring failure, partition the failure surface against `script_execution_log` by (exit_reason, error_message, error_code) and DROP from current-session scope any mode that turns out to be upstream-resolved to avoid masking re-regression). Settled decisions added: TD-S30-CANDIDATE-1 CLOSED-MISDIAGNOSIS 2026-05-25 (live `compute_gamma_metrics_local.py` was NEVER regressed — direct read of writer function body in S36 first turn via `findstr /N /C:"1e7" backfill_gamma_metrics_to_main.py` + `signed_gamma_exposure` inspection confirmed `/1e7` Cr conversion intact at S27 commit `241f943`; three-epoch SQL on `gamma_metrics.net_gex` magnitude-by-week revealed historical data unit-mix: pre-2026-03 Cr-correct backfill / 2026-03-02→2026-05-04 raw-rupees ~3,500 rows from pre-S27 live writer's pre-/1e7-patch window / post-2026-05-11 Cr-correct post-/1e7 patch; March epoch recovered via `backfill_gamma_metrics_to_main.py --start 2026-03-02 --end 2026-05-04 --mode overwrite --symbol both` writing 2,850 cycles; April-early-May ~4,300 rows DELETEd as confirmed gaps under TD-S35-NEW-1 chain coverage residual; 1 outlier 2026-05-19 09:55:14 NIFTY -4.7B DELETEd separately; post-cleanup `gamma_metrics` single-unit Cr globally — foundational diagnosis-ordering lesson codified §D.18.1, with 7-session carry-forward cost S29 → S35 as the empirical foundation); TD-080 CLOSED via ENH-99 SHIPPED 2026-05-25 (failure-shape diagnosis against `script_execution_log` partitioned the failure surface into Mode A 429 quota / Mode B 401 token / Mode C orphan RUNNING; Mode B DROPPED from S36 scope as upstream-resolved S29 — zero post-S29 401s in 11 instrumented days; Component 1 — `gamma_engine_retry_utils.retry_call` extended with `retry_predicate` kwarg + `[RETRY_BURN_DOWN]` stderr telemetry via `patch_s36_enh99_v3.py` regex-anchor approach after v1+v2 literal-anchor approaches failed on CRLF/LF straddle and deeper-body mismatch; 947 matched chars; backwards-compatible; 2 Dhan retry sites in `ingest_option_chain_local.py` at lines 316 + 346 bumped to `attempts=6, delay=15.0, backoff_multiplier=1.5, retry_predicate=is_dhan_429`; ~96s budget vs ~60s Dhan quota window; Component 2 — NEW `orphan_run_janitor.py` Local file deployed to `C:\\GammaEnginePython\\` with env-aware raw HTTP per house convention + int4 duration clamp at `2^31-1` per D.18.4 + per-row error tolerance; `MERDIAN_Orphan_Janitor` Task Scheduler task registered weekly Mon-Fri 09:14 IST via PowerShell `Register-ScheduledTask` Hidden+IgnoreNew+5min limit+battery flags+Interactive Limited principal; smoke-fire test 2026-05-25 17:26:26 closed 22/24 orphans + 2 REPL stragglers after int4 clamp fix; task count 19 → 20; Component 3 telemetry `[RETRY_BURN_DOWN]` stderr tag visible to Task Scheduler log + supervisor pipe + Telegram alert daemon; Component 4 audit thresholds in `merdian_daily_audit.py` DEFERRED to S37+ pending config schema visibility); ENH-99 PROPOSED → SHIPPED (consumed S29 reservation slot; three components live in production; no new ENH filings beyond status update); §D.18 added to Assumption Register (4 rows D.18.1 writer-state-vs-data-state diagnosis ordering / D.18.2 capture-layer Mode A/B/C taxonomy with Mode B SOLVED upstream / D.18.3 PostgREST `Prefer: return=representation` RLS interaction / D.18.4 int4 `duration_ms` clamp pattern); MERDIAN_Decision_Index.md UNCHANGED (no new ADR accepted S36). New TDs filed S36 (pending S37+): TD-S36-NEW-1 S3 (`gamma_metrics` Apr-early-May row gap — residual from TD-S30-CANDIDATE-1 cleanup; ~4,300 rows DELETEd as confirmed gaps because vendor `hist_option_bars_1m` is sparse post-Apr-2026 per TD-S35-NEW-1; closeable in same recovery window as ADR-013 Breeze fallback graduation at n≥3 successful Breeze-tier backfills accumulated); TD-S36-NEW-2 S3 (`MERDIAN_Dhan_Token_Refresh` Task Scheduler task not instrumented to `script_execution_log` — surfaced during ENH-99 failure-shape diagnosis; daily audit pipeline cannot detect task-firing failures; quick-win retrofit ~30 min when next operating on token-refresh path); TD-S36-NEW-3 S4 (`dhan_token_probe_log` forward-only from 2026-05-10 — pre-S29 token incidents invisible to probe log; S26 instrumentation forward-only by design; documentation-only gap); TD-S36-NEW-4 S3 (`script_execution_log.duration_ms` int4 schema constraint — clamp-at-write-time operational pattern works, int8 migration via `ALTER TABLE script_execution_log ALTER COLUMN duration_ms TYPE bigint USING duration_ms::bigint` is proper fix non-urgent ~30 min). Three production patches deployed: `patch_s36_enh99.py` v1 abandoned at dry-run RETRY_UTILS anchor failure CRLF/LF straddle (INGEST + JANITOR blocks dry-ran clean but not deployed in v1 to preserve atomic commit semantics); `patch_s36_enh99_v2.py` INGEST + JANITOR APPLIED live with backup `ingest_option_chain_local_PRE_S36.py` (+504 bytes net AST validated); `patch_s36_enh99_v3.py` RETRY_UTILS APPLIED via regex-anchor `def retry_call\\(.*?raise RuntimeError\\([^)]*\\)` with re.DOTALL match 947 chars with backup `gamma_engine_retry_utils_PRE_S36.py` (+996 bytes net AST validated). One new Local file: `orphan_run_janitor.py` deployed Local. One new Task Scheduler task: `MERDIAN_Orphan_Janitor` weekly Mon-Fri 09:14 IST. Zero AWS deployments this session (capture-layer Local-only). Nine canonical files modified at session close per Doc Protocol v4 Rule 3 + Rule 9 — no-crunch / no-append discipline maintained throughout: 6 strict canonical (`CLAUDE.md` v1.25 → v1.26 this update + `CURRENT.md` S36 Last session block prepended + S35 demoted via header rename + `session_log.md` S36 paragraph prepended + `tech_debt.md` 4 new TDs prepended + TD-S30-CANDIDATE-1 + TD-080 Resolution blocks appended in-place + footer S36 update + `MERDIAN_Assumption_Register.md` §D.18 added with 4 rows + footer S35 → S36 + `merdian_reference.json` v26 → v27 with S36 change_log + session_log entries + `orphan_run_janitor.py` + `MERDIAN_Orphan_Janitor` entries) + 3 supplementary (`MERDIAN_Enhancement_Register.md` ENH-99 status row + detail block + Change Log row + Last updated S36 prepended + `MERDIAN_Deployment_Topology.md` §7.2 19 → 20 task table + MERDIAN_Orphan_Janitor row + §1 task count + S36 counts addendum + Update log row + footer S35 → S36 + `MERDIAN_System_Map.md` §A.S36 + §B.S36 sections + Update log row + footer S35 → S36). Full file downloads no-crunch discipline maintained throughout. Project knowledge upload pending S37 = 8-session backlog of all modified files + new `orphan_run_janitor.py` + 3 patch scripts + Task Scheduler PowerShell snippet.* *CLAUDE.md v1.25 — 2026-05-25 (Session 35 close). Added: Nine S35 operational findings (tier transition has three surfaces sharing the same 2026-04-01 boundary — chain prices + expiry calendar + ATM strike picking; audit all consumers of a tiered data source not just the obvious one; `select("col").eq().range()` over 2.67M rows finding 10 distinct values is wrong shape — DISTINCT must move to DB function + covering index for sub-100ms execution under PostgREST 8s window; PostgREST 8s timeout doesn't apply to direct DB connections — index builds on multi-million-row tables require `SET statement_timeout=0` and direct connection; `historical_option_chain_snapshots` (HOCS) is the post-Apr-2026 canonical chain history — keyed on `symbol` text not `instrument_id` uuid, `ltp` not `close`, written by `ingest_option_chain_local.py` (a name that obscures its function as write-once historical archive); `run_id` NOT NULL on HOCS — backfill scripts that don't set it lie silently when supabase-py wraps the failure in try/except but doesn't roll back local accumulator state; `stock_code="BSESEN"` for SENSEX on Breeze — not in public docs, empirical 6-variant probe is canonical SENSEX-on-Breeze identification step; writer is INSERT-only on `ict_primitive_outcomes` — new schema columns don't backfill on existing rows; either TRUNCATE+recompute or DELETE-targeted-subset+recompute per cohort; multi-line nano paste into SSM beats base64 single-line paste for code transfer to MERDIAN AWS — terminal buffer limit ~4KB single line vs multi-line file content streams cleanly; Breeze proved its surgical-fill capability on one-shot 107,630-row HOCS write in 4-5min wallclock — qualifies as architectural fallback for any future single-day chain outage). Settled decisions added: ADR-012 ACCEPTED → IMPLEMENTED 2026-05-24 (writer v9 ships 5 sl_* columns + spot-anchored SL evaluation block in `compute_retest_atm_pnl` walking 5m clock-aligned spot bars from `first_retest_ts + 5min` through 15:25 IST EOD bucket close; BULL trigger close < zone_low × (1 − X) / BEAR close > zone_high × (1 + X) with `SL_BUFFER_PCT_DEFAULT = 0.005` per ADR-012 §3; trigger lookups use existing v8 chain cache with no new DB round-trips; level primitives skip SL by design; `pnl_with_sl_pct` falls back to `option_pnl_eod` on no-trigger; single-cell n=5 verified BEAR triggered SL exits 13-14pp better than held-to-EOD on 2026-05-14 NIFTY M5 cohort); ADR-013 PROPOSED (Breeze ICICI Direct API as canonical historical options backfill source — replaces vendor renewal cycle dependency; full-chain capability with no ATM±N cap; 3-year lookback / 5000 calls/day / 100/min throttle / SEBI static-IP whitelist post-2026-04-01; SENSEX symbology `BSESEN/BFO`; linked to ENH-109); ENH-106 v8/v8.1/v8.2 IMPLEMENTED (dual-source chain reader on `build_ict_primitives.py` — boundary 2026-04-01 UTC, per-tuple split routing pre_only/post_only/mixed, `option_pnl_source TEXT` audit column on outcomes with values vendor_hist_1m / merdian_hist_5m / NULL; v8.1 UNIONs vendor + HOCS expiry calendars; v8.2 swaps HOCS pagination for RPC `get_hocs_distinct_expiries(text)` STABLE function + `(symbol, expiry_date)` covering `idx_hocs_symbol_expiry` index for sub-100ms DISTINCT under PostgREST 8s window); ENH-109 PROPOSED (graduate Breeze rollingoption + get_historical_data_v2 as canonical historical backfill — replaces vendor purchase + S22 Kite + Dhan rollingoption ATM±10 cap); TD-S34-NEW-4 CLOSED-MECHANICAL (81% zone-primitive recovery on post-Apr-2026 retest cohort via v8 dual-source reader; +14 via Breeze 2026-04-16 surgical fill; 331/409 = 81% with 132 architecturally-exempt level primitives excluded; structural residual carries to TD-S35-NEW-1); MERDIAN AWS instance rebuilt to `i-0878c118835386ec2` (memory shows older `i-0e60e4ed9ce20cefb` — reconcile at S36); §D.17 added to Assumption Register (4 rows D.17.1-D.17.4 codifying S35 findings: dual-source chain reader VALIDATED, HOCS strike-coverage structural limit VALIDATED → TD-S35-NEW-1, Breeze full-chain capability VALIDATED → ADR-013 PROPOSED + ENH-109 PROPOSED, ADR-012 SL writer integration VALIDATED on n=5 single-cell). New TDs filed S35 (pending S36+): TD-S35-NEW-1 S2 (HOCS strike-coverage structural limit; `ingest_option_chain_local` ATM±N capture window; recovery requires writer widening OR ADR-013 Breeze canonical adoption); TD-S35-NEW-2 S1 (pre-Apr-2026 chain vendor uncatalogued — critical institutional knowledge at risk, bus-factor-of-one; vendor identity + contract terms + refresh cadence + `stock_code` mappings + contact details not documented anywhere); TD-S35-NEW-3 S4 (SENSEX Breeze symbology `BSESEN` not in public docs); TD-S35-NEW-4 S3 (writer INSERT-only on outcomes table — schema column adds require manual DELETE before recompute populates new columns). Four production patches deployed: ENH-106 v8 + v8.1 + v8.2 + ADR-012 v9 (all AST-validated pre+post, four `_PRE_S35_*.py` backups preserved Local). One AWS deployment: Breeze backfill of 2026-04-16 chain gap from MERDIAN AWS via SSM + multi-line nano paste (107,630 HOCS rows written: NIFTY 61,899 + SENSEX 45,731). Ten canonical files modified at session close per Doc Protocol v4 Rule 3 + Rule 9 + Rule 10 + Rule 11 (7 strict canonical: this `CLAUDE.md` update + `CURRENT.md` + `session_log.md` + `tech_debt.md` 4 new TDs + `MERDIAN_Decision_Index.md` ADR-012 IMPLEMENTED + ADR-013 PROPOSED + `MERDIAN_Assumption_Register.md` §D.17 + `merdian_reference.json` v25 → v26; 3 supplementary: `MERDIAN_Enhancement_Register.md` ENH-109 + `MERDIAN_Deployment_Topology.md` Breeze MALPHA-analog catalog + MERDIAN AWS instance ID update + `MERDIAN_System_Map.md` HOCS table inventory). Full file downloads no-crunch discipline maintained throughout.* *CLAUDE.md v1.24 — 2026-05-24 (Session 34 close). Added: Six S34 operational findings (capture is not the binding constraint, selection is — 99.2% capture rate over 14-month window; the retest is structurally the moment of maximum local risk for an ICT trade — once survived in first 5-15 min the trade is what it is; premium SL on ATM held strikes is wrong instrument-stop pairing — measures microstructure not thesis; vendor-purchased vs self-ingested data tier transition creates silent coverage gaps — diagnostic path is tier-transition not token-failure pattern-match; prior research stands when cohort definition becomes more restrictive — a smaller N is a different population not an unstable metric; operator domain-knowledge pushback continues to be highest-bandwidth bug surface — S33 source-table category error + S34 sparse-data framing + S34 token-vs-tier-transition all caught within one cycle by operator pushback). Settled decisions added: ADR-012 ACCEPTED 2026-05-24 (spot-anchored stop-loss doctrine for ICT retest entries — empirical n=7 0/7 SL triggers at 0.3-0.8% spot vs 0/7 winners at ≤10% premium SL; held-to-EOD total +₹106,888 on 1 lot); universal capture rate 99.2% (118/119 over 14 months — codified D.16.1+D.16.2); selection-research arc framed as 3-5 session work item with five candidate features (D.16.2); ENH-108 PROPOSED second-touch/N-touch retest detection (foundational schema unlock for selection research, `ict_primitive_touches` table recommendation); §D.16 added to Assumption Register (4 rows D.16.1-D.16.4 codifying S34 findings). New TDs filed S34 (pending S35+): TD-S34-NEW-4 S2 (`hist_option_bars_1m` post-2026-04-01 coverage gap; vendor → MERDIAN-ingest tier transition root cause per operator framing, NOT Kite token expiry as initially diagnosed — recorded as superseded; nine probed dates confirmed empty; workaround `trade_date <= '2026-03-30'`; 1-2 sessions to fix). Zero production code patches this session. Eight canonical files modified at session close per Doc Protocol v4 Rule 3 + Rule 9 + Rule 10 + Rule 11 (tech_debt.md + Enhancement Register + Decision Index + Assumption Register + CLAUDE.md this update + CURRENT.md + session_log.md + merdian_reference.json) + 1 new doc file (ADR-012-spot-anchored-sl-doctrine.md 141 lines / 12565 bytes). Full file downloads no-crunch discipline maintained throughout.* *CLAUDE.md v1.23 — 2026-05-22 (Session 33 close). Added: Eight S33 operational findings (source-table fitness-for-purpose is category error class; experiments 1-50 made inverse error compared to ENH-100 v6; operator-prompted source-table challenge surfaced the real bug; per-tuple range-prefetch canonical pattern for chain reads at scale; empirical expiry calendar beats DOW rules including 2025-09-01 NSE/BSE swap + ~15% holiday-shifted weeks + same-week double expiries; held-strike chain reads flip universally-negative median to universally-positive on directional cells; M5 OB retest paradox confirmed at option-PnL level — FALSIFIED as edge; DTE 3-bucket structure DTE=0 feast-or-famine / DTE=1-2 M5 sweet spot / DTE≥3 H-TF home). Settled decisions added: ADR-011 ACCEPTED 2026-05-22 (chain-table held-strike ATM PnL supersedes ADR-010 v3 §"Source table choice" only — schema unchanged; ENH-106 v7 writer implements; 19,399 outcomes rebuilt 1775.9s; coverage 20-44× lift; median PnL flipped positive on directional cells); ENH-100 v3→v7 IMPLEMENTED; ENH-103 v6→v7 IMPLEMENTED; ENH-106 IMPLEMENTED; ADR-009 §Phase 1 H-FVG retest finding empirically confirmed cross-symbol with held-strike option PnL (two independent metrics agreeing — spot-WR S31-B + option-PnL S33); §D.15 added to Assumption Register (4 rows D.15.1-D.15.4); TD-S32-NEW-4 CLOSED via architectural retirement. New TDs filed S33 (pending S34+): TD-S33-NEW-1 S3 (`hist_atm_option_bars_5m` re-evaluation post-wick-experiment retirement); TD-S33-NEW-2 S4 (v6 dead code cleanup in `compute_atm_pnl_and_dte`); TD-S33-NEW-3 S2 (post-v7 falsification audit re-scope); TD-S33-NEW-4 S3 (D-TF horizon extension — `mfe_pct` + `time_to_mfe_min` columns may already answer "what horizon makes D-TF tradeable").* *CLAUDE.md v1.22 — 2026-05-21 (Session 31-B close). Added: Pine v6 engineering catalog (6 entries codified for future Pine work — descending for-loop `by -1` requirement silent zero-iterations; if/else type-compat CE10235 fixed by standalone if no else for label.new; `var int` global mutation CE10088 wrapped in `var array<int> = array.from(0)`; `close[1] + lookahead_off` double-shift silent wrong value; PDH/PDL canonical fetch idiom `[high[1], low[1]] + lookahead=barmerge.lookahead_on`; `max_boxes_count=500` hard cap with silent GC of overflow boxes — mutations on GC'd boxes silently no-op, not errors). Seven S31-B operational findings: ADR-004 Wave 1 was the single largest end-to-end build in MERDIAN history (19,399 primitives + 19,399 outcomes in 156.3s — build discipline held S31-A spec → Task 1 detector → Task 2 writer → Task 3 DDL → Task 4 backfill → Task 5 visual descoped; ADR-009 Phase-1 holdout split as immediate validation produced headline finding same-session; codified as: for major data-layer builds design holdout validation pipeline in same plan as build pipeline); Pine v6 visual overlay is no longer a "quick task" estimate this class generously (Task 5 estimated 0.5-1 session grew to 5 iterations and descoped; six independent Pine v6 bug classes surfaced; constraint set was not understood up front — discovered through iteration; codified as: for Pine v6 work involving MTF + dynamic state + >500-element rendering, design architectural constraint set BEFORE writing code); holdout-validation is non-negotiable per ADR-009 Phase 1 (S31-B's clean 10-month train / 2.5-month holdout split produced 4 independently-validating buckets with deltas large and in favorable direction — opposite of overfit signature; but verdict has not yet been transfer-checked against signal_snapshots runtime cohort via D.13.1 pre-flight — gated before ENH-102 flag flip; codified as: holdout-validation on build cohort is necessary but not sufficient — cohort-translation pre-flight on live runtime cohort is final transferability check; two layers not one); schema extensions ship with ADR per Doc Protocol v4 Rule 10 (ENH-100 + ADR-010 ship together; additive columns on research-tier table is lightweight ADR but discipline applies; codified as: any change adds/removes/restructures table columns triggers Rule 10 ADR mandatory even if "just columns"); ENH-99 reservation preserved across three sessions S29→S30→S31-B (Doc Protocol v2 Rule 5 monotonic-no-reuse discipline applied correctly; ENH-100/101/102 explicitly skipped reserved slot for TD-080 Dhan 429 retry layer + circuit breaker); OB retest paradox is a structural finding worth investigation not noise artifact (Q2 in-sample WR showed M5 OB form 77-87% vs retest 47-58% across all symbol×direction cells — 30-40pp consistent gap; three hypotheses to test: OB retest-entry needs different timing OR detector mode='retest' for OBs is mis-classified vs canon OR trade architecture asymmetry where OBs are formation-only signals FVGs are retest-primary consistent with ICT instructor interpretations; paradox does NOT block ENH-100/101/102 deployment which focus on FVG retest where retest > formation holds); SENSEX H DISPLACEMENT_DOWN 28.6% holdout reversal is cleanest inverse-signal candidate seen to date in MERDIAN (NIFTY equivalent holds directionally 53.3% holdout from 68.2% train overfit but still edge in assumed direction; SENSEX consistently inverse train 38.7% holdout 28.6% — two hypotheses inverse signal OR detector directionality bug `_detect_displacement()` SENSEX-specific path; S32 P3 investigation cross-check detector source first before assuming inverse signal). Settled decisions added: ADR-004 Wave 1 IMPLEMENTED 2026-05-21 (5 primitives × 2 symbols × 4 timeframes × 14-month window producing 19,399 primitives + 19,399 outcomes in 156.3s combined; S31-A spec → S31-B Task 4 backfill execution validated end-to-end build; two tables `public.ict_primitives` + `public.ict_primitive_outcomes` with FK CASCADE and UNIQUE INDEX `ict_primitives_unique_per_source` per §10 erratum; Wave 2 ten remaining primitives deferred); H FVG retest validated edge cross-symbol cross-direction (4 buckets simultaneously HOLD AND STRENGTHEN on holdout: NIFTY H BEAR_FVG retest 75.0→85.2 +10.2 N=29; NIFTY H BULL_FVG retest 64.3→90.0 +25.7 N=27; SENSEX H BEAR_FVG retest 76.6→92.0 +15.4 N=30; SENSEX H BULL_FVG retest 70.2→80.8 +10.6 N=30; deltas in favorable direction opposite of overfit signature; codified D.14.1 and into ENH-102 live routing rule with `MERDIAN_ENH102_ENABLED` env flag default OFF pending D.13.1 cohort-translation pre-flight + ENH-100 sizing/stop calibration); ENH-100 PROPOSED P0 outcomes-magnitude profiling + ATM PnL + DTE (13 additive columns on `ict_primitive_outcomes` — forward_5/15/60/120m_pct + forward_eod_pct + mfe_pct + mae_pct + time_to_mfe_min + atm_pnl_5/15/30/60m_pct + dte_at_formation; gates ENH-101 + answers operator's six S31-B Q2 follow-up questions; ships with ADR-010 schema extension at build time per Doc Protocol v4 Rule 10); ENH-101 PROPOSED P1 BLOCKED-BY-ENH-100 stop-loss optimization from MAE distribution (90th percentile of mae_pct among trades profitable at canonical exit horizon per symbol×TF×primitive_type×direction×retest_or_formation cell with N≥30; per-cell stop_distance_pct table + Pine overlay annotation + live routing wire; `MERDIAN_ENH101_ENABLED` env flag default OFF); ENH-102 PROPOSED P1 H-FVG-retest signal isolation (codify ADR-009 Phase-1 holdout 4-bucket finding into live routing rule with `MERDIAN_ENH102_ENABLED` env flag default OFF; integration target S31-C consumer rewire; live deployment additionally gated on ENH-100 sizing prerequisite + D.13.1 cohort-translation pre-flight on signal_snapshots runtime cohort with 10pp transfer tolerance per D.8.2 graduated-strictness); §D.14 added to Assumption Register (5 rows D.14.1-D.14.5: H FVG retest VALIDATED on holdout, M5 FVG standalone REFUTED on holdout, OB retest paradox documented for investigation, SENSEX H DISPLACEMENT_DOWN reversal anomaly worth investigation, ADR-004 §5.1 body-only OB body bounds + close-through-only breach rule canonical with wickExtended toggle deferred wave 1.5). New TD filed S31-B (pending S32+): TD-S31B-NEW-1 S3 (Pine v6 visual MTF-breach overlay deferred — Task 5 descoped after 5-version iteration with 6 Pine v6 ergonomic walls; Pine v6 ergonomic catalog codified inline in TD body for future Pine work; architectural redesign required not code-and-paste; cost estimate 2-3 sessions Pine-v6 path OR 1 session external rendering substitute; blocked by operator decision on visual MTF-breach value vs Pine-v6 architectural complexity).* *CLAUDE.md v1.21 — 2026-05-17 (Session 30 close). Added: B29 (cohort-prior gate hazard — gates promoted on research cohort and shipped without live-cohort re-validation are silent edge destroyers; live cohort 8-week N=211 pure-ICT setups WR 58.3% mean +6.96% median +3.38%, MERDIAN gate stack rejected 99.4%; cohort-translation §D.9.3 ENH-55-specific promoted to general principle; env-flag default OFF with N≥30 per-pattern live-cohort validation threshold; cross-link D.8.2 graduated split + D.9.3 cohort-translation + ADR-009 §Phase 1 S30 sub-rule). Six S30 operational findings: cohort-translation discipline §D.9.3 caught itself (S29 5-day Phase 0b investment on hist_pattern_signals cohort verdict FAIL stands on that cohort but did NOT transfer to live cohort which is median-positive; discipline prevented compounding misallocation); OB attachment broken at signal-builder layer not detection layer (BULL_OB 0.5% attachment rate on 4,882 zone-touches; BEAR_OB 0% on 3,139; filed TD-S30-NEW-3 S1; localize in enrich_signal_with_ict()); MERDIAN gate stack inverts edge direction on three context dimensions for live cohort (LONG_GAMMA WR 55.1% gated BLOCK, BEARISH-ALIGNED WR 64.7%, HIGH VIX WR 61.2% — gates left in place pending live-cohort N accumulation, ENH-76/77/88 env-flagged off this session); patch script + env-flag pattern is canonical "ship reversible disablement" workflow (apply_s30_patches.py 227 lines AST-validated patched-copy pattern; .env explicit =0 documentation auditable vs absence-of-line ambiguous); AWS sync via git pull is canonical deployment vector SSH/SCP/heredoc-paste fragile and discouraged (S30 attempted SSH blocked + heredoc paste truncation cascade; final successful path Local commit → push → AWS pull; git ensures byte-identical state cross-environment); .env is gitignored — every git pull on AWS requires manual .env reconciliation (commit message should include exact cat >> .env <<EOF block for paste-able reconciliation). Settled decisions added: Pure-ICT live cohort empirical edge (8wk N=211 WR 58.3% mean +6.96% median +3.38% pooled +1469.13pp; BULL_FVG N=99 58.6% beats Compendium settled 50% z=1.72 p=0.043; BEAR_FVG N=107 58.9% beats Compendium settled 46% z=2.65 p=0.008; codified D.13.3); ENH-76 + ENH-77 + ENH-88 ENV-DISABLED (commit 2604fc2; build_trade_signal_local.py adds 4 env flags MERDIAN_ENH76_ENABLED=0 MERDIAN_ENH77_ENABLED=0 MERDIAN_ENH88_ENABLED=0 MERDIAN_TIER_MULT_DISABLE=1; defaults disable; re-enable requires N≥30 per-pattern live-cohort validation per §D.13.1; reversible without code change); ENH-97 RE-EVALUATE queue (S29 SHIPPED-AS-LOGGING disposition on 5m-batch cohort preserved; live cohort re-evaluation gated on vol_analytics N≥30 per regime × pattern accumulation ~3 calendar months; ADR-009 §S30 case study codifies); ADR-009 §S30 case study added (cohort-translation save + cohort-prior gate hazard sub-rule in §Phase 1); §D.13 added to Assumption Register (5 rows D.13.1-D.13.5 + 5 open follow-ups + cross-refs). New TDs filed S30 (pending S31+): TD-S30-NEW-3 S1 (OB attachment broken at signal-builder layer); TD-S30-NEW-4 S2 (DTE=0 cohort behavior — gate enabled by ENH-78 PDH conditional sweep; live cohort N too small for verdict, queued); TD-S30-NEW-5 S2 (gate stack inversion on gamma/breadth/vix — investigation queued S31+); TD-S30-NEW-6 S3 (replay_build_trade_signal.py lacks ENH-88 per ADR-008 header line 15; ENH-88 was added S17 after replay was built S24; ~30 min patch); TD-S30-NEW-7 S3 (hold-time bucket study scope — N≥100 per exit-bucket measurement on live cohort). Bug found and closed in-flight: SSH heredoc paste from Windows clipboard to AWS SSM via Get-Content | Set-Clipboard produces line-ending mismatches that bash parses as command boundaries — discovered after first paste attempt cascaded ~200 bash syntax errors; canonical resolution is git pull not heredoc.* v1.20 — 2026-05-16 (Session 29 close). Added: B24 (`.env` edits do not propagate to running processes — token files read at startup; long-running consumer holds value loaded at startup until killed; `.env` changes require consumer restart, full stop; filing rule for any new `.env`-mutation runbook to include "Step Nd — Restart consumers" mandatory clause); B25 (TRUNCATE vs DELETE on bloated tables — for Postgres tables under statement_timeout pressure DELETE itself is what's timing out; TRUNCATE is O(1) DDL primitive; safe only for ephemeral-rolling-window tables like `market_ticks` 10-min window; for any table whose retention horizon is shorter than its read-window-floor, TRUNCATE is canonical recovery, DELETE structurally unsafe); B26 (`pg_cron` failures invisible by default — `cron.job_run_details` records failures but no MERDIAN telemetry polls it; any pg_cron job in production must be accompanied by polling check + Telegram alert within 24h OR explicit entry in operator session-start checklist; workaround SQL provided); B27 (generate-series with `timezone()` cast of wall-clock IST strings produces phantom misses — string parsed as UTC, `timezone()` shifts +5h30m; build expected-time series in UTC at actual UTC times, let join handle TZ conversion in output formatting only; never use `timezone()` cast to construct IST series from wall-clock literal); B28 (regex capture of arbitrary trailing tokens unsafe in command-injection contexts — v1 `migrate_to_pythonw.ps1` greedily captured shell redirection metacharacters as `pythonw` args; explicitly whitelist syntactically-meaningful argument shapes AND blacklist shell metacharacters when re-emitting captured tokens into different execution context). Ten S29 operational findings: compound incidents produce single observable symptoms — don't pattern-match the first hypothesis (2-attempt-no-change rule, generalizes B17); TD body-state must match footer-claim — Doc Protocol v4 candidate Rule N (S18 footer-only-resolved TD-061 stayed Active in body, S23 audit caught the discrepancy, S29 audit caught further drift); Topology audits go stale silently — Task Scheduler audit cadence required as Doc Protocol candidate Rule N; wrapper-to-direct migration: comments vs code state alignment when renaming literals; single-trigger Set-ScheduledTask reliability ≠ multi-trigger (Intraday_Supervisor_Start XML quirk known limitation); TD-094 was stale documentation not a code defect — verify data limitation TDs against current table state before designing around them (vendor data replaced broken S22 Kite backfill; OI populated 99.9%); `option_chain_snapshots` vs `hist_option_bars_1m` is the historical-source-of-truth pivot — live writer's source table is not the backfill writer's source table; `hist_option_bars_1m` 5m-boundary access requires custom index (54.8M rows; PostgREST 8s timeout binding; `ix_hist_option_bars_1m_bar_ts_strike` created S29); live writer net_gex magnitudes regressed on TD-NEW-3 (~10^7 factor too large — Cr conversion missing in live; filed as S30 TD candidate); Phase 0b verdicts on ADR-002 v2 P7 are FAIL not inconclusive — abandon that dimension as a gate, ENH-97 pivots to logging-only. Settled decisions added: TD-061 RESOLVED 2026-05-14 (Task Scheduler pythonw migration — 13/19 tasks on pythonw, 18/19 with Hidden+IgnoreNew, 5 residual flashes are low-frequency `.ps1` supervisors/observers; Code: `migrate_to_pythonw.ps1` one-off, new `run_ict_htf_zones_daily.py` Python orchestrator replacing 3-step `.bat`; Backups: `backups/scheduler/20260514_184211/` + `20260514_190443/`); TD-063 RESOLVED 2026-05-14 (single-instance enforcement via MultipleInstances=IgnoreNew applied to 18/19 tasks; 1 failure on Intraday_Supervisor_Start documented as known limitation); OI-12 + TD-NEW-A RESOLVED 2026-05-14 (market_ticks retention runaway — original pg_cron `delete-old-market-ticks` jobid 45 failing daily for 14+ consecutive weekdays from 2026-04-30 accumulating 62 GB bloat; INSERT timeouts cascaded into 6+ hour breadth outage on 2026-05-14; resolved via `TRUNCATE public.market_ticks` 62GB→856kB in <1s + new cron `prune-market-ticks` jobid 46 `*/30 * * * 1-5` 1-hour horizon; schedule design intentional: cadence 1/day→1/30min, horizon 2days→1hour decouples worst-case DELETE workload from cron cadence); TD-080 PROMOTED 2026-05-14 (Dhan option chain endpoint instability confirmed recurring failure mode — 3 documented occurrences S22 2026-05-07 151/299, S28 2026-05-13 alluded, S29 2026-05-14 99/808; same token same endpoint same alternating-window symptom shape; per-token rate-limit instability hypothesis corroborated; priority promoted to S1/RECURRING; ENH spec for rate-limit-aware retry layer + circuit breaker in `ingest_option_chain_local.py` is P0 carry-forward to S30); TD-083 = TD-NEW-J RESOLVED 2026-05-14 (capture_spot_1m_v2.py recording `OUTSIDE_MARKET_HOURS` as CRASH against `chk_exit_reason_valid` closed-set constraint; patched call-site L346 + docstring L36 to `OFF_HOURS` via `patch_s29_td_new_i_j_v2.py`); TD-NEW-I RESOLVED 2026-05-14 (daily audit thresholds `spot_bars_per_symbol_min` + `market_spot_snapshots_per_symbol` 370→365 — was flagging FAIL on 98% coverage days); TD-NEW-E CLOSED 2026-05-14 (Topology §7.2 task inventory stale 17→19 entries — closed by §7.2 rewrite at S29 close); TD-NEW-F RESOLVED 2026-05-14 (`runbook_update_kite_flow.md` missing Step 2d consumer-restart step; 5 verbatim markdown edits applied at S29 close); ENH-97 verdict (vol_analytics backfill + Phase 0b P7 4-way regime gate test) — **FAIL on ADR-002 v2 P7**; backfill completed (19,520 vol_analytics rows full year); RR-conditional-WR study on 1,968 regime-tagged signals produced χ²=1.56, p≈0.30 across regimes; COMPRESSED-veto binary salvage test FAILED on power (bootstrap CI [-3.36, +17.69]pp includes zero); pivot from gating to logging-only — `vol_analytics` regime column written for future analysis, no signal-time gating; ENH-97 status: SHIPPED-AS-LOGGING; gamma_metrics backfill completing at S29 close for re-test of Phase 0b P5 PINNED dimension on full 14-month cohort (was N=3 in 5-week live cohort, will be N≈1500-2000 on backfilled); new DB index `ix_hist_option_bars_1m_bar_ts_strike` created 2026-05-15 (unblocks per-cycle gamma backfill); MERDIAN_OpenItems_Register_v7 RE-RESOLUTION block appended for OI-12 (permanently-closed marker preserved; original 2026-04-14 closure preserved verbatim per no-crunch). New TDs filed S29 (pending S30+): TD-NEW-B S1 (`pg_cron` failures invisible — needs Telegram alert daemon extension or dashboard widget); TD-NEW-C S2 (`ws_feed_zerodha.py` silent on Supabase 500/token errors; merge with TD-NEW-9 S2); TD-NEW-D S2 (`ws_feed_zerodha.py` log timestamps mislabeled UTC-as-IST); TD-NEW-H S2 (`backfill_volatility_snapshots.py` NULL `expiry_date` schema violation); TD-S30-CANDIDATE (live compute_gamma_metrics_local.py regressed on TD-NEW-3 Cr unit — net_gex stored in raw units not Cr, ratio 10^7 vs backfill — investigate S30); TD-094 RECLASSIFIED-STALE (vendor data replaced broken S22 Kite backfill; OI populated 99.9% across full year; reopen and restate at S30). v1.19 — 2026-05-13 (Session 28 close). Added: B22 (cross-Python-version stdlib semantics — Local 3.12 vs AWS 3.10 `fromisoformat()` microsecond precision incompatibility; canonical normalize-to-6-digits pattern + AWS smoke discipline); B23 (Phase 0 closure exercises surface residual defects — schedule discovery budget alongside execution budget). Six S28 operational findings: AWS shadow architecture silently violated for 13 days due to schema-present-behavior-absent deployment; PGRST204 schema cache requires explicit reload after ALTER; 42P10 ON CONFLICT errors indicate missing UNIQUE constraint after CREATE LIKE without INCLUDING ALL; run_id-keyed retry as canonical backfill recovery pattern; TD-NEW-10 closed filed-in-error as canonical "audited live, confirmed intentional" closure pattern; operational drift after P0 closure is structural session shape — budget for it explicitly. Settled decisions added: TD-NEW-2 + TD-NEW-3 P0 live cycle PASS (S28 09:25 IST 2026-05-12 — flip_level dynamic, net_gex in Cr range, confirms S27 patches working on live data); TD-NEW-12 RESOLVED (shadow architecture restoration — `--shadow` flag plumbed + TARGET_TABLE constant + schema reconciliation 7 cols + UNIQUE constraint; commits `72622a9` + `de23467`); TD-NEW-4 RESOLVED (dte from result.ts.date() not date.today(); bundled commit `72622a9`); TD-NEW-13 RESOLVED (Python 3.10 microsecond normalization in `_dte_from_ts`; commit `447634c`; 60/60 retry success); TD-NEW-5 + TD-NEW-6 + TD-NEW-8 RESOLVED (Pine overlay scheduler chaining + Local MERDIAN_WS_Feed_0900 disabled + AWS WS stop pkill SIGTERM→SIGKILL; config-only no code); P1 broken-window gamma_metrics backfill CLOSED (587/587 rows clean across 2026-05-08 + 2026-05-11; Phase 0b/0c data prerequisite met); P2 Enhancement Register ENH-84 + ENH-85 PROPOSED filings complete (deferred from S27 by operator consent; build deferred to S29+); MALPHA catalogued as third environment in Deployment Topology (§1.5 new + side-by-side updated + Zerodha token flow TD-NEW-7 S1 fix design); `merdian_order_placer.py` catalogued as MERDIAN-AWS-only service (Topology §3 + §7.1 + §8.2; TD-NEW-10 closed filed-in-error + TD-NEW-11 documentation gap closed). New TDs filed S28 (pending S29+): TD-NEW-7 S1 (MALPHA → MERDIAN AWS Zerodha token Supabase-based automation; ~60-90 min spans MALPHA + MERDIAN AWS + Supabase; same pattern as Dhan token); TD-NEW-9 S2 (`ws_feed_zerodha.py` silent-on-success logging; add per-N-tick INFO heartbeat). v1.18 — 2026-05-11 (Session 27 close). Added: B20 (Phase 0 calibration discipline is justified by its first execution — sign audit scoped as 2-hour PASS/FAIL on ADR-002 v2 build path surfaced TWO production bugs TD-NEW-2 + TD-NEW-3 writing bad data for 3+ days; defers ~3-4 sessions of calibration, prevents weeks of wasted build); B21 (unit-scale bugs are silent when all consumers sign-only — TD-NEW-3 wrote 10³-too-large for entire gamma-engine deployment history; surfaced only via cross-comparison to source-material Cr references; when introducing magnitude-consuming gates, audit existing column unit conventions against source-of-truth FIRST before threshold tuning); five Session 27 operational findings (latent algorithm fragility surfaces only under input-shape change — TD-NEW-2 was stable for months until Dhan response shape changed 2026-05-08; defense-in-depth Part A input filter + Part B algorithm hardening shipped to prevent the class; smoke test caught honest framing of fix architecture — Part B alone insufficient against specific bug pattern, honest reframing required Part A necessary + Part B hardening; source-material disagreement is not always inversion — Ref 2 source-vs-MERDIAN disagreement was LIVE-vs-PROJECTED reader-side categorisation; ADR-002 v2 buyer/writer inversion as first-class architectural concept eliminates v1 category error of building writer's intelligence as Phase 3-deferred when same data layer serves Phase 1 buyer with mirrored interpretation; Rule 11 mechanical follow-throughs must execute in same session as ADR acceptance — defer-to-next-session creates Decision Index drift and CLAUDE.md settled-decisions list drift). Settled decisions added: ADR-002 v2 ACCEPTED (six v1 principles preserved + P7 vol-pricing + P8 second-order Greeks + buyer/writer inversion first-class + Positioning Landscape five scalars + six-scenario dealer-flow grid + Phase 0 calibration discipline + Phase 3 four parallel tracks); TD-NEW-2 RESOLVED (flip_level walk-from-ATM + signed_gamma_exposure deep-ITM sanity guard; commit `241f943`); TD-NEW-3 RESOLVED (net_gex /1e7 Crore standardisation across three writer files; commit `241f943`); Phase 0a §3 sign-convention audit PASS (25 of 26 sessions verified correct direction). ENH-84 NEW (vol_analytics + RR ratio Phase 0b dependency) + ENH-85 NEW (vanna/charm Phase 3 prep) filed in ADR-002 v2 scope; formal Enhancement Register filing deferred to S28 P2 carry-forward. *CLAUDE.md v1.17 — 2026-05-10 (Session 26 close). Added: B19 (OI-18 class fix at one site does not close the class — runtime-verify all candidate sites including writer-side helpers, not just grep matches; TD-097 → TD-099 grep audit closed nothing real, TD-101 was the real propagation instance the grep couldn't reach because the anti-pattern was buried in a helper); seven Session 26 operational findings (production cohort trumps research cohort when they disagree, env-flag disablement preserves reversibility for hypothesis-falsified parameters, grep-based "N silent siblings" claims require URL-spy verification before priority assignment, OI-18 unbounded query class extends to writer-side helpers, gates guarded on `not None` writer values need parallel writer-cadence diagnostics, same-session TD close acceptable when discovery also designs the fix, ENH ship gates can be data-availability-gated and dischargeable out-of-band, architecture-defect TDs implementable before formal ADR when Phase α answer in hand). Settled decisions added: TD-101 NEW + RESOLVED same-session (S1 — `build_momentum_features_local.py::get_session_open_spot()` unbounded query; bounded with `gte("ts", today_start_utc_iso)` + limit=20; commit `3cb84e2`; 03:35 UTC threshold preserved per ENH-01/V18G regression history); TD-099 RESOLVED filed-in-error (5 scripts grep-matched were standard-correct, ~3 hours of patching avoided; URL-spy verification rule established); TD-079 RESOLVED (ADR-005 zone validity rewrite — D/W OB/FVG `valid_to=NULL` price-breach only + 1H 1-week tactical fallback; 18 SENSEX zones revived; Pine 36 → 62 zones); TD-080 instrumentation deployed (probe-log table `dhan_token_probe_log` + view `v_dhan_token_probe_today` on Supabase; `pull_token_from_supabase.py` extended 50 → 355 lines with atomic .env write + readback verify + post-write Dhan probes + audit logging + asymmetry verdict; root-cause investigation pending Mon 2026-05-12 first probe-log triage); ENH-88 SHIPPED (BULL_FVG cluster gate, 90-min BULL_OB lookback, `MERDIAN_ENH88_ENABLED` runtime sets cleanly; commit `8407169`); ENH-55 DISABLED by env flag (Exp 20 hypothesis falsified by 24-day production cohort N=44 opposed at 79.5% WR vs Exp 20's claim of opposed at 38.3%; opposition block AND alignment +10 bonus both gated; default OFF; reversible via `MERDIAN_ENH55_ENABLED=1`; commit `5b94c78`); §D.9 added to Assumption Register (5 rows D.9.1–D.9.5 + 4 open follow-ups + ADR-009 first-case-study material). v1.16 — 2026-05-10 (Session 25 close). Added: B18 (anti-pattern bugs ship in N silent siblings — fix all when one is found; TD-097 → TD-099 5-script sweep is canonical); six Session 25 operational findings (boundary disposal needs operator semantic + code dependency check; ict_htf_zones.source_bar_date timeframe-aware semantics; ADR-008 replay as validation tool for code-dependency migrations; TD reframing via cross-script evidence narrows investigation surface; methodology decisions at thin data scales require graduated commitment; CLAUDE.md "settled" bullets are for any durable answer not just ADRs). Settled decisions added: Phase α Q1 ANSWERED (zone validity model — pure price-based with intraday 1-week fallback); Phase α Q2 ANSWERED (AWS migration scope — capture/derived split with four-stage decomposition); Phase α Q3 ANSWERED (sequencing — token reliability FIRST, ADR-006 BLOCKED on TD-080); Phase α Q4 ANSWERED (calibration discipline — graduated-strictness holdout, status quo REJECTED); TD-097 RESOLVED + ENH-96 SHIPPED same-session; TD-080 REFRAMED (AWS Dhan token refresh failure mode); TD-078 RESOLVED (TD-070 v2 verified; ict_htf_zones.source_bar_date convention codified); MERDIAN_PreOpen Local 09:05 IST DISABLED (durable; ret_session anchor migrated 09:05 → 09:08 validated via replay); Topology §9 Q1 + Q2 CLOSED + new §9.A boundary disposal section. v1.15 — 2026-05-09 (Session 24 close). Added: ADR-008 settled decision bullet (replay architecture + what-if methodology canonical home; ENH-93 CANDIDATE → CLOSED via ADR-008; 10 new `*_replay` Supabase tables + 11 new files in `C:\GammaEnginePython\replay\`; zero touch on live; boundary-driven orchestrator). v1.14 — 2026-05-07 (Session 22 close). Added: Rule 24 (upsert ON CONFLICT predicate verification before filter relaxation); B17 (hypothesis exhaustion without controlled test, 3-refutation rule); five Session 21 operational findings (pipeline order detect→upsert→recheck→expire idempotency, Task Scheduler battery flags requirement, PowerShell copy /Y fails use Copy-Item -Force, PowerShell Add-Content after exit /b breakage, single-commit-per-session must commit-with-tag-at-end no exceptions); six Session 22 operational findings (Kite IST timezone bug pattern, vendor endpoint per-endpoint stability, NIFTY Tue / SENSEX Thu expiry calendars, find_dotenv heredoc failure use file approach, MALPHA Kite gateway as Dhan outage recovery path, ADR requirement for architectural decisions). Settled decisions added: TD-070/TD-071/TD-072 RESOLVED Session 21; TD-084 RESOLVED same-session Session 22 (Kite IST timezone bug); TD-079 HIGH zone date-expiry architectural defect filed (pending ADR-005); TD-080 HIGH Dhan option chain endpoint intermittent 401s root cause UNCONFIRMED (P0 reproducer test Session 23 09:15 IST cron); TD-081 HIGH no data-freshness guard between primary ingestion and derived layers; TD-082/083 NEW; TD-073/074/075/076/077/078 NEW from Session 21; ENH-93 CANDIDATE filed (replay/simulation harness exact mimic of live runner cycle per V18G § 7.2 6-step pipeline). v1.13 (Session 20 close) Rule 23 + B15/B16 + 7 operational findings. v1.12 (Session 17 close) Rule 22 + B13/B14. v1.11 (Session 16 close) Rule 21 + B11/B12. v1.10 (Session 15 close) Rule 20 + B9/B10. v1.9 (Session 14 close) Rule 18/19. v1.8 (Session 13 close) Rule 17. v1.7 (Session 11 ext). v1.6 Rules 14/15/16. v1.5 compendium-replicates. v1.4 Python path. v1.3 Rule 13. v1.2 Rule 12. v1.1 Rule 11.*

- **ADR-019 (S58) — signal-subsystem orphans are PORTED, not retired, and retirement requires evidence a capability has NO value — never an experiment verdict.** The three S49 orphans (options_flow + iv_context -> shadow-v3) went dark from a migration-scope wiring gap, not a failed test. Verdicts don't reliably transfer (SMDM scored NEUTRAL then was rebuilt as ENH-SDM). Default for a migration-orphaned subsystem is port-sequenced-behind-a-live-consumer; deletion needs a separate explicit decision once its owning ENH closes.
- **The Zerodha ws_feed is supervised on MERDIAN AWS, NOT MALPHA (ADR-018 D1 corrected, S58).** Unit files are ground truth: `User=ssm-user`, `WorkingDirectory=/home/ssm-user/meridian-engine`. MALPHA is the token gateway only. ADR-018 D1's "MALPHA" was an over-rotation on a memory note, corrected after reading the actual `.service` files. Codified: when docs and a unit/config file disagree on a host, the unit file wins.
- **ENH-SDM is observability-first: a single validated case justifies MEASURING conditions, never ACTING on them (S58).** CASE-2026-06-02 (+71%) is one day; N~8 across the gamma_metrics history; the backward study to grow N is blocked behind a Greeks backfill (purchased chain has 0% Greeks, TD-S58-NEW-1). So ENH-SDM ships as a display-not-gate monitor (per S37 GEX doctrine) and the cohort accrues forward; signal/modes gated on N. Codified: do not build a signal off one case — build the monitor, let the cohort form.

<!-- S58 (2026-06-22): +3 settled decisions (ADR-019 port-not-retire; ws_feed host=AWS not MALPHA; ENH-SDM observability-first). -->

- **`equity_intraday_last` freshness is measured on `ts`, never `created_at` (S59).** The table is upsert-written by `refresh_equity_intraday_last.py`; `ts` moves on every upsert, `created_at` is row-birth and static. Every breadth-reference freshness check (incl. the `eod_health_check` REFERENCE FRESHNESS guard, commit `6b58587`) reads `ts`. Anchoring on `created_at` false-alarms STALE forever. Codified §D.25.1.
- **The breadth prev-close baseline is refreshed by a cron on MERDIAN AWS — `35 3 * * 1-5` (09:05 IST), AFTER the 03:00 UTC token sync (S59).** It was missing from the AWS-only hosts crontab, freezing the baseline 2026-05-20→2026-06-24 and making breadth read BULLISH on down days — a verbatim re-run of **C-09 / ADR-001**. Re-added + verified self-firing 03:35 UTC on the 06-24 open. A stale reference price silently invalidates correct data; suspect baseline freshness first when a derived read contradicts an independent source (VRD/chart). Codified §D.25.2.
- **Daily ICT PDH/PDL are written unconditionally; weekly PDH/PDL stay proximity-filtered (S59, commit `2b40a4b`).** `detect_daily_zones` emits exactly one PDH + one PDL per run, so filtering the fresh pair against the prior-day CLOSE (the pre-open `current_spot`) wrongly dropped down-day PDLs as "already breached"; the daily block no longer calls `filter_breached_zones`. Weekly keeps the filter — `detect_weekly_zones` loops the lookback and emits many PDH/PDL needing the nearest-2 prune. `build_ict_htf_zones.py` is a **Local** Task-Scheduler job, not AWS.

<!-- S59 (2026-06-24): +3 settled decisions (equity_intraday_last freshness=ts; breadth prev-close cron on MERDIAN AWS = re-run of C-09; daily ICT PDH/PDL unconditional, weekly filtered). -->

- **`trading_calendar` is the system-wide holiday trust-anchor; it was wrong for ~3 months and silently defeated every gate (S60, TD-S60-NEW-2).** `trading_calendar.json` held 2 of 15 NSE-2026 equity holidays (Good Friday even misdated 04-18 vs 04-03), so the table read `is_open=true` on every holiday since ~April; the proven marker-writer gate trusted it and passed on Muharram, running the full compute chain on a closed market. Verbatim C-09/ADR-001 at the trust-anchor level. Fixed at source (15 official holidays, commit `bafddc2`, reseeded + stale rows flipped closed). Lesson: a gate is only as correct as its calendar — validate the calendar against the official NSE source before trusting any gate.
- **The canonical holiday gate is `core/trading_calendar_gate.py` — self-sufficient, fail-open; import it, never roll a new inline copy (S60, TD-S60-NEW-3).** It calls `load_dotenv()` itself + raw `requests` + `os.getenv`, deliberately bypassing `core.config.get_settings()`/`SupabaseClient` (which hardcodes a Windows `BASE_DIR` and finds no `.env` on AWS — TD-S60-NEW-5 — so a SupabaseClient-routed gate would silently fail-open every day, a no-op gate). The orchestrator is cut over to it (`38a82ff`); ~28 other bespoke gates migrate incrementally.

<!-- S60 (2026-06-26): +2 settled decisions (trading_calendar trust-anchor / fixed-at-source; canonical core/trading_calendar_gate.py — import not inline). Rule 18 added (calendar trust-anchor). No ADR (bug-fix + helper-consolidation; Rule 10 bar not met). -->

- **ENH-07 A has no live blast radius — the live gamma engine reads VENDOR (Dhan) Greeks; there is no live BS solver and no live risk-free-rate parameter (S61).** The risk-free rate only matters inside the historical Greeks backfill (TD-S58-NEW-1 / TD-095). `core/bs_engine.py` was built + validated (S=25218, K=24000, P=18, T=8/365, r=6.5% → IV=21.45%, γ=0.000134) as that backfill's substrate; ENH-07 A stays a historical/shadow track, not a live-writer change. Codified: don't chase a "live 6.5% rate" — there isn't one; the rate is a backfill parameter.
- **Both `hist_future_bars_1m` and `hist_spot_bars_1m` store IST-clock-as-UTC and pair with ZERO shift (S61, TD-S61-NEW-2).** The assumed −5h30m futures→spot shift was wrong: a UTC-forced diagnostic showed both tables carry hours 9–15 (IST clock mislabeled UTC) and are mutually consistent, so the −5h30m shift dropped 86% of pairs (14% yield) where zero-shift restores ~99%. Refines the TD-087 −5h30m note for the bar-pairing case. Verify the actual clock of BOTH sides before applying any offset.
- **Every signal-feeding reader carries the ADR-018 D2 recency floor — re-audited S61.** Two more floorless readers surfaced and were fixed: `_fetch_options_flow` (ENH-02/04 ±3/4/5 modifiers had run off a ~24-day-stale `options_flow_snapshots` row since S49 — TD-S61-NEW-1, `MERDIAN_FLOW_RECENCY_FLOOR_MIN`=15) and the new `_fetch_basis_context` (`MERDIAN_BASIS_RECENCY_FLOOR_MIN`=15). Basis context is display-only (context-not-gate per S37) — surfaced into `signal_snapshots.raw`, no confidence modifier.

<!-- S61 (2026-06-27): +3 settled decisions (ENH-07 A no-live-solver reframe; hist bar tables IST-clock-as-UTC zero-shift; ADR-018 D2 floor re-audited — options_flow + basis readers). No ADR (Rule 10 bar not met). -->

- **ENH-115 P1 — the FII/DII participant/cash EOD positioning *source* is LIVE; display-not-gate, feeds ENH-116 Lens 3 (S63).** Participant-wise OI is **NSE(NSCCL)-only** (there is no BSE participant-OI equivalent — the BSE participant stub was dropped in-code); FII/DII cash is **ONE consolidated NSE+BSE+MSEI** `fiidiiTradeReact` report. `ingest_participant_positioning.py` writes `participant_oi_daily` + `fii_dii_cash_daily` (+ freshness view `v_participant_oi_latest`) on two Mon–Fri AWS cron lines; a 270-day backfill seeds the ENH-116 expiry-memory cohort. ENH-115 adds only the **source** — the positioning *magnitude* stays ADR-002 v2 `v_dealer_flow_sim`.
- **Historical backfills must NOT trust the DB `trading_calendar` gate — use a local weekday filter + API 404 as holiday ground truth (S63, Rule 18 corollary).** The gate fail-opens on historical dates the (days-220-reseeded) table doesn't cover, inflating a 270-day plan to 382 and running ~400 slow sequential lookups. For any historical backfill, filter Mon–Fri locally and treat the NSE-archive 404 as the holiday signal (MISS ≠ FAIL). Also: PostgREST query params use **list-of-tuples**, never a dict — Python silently drops a duplicate key.
- **SENSEX `compute_flip_level` is regime-conditional — near-spot sign-change walk + short-γ display guard (S63, `dc63bb3`, TD-S62-NEW).** Under NEGATIVE_γ the near-spot region can be a uniform short-γ pit with the only long-γ wall *above* spot; the old ATM-outward cumulative walk fell through to a spurious deep-tail crossing (SENSEX flip −6.75%/−7.11% vs the real ~76,850). The fix scans for the first cumulative-GEX sign change nearest spot and labels the flip ill-defined when there is no clean near crossing. Same failure family as the S41/2026-05-08 deep-strike flip; StockMojo cross-engine parity confirms.

- **The equity daily-breadth chain is DECOUPLED from `equity_eod` — re-fetching EOD candles does NOT rebuild the DMA table (S66).** `equity_eod` (raw OHLC candles, written by `ingest_equity_eod_local.py`) and `breadth_indicators_daily` (per-ticker DMA10/20/40 + prev_close + above-flags, the table WCB/Lens-2 actually reads via `build_wcb_snapshot_local.py`) are separate writers. Running the EOD ingest laps advances candles, not the per-ticker DMAs — the DMA layer has its own (currently frozen-since-06-04, unidentified) builder. Do not conflate them; the Lens-2 unfreeze is the DMA builder, not the candle sweep (TD-S66-NEW-4).
- **A Dhan `DH-901` 401 from one writer while probes/other endpoints return 200 is token-TIMING, not token-expiry (S66).** `ingest_equity_eod_local.py` reads `DHAN_API_TOKEN` as a module-level global at import (L18); a run that crosses the daily rotation sends a token that rotated underneath it and 401s the whole sweep, silently, while the `.env`/Supabase token is healthy and `/v2/charts/historical` serves candles. The token pipeline (`pull_token_from_supabase.py` daily refresh + `refresh_dhan_token.py` 03:05 self-mint, both write Supabase; `.env` atomic-write+readback) is sound. Fix = read the token at USE, not at import (TD-S66-NEW-1). Do not chase the token when one endpoint 200s and another 401s the same minute.
- **Freshness guards must measure per-ticker COVERAGE %, not table-MAX trade_date (S66).** A `breadth_indicators_daily`/`equity_eod` table can read “fresh to 07-05” on its max date while ~91% of tickers are a month stale — the exact S66 freeze. `build_daily_map`'s floorless `>=`-max served 34-day-old DMAs as current and reported 98.3% “coverage” (TD-S66-NEW-2), and the just-shipped `check_eod_coverage_freshness.py` reads table-max so it flashed green over it (TD-S66-NEW-3). ADR-018 D2's recency-floor applies to per-ticker readers too; add a coverage-fraction dimension.
- **`v_expiry_base_rates` is live-cohort-only; the seed is degenerate by construction (S66, `4d25733`).** The 65-row seed came from `backfill_expiry_outcomes.py` calling `reconcile(net_gex_regime, "NEUTRAL")` with `div` HARDCODED NEUTRAL ([M4], breadth/participant NULL for the historical window) — DISTRIBUTION/ACCUMULATION/DIVERGENT are structurally unreachable, every seed row is RANGE/ALIGNED, and `--force` reproduces the identical all-RANGE table. Seed re-label is a DEAD END; the view is scoped `source = 'expiry_memory_live'` so the four-lens note never blends with gamma-only seed rows. Base rates fill by forward accrual only. Phase-B engine IS built (`1da0af6`/`1be5239`) — “get latest and hold” was already the design.
- **Diagnose by running the failing call, not by inferring — and verify column/table names against live schema before querying (S66).** The S66 breadth-freeze diagnosis walked down SIX wrong theories (separate-chain / lap-timing / cursor-writeback / expired-token / clobber-race / wrong-URL), each killed by the next query; one real POST to the failing endpoint would have collapsed the tree turns earlier. The session ended on a hallucinated `equity_eod.trade_date` column (does not exist) — pull `information_schema` before writing SQL against an unfamiliar table.

<!-- S63 (2026-07-02): +3 settled decisions (ENH-115 P1 participant/cash EOD source live — display-not-gate, feeds ENH-116 Lens 3; historical-backfill calendar-gate corollary to Rule 18 — local weekday filter + API 404; SENSEX compute_flip_level regime-conditional fix). No ADR (bug-fix + ENH build + doc task; Rule 10 bar not met). -->
<!-- S64 (2026-07-04): +5 settled decisions (ENH-116 built/deployed end-to-end; ast.parse-alone-insufficient live case; Marketview public+TLS+Google-auth at marketview.meridianalpha.in; Marketview v5 six-page terminal). No ADR (ENH build/deploy + frontend + infra + carry-in bug-fix; Rule 10 bar not met). -->
<!-- S65 (2026-07-07): +3 settled decisions (Dhan EOD publish-lag is expected not a stall / compute_date_window T-1/220 correct; EOD coverage guard denominator = active-universe ~1,159 not nominal 1,385 + staleness off last trading day w/ ~3-day Dhan-lag tolerance [TD-S65-NEW-1]; OAuth new-client rotation changes id+secret, exposure closes on old-client delete [ENH-117 security fold]). No ADR (OAuth rotation + banner prompt + breadth diagnosis + guard-tune; Rule 10 bar not met). -->
<!-- S66 (2026-07-09): +5 settled decisions (equity_eod<->breadth_indicators_daily DECOUPLED, re-fetching candles != DMA rebuild; DH-901 from one writer while others 200 = token-timing not expiry, read-token-at-use; freshness guards measure per-ticker coverage% not table-max; v_expiry_base_rates live-cohort-only, seed degenerate by construction [M4], re-label dead end; diagnose by running the failing call + verify schema before SQL). No ADR (guard tune + view segregation + breadth diagnosis; Rule 10 bar not met). -->
