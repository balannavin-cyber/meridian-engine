# MERDIAN Decision Index

**Market Structure Intelligence & Options Decision Engine**

---

| Field | Value |
|---|---|
| Document | `MERDIAN_Decision_Index.md` |
| Location | `docs/decisions/` |
| Type | Flat lookup of every accepted architectural decision |
| Established | 2026-05-09 (Session 23 — ADR-007 acceptance, Doc Protocol v4 Rule 9.3 introduction) |
| Update rule | Mechanically prepended on every accepted ADR per Doc Protocol v4 Rule 11.1 |

---

## Purpose

A single flat table of every architectural decision MERDIAN has accepted. Two uses:

1. **Pre-flight check at session start.** Before proposing an architectural change, scan this index for "have we decided this already?" Saves session time on re-litigation. Used by future Claude as part of session-start read order (CLAUDE.md Rule 0).
2. **Audit trail across versions.** Each row links back to the full ADR, where the context, evidence, alternatives and consequences live in detail. The index itself is one row per decision; the ADR is the body.

This is not a substitute for the ADRs themselves. It is the lookup that makes the ADR collection navigable.

---

## Conventions

- **Newest first.** New entries prepend at the top of the table. Order = reverse-chronological by Date.
- **One row per accepted ADR.** Rejected ADRs do not enter the index (their status remains in the ADR file itself for audit). Superseded ADRs stay in the index with a `[SUPERSEDED by ADR-NNN]` annotation in the Decision column.
- **Mechanical sourcing.** Each row is sourced from the metadata header + Decision section + Alternatives section + Governance language footer of the ADR. Rule 11.1 specifies the field-to-column mapping.
- **One-liner discipline.** Decision / Rationale / Rejected columns each fit on one line. Long-form lives in the ADR.
- **CASE files separate.** Single-event case studies (`CASE-YYYY-MM-DD-<topic>.md`) are not entered here. They live in `docs/decisions/` alongside ADRs but are diagnostic records, not architectural decisions.

---

## Index

| ID | Date | Topic | Decision | Rationale | Rejected alternative | Source |
|---|---|---|---|---|---|---|
| **ADR-018** | 2026-06-19 | Breadth-Feed Supervision Model & Signal-Subsystem Disposition | (D1) `ws_feed_zerodha.py` re-homed to canonical host MALPHA under a `systemd` unit (Restart=on-failure + single-instance enforcement + journald) — one host owns the Zerodha session end-to-end; WCB cron arg fixed same pass. Units + wsfeed preflight + alert were already built+committed in S56 (`deploy/systemd/`, afe8112/30cca59/b627914); remaining work is enable/cutover onto MALPHA, not build (TD-S57-NEW-1). (D2) every reader of `market_breadth_intraday`/WCB/`structural_divergence_snapshots` applies a recency-floor guard on `fetch_latest_row` so a silent upstream stop self-flags STALE — MANDATORY, precondition for ENH-SDM. (D3) SMDM RETIRED as built (evidence-based vs ENH-30): drop STOP_HUNT/SQUEEZE flags + gamma-squeeze scalar (Thesis B); carry manipulation-footprint (A, redefined) + flow-velocity (C). (D4) ENH-SDM `compute_structural_divergence_local.py` → `structural_divergence_snapshots` (4 primitives + phase classifier + direction; offensive/defensive modes) placed AWS orchestrator-integrated per ADR-006 — kept subsystem, NOT a Local orphan. | S57 found `ws_feed_zerodha.py` running on AWS (not MALPHA as documented) since 06-11 in an unsupervised `screen`, holding an expired Zerodha token, 403-looping, writing zero-coverage rows — a 23-day silent breadth outage, exactly TD-NEW-L's predicted failure mode. SMDM Exp 9 NEUTRAL + gamma-squeeze scalar redundant vs the gamma engine; only the SEBI-documented manipulation-footprint + flow-velocity are defensible, and they map to capturable primitives. | Cron watchdog on the screen (rejected — shares the feed's failure chain, the S53 watchdog-and-patient lesson); re-home feed to AWS under systemd (rejected — keeps the Zerodha session split across two hosts, the root of the expired-token loop); port SMDM to AWS as-is (rejected — Exp 9 NEUTRAL, no demonstrated edge); deprecate SMDM with no replacement (rejected on Thesis A only — discards the one measurable, SEBI-documented footprint). | [ADR-018-breadth-feed-supervision-and-signal-subsystem-disposition.md](./ADR-018-breadth-feed-supervision-and-signal-subsystem-disposition.md) |
| **ADR-017** | 2026-05-26 | Operator Console Design Principles | Six principles + keyboard corollary codifying decision-surface design (three-filter rule: cut implicit + redundant + systemic; role contract Marketview/Health/Settings; asymmetric surfacing silence=healthy; confluence-as-bidirectional-context can confirm OR invalidate; motion replaces timestamps; distance×time salience for PIN/ACCEL; keyboard-first ergonomics) | Live integration check of S37 substrate validated PIN-edge framing across full trading day; consolidation debt surfaced via operator switching between 3 dashboards + external VRDNation; six principles emerged from operator's actual decision flow + Exp 53 PnL-framing rule re-instance | Per-screen ad-hoc UI design (rejected — produces 3-dashboard consolidation debt); single screen with mode toggles (rejected — violates role contract); per-feature dashboards (rejected — violates three-filter rule) | [ADR-017-operator-console-design-principles.md](ADR-017-operator-console-design-principles.md) |
| **ADR-015** | 2026-05-25 (decided + documented S37) | Per-strike GEX schema v2 — supersedes ADR-014 same session | 12-column minimum-sufficient-statistic schema for `gex_strike_snapshots`: `(run_id, strike, expiry_date, symbol, ts, spot, gamma_call, gamma_put, oi_total_calls, oi_total_puts, gex_cr, source_table)`. Sign convention: positive `gex_cr` = dampening (CE +base, PE -base). UNIQUE (run_id, strike, expiry_date). Drops 4 derived columns from ADR-014 v1: `gamma` (replaced by split `gamma_call`/`gamma_put` to preserve IV skew signal), `is_local_max`, `is_flip_zone`, `is_pin_candidate_bool` (boolean derivations had near-zero information content — fired on 39% NIFTY rows / 20% SENSEX rows respectively, indiscriminable noise at write layer). Derivations belong in read layer (SQL views like `v_gex_strike_pin_zone`), not write layer. §F1 dealer-vs-positioning GEX split deferred — `v_oi_prev_close_snapshots` shipped as scaffold for future integration via OI-change history. Writer patched into `compute_gamma_metrics_local.py` via `patch_s37_enh80_writer_v2.py` (marker `# ENH-80 (S37) v2`, backup `_PRE_S37_v2.py`). | S37 v1 writer (ADR-014) deployed clean smoke-fire (NIFTY abs_diff 9.8e-11 Cr, SENSEX abs_diff 1.2e-14 Cr) — fidelity OK at total-net-GEX level. Post-deploy quant review: derived booleans fire on 39% NIFTY rows / 20% SENSEX rows producing marker noise — booleans designed as "strike has structural significance" markers but threshold relativity makes them positive on too many rows to discriminate. IV skew evidence at NIFTY 24050 ATM: gamma_call=0.00164, gamma_put=0.00234 (+43% PE-richer = put-skew regime). Single `gamma` column collapses this signal; split preserves it. Operator's "Meridian doesn't need anymore gates" framing (S37 GEX-as-context-not-gate decision) reinforces minimum-sufficient-statistic at write layer — fewer columns to maintain, derivations computed at query time. Schema v2 re-fire on smoke-fire data: NIFTY 144 strikes / 96 call + 73 put gamma coverage (deep-ITM strikes only have one liquid side, expected); SENSEX 261 strikes / 149 call + 92 put. | Keep ADR-014 v1 boolean derivations (rejected — 39%/20% fire rate at write layer is anti-signal, derivations should be view-layer not column-layer); single `gamma` column without split (rejected — collapses IV skew signal real and material, gamma_call/put split preserves it at minimal storage cost); add full Greeks (delta, vega, theta) to schema (rejected as bloat — derivable from spot + strike + premium, write layer should not duplicate); compute dealer-vs-positioning GEX split inline (deferred to ADR-015 §F1 — needs OI-change history; `v_oi_prev_close_snapshots` scaffold shipped, writer integration future work). | [ADR-015-per-strike-gex-schema-v2.md](./ADR-015-per-strike-gex-schema-v2.md) |
| **ADR-014** | 2026-05-25 (accepted + superseded same session S37) | Per-strike GEX schema v1 — **[SUPERSEDED by ADR-015]** | Original 16-column schema for `gex_strike_snapshots` table with derived boolean columns `is_local_max` / `is_flip_zone` / `is_pin_candidate_bool` and single `gamma` column. Sign convention: positive `gex_cr` = dampening (CE +base, PE -base). UNIQUE (run_id, strike, expiry_date). Writer first deployed via `patch_s37_enh80_writer.py` (marker `# ENH-80 (S37) v1`). | Smoke-fire passed cross-symbol (NIFTY 144 strikes abs_diff=9.8e-11 Cr, SENSEX 261 strikes abs_diff=1.2e-14 Cr) confirming fidelity at total-net-GEX aggregate level. Status SUPERSEDED post smoke-fire upon quant review surfacing boolean-derivation marker noise (39% NIFTY / 20% SENSEX fire rate, near-zero information content) — see ADR-015 Rationale. | N/A — same-session supersession by ADR-015. v1 preserved in git history; v2 replaces it before any consumer code locks against v1 schema. | [ADR-014-per-strike-gex-schema.md](./ADR-014-per-strike-gex-schema.md) |
| **ADR-012** | 2026-05-24 (decided + documented S34) | Spot-anchored stop-loss doctrine for ICT retest entries | For ICT retest entries on zone primitives (OB, FVG, Breaker, Mitigation, Premium/Discount), the stop-loss is anchored to spot zone-invalidation on 5m-close-through, NOT to option premium decay. BEAR retest stopped out when 5m spot bar closes at price ≥ `zone_high * (1 + X)`; BULL retest stopped out when 5m spot bar closes at price ≤ `zone_low * (1 - X)`. Buffer X = 0.5% provisional default (initial empirical bound 0.3-0.8% across S34 cohort, none triggered at any level). Premium-based stops on ATM held strikes are the wrong instrument-stop pairing — measure microstructure noise (bid-ask, theta, IV crush) not thesis validity. Time-based exit policy (ENH-107) is orthogonal and composes independently. Implementation: signal-emitter computes `sl_level` at signal time using `zone_high`/`zone_low` already exposed on `ict_primitives`; order placer subscribes to 5m bar-close events and submits exit market order when triggered; Pine overlay renders SL level as horizontal line for discretionary corroboration; backtest infrastructure walks `hist_spot_bars_5m` from `first_retest_ts` forward; `ict_primitive_outcomes` schema adds `sl_triggered_ts`/`sl_exit_prem`/`pnl_with_sl_pct` columns (sub-task under ENH-108 or standalone follow-on ENH). Validation cohort n=7 (S34 same-day retest backtest restricted to pre-Apr-2026 by TD-S34-NEW-4 chain coverage gap); doctrine accepted as default rule on signal strength but parameter X provisional until n≥50. | S34 backtest of 7 same-day ICT FVG retest entries (filtered from 22 cohort by TD-S34-NEW-4): all 7 directionally correct; held-to-EOD total +₹106,888 on 1 lot (NIFTY ₹87,266 + SENSEX ₹19,622). SL variant comparison: 5% intra-bar wick → 0/7 winners, mean -8.13 pts; 10% intra-bar → 0/7, mean -16.30; 15% intra-bar → 3/7, mean +197.66; 5% 1m-close-through → 0/7; 10% 1m-close-through → 3/7. Spot-anchored SL variants: 0.3% adverse 5m-close → 0/7 SL triggers, 7/7 winners; 0.5% → 0/7 triggers; 0.8% → 0/7 triggers. The tightest spot SL never triggered on any of the 7 retest entries — once price has tagged the zone and begun rejecting, it does not return through within the same-day window. Premium wicked 5-15% on the same trades from microstructure alone; spot moved monotonically in thesis direction throughout the day. The retest is structurally the moment of maximum local risk; once survived, the trade is what it is. SL must measure thesis failure (close-through zone invalidation), not microstructure noise. Codified consistent with ADR-004 §10 primitive breach rule template ("a primitive is BREACHED when spot closes beyond the invalidation boundary on the timeframe of the primitive"). | Premium SL at 5% (rejected — eliminated 100% of structurally-correct winners; wick noise on 1m option lows on ATM held strikes routinely consumes 5-15% in first 30-60 min from bid-ask + theta + IV crush, uncorrelated with thesis); premium SL at 10% (rejected — same problem, 0/7 winners); premium SL at 15% (rejected — partial save at 3/7 but still asymmetric instrument-stop pairing, and depends on premium volatility regime not on thesis); 1m-close-through SL on premium at 5%/10% (rejected — same architectural problem, slightly delayed); wider premium SL (Compendium-era 30%+) (rejected as canonical default — approximates correct intent indirectly but couples SL hit rate to volatility regime, expiry distance, and bid-ask conditions unrelated to structural premise); no SL at all (rejected — leaves position uncapped on thesis-invalidation days; spot SL provides the structural cap without coupling to microstructure). | [ADR-012-spot-anchored-sl-doctrine.md](./ADR-012-spot-anchored-sl-doctrine.md) |
| **ADR-011** | 2026-05-22 (decided + documented S33) | Chain-table held-strike ATM PnL — reverses ADR-010 v3 §"Source table choice" | ATM PnL across horizons (formation-anchored ENH-100 + retest-anchored ENH-103, 9 columns total) reads from `hist_option_bars_1m` chain (54.8M-row dense table) with manual ATM strike picking via existing `STRIKE_INTERVAL` constant (NIFTY/50, SENSEX/100), empirical expiry calendar via DISTINCT on `hist_atm_option_bars_5m.expiry_date` (handles NSE/BSE 2025-09-01 weekday swap + ~15% holiday-shifted weeks + same-week double expiries), and held strike across all horizons (no vendor-roll attrition). Per-tuple range prefetch architecture (~700k-970k chain bars per symbol across 839-1417 unique tuples in ~10 min each). Schema (13 columns on `ict_primitive_outcomes`) UNCHANGED — this is a source-table + compute-method reversal not a schema change. Implemented via ENH-106 v7 writer (`build_ict_primitives.py` with marker `ENH-106 (S33) v7`); v6 helpers `_atm_anchor_at` + `_atm_future_at` retired as dead code; v3 trailing for-loop in `compute_atm_pnl_and_dte` unreachable. | Post-v7 backfill (NIFTY+SENSEX 2025-04-01→2026-05-19, 19,399 outcomes in 1775.9s) showed coverage 20-44× lift on populated cells (NIFTY M5 BEAR 85→2650, SENSEX M5 BULL 60→2662) and median PnL flipped from universally negative (v6) to positive on every directional cell with real spot edge: NIFTY M5 BULL_OB +17.2% median 30m / 82.1% opt WR / N=28; NIFTY M5 DISPLACEMENT_DOWN +14.8% median / 77.4% / N=93; NIFTY H DISPLACEMENT_DOWN +34.7% median / 100% / N=17. The v6 "universally negative median" was an artifact of same-strike enforcement applied to vendor's rolling-ATM source — same-strike check filtered the very primitives where spot moved enough for the edge to play out (self-selection bias against winners). Held-strike chain reads recover those primitives + reflect what a trader actually experiences. Retest cohort (now at 65% coverage from 12%) confirms ADR-009 §Phase 1 H-FVG retest finding cross-symbol with held-strike option PnL — strengthens D.14.1 from spot-WR-only to spot-WR + option-PnL. M5 OB retest paradox confirmed at option-PnL level — falsified as edge (median 30m negative across NIFTY+SENSEX M5 OB retest cells). DTE structure discovered: DTE=0 feast-or-famine exit-at-30m, DTE=1-2 sweet spot, DTE≥3 best for H-TF. | Keep v6 architecture accepting attrition (rejected — diagnostically misleading + non-random self-selection against winners); drop same-strike enforcement to mirror Exp 2 methodology (rejected — comparing two different vendor-picked strikes' premiums is not tradeable PnL); use chain table but keep vendor's atm_strike at anchor (rejected — vendor's pick is identical to manual rounding in nearly all cases + couples writer to aggregated table); add cross-tier delta column (rejected as bloat — post-rebuild cohort IS the diff). | [ADR-011-chain-table-held-strike-atm-pnl.md](./ADR-011-chain-table-held-strike-atm-pnl.md) |
| **ADR-004** | 2026-05-20 (decided S31-A) · 2026-05-21 (IMPLEMENTED via Wave 1 S31-B) | ICT Canonical Primitives (Wave 1 = 5 of 15 primitives) | Wave 1 implements 5 canonical ICT primitives end-to-end: OB §5.1 (body-only bounds, close-through-only breach rule, wickExtended toggle deferred to wave 1.5); FVG §5.2 (3-bar gap canonical detection with retest semantics distinguishable from formation); PDH/PDL §6.1 (prior day high/low with date-expire); Sweep §7.1 (high/low sweep with dedup patch); Displacement §7.2 (high-velocity directional candle). Schema: two tables `public.ict_primitives` + `public.ict_primitive_outcomes` with FK CASCADE (UNIQUE constraint reformulated as UNIQUE INDEX `ict_primitives_unique_per_source` per §10 erratum because Postgres rejected COALESCE in UNIQUE). Backfill 2025-04-01→2026-05-19 produced NIFTY 8838 + SENSEX 10561 primitives + 1:1 outcomes (38,798 total rows) in 156.3s. ADR-009 Phase-1 holdout split executed same-session as build — H FVG retest holds AND strengthens on holdout across 4 buckets cross-symbol cross-direction; M5 FVG standalone refuted as coin-flip. | S31-A spec authored ADR-004 §14 sign-off Wave 1 = 5 primitives; S31-B Task 4 backfill execution validated end-to-end build (detector → writer → DDL → backfill → outcomes); ADR-009 Phase-1 holdout split on resulting cohort produced the headline H-FVG-retest validation that justifies the entire S31-B effort; data layer is unblocked for ENH-100 magnitude extension + ENH-101 stop-loss optimization + ENH-102 H-FVG-retest live routing. | "ICT canon deviations only" (the original Reserved scope from CLAUDE.md notes: D-OB definition, D-zone validity, PDH/PDL ±20pt hardcoded — TD-049/050/051) — rejected as too narrow: S31-A scope expansion to canonical primitives Wave 1 covers the deviation set plus all primary ICT primitive types in a unified table layer; deviations are a subset of the canonical spec; building deviation-only would have required a second table layer for canonical primitives later. Maintain V18F detector layer as-is — rejected: V18F `detect_ict_patterns.py` writes to `ict_zones` (zone-life table) not a primitive event-log table; outcomes characterization (holdout validation, magnitude, ATM PnL, stop optimization) requires per-event row not per-zone row. | [ADR-004-ict-primitive-canon.md](./ADR-004-ict-primitive-canon.md) |
| **ADR-002 v2** | 2026-05-11 | Market structure philosophy v2: force, zones, vol-pricing, buyer/writer inversion | Six v1 principles preserved (P1 zones, P2 force, P3 trapped sellers, P4 velocity, P5 PINNED, P6 DTE multiplier); two new — P7 vol-pricing (RR ratio thresholds HIGH>1.2 / FAIR / LOW<0.85 / COMPRESSED<0.4) and P8 second-order Greeks (vanna/charm for Phase 3); buyer/writer inversion named as first-class architectural concept (same gamma layer, mirrored interpretation by strategy mode); Positioning Landscape refined to five named scalars (gamma_wall_strike, short_strike_for_strike, gradient_max_strike, hedged_long_cr, lambda_score_pct); six-scenario dealer-flow grid (±0.5%, ±1%, ±2% with contracts + Cr + trajectory); new `vol_analytics` table spec; methodology specifications mandatory before build (zone-bound definition, dealer-flow assumption stack, sign-convention audit, λ-score derivation, RR-window); Phase 0 calibration discipline introduced (~3-4 sessions before ENH-80 schema lands); Phase 3 prerequisites named explicitly — four parallel tracks beyond P1-P8 (risk framework, multi-leg execution, SPAN+ELM margin model, tax model). Supersedes ADR-002 v1 (file replaced in place; v1 preserved in git history at b46249e and prior). | Source-material re-read Session 27: PDF article opens with five operational questions (one — 'when does IV stop matching realized movement?' — has no v1 analog); dashboard's Positioning Landscape view names five concrete strike-level metrics more usable than v1's generic zone bounds; dashboard tracks RR ratio as primary signal with explicit thresholds; dashboard exposes vanna as separate Greek panel; Twitter commentary makes buyer/writer inversion explicit ('if 23719 breaks, dealers forced to sell futures — that's where panic enters the system'); v1 specified columns but no algorithms (practitioner inheriting v1 cannot reproduce metrics). Phase 0a §3 sign-convention audit Session 27 PASS validates foundation soundness. | Maintain ADR-002 v1 as-is — rejected: six material additions identified in source material, three of which (P7 vol-pricing, P8 vanna, buyer/writer inversion as first-class) are non-additive to v1; v1's framing treats writer's intelligence as Phase 3-deferred when same data layer serves Phase 1 buyer with mirrored interpretation; v1 risks category error of building 'writer's intelligence' separately from buyer's. | [ADR-002-market-structure-philosophy.md](./ADR-002-market-structure-philosophy.md) |
| **ADR-008** | 2026-05-09 | Replay architecture: zero-touch parallel-pipeline sandbox for what-if signal experiments | Build parallel `*_replay` table layer (10 mirrors via CREATE TABLE LIKE INCLUDING ALL) + parallel `replay/*` script layer (7 scripts mirroring live counterparts); CLI `--replay-ts` time injection; out-of-hours hard guard 08:00-16:30 IST weekdays; orchestrator runs scripts in V19 §5.2 order PER BOUNDARY (not script-by-script across boundaries); validation philosophy is replay-vs-replay with one variable changed, NOT replay-vs-live. | ENH-93 CANDIDATE matured into requirement for what-if signal-logic experiments. Live signal_snapshots cannot be replayed under modified code without touching production. Phase 4b 2026-05-07 full-day run: 1056/1064 invocations (99.2%) succeeded; per-script success ≥95% across all 7. NIFTY direction-of-edge match with live: 100% gamma_regime, 68% direction_bias (32% divergence traces to documented spot-granularity property). | Re-run live ingest in sandbox (rejected — wall-clock dependent + side effects); monkey-patch live scripts (rejected — zero-touch violation, diagnostic blast radius); single-script replay (rejected — gates need full upstream chain); live-table re-write with `is_replay` flag column (rejected — coupling + regression risk). | [ADR-008-replay-architecture.md](./ADR-008-replay-architecture.md) |
| **ADR-007** | 2026-04-11/12 (decided) · 2026-05-09 (documented) | V18F ICT signal architecture pivot | ICT pattern detection becomes primary signal trigger; Kelly tier sizing replaces uniform confidence-based sizing; LONG_GAMMA / NO_FLIP gates preserved; CONFLICT terminal rule lifted; VIX>20 gate removed; MIN_CONFIDENCE 60→40; confidence-score becomes adjustment layer; T+30m exit confirmed. | Exp 15: pure ICT 86–94% standalone WR on OB patterns. Exp 17/19: LONG_GAMMA symmetric block correct (47.7% WR — below random). ENH-35 full-year backtest: NIFTY 244 signals, 58.6% T+30m accuracy. Phase 4 promotion gate met (V18G §10.1). | Implement V15.1 spec'd remediation in full (three-zone gamma + multi-horizon voting + regret-log threshold gate within confidence-scoring architecture) — rejected: Exp 17/19 evidence shows LONG_GAMMA is correctly binary; Exp 20 evidence shows momentum is confirmation not vote-member; the V15.1 path leaves discrete ICT-pattern edge on the table. | [ADR-007-v18f-ict-pivot.md](./ADR-007-v18f-ict-pivot.md) |
| **ADR-002** | 2026-04-28 | Market structure philosophy: force over direction, zones over points, scale over time | **[SUPERSEDED by ADR-002 v2]** Six principles (P1–P6) govern the gamma layer and signal engine: P1 markets are zones not points; P2 force over direction; P3 GEX walls are non-stationary; P4 dealer flow drives intraday structure; P5 PINNED gamma regime as new state class; P6 capital scaling demands depth-aware position sizing. Companion to ADR-001. | Practitioner-validated dashboard analysis (Session 12). MERDIAN's V15.1 Appendix D assumption gaps (binary regime, point-based proximity). Capital scaling reality — strategy must evolve as AUM grows. | Maintain V15.1 binary regime + point-based proximity computations as "good enough" — rejected: Appendix D explicitly flagged these as unvalidated, and the practitioner dashboard's success demonstrated the premium of zone- and force-based reasoning. | [ADR-002-market-structure-philosophy.md](./ADR-002-market-structure-philosophy.md) |
| **ADR-001** | 2026-04-23 | Stable lies defeat duration gates: validity requires cross-reference | All MERDIAN gates (shadow gates, promotion gates, validation gates) MUST pair stability testing with validity testing before any component is promoted. Validity layer = cross-reference against external independent source OR internal consistency rule. Validity checks run every cycle, not just at gate boundaries. | Session 7 root-cause: 27-day breadth cascade failure. `equity_intraday_last` reference froze 2026-03-27 when ingest_breadth_intraday_local.py was retired without writer-replacement. Every breadth cycle for 27 days produced fabricated BULLISH 92.x readings. 10-session shadow gate passed during the corruption — duration gate cannot detect a stable lie. | Lengthen duration gate (e.g. 20+ or 30+ sessions) — rejected: more sessions of corrupted data cannot surface corruption. The lever is cross-reference, not duration. | [ADR-001-stable-lies-defeat-duration-gates.md](./ADR-001-stable-lies-defeat-duration-gates.md) |

---

## Pending ADRs — reserved IDs

These IDs are reserved for upcoming architectural decisions. They are not yet drafted; they are placeholders called out in CLAUDE.md to prevent ID collision.

| ID | Topic | Owner | Status |
|---|---|---|---|
| ADR-003 | Phase 1 closure (Operational Core complete declaration) | Navin / Claude | Reserved per CLAUDE.md notes |
| ADR-004 | ICT Canonical Primitives (Wave 1 = OB §5.1 + FVG §5.2 + PDH/PDL §6.1 + Sweep §7.1 + Displacement §7.2; 5 of 15 primitives) | Navin / Claude | **IMPLEMENTED via Wave 1 — S31-A spec → S31-B build (2026-05-21)**. ADR scope expanded from original Reserved framing (deviations-only: D-OB definition, D-zone validity, PDH/PDL ±20pt hardcoded — TD-049/050/051) to canonical primitives Wave 1 covering deviation set plus all primary ICT primitive types in unified table layer. Two tables shipped: `public.ict_primitives` + `public.ict_primitive_outcomes` with FK CASCADE (UNIQUE constraint reformulated as UNIQUE INDEX `ict_primitives_unique_per_source` per S31-A §10 erratum). Backfill 2025-04-01→2026-05-19 produced NIFTY 8838 + SENSEX 10561 primitives + 1:1 outcomes (38,798 total rows) in 156.3s combined. ADR-009 Phase-1 holdout split executed same-session — H FVG retest holds AND strengthens on holdout 4 buckets cross-symbol cross-direction (NIFTY/SENSEX × BEAR/BULL, deltas +10.2 to +25.7pp); M5 FVG standalone refuted as coin-flip at-or-below baseline. Wave 2 (10 remaining primitives) deferred to later session. Task 5 Pine v6 visual overlay descoped per TD-S31B-NEW-1 (6 Pine v6 ergonomic walls; architectural redesign required). Decision Index ADR-004 IMPLEMENTED row prepended above ADR-002 v2 per Rule 11.1; CLAUDE.md settled-decisions appended per Rule 11.3. |
| ADR-005 | Zone validity model (price-based canonical, timeframe-tiered date fallback intraday-only — TD-079) | Navin / Claude | **Phase α Q1 answered S25 2026-05-10**: (a) pure price-based canonical with timeframe-tiered fallback for intraday only — 1H OB/FVG price-breach OR 1 week (whichever first); D/W OB/FVG price-breach only (`valid_to=NULL`); PDH/PDL date-expire unchanged. Three implementation actions: `expire_old_zones()` rewrite by `(pattern_type, timeframe)`, `recheck_breached_zones()` as primary D/W transition, backfill pass for D/W zones currently date-EXPIRED unbreached. Independent of ADR-006 — can draft anytime. |
| ADR-006 | AWS migration scope (what runs on AWS vs Local — durable boundary) | Navin / Claude | **Phase α Q2 answered S25 2026-05-10**: (a) capture/derived split, with four-stage decomposition — Capture (`market_spot_snapshots`, `option_chain_snapshots`, `india_vix`, `market_breadth_intraday`, `ict_htf_zones`) → AWS canonical, no Local writers. Derived (`gamma_metrics`, `volatility_snapshots`, `momentum_snapshots`, `market_state_snapshots`, `signal_snapshots`) → Local canonical for production; AWS shadow continues writing to `*_shadow` tables. Orchestration (runner) → both Local (production) and AWS (shadow) parallel. Operator-facing tooling → Local only. **Phase α Q3 answered S25 2026-05-10**: (a) token reliability FIRST, ADR-006 actions second — investigate `refresh_dhan_token.py` failure mode (TD-080) → fix → observe N clean trading days → only then execute ADR-006 disposals. Local writers stay as redundancy until AWS reliability empirically established. **Drafting blocked on TD-080 closure.** |
| ADR-009 | Calibration discipline — walk-forward holdout vs single-cohort | Navin / Claude | **Phase α Q4 answered S25 2026-05-10** (operator deferred to architect recommendation): graduated-strictness holdout. Phase 1 (now → ~April 2027 / Y2 close): mandatory holdout split for new parameter changes scaled by N — N≥60 → 67/33 split, 10pp tolerance; 30≤N<60 → 75/25, 15pp; N<30 → "low-N calibration-only" tag, no split required. Existing Exp 15-era parameters get 60-day prospective parity check, flag drift >15pp. Phase 2 (Y2 close): migrate to rolling walk-forward — 12-month calibration / 3-month holdout, slide quarterly. Parameter versioning via git tag + `merdian_reference.json`. (d) status quo explicitly rejected. Independent — can draft anytime. **2026-05-22 (S33) cross-reference:** ADR-011 v7 retest cohort empirically confirms the Phase 1 H-FVG retest finding cross-symbol with real held-strike option PnL — strengthens D.14.1 from spot-WR-only to spot-WR + option-PnL validation. |
| ADR-011 | Chain-table held-strike ATM PnL — supersedes ADR-010 v3 §"Source table choice" | Navin / Claude | **ACCEPTED S33 2026-05-22** — full row above in Index. ENH-106 v7 writer implemented and deployed; 19,399 outcomes rebuilt in 1775.9s; coverage 20-44× lift; median PnL flipped positive on directional cells. |
| ADR-012 | Spot-anchored stop-loss doctrine for ICT retest entries — supersedes informal Compendium-era 30%+ premium SL default | Navin / Claude | **ACCEPTED S34 2026-05-24** — full row above in Index. Doctrine accepted on n=7 empirical signal strength (0/7 SL triggers at 0.3-0.8% spot vs 0/7 winners at ≤10% premium SL). X = 0.5% provisional default; validation cohort expansion to n≥50 gated on TD-S34-NEW-4 closure (15 missing post-Apr-2026 entries) + ENH-108 N-touch retest detection (unlocks 112 invisible-retest move-days). Implementation pending future session (signal-emitter `sl_level` computation, order placer 5m bar-close exit logic, Pine overlay SL line, `ict_primitive_outcomes` SL columns). |
| ADR-013 | Breeze canonical historical backfill (ICICI Direct API as architectural fallback for post-Apr-2026 chain coverage) | Navin / Claude | **PROPOSED S35 2026-05-24** — surgical fill of 2026-04-16 chain gap (107,630 HOCS rows in 5min wallclock from MERDIAN AWS) qualifies Breeze as architectural fallback per TD-S35-NEW-1 closeable window. Promote to ACCEPTED once n≥3 successful Breeze-tier backfills accumulated. ENH-109 PROPOSED for Breeze `rollingoption` + `get_historical_data_v2` wrapping. |
| ADR-014 | Per-strike GEX schema v1 | Navin / Claude | **ACCEPTED + SUPERSEDED same session S37 2026-05-25** — full row above in Index. Brief acceptance post smoke-fire (NIFTY/SENSEX fidelity OK); superseded same session by ADR-015 upon quant review of boolean-derivation marker noise. v1 patch deployed live then schema migrated to v2 via TRUNCATE + ALTER (4 cols dropped, 2 added). |
| ADR-015 | Per-strike GEX schema v2 — 12-col minimum-sufficient-statistic with `gamma_call`/`gamma_put` split | Navin / Claude | **ACCEPTED S37 2026-05-25** — full row above in Index. Writer shipped via `patch_s37_enh80_writer_v2.py` (marker `# ENH-80 (S37) v2`). Cross-symbol smoke-fire re-validated post-migration. §F1 dealer-vs-positioning split scaffolded via `v_oi_prev_close_snapshots` view, writer integration deferred. |
| ADR-016 | Parameter calibration pattern (temporal-immutable `merdian_parameters` table + read API + CLI) | Navin / Claude | **PROPOSED S37 2026-05-25** — build deferred per operator. Design surfaced 9 magic-number clusters needing same treatment (pin/accel τ, dealer-flow scenarios ±0.5/1/2%, audit thresholds, retry budgets, etc.). Single table `merdian_parameters` (id, key, value jsonb, value_type, category, description, min_value, max_value, valid_from, valid_to, changed_by, change_reason); mandatory non-empty `change_reason` enforced at DB layer; dot-hierarchical keys (`pin_zone.tau.NIFTY`); TTL-cached read API `core/parameters.py`; CLI `merdian_calibrate.py`. ENH-83 build deferred — hardcoded τ=0.3 in ENH-81 views sufficient for single-cohort use. ENH-81 ships with `// TAU_PIN — swap for ENH-83 lookup` markers throughout views. |
| ADR-017 | Operator Console Design Principles — 6 principles + keyboard corollary codifying decision-surface design | Navin / Claude | **ACCEPTED S38 2026-05-26** — full row above in Index. 6 principles (three-filter rule + role contract Marketview/Health/Settings + asymmetric surfacing silence=healthy + confluence-as-bidirectional-context + motion replaces timestamps + distance×time salience + keyboard-first ergonomics) + Principle 4 amended post-Exp 53 from confluence-as-headline to confluence-as-bidirectional-context (can confirm OR invalidate). Cross-references ENH-110 Consolidated Marketview Build (Phase 1 Marketview + Settings consolidation per ADR-017 role contract). |
| ADR-019 | 2026-06-22 | Signal-subsystem orphan final disposition | Port-not-retire all three (shadow-v3 / iv_context / options_flow); retain pending re-home, do not deprecate | Orphaned by S49 migration-scope gap, not by a failed test; experiment verdicts don't reliably transfer (SMDM-NEUTRAL-then-rebuilt) | Rejected: deprecate-on-concluded-shadow (initial verdict, reversed) | ADR-019-signal-orphan-final-disposition.md |
| ADR-018+ | Next-free | — | Available |

---

## Migration note — pre-ADR settled decisions awaiting promotion

The pre-ADR habit, V19 §8.5 SRB rules and several appendix-§17 decision registries contain **settled architectural decisions that are not yet captured as ADRs**. Two queues:

### V18 master §17 — Consolidated Decision Registry (~12 entries)

V18 master Section 17 records decisions accumulated across V17A–V17E sessions. Examples include the V18 health-monitoring architecture choices, the Dhan→Zerodha WebSocket architecture decision, calendar control rules, and the AWS shadow scope. These predate the ADR habit.

### V18G master §10 — Decision Log (~7 entries)

V18G §10 records decisions from the WebSocket deployment / Phase 4A session. Entries include: Phase 4 promotion gate waiver (10.1), Phase 4 execution path A→B→C (10.2), WebSocket broker choice — Zerodha for NIFTY, Dhan REST for SENSEX (10.3), and four further operational decisions.

### Disposition

These decisions remain authoritative and are cited by current code. They are not orphans — their rationale lives in the cited V18/V18G master sections. But they are not lookup-discoverable from this index, which is the gap.

Two paths forward (pick per ADR-by-ADR judgment, no bulk migration required):

1. **Promote to full ADR.** When a decision is materially load-bearing (e.g. WebSocket broker choice — ADR-006 reserved territory), draft a retroactive ADR using the ADR-007 precedent. The ADR cites the V18 source as the original record; this index gets a new row.
2. **Leave in source.** Operational decisions (e.g. dashboard rewrite specifics, capital floor lowered ₹200K → ₹10K) can stay in their V18 source. They appear in CLAUDE.md "Things that are settled" if directly relevant to future sessions, but do not require an ADR.

This index does not introduce a `DEC-NNN` ID prefix. Per Doc Protocol v4 Rule 5, only ADR-NNN and CASE-YYYY-MM-DD IDs live here.

---

## Index health checks

Suggested cadence — once per phase boundary or quarterly, whichever first:

- Verify every accepted ADR in `docs/decisions/` has a row in this index
- Verify every row in this index has a working `Source` link
- Verify the CLAUDE.md "Things that are settled" footer contains a one-liner for every ADR in this index (Doc Protocol v4 Rule 11.3 reverse check)
- Verify no ADR is in Status: Proposed for more than two sessions without movement (escalate or reject)

---

*MERDIAN Decision Index — established Session 23, 2026-05-09. Prepend-only. Do not edit existing rows except to add `[SUPERSEDED by ADR-NNN]` annotations. Last maintenance: Session 40 (2026-05-29 — no new ADRs S40; execution against ADR-016 parameter calibration pattern via TD-S37-01 closure (view-tau swap to runtime `get_parameter_num` lookup) + ADR-017 operator console design principles via Marketview v4 atomic-card redesign Lovable build + AWS deploy pipeline establishment at `/home/ssm-user/meridian-connect`). Previous maintenance: Session 39 (2026-05-27 — no new ADRs S39; execution against ADR-016 + ADR-017 via ENH-110 Phase 1 ship).*
*MERDIAN Decision Index — established Session 23, 2026-05-09. Prepend-only. Do not edit existing rows except to add `[SUPERSEDED by ADR-NNN]` annotations. Last maintenance: Session 59 (2026-06-24 — no new ADRs S59; bug-fix session — breadth prev-close reference freeze fixed (missing AWS cron, re-run of C-09/ADR-001) + ICT daily-PDL write-time filter fixed; Doc Protocol v4 Rule 10 excludes bug fixes from ADR triggers). Previous maintenance: Session 43 (2026-06-02 — no new ADRs S43; filed CASE-2026-06-02 short-covering-setup case file + TD-NEW-14 ingestion-table gaps + tech debt rewrite).*
*MERDIAN Decision Index — established Session 23, 2026-05-09. Prepend-only. Do not edit existing rows except to add `[SUPERSEDED by ADR-NNN]` annotations. Last maintenance: Session 43 (2026-06-02 — no new ADRs S43; filed CASE-2026-06-02 short-covering-setup case file + TD-NEW-14 ingestion-table gaps + tech debt rewrite). Previous maintenance: Session 40 (2026-05-29 — no new ADRs S40; execution against ADR-016 parameter calibration pattern via TD-S37-01 closure (view-tau swap to runtime `get_parameter_num` lookup) + ADR-017 operator console design principles via Marketview v4 atomic-card redesign Lovable build + AWS deploy pipeline establishment at `/home/ssm-user/meridian-connect`). Earlier: Session 39 (2026-05-27 — no new ADRs S39; execution against ADR-016 + ADR-017 via ENH-110 Phase 1 ship).*